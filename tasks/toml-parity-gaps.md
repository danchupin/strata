# TOML parity gap-list (US-001 artifact)

Generated by `make audit-toml-parity` against the audit jsonl at
`scripts/audit-results/env-toml-parity-2026-05-25.jsonl`.

This file is the **contract** for US-002..US-005 of `ralph/toml-parity`:
every row below MUST land an envMap entry, a Config struct field, and a
matching commented line in `deploy/strata.toml.example` by the time US-006
runs `env_toml_parity_test.go`.

**Naming convention 2A**: `STRATA_<SECTION>_<KNOB>` → `<section>.<knob>`
(lowercase). First underscore splits section; remaining underscores stay
snake_case in the knob. Multi-word sections (e.g. `usage_rollup`,
`manifest_rewriter`, `audit_export`, `access_log`, `bucket_stats`, `quota_reconcile`)
MUST be declared explicitly in envMap so the wired TOML key matches the
canonical section name — the heuristic shown in the **Expected TOML key**
column below is the naive first-underscore split and WILL be wrong for
multi-word sections. The actual envMap entry is the source of truth.

**Counts** (post US-005 audit, 2026-05-25):

| Bucket                  | Count |
|-------------------------|------:|
| Total STRATA_* env vars |   136 |
| In envMap (wired)       |   112 |
| Exempt (test/bootstrap) |    24 |
| Unmapped (gap)          |     0 |
| Present in example TOML |    58 |

Story coverage hints (final placement decided by each story's author):

- **US-002 workers** — gc.* / lifecycle.* / rebalance.* / usage_rollup.* /
  manifest_rewriter.* / audit_export.* / quota_reconcile.* / `workers` /
  notify.* / replicator.* / access_log.* / inventory.* knobs. ✅ DONE.
- **US-003 auth + KMS** — auth.* / kms.* / key.max_age / sse.vault.* /
  kms.dek_cache_ttl. ✅ DONE.
- **US-004 observability** — otel.* / logging.level / logging.format /
  audit_log.retention. ✅ DONE. (cassandra.slow_ms / console.theme_default /
  prometheus.url remain — moved to US-005 sweep.)
- **US-005 misc sweep** — bucketstats.* / rados.health_oid / rados.pool_size /
  rados.batch_ops / rados.get_prefetch / rados.put_concurrency /
  manifest.format / vhost.pattern / cluster.name / node.id /
  sse.master_* / mfa.secrets / console.jwt_secret / jwt.secret_file /
  jwt.shared. ✅ DONE.

## Gap rows (remaining after US-005)

None — every non-exempt STRATA_* env is wired through envMap + Config.
US-006 drift-lint will hard-fail any future addition that skips this
pipeline.

## Exempt rows (no action — kept for transparency)

These appear in the audit jsonl but skip the gap list. Membership lives in
`internal/config/exempt_env_vars.go` (`ExemptEnvVars` struct, consumed by
both the audit script and the US-006 drift-lint test):

- Bootstrap: `STRATA_CONFIG_FILE`.
- Retired (WARN-and-ignore): `STRATA_DRAIN_STRICT`.
- Build metadata: `STRATA_VERSION`.
- CLI client convenience: `STRATA_ADMIN_ENDPOINT`, `STRATA_ADMIN_PRINCIPAL`.
- Bench output: `STRATA_PROM_PUSHGATEWAY`.
- Test-only fixture regen: `STRATA_REGEN_TRAILER_FIXTURES`.
- Test-only override: `STRATA_STORAGE_HEALTH_OVERRIDE`,
  `STRATA_TIKV_TEST_PD_ENDPOINTS`.
- Scylla integration suite: `STRATA_SCYLLA_IMAGE`, `STRATA_SCYLLA_TEST`.
- Non-env literal (lifecycle storage-class identifier): `STRATA_COLD`.
- Prefix families: `STRATA_BENCH_*`, `STRATA_TEST_*`.
- Test-sentinel suffixes: `*_TEST`, `*_TEST_ABSENT`, `*_TEST_HI`, `*_TEST_LO`,
  `*_UNSET`, `*_INT64_TEST`, `*_BOOL_TEST`.

## Already wired (envMap rows — no action required)

See `internal/config/config.go::envMap` for the full set (89 entries today).
Highlights:

- Listen / region / data_backend / meta_backend / shutdown_wait /
  default_bucket_shards.
- Cassandra block (hosts / keyspace / local_dc / replication / username /
  password / timeout).
- TiKV PD endpoints.
- RADOS block (config_file / user / keyring / pool / namespace / classes /
  clusters).
- S3 backend (clusters / classes).
- Auth (mode / static_credentials / sts_duration / key_max_age).
- KMS (adapter / dek_cache_ttl / default_key_id / aws.* / vault.* /
  local_hsm.seed).
- Workers (`workers.enabled` + per-worker sub-sections: gc / lifecycle /
  rebalance / usage_rollup / manifest_rewriter / audit_export /
  quota_reconcile / notify / replicator / access_log / inventory).
- OTel (endpoint / sample_ratio / ringbuf / ringbuf_bytes).
- Logging (level / format).
- Audit log (retention).

These rows MUST stay wired through US-002..US-005 and US-006 — any
regression failing the drift lint is a CI break.

# Ralph Progress Log
Started: Sun May 17 10:00:00 MSK 2026
Cycle: ralph/rebalance-scale-phase-2

## Codebase Patterns
- 5-story cycle closing 1 P2 ROADMAP entry ("Rebalance worker not sharded — single goroutine bottleneck on large deploys"). Mirrors `ralph/gc-lifecycle-scale-phase-2` cycle (gc + lifecycle Phase 2). Per-shard leader-election fan-out for the rebalance worker. Single-replica SHARDS=1 reproduces Phase 1 byte-for-byte.
- **Reference impl**: `internal/gc/fanout.go` (FanOut struct: Name, Locker, ShardCount, Build, LeaderTTL, LeaderRenew, AcquireRetry, OnLeader, Logger, holders map+RWMutex). FanOut.Run spawns one goroutine per shard via wg.Go; each runs `superviseShard` with panic-recovery backoff (1s→5s→30s→2m, reset after 5min healthy); inner `runShardOnce` acquires `<name>-leader-<shardID>` lease via `leader.Session.AwaitAcquire`, calls Build(shardID).RunShard(workCtx, shardID, shardCount), releases on exit.
- **OnLeader folding**: FanOut tracks shards owned per replica via `holders map[int]bool` + `markHeld(shardID, held bool)`. Atomic counter goes 0→1 on first acquire (emit acquired=true ONCE) and 1→0 on last release (emit acquired=false ONCE). Intermediate transitions emit NO events. Mirror this exactly in `internal/rebalance.ShardedFanOut`.
- **Worker registration**: `cmd/strata/workers/rebalance.go` Build constructor reads `STRATA_REBALANCE_SHARDS` env at construct time. Pattern from `cmd/strata/workers/gc.go::clampShards` (default 1, range [1, 1024], out-of-range clamped + WARN-logged via deps.Logger). Register with `workers.Register(workers.Worker{Name: "rebalance", SkipLease: true, Build: buildRebalance})`. SkipLease=true means the supervisor skips the outer `rebalance-leader` lease and the worker owns its own leader-election.
- **Panic metrics**: increment `strata_worker_panic_total{worker="rebalance",shard="<i>"}` on per-shard panic; gc fan-out is canonical. Non-fan-out workers carry shard label `"-"`.
- **meta.Store.ListBucketsShard**: new contract method `ListBucketsShard(ctx, shardID, totalShards int) ([]*meta.Bucket, error)` filters via `fnv32a(bucket.ID) % totalShards == shardID`. New `ErrInvalidShard` sentinel for out-of-range. Implement in memory + Cassandra + TiKV; new `caseListBucketsShard` contract test seeds 100 buckets, asserts 3-shard union covers all + ~33/33/33 distribution within 5% tolerance.
- **Cassandra impl** streams via existing `ListBuckets` paging + in-process filter (no schema change — buckets partition key is `name`, not bucket_id; secondary index by shard not worth it for operator-driven scan rate).
- **TiKV impl** scans existing `s/bk/` key prefix + per-row hash filter (existing prefix scan shape from `internal/meta/tikv/buckets.go`).
- **Memory impl** iterates in-mem map + filter.
- **Worker iteration**: `internal/rebalance.Worker.RunShard(ctx, shardID, totalShards)` same loop as today but iterates only `ListBucketsShard(ctx, shardID, totalShards)` instead of full `ListBuckets`. Span attrs gain `strata.rebalance.shard=<i>`.
- **ProgressTracker merge**: `progressCache map[clusterID]ProgressSnapshot` widens to `progressCache map[clusterID]map[shardID]ProgressSnapshot`. `CommitScan(...)` signature extended with shardID; `Snapshot(clusterID)` returns merged snapshot — sums Migratable/StuckSinglePolicy/StuckNoPolicy/Bytes across shards; latest LastScanAt; merges per-bucket maps (no key collision — each bucket lives in exactly one shard).
- **Completion fire-once**: track `mergedCompletionFiredAt` at the per-cluster level (not per-shard). Sum-across-shards transitioning 0→0 must not double-fire when multiple shards hit 0 simultaneously. Re-fire allowed only after sum goes back >0 then back to 0 (idempotent-but-replayable contract from drain-cleanup US-005).
- **Admin endpoints**: `/drain-progress` reads `ProgressTracker.Snapshot(clusterID)` — operator-facing wire shape unchanged. `/drain-impact` synchronous-scan path keeps using full `ListBuckets` (one-shot admin scan, not per-tick worker iteration) — documented in code comment.
- **lab-tikv-3 bench**: `scripts/bench-rebalance-multi.sh` mirrors `scripts/multi-replica-smoke.sh` shape. Plants 1000 buckets × 10 chunks via aws-cli PUT loop with admin creds. Seeds `Placement={default:1, cephb:1}` mode=weighted. Drains `default` evacuate; measures wall-clock until `chunks_on_cluster=0` via /drain-progress polling. Runs SHARDS=1 baseline + SHARDS=3; expected SHARDS=3 ≤ 40% of SHARDS=1 (≥2.5× speedup), regression guard EXITs NON-ZERO if SHARDS=3 > 70% of SHARDS=1. `docs/site/content/architecture/benchmarks/rebalance.md` table published with numbers.
- **Smoke**: `scripts/smoke-rebalance-scale.sh` covers 4 scenarios: A (single-replica, SHARDS=3, 3 shard goroutines in one process, folded heartbeat = exactly 1 acquired event), B (lab-tikv-3 multi-leader, 3 leases distributed across 3 replicas via worker_locks inspection), C (back-compat SHARDS=1 = identical legacy log shape), D (replica failover, surviving replicas acquire freed lease within STRATA_GC_LEASE_TTL).
- **Docs**: `docs/site/content/best-practices/placement-rebalance.md` gains "Multi-leader scaling" subsection. `docs/site/content/architecture/benchmarks/rebalance.md` new. Project `CLAUDE.md` "Background workers" section gets refreshed rebalance bullet (SkipLease=true + per-shard leases + STRATA_REBALANCE_SHARDS env + folded heartbeat chip).
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md`): walkthrough in PRD covers 10-step happy path (operator sets SHARDS=3 → replicas race for 3 leases → heartbeat propagates → operator drains → 3 replicas concurrently iterate shard subsets → ProgressTracker aggregates → /drain-progress shows merged counts → migration completes ~3× faster → state=evacuating chunks_on_cluster=0 across shards → deregister_ready=true). Edge cases: single-replica at SHARDS=1, multi-shard ownership within one replica, per-shard panic restart, folded leader chip emission. Final US-005 gated on `make smoke-rebalance-scale` passing.
- **ROADMAP close-flip** in US-005: P2 entry → `~~**P2 — Rebalance worker not sharded — single goroutine bottleneck on large deploys.**~~ — **Done.** <one-line summary referencing lab-tikv-3 bench + STRATA_REBALANCE_SHARDS env>. (commit \`<pending>\`)`. Closing SHA backfilled in follow-up commit on main.
- **`tasks/prd-rebalance-scale-phase-2.md` REMOVED** in US-005 per CLAUDE.md PRD lifecycle rule.
- **Test surfaces**: `internal/meta/storetest/contract.go` new `caseListBucketsShard`; `internal/rebalance/fanout_test.go` new (mirror `internal/gc/fanout_test.go`); `internal/rebalance/progress_test.go` extended (merge sum + completion fire-once); `internal/rebalance/worker_integration_test.go` 3-shard multi-replica simulation against memory backend.
- **Contract-test bucket count**: PRD AC said n=100 with 5% tolerance, but FNV-1a binomial σ≈4.7 per shard at n=100 means 3σ ≈ ±14 which exceeds ±5. Bumped n→300 with ±15% tolerance (±45 covers ~3σ at σ≈8) for the contract test; n=1000 with 5%/10% tolerance lives in the meta package's `TestBucketShardIDDistribution` unit test where the statistical assertion is reliable. Future-iter doing similar distribution tests against `CreateBucket`-assigned random UUIDs: pick n such that 3σ < tolerance window.
- **Hash-function locality**: kept the hash helper inside `internal/meta/store.go` (next to `GCShardID`) — new `BucketShardID(uuid.UUID, totalShards)` + `ValidateShard(shardID, totalShards)`. Reusing `fnv32aGC` so the meta package still doesn't need to import `hash/fnv` from callers. Backends call `meta.BucketShardID` + `meta.ValidateShard` only; do not duplicate hash logic per-backend.
- **RunShard contract**: `internal/rebalance.Worker.RunShard(ctx, shardID, shardCount)` is the fan-out's entrypoint into a per-shard worker; it stamps `cfg.ShardID`/`cfg.ShardCount` and delegates to `Run`. US-003 wires the actual `meta.ListBucketsShard` filter inside `runOnce` so the slot fields drive the bucket iteration. Phase 1 callers (single-leader path) still use `Run` directly — shard fields default to (0,1) which reproduces the legacy "scan everything" behaviour.

## 2026-05-17 - US-002
- New `internal/rebalance/fanout.go` — `ShardedFanOut` struct mirroring `internal/gc/fanout.go`: Locker, ShardCount, Build(shardID) *Worker, OnLeader, Logger, Backoff, Sleep, StableAfter, LeaderTTL/Renew/AcquireRetry, holders map+RWMutex. `Run(ctx)` spawns one goroutine per shard via wg.Go; `superviseShard` runs the panic-recovery backoff loop; `runShardOnce` acquires `rebalance-leader-<shardID>` lease via `leader.Session.AwaitAcquire`, calls `Build(shardID).RunShard(workCtx, shardID, shardCount)`, releases on exit; per-shard panics bump `strata_worker_panic_total{worker="rebalance",shard="<i>"}`; `markHeld` folds OnLeader so a replica holding N shards emits exactly one acquired/released pair.
- Extended `internal/rebalance/worker.go`: added `ShardID, ShardCount int` fields to `Config`; new `RunShard(ctx, shardID, shardCount) error` entrypoint that stamps the slot on the config and delegates to `Run`; `Run` clamps and logs shard_id/shard_count. US-003 will wire the per-shard iteration via meta.Store.ListBucketsShard.
- Updated `cmd/strata/workers/rebalance.go`: flipped to `SkipLease: true`; reads `STRATA_REBALANCE_SHARDS` (clamped via shared `clampShards` from gc.go); buildRebalance returns `*rebalance.ShardedFanOut`; folded `OnLeader` calls `deps.EmitLeader("rebalance", acquired)` so the supervisor's heartbeat chip flips once per replica regardless of held-shard count. New package var `RebalanceFanOut` captures the most-recent fan-out for future diagnostics use (mirrors `GCFanOut`).
- New unit tests in `internal/rebalance/fanout_test.go`: TestShardedFanOutThreeReplicasDisjointShards (3 replicas, 3 shards, asserts full coverage + disjointness invariant), TestShardedFanOutSingleReplicaSingleShard (Phase 1 back-compat), TestShardedFanOutPanicIsolation (shard 0 panics, shard 1 lease retained, panic_total counter ticks), TestShardedFanOutOnLeaderTransitions (folding: 3 shards → exactly 1 acquired + 1 released event), TestShardedFanOutLeaseNamePin (`rebalance-leader-<i>` shape), TestShardedFanOutHeldShardsSorted, TestShardedFanOutRequiresLockerAndBuild (missing dep guard rails), TestShardedFanOutThreeShardsAllRunShardCalled (Build invoked per-shard).
- Updated `cmd/strata/workers/rebalance_test.go`: flipped TestRebalanceWorkerRegistered to require SkipLease=true; added TestBuildRebalanceShardsEnv covering STRATA_REBALANCE_SHARDS; build-returns-*ShardedFanOut assertions in TestBuildRebalanceReadsEnv + TestBuildRebalanceClampsOutOfRange.
- Files changed: `internal/rebalance/worker.go`, `internal/rebalance/fanout.go`, `internal/rebalance/fanout_test.go`, `cmd/strata/workers/rebalance.go`, `cmd/strata/workers/rebalance_test.go`.
- **Learnings for future iterations:**
  - `clampShards` lives in `cmd/strata/workers/gc.go` and is shared across the package — reused unchanged for rebalance. Same `[1, 1024]` ceiling baked into `meta.GCShardCount` keeps the env contract consistent across worker shard fan-outs.
  - `deps.Tracer` nil is safe (`*strataotel.Provider.Tracer(name)` short-circuits when receiver nil) — no need for a NewNoopProvider helper in tests. Mirrors what gc_test.go does.
  - Tests instantiate `Worker` via `rebalance.New(rebalance.Config{...})` not by hand-rolling the struct (cfg is private). Saved a refactor by routing the new ShardedFanOut Build closure through `rebalance.New` and reusing the existing Worker construction guardrails.
  - Worker.Run's shard clamp is defense-in-depth — RunShard already clamps but `Run` is still public and might be called without RunShard (Phase 1 code path). The double clamp is intentional, not redundant.
  - `racetest.TestRunCleanRunNoInconsistencies` flakes occasionally (unrelated to this cycle's surface) — a single retry covers it.
---

## 2026-05-17 - US-003
- `internal/rebalance/progress.go` rebuilt around per-shard storage: `clusterEntry{shards map[int]*shardContribution; completionFiredAt}` replaces the flat `*ProgressSnapshot` cache. `CommitScan(shardID int, draining []string, scans map[string]ScanResult, now time.Time)` upserts only the shard's slot; reap rule drops the shard slot whenever its cluster id leaves `draining`, then deletes the cluster entry once every shard slot is gone. Snapshot still returns `ProgressSnapshot` — counters / Bytes / BaseChunks / BaseBytes sum across shards, LastScanAt is max-across-shards, ByBucket merges per-bucket entries (each bucket lives in exactly one shard so keys do not collide; defensive sum on accidental duplicates). CompletionFiredAt tracked at cluster level so multiple shards hitting zero in the same tick fire exactly one event; refill on any shard re-arms the gate. CompletionEvent.BaseChunks/BaseBytes = sum of per-shard bases.
- `internal/rebalance/worker.go` — `runOnce` now iterates `meta.Store.ListBucketsShard(ctx, w.cfg.ShardID, w.cfg.ShardCount)` instead of full `ListBuckets`; `progressAcc.commit` accepts shardID and threads through to `CommitScan`. `RunOnce` stamps `strata.rebalance.shard` + `strata.rebalance.shard_count` attrs on the existing `worker.rebalance.tick` parent span (span name unchanged). Defense-in-depth shard clamps mirror `Run`.
- `internal/adminapi/clusters_drain_impact.go` — added in-code comment documenting that the synchronous-scan path intentionally keeps using full `ListBuckets` (one-shot operator preview behind the 5-minute cache, not a per-tick worker iteration).
- `internal/rebalance/fanout_test.go` — `panicOnListMeta` shim now also panics on `ListBucketsShard`; otherwise the worker's new sharded entrypoint would bypass the rigged panic and `TestShardedFanOutPanicIsolation` would never trip the recovery path.
- New tests: `progress_test.go` gains `TestProgressTrackerShardedSnapshotMergesCounters`, `TestProgressTrackerShardedSnapshotMergesByBucket`, `TestProgressTrackerShardedCompletionFiresOnceWhenAllShardsReachZero`, `TestProgressTrackerShardedCompletionRefiresAfterRefill`, `TestProgressTrackerShardedReapDropsClusterWhenAllShardsUndrain`. New file `worker_sharded_test.go`: `TestWorkerRunShardIteratesOnlyOwnedBuckets` runs 3 worker instances (shardID 0/1/2 each) against one memory backend with 9 seeded buckets and asserts the merged snapshot covers every bucket exactly once; `TestWorkerSingleShardCountReproducesPhase1` proves ShardCount=1 byte-for-byte parity with legacy Phase 1.
- All existing callers updated to pass shardID=0 (single-shard back-compat): `internal/rebalance/progress_test.go` + `internal/adminapi/clusters_drain_progress_test.go`.
- Files changed: `internal/rebalance/progress.go`, `internal/rebalance/worker.go`, `internal/rebalance/progress_test.go`, `internal/rebalance/fanout_test.go`, `internal/rebalance/worker_sharded_test.go` (new), `internal/adminapi/clusters_drain_impact.go`, `internal/adminapi/clusters_drain_progress_test.go`.
- **Learnings for future iterations:**
  - The `panicOnListMeta` test shim wraps `*metamem.Store` and overrides only the methods it wants to panic on. Whenever the worker switches from `ListBuckets` → `ListBucketsShard` (or any other meta entry point), the shim needs the same override or the panic path is silently bypassed by Go's method-resolution falling through to the embedded type. Watch for this whenever a worker's iteration entry point changes.
  - `CommitScan` reap rule is per-shard (only this shard's slot drops when cluster leaves draining), not per-call-cluster. This means a transient stale Snapshot from another shard can survive one tick after an undrain — acceptable because the next tick of the other shard reaps. Important to remember when reasoning about "instant" deregister_ready flips after undrain.
  - Per-shard BaseChunks captures each shard's first non-zero observation; merged BaseChunks = sum across shards. With all shards running on one replica's fan-out the per-shard first-non-zero scans fire approximately together, so merged BaseChunks ≈ true "chunks at drain start". Approximation is close enough for the UI percent-filled rendering.
  - The merged ProgressSnapshot path is the only operator-facing wire shape — every admin handler that reads from the tracker (`/drain-progress`, `/drain-impact` if it ever moves), and every test that drives `tracker.CommitScan(...)` directly, MUST pass an integer shardID as the first arg now. Search `CommitScan(` after any tracker change to make sure.
---

## 2026-05-17 - US-004
- New `scripts/bench-rebalance-multi.sh` — end-to-end bench that drives the rebalance worker through the HTTP admin API: validates lab (clusters listed, rebalance worker reachable via `/drain-progress` probe), seeds N buckets × M chunks with `Placement={default:1, cephb:1}` mode=weighted, drains `default` evacuate, polls `/admin/v1/clusters/default/drain-progress` until `chunks_on_cluster=0`, repeats at SHARDS=1 baseline + SHARDS=3 fan-out, prints ratio + verdict (`SPEEDUP_OK` ≤40%, `SPEEDUP_PARTIAL` 40-70%, `SPEEDUP_FAILED` >70% → exit 1). Skip-77 on lab unreachable; `REQUIRE_LAB=1` converts to fail. Configurable via `BENCH_BUCKETS` / `BENCH_CHUNKS_PER_BUCKET` / `BENCH_OBJECT_SIZE_KB` / `BENCH_DRAIN_TIMEOUT_S` / `BENCH_SHARDS_BASELINE` / `BENCH_SHARDS_FANOUT` / `BENCH_SPEEDUP_TARGET_PCT` / `BENCH_REGRESSION_PCT`.
- Restart-between-passes via `BENCH_RESTART_HOOK` env (default: docker compose force-recreate of the three strata-tikv replicas with STRATA_REBALANCE_SHARDS=$SHARDS + STRATA_WORKERS=gc,lifecycle,rebalance exported). The hook is a bash expression `eval`-d each pass; operators on k8s / systemd override to drive their lab.
- New `make bench-rebalance-multi` target wraps the script; `clean` target gained `bench-rebalance-multi-results.jsonl` cleanup line; `.PHONY` updated.
- New `docs/site/content/architecture/benchmarks/rebalance.md` — Phase 2 multi-leader doc: harness (compose recipe + make target + results JSONL shape), methodology (stack, hardware, workload, drain trigger, restart-between-passes), expected production multiplier table (verdict thresholds + reasons), cap shape (target RADOS pool, source RADOS pool, TiKV region heat, bucket-count-vs-shard-count), operator-facing recommendation (3-replica defaults + N-replica scaling + single-replica fallback + folded heartbeat), caveats (drain wall-clock vs per-chunk cost, uniform bucket sizes, single-PD lab, cross-host lease lottery deferred to US-005 smoke). Cross-link from `_index.md` benchmarks index added.
- Files changed: `scripts/bench-rebalance-multi.sh` (new), `Makefile` (bench-rebalance-multi target + clean line + .PHONY), `docs/site/content/architecture/benchmarks/rebalance.md` (new), `docs/site/content/architecture/benchmarks/_index.md`.
- **Learnings for future iterations:**
  - The bench script intentionally does NOT manage the docker-compose lifecycle. `lab-tikv-3 + multi-cluster` profile composition is operator-responsibility (lab-tikv-3 ships single-ceph; bench requires a `docker-compose.lab-tikv-3-multi.yml` overlay that the operator maintains because the shape is bench-specific). The script's only orchestration is the restart-between-passes via `BENCH_RESTART_HOOK`.
  - Existing bench scripts that DO live in the repo (`bench-gc-multi` / `bench-lifecycle-multi` make targets) drive `strata admin bench-*` against in-process simulations — they don't need a multi-replica stack. The rebalance bench is structurally different because it measures end-to-end drain wall-clock through the HTTP admin API (a single `bin/strata admin bench-rebalance` flag could be added later for parity, but the end-to-end shape captures the operator-facing reality which the in-process sim misses).
  - The `SPEEDUP_OK` threshold is 2.5× (ratio ≤40%) not 3× because read+write share the per-replica token-bucket — one chunk move costs `chunkSize × 2` tokens, so a single replica at `STRATA_REBALANCE_RATE_MB_S=100` caps near 50 MB/s effective per-direction. Three replicas concurrently writing the same target RADOS pool also stack on shared OSD bandwidth. Documented in the docs page's "Expected production multiplier" + "Cap shape" sections.
  - Skip-77 convention (operator-readable SKIP message + exit code 77) is shared across `smoke-drain-*.sh` and the new bench script. CI gating turns it into a fail via `REQUIRE_LAB=1`. Future bench / smoke scripts should follow the same pattern for graceful no-lab handling.
  - The `BENCH_RESULTS_FILE` env knob defaults to `bench-rebalance-multi-results.jsonl` in $PWD; the `clean` Makefile target removes it alongside the gc/lifecycle JSONL files. Pattern: any bench script that writes a JSONL artifact should have a matching `clean` entry to keep `make clean` semantics.
---

## 2026-05-17 - US-001
- Added `meta.ErrInvalidShard` sentinel + `meta.BucketShardID(uuid.UUID, totalShards)` + `meta.ValidateShard(shardID, totalShards)` helpers in `internal/meta/store.go`.
- Added `Store.ListBucketsShard(ctx, shardID, totalShards) ([]*Bucket, error)` to the interface.
- Implemented in memory (`internal/meta/memory/store.go`), cassandra (`internal/meta/cassandra/store.go` — reuses `ListBuckets` iterator + in-process filter), and tikv (`internal/meta/tikv/buckets.go` — reuses prefix scan + in-process filter).
- New contract case `caseListBucketsShard` in `internal/meta/storetest/contract.go` (n=300, ±15% tolerance, includes union+disjoint+edge cases for totalShards=1 / off-by-one / negative shardID / zero totalShards).
- New unit tests in `internal/meta/store_test.go`: `TestValidateShard`, `TestBucketShardIDDistribution` (n=1000 with 5%/10% tolerance), `TestBucketShardIDStable`.
- Files changed: `internal/meta/store.go`, `internal/meta/store_test.go`, `internal/meta/memory/store.go`, `internal/meta/cassandra/store.go`, `internal/meta/tikv/buckets.go`, `internal/meta/storetest/contract.go`.
- **Learnings for future iterations:**
  - `s.CreateBucket` assigns random UUIDs internally; the contract test cannot control bucket IDs. Distribution tolerance must absorb FNV-1a's natural binomial variance — see Codebase Patterns "Contract-test bucket count".
  - Adding any new method to `meta.Store` produces a fanout of diagnostic errors in every consumer/test file that uses `*memory.Store` / `*cassandra.Store` / `*tikv.Store` against the `meta.Store` interface (`InvalidIfaceAssign` + `ImpossibleAssert`). Implement all three backends in the same commit; otherwise nothing else compiles.
  - The new `BucketShardID` helper deliberately lives in `internal/meta/store.go` alongside `GCShardID` so the rebalance worker fan-out (US-002+) imports `meta.BucketShardID` instead of pulling in `hash/fnv` directly — single source of truth.
---

## 2026-05-17 - US-005
- New `scripts/smoke-rebalance-scale.sh` — four-scenario harness mirroring `scripts/smoke-drain-cleanup.sh` shape: Scenario A (single-replica fan-out at SHARDS=3, folded chip emits once, drain converges), Scenario B (lab-tikv-3 multi-leader at SHARDS=3, one chip per replica via `/admin/v1/cluster/nodes` leader_for inspection — replaces the cqlsh-on-Cassandra worker_locks AC since the lab is TiKV-backed; same operator-facing invariant), Scenario C (back-compat SHARDS=1 with EXACTLY one chip holder), Scenario D (replica failover via `docker stop strata-tikv-c` + lease-TTL sleep + survivor chip-presence reassert). Lab-shape is operator-managed via `SMOKE_RESTART_HOOK` (mirror of bench-rebalance-multi.sh's BENCH_RESTART_HOOK). Auto-skip B + D on single-replica labs. SKIP-77 on /readyz unreachable; REQUIRE_LAB=1 converts to fail.
- New `make smoke-rebalance-scale` Makefile target; `.PHONY` updated.
- `docs/site/content/best-practices/placement-rebalance.md` gains "Multi-leader scaling" section: `STRATA_REBALANCE_SHARDS` env doc, distribution table (R replicas vs N shards), bench expectations (lab-tikv-3 ≥2.5× speedup), single-replica fallback narrative, smoke harness reference. Cross-link to `docs/site/content/architecture/benchmarks/rebalance.md` (created in US-004).
- Project root `CLAUDE.md` "Background workers" section gets refreshed `rebalance` bullet: per-shard `rebalance-leader-0..N-1` leases, SkipLease=true, `STRATA_REBALANCE_SHARDS` env (default 1, range [1, 1024]), folded heartbeat chip, per-shard panic counter, widened ProgressTracker shape, completion fire-once cluster-level tracking, /drain-progress merged read, /drain-impact sync-scan path documented.
- `ROADMAP.md` close-flipped the P2 entry → `~~**P2 — Rebalance worker not sharded — single goroutine bottleneck on large deploys.**~~ — **Done.** <one-line summary referencing lab-tikv-3 bench + STRATA_REBALANCE_SHARDS env>. (commit \`<pending>\`)` — closing SHA backfilled in follow-up commit on main.
- `tasks/prd-rebalance-scale-phase-2.md` REMOVED in the same commit per CLAUDE.md PRD lifecycle rule.
- Quality gates green: `make vet`, `make test` (single retry on the pre-existing `TestThreeReplicaDistribution` lifecycle distribution flake — random UUIDs, unrelated to this cycle), `make docs-build`, `pnpm run build`.
- Files changed: `scripts/smoke-rebalance-scale.sh` (new), `Makefile` (smoke target + .PHONY), `docs/site/content/best-practices/placement-rebalance.md`, `CLAUDE.md`, `ROADMAP.md`, `tasks/prd-rebalance-scale-phase-2.md` (REMOVED), `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The lab-tikv-3 stack uses TiKV for both meta and leases; the AC text in this cycle's prd.json mentioned "Cassandra worker_locks inspection" for Scenario B but the actual lease store is TiKV. The right operator-facing probe for lease distribution is `/admin/v1/cluster/nodes` leader_for arrays (Heartbeat surface, backend-agnostic) — same shape `scripts/multi-replica-smoke.sh` already uses to assert chip rotation. The cqlsh path would only apply to a cassandra-backed lab-tikv variant that doesn't exist.
  - Smoke scripts that drive multi-pass restarts share the `*_RESTART_HOOK` env pattern with bench scripts (e.g. `BENCH_RESTART_HOOK` in `scripts/bench-rebalance-multi.sh`, now `SMOKE_RESTART_HOOK` in `scripts/smoke-rebalance-scale.sh`). When adding a new multi-pass harness, reuse the hook contract: a bash expression `eval`-d each pass with `STRATA_REBALANCE_SHARDS=$SHARDS` exported, default to `docker compose up -d --force-recreate ...`, operators on k8s/systemd override via env. Keeps the script lab-shape-agnostic.
  - `TestThreeReplicaDistribution` in `internal/lifecycle/distribute_test.go` is a known sporadic flake — uses random UUIDs to seed 9 buckets and asserts the 3-replica hash distribution is roughly 3-3-3 with a `[1, 5]` window. FNV-1a binomial variance can produce 0-X-Y splits ~1-2% of the time. Single retry covers it. Future iterations debugging "replica N expected to process M buckets" should not chase the rebalance/lifecycle fan-out code — the flake is in the test's distribution assertion.
  - `make smoke-rebalance-scale` does NOT depend on the gateway being built locally (no `: build` prereq); the script drives an already-running lab via the LB. Matches `make smoke-drain-cleanup` shape. Don't add a build prereq to smoke targets that drive a Docker-hosted gateway.
---


{
  "project": "Strata",
  "branchName": "ralph/complete-tracing",
  "description": "Complete OpenTelemetry tracing coverage across all backends + workers. Closes ROADMAP P2 'TiKV meta backend emits no trace spans' + P3 'S3-over-S3 data backend emits no trace spans' AND fills the worker tracing gap noted in project CLAUDE.md (all 10 workers — gc / lifecycle / replicator / notify / access-log / inventory / audit-export / manifest-rewriter / quota-reconcile / usage-rollup — pass nil tracer today). Current coverage: HTTP middleware + Cassandra meta (`meta.cassandra.<table>.<op>` via gocql.QueryObserver) + RADOS data (`data.rados.<op>` via ObserveOp). Gaps: (1) TiKV meta — no observer; (2) S3-over-S3 data — no observer, AWS SDK calls untraced; (3) Workers — every worker emits nothing. Fix: stamp `strata.component=\"gateway\"` on existing gateway-side observers first (clean baseline), then TiKV observer mirroring cassandra shape, S3 backend via otelaws AWS SDK middleware (auto-spans per SDK call with semconv rpc.* attrs), workers use existing `deps.Tracer.Tracer(\"strata.worker.<name>\")` pattern + new `StartIteration / EndIteration` helper emitting `worker.<name>.tick` spans per iteration with attributes `strata.component=\"worker\"` + `strata.worker=<name>` + atomic uint64 `strata.iteration_id`. Per-iteration only (no long-running parent). Worker sampling via same `STRATA_OTEL_SAMPLE_RATIO` tail-sampler; failing iterations always export (status=Error). NOTE: `workers.Dependencies.Tracer` is already `*strataotel.Provider` wired at `serverapp.go:254` — workers just need to USE it (no struct change). 6 stories. Closes ROADMAP P2 'TiKV meta backend emits no trace spans' + P3 'S3-over-S3 data backend emits no trace spans' on cycle close.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Gateway-side observers stamp strata.component=gateway",
      "description": "As an operator, I want every existing gateway-side span (HTTP middleware, Cassandra meta observer, RADOS data observer) tagged with `strata.component=gateway` so the Jaeger filter scheme rolled out in later stories works on day-one.",
      "acceptanceCriteria": [
        "`internal/otel/middleware.go` HTTP middleware: after `tracer.Start(ctx, ...)`, call `span.SetAttributes(attribute.String(\"strata.component\", \"gateway\"))`",
        "`internal/meta/cassandra/observer.go::SlowQueryObserver.ObserveQuery`: inside the span emission branch, stamp `strata.component=gateway`",
        "`internal/data/rados/observer.go::ObserveOp`: inside the span emission branch, stamp `strata.component=gateway`",
        "Add an exported constant `internal/otel.AttrComponentGateway = attribute.String(\"strata.component\", \"gateway\")` AND `AttrComponentWorker = attribute.String(\"strata.component\", \"worker\")` to avoid magic-string duplication across files",
        "Unit tests in each of the three observers (`middleware_test.go`, `observer_test.go` for cassandra + rados) extended with one assertion: emitted span carries `strata.component=gateway`",
        "Race detector clean",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Shipped: internal/otel/component.go exports AttrComponentKey + AttrComponentGateway + AttrComponentWorker. Middleware + cassandra emitSpan + rados ObserveOp all stamp strata.component=gateway. Tests extended: internal/otel/middleware_test.go (new), cassandra/observer_test.go (gateway assertion in wantAttrs), rados/observer_test.go (gateway pair in attr table)."
    },
    {
      "id": "US-002",
      "title": "TiKV meta backend observer + tracer wiring",
      "description": "As an operator inspecting a Jaeger waterfall on the lab-tikv stack, I want every TiKV transactional op to appear as a child span of its request so the meta-write step is visible.",
      "acceptanceCriteria": [
        "Add `internal/meta/tikv/observer.go` with `type Observer struct { logger *slog.Logger; tracer trace.Tracer }` and `WrapOp(ctx, op, table string, fn func(ctx) error) error` helper that starts a child span `meta.tikv.<table>.<op>`, runs `fn`, marks status from `err`, ends span. Attributes: `db.system=\"tikv\"`, `db.operation=<op>`, `db.table=<table>`, `strata.component=\"gateway\"` (via `internal/otel.AttrComponentGateway` from US-001)",
        "Add `Tracer trace.Tracer` field to `internal/meta/tikv/config.go::Config` (matches `cassandra.SessionConfig.Tracer` shape)",
        "`internal/meta/tikv/store.go` constructor stores tracer; every public Store method that hits TiKV (CreateBucket, GetBucket, PutObject, GetObject, DeleteObject, ListObjects, SavePart, CompleteMultipart, abort/complete/get*Multipart, SetBucketBlob/GetBucketBlob/DeleteBucketBlob, PutBucketStats/GetBucketStats — full enumeration via grep) wraps its inner txn body in `observer.WrapOp(ctx, \"<op>\", \"<table>\", func(ctx) error { ... })`",
        "Wire `cfg.Tracer = tp.Tracer(\"strata.meta.tikv\")` in `internal/serverapp/serverapp.go::buildMetaStore` after `strataotel.Init` runs, mirror cassandra wiring at line ~545",
        "Failing op (`err != nil`) sets span status to Error so tail-sampler exports",
        "Race detector `go test -race ./internal/meta/tikv/...` clean",
        "Unit test `internal/meta/tikv/observer_test.go`: fake tracer captures span name / attributes / status for synthetic op; success vs error paths verified",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Shipped: internal/meta/tikv/observer.go (already present) wires *Observer with NewObserver(tracer)/WrapOp/Start. internal/meta/tikv/config.go::Config.Tracer wired. Tracer wired in serverapp.buildMetaStore at the tikv branch via tp.Tracer(\"strata.meta.tikv\"). Every public Store method that hits TiKV is now wrapped via s.observer.Start: buckets.go (Create/Get/Delete/List + updateBucket), objects.go (Put/Get/Delete/DeleteNullReplacement/SetObjectStorage), multipart.go (all 10 methods incl Complete/Abort/Record/Get/UpdateSSE), blobs.go (set/get/delete + Quota helpers + Inventory configs), bucket_stats.go (Get/Bump), iam.go (8 methods), managed_policies.go (8 methods), audit.go (8 methods), queues.go (GC/Notify/DLQ/Replication/AccessLog enq/list/ack), usage.go (Write/List/ListUser), reshard.go (Start/Get/Update/Complete/List), admin_jobs.go (Create/Get/Update), meta_health.go (MetaHealth). Tests: internal/meta/tikv/observer_test.go: WrapOp emits client-kind child span with db.system=tikv + db.tikv.table + db.operation + strata.component=gateway; error path flips status to Error; nil tracer is no-op; Store with backend-and-observer emits meta.tikv.buckets.CreateBucket span end-to-end."
    },
    {
      "id": "US-003",
      "title": "S3 backend AWS SDK otelaws middleware",
      "description": "As an operator running an S3-over-S3 deployment, I want every AWS SDK call from `data.s3.Backend` to land as a span in the trace so PutChunk / GetChunk paths are visible.",
      "acceptanceCriteria": [
        "Add `go.opentelemetry.io/contrib/instrumentation/github.com/aws/aws-sdk-go-v2/otelaws` as project dependency",
        "`internal/data/s3/backend.go::connFor` installs `otelaws.AppendMiddlewares(&awsCfg.APIOptions)` BEFORE client construction. Spans emit per SDK call with auto-attributes `rpc.service=S3`, `rpc.method=<op>`, `aws.region=<region>`, `http.status_code=<code>`",
        "Custom span-attribute hook stamps `strata.component=\"gateway\"` (via `internal/otel.AttrComponentGateway` from US-001) + `strata.s3_cluster=<cluster-id>` on every span so traces are filterable per cluster",
        "Add `Tracer trace.Tracer` field to `internal/data/s3/config.go::Config`; wire `cfg.Tracer = tp.Tracer(\"strata.data.s3\")` in `internal/serverapp/serverapp.go::buildDataBackend` for `case \"s3\":` (mirror rados wiring at line ~368)",
        "If `cfg.Tracer == nil` (tests), skip middleware registration — preserves zero-config test fixture from `s3test.NewFixture`",
        "Failing SDK call (`http.status_code >= 400`) marks span status Error so tail-sampler exports",
        "Race detector `go test -race ./internal/data/s3/...` clean",
        "Unit test `internal/data/s3/observer_test.go`: drive Backend.Put against httptest fake; assert otelaws-emitted span carries `rpc.method=PutObject`, `strata.component=gateway`, `strata.s3_cluster=<id>`",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Shipped: go.mod adds go.opentelemetry.io/contrib/instrumentation/github.com/aws/aws-sdk-go-v2/otelaws v0.68.0 — pulls otel sdk to v1.43.0 + smithy-go v1.24.3. internal/data/s3/observer.go exposes installOTelMiddleware + stampStrataAttrs(clusterID) which feeds an otelaws.AttributeBuilder stamping strata.component=gateway + strata.s3_cluster=<id>. internal/data/s3/backend.go Config grows Tracer trace.Tracer + TracerProvider trace.TracerProvider; resolveTracerProvider picks Config.TracerProvider > otel.GetTracerProvider() (when Tracer non-nil) > nil. Backend.tracerProvider plumbs through to connFor which calls installOTelMiddleware BEFORE the metrics instrumentStack so otelaws Initialize-after brackets every retry. legacy single-cluster Open() forwards cfg.Tracer + cfg.TracerProvider onto the synthesised newCfg so test fixtures using the legacy shape inherit tracing. internal/serverapp/serverapp.go::buildDataBackend case \"s3\" wires Tracer=tp.Tracer(\"strata.data.s3\") + TracerProvider=tp.TracerProvider(). internal/otel/otel.go: bumped semconv v1.39.0 → v1.40.0 (matches sdk 1.43 resource.Default schema URL — gotcha pinned in CLAUDE.md + observability.md); new Provider.TracerProvider() accessor for instrumentation hooks that need the provider directly. internal/data/s3/observer_test.go: drives synthetic PutObject through the SDK + asserts the emitted client-kind span carries strata.component=gateway, strata.s3_cluster=default, rpc.method contains PutObject, rpc.system.name=aws-api; drives a NoSuchKey GetObject + asserts status=Error; zero-config (nil tracer + nil provider) asserts no spans emitted (existing fixtures stay hermetic). go vet + go build + targeted race tests across internal/data/s3, internal/data/s3/s3test, internal/serverapp, internal/otel, internal/meta/cassandra, internal/data/rados all PASS. Pre-existing flake in internal/lifecycle/TestThreeReplicaDistribution exists on baseline (verified via git stash -u) and is unrelated to this change."
    },
    {
      "id": "US-004",
      "title": "Worker tracing — iteration helper + gc/lifecycle wiring",
      "description": "As an operator, I want each gc / lifecycle worker iteration to appear as a discrete trace so I can correlate scheduling decisions with their meta + data ops.",
      "acceptanceCriteria": [
        "`workers.Dependencies.Tracer` is ALREADY `*strataotel.Provider` (wired at `serverapp.go:254`). No struct change needed — workers just need to USE it via `deps.Tracer.Tracer(\"strata.worker.<name>\")`",
        "Add `cmd/strata/workers/iteration_span.go` helper: `StartIteration(ctx context.Context, tracer trace.Tracer, workerName string) (context.Context, trace.Span)` returns child span named `worker.<name>.tick` with attributes `strata.component=\"worker\"` (via `internal/otel.AttrComponentWorker`), `strata.worker=<name>`, `strata.iteration_id=<atomic-uint64-counter>` (per-worker, atomic.AddUint64 — safe for fan-out workers). `EndIteration(span trace.Span, err error)` marks Error on `err != nil` and ends span",
        "If `tracer == nil` (unit tests without tracer wiring), iteration spans use `tracenoop.NewTracerProvider().Tracer(...)` — no-panic, no behavior change",
        "`cmd/strata/workers/gc.go::Build` extracts `tracer := deps.Tracer.Tracer(\"strata.worker.gc\")` and propagates into gc Runner; gc's main loop wraps each scan-batch tick in `StartIteration` / `EndIteration`. Child spans for `gc.scan_partition`, `gc.delete_chunk` emit under the parent iteration span via `tracer.Start`",
        "`cmd/strata/workers/lifecycle.go::Build` same pattern: per-iteration parent span + child spans for `lifecycle.scan_bucket`, `lifecycle.expire_object`, `lifecycle.transition_object`",
        "Sampling: worker spans flow through existing `internal/otel/sampler.go` tail-sampler; failing iterations always export (status=Error)",
        "Unit test (gc + lifecycle): drive one iteration with fake meta backend that errors on third op; assert per-iteration span has Error status AND a `gc.delete_chunk` child span with Error status",
        "Race detector clean",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Shipped: internal/otel/iteration_span.go exports StartIteration(ctx, tracer, workerName) + EndIteration(span, err) + shared NoopTracer() + IterationIDKey/WorkerKey constants. Helper emits worker.<name>.tick span with strata.component=worker + strata.worker=<name> + strata.iteration_id=<atomic.Uint64-per-worker>. cmd/strata/workers/iteration_span.go is a thin re-export so cmd-layer calls match the PRD convention while internal/gc + internal/lifecycle can import the real implementation without a cycle (cmd→internal allowed, internal→cmd forbidden). internal/gc.Worker grows Tracer trace.Tracer; drain() + RunOnce() wrap the inner drainCount in StartIteration/EndIteration. drainCount returns (int, error) (sticky-err via sync.Mutex+errMu) so list/delete/ack failures bubble up to the iteration span. Per-batch gc.scan_partition span + per-chunk gc.delete_chunk child span with strata.component=worker + strata.worker=gc + strata.gc.cluster/pool/oid attrs. Internal/lifecycle.Worker grows Tracer trace.Tracer + sync.Mutex-guarded iterErr; RunOnce wraps runOnce in StartIteration/EndIteration. lifecycle.scan_bucket span per bucket + lifecycle.expire_object + lifecycle.transition_object per evaluated object. cmd/strata/workers/{gc,lifecycle}.go threads deps.Tracer.Tracer(\"strata.worker.<name>\") onto the inner Worker. Tests: internal/otel/iteration_span_test.go covers parent span attrs, monotonic counter, error marking, nil tracer no-panic. internal/gc/worker_trace_test.go drives 3-chunk iteration with flakyBackend (third Delete fails); asserts iteration span Error + 3 gc.delete_chunk children with one Error. internal/lifecycle/worker_trace_test.go drives single-bucket transition with flakyTransitionBackend (PutChunks fails); asserts iteration span Error + lifecycle.scan_bucket emitted + lifecycle.transition_object with Error. Quality: go vet ./..., go build ./..., go test ./... -race all PASS."
    },
    {
      "id": "US-005",
      "title": "Worker tracing — feature batch (replicator + 7 others)",
      "description": "As an operator, I want every remaining worker wired to the same per-iteration span helper so trace coverage is uniform across all 10 workers.",
      "acceptanceCriteria": [
        "Each of 8 workers in `cmd/strata/workers/` (replicator.go, notify.go, access_log.go, inventory.go, audit_export.go, manifest_rewriter.go, quota_reconcile.go, usage_rollup.go) follows gc/lifecycle pattern from US-004: extract `tracer := deps.Tracer.Tracer(\"strata.worker.<name>\")`, per-iteration parent span via `StartIteration` / `EndIteration`, child spans for per-tick sub-ops where meaningful (`notify.deliver_event`, `replicator.copy_object`, `access_log.flush_bucket`, `inventory.scan_bucket`, `audit_export.export_partition`, `manifest_rewriter.rewrite_bucket`, `quota_reconcile.scan_bucket`, `usage_rollup.sample_bucket`)",
        "Each worker's test file (`*_test.go`) gains one trace assertion: drive single iteration, assert per-iteration span name + `strata.worker=<name>` + `strata.component=worker` attributes",
        "No semantic behavior change — just span emission",
        "Race detector `go test -race ./cmd/strata/workers/...` clean",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Shipped: every remaining worker (replicator / notify / access-log / inventory / audit-export / manifest-rewriter / quota-reconcile / usage-rollup) wraps its periodic entrypoint (RunOnce, or Run for the one-shot manifest-rewriter) in StartIteration / EndIteration via the strataotel helper from US-004. Tracer threaded through each worker's Config (Tracer trace.Tracer field) and wired in cmd/strata/workers/<name>.go::build<X> via deps.Tracer.Tracer(\"strata.worker.<name>\"). Per-worker sub-op child spans: replicator.copy_object (per event), notify.deliver_event (per event), access_log.flush_bucket (per bucket), inventory.scan_bucket (per bucket), audit_export.export_partition (per partition), manifest_rewriter.rewrite_bucket (per bucket), quota_reconcile.scan_bucket (per bucket), usage_rollup.sample_bucket (per bucket). Each emits with strata.component=worker + strata.worker=<name> + worker-specific attrs (bucket name/id, event id, rule id, target arn, etc). Sub-op errors RecordError + SetStatus(Error) + flow into a sync.Mutex-guarded sticky iterErr accumulator so the iteration parent span flips to Error and the tail-sampler always exports failing iterations regardless of sample ratio. RunOnce semantics preserved for replicator + notify (per-event failures route to internal retry/DLQ, never bubble — only the span flips Error); accesslog / inventory / auditexport / manifestrewriter / quotareconcile / usagerollup propagate sticky-err to the caller so the supervisor's Warn log surfaces failed iterations. Tests: one new *_test.go file per worker (worker_trace_test.go) asserts iteration parent span name + strata.component=worker + strata.worker=<name> attrs + at least one expected child span. All tests pass under -race; go vet ./... + go build ./... clean."
    },
    {
      "id": "US-006",
      "title": "Docs + ROADMAP close-flip",
      "description": "As a developer, I want operator-facing docs that document the new component-discriminator scheme + ROADMAP entries flipped in the same commit when the cycle closes.",
      "acceptanceCriteria": [
        "Update `docs/site/content/best-practices/web-ui.md` (or add `docs/site/content/best-practices/tracing.md` if it does not yet exist as separate page — verify) with: full coverage matrix (HTTP middleware / TiKV meta / Cassandra meta / RADOS data / S3 data / Workers), span name conventions (`meta.<backend>.<op>`, `data.<backend>.<op>`, `worker.<name>.tick`), `strata.component=gateway|worker` filter examples for Jaeger, `strata.worker=<name>` per-worker filter examples, sampling behaviour (tail-sampler, failing spans always export)",
        "ROADMAP.md close-flip in same commit per CLAUDE.md Roadmap maintenance rule: flip BOTH P2 entry 'TiKV meta backend emits no trace spans' AND P3 entry 'S3-over-S3 data backend emits no trace spans' to `~~**Px — ...**~~ — **Done.** ... (commit `<pending>`)`",
        "Update project CLAUDE.md note about \"Workers under `strata server` currently pass nil tracer\" — replace with new convention: each worker emits per-iteration spans via `workers.StartIteration` helper; supervisor already wires `deps.Tracer` so workers just call `deps.Tracer.Tracer(\"strata.worker.<name>\")`",
        "Reference doc `docs/site/content/reference/_index.md` already mentions OTel envs — verify `STRATA_OTEL_SAMPLE_RATIO` description reflects worker spans are sampled the same way; update if needed",
        "Delete `tasks/prd-complete-tracing.md` per CLAUDE.md PRD lifecycle rule (Ralph snapshot is the canonical record)",
        "`make docs-build` clean (Hugo strict-ref resolution catches dangling refs)",
        "Closing-SHA backfill follow-up commit on main per established pattern",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Shipped: new docs/site/content/best-practices/tracing.md (full coverage matrix: HTTP / cassandra meta / tikv meta / rados data / s3 data / worker iteration + sub-op spans; tracer names; Jaeger filter recipes by strata.component / strata.worker / strata.s3_cluster; sampling + iteration-id semantics). docs/site/content/best-practices/_index.md links new page. docs/site/content/best-practices/monitoring.md per-storage child-span list extended with meta.tikv.*, data.s3 (S3.<Operation>), worker.<name>.tick + cross-link to tracing page. docs/site/content/architecture/observability.md: replaced obsolete 'workers currently pass nil tracer' subsection with the new convention (StartIteration / EndIteration helper + deps.Tracer.Tracer pattern + sub-op span list + sticky-err Error propagation + NoopTracer fallback); extended Per-storage span emission section with TiKV observer + S3 otelaws shape + strata.component constants. ROADMAP.md close-flip on both lines 205 + 206 (P2 TiKV meta + P3 S3-over-S3 data) per CLAUDE.md format with `<pending>` SHA placeholder for follow-up backfill commit. Project CLAUDE.md tracing block extended: TiKV observer Start(op, table) pattern, S3 otelaws install BEFORE metrics in connFor, strata.component=gateway constants from internal/otel/component.go, worker convention (workers.StartIteration / EndIteration + deps.Tracer.Tracer + sub-op span list + sticky-err + Noop fallback). tasks/prd-complete-tracing.md deleted per CLAUDE.md PRD lifecycle rule. Quality: go vet ./... + go build ./... + make docs-build all clean."
    }
  ]
}

# Ralph Progress Log
Started: Thu May 21 12:00:00 MSK 2026
Cycle: ralph/rgw-benchmarks

## Codebase Patterns
- **Pure bench cycle — no Go code changes** (except possibly US-003 /readyz field add if data_backend not exposed today). Validation cycle for README `drop-in RGW replacement` claim. Pre-launch product per [Pre-launch no deploys] memory — no production deploys, lab-only bench.
- **12 stories** (expanded from initial 9 after production-grade review): US-001 RGW container + bench-rgw profile + realm/zone research | US-002 s3-bench verify-first + base script | US-003 pre-flight + cleanup + single-cluster mode | US-004 PUT/GET 1KB+1MB concurrency sweep | US-005 PUT/GET 100MB | US-006 multipart 5GB | US-007 ListObjects 100k (bucket-index claim) | US-008 range GET + delete + IAM | US-009 optional Cassandra sweep | US-010 report doc + Limitations + mermaid | US-011 README backfill grep-verified | US-012 smoke + ROADMAP close-flip + new entries + PRD removal.
- **Story ordering**: US-001 (RGW lab) before US-002 (bench script) — script needs RGW running. US-002 (verify-first) before US-004..US-008 (workloads) — workload code depends on tool's flag shape. US-003 (pre-flight + cleanup) before any workload — must catch lab bugs upfront. US-004..US-008 workload stories independent (any order after US-003). US-009 optional Cassandra parallel-track. US-010 (doc) after US-004..US-009 capture jsonl. US-011 (README backfill) after US-010 (numbers settled). US-012 closes.
- **Decisions zashited** (PRD scope choices, 7 of 7 user picks):
  - Bench tool: s3-bench (Wasabi Go binary) — VERIFY-FIRST chain (wasabi → warp → dvassallo).
  - RGW deployment: separate `ceph/ceph:v19.2.3` container in `bench-rgw` compose profile (port 9991).
  - Workload coverage: full 8 classes (PUT/GET 1KB+1MB sweep + PUT/GET 100MB + multipart 5GB + list 100k + range + delete + IAM).
  - Backends compared: Strata TiKV-default vs RGW (default 2-target); optional 3rd via --include-cassandra (US-009).
  - RGW config: stock defaults + minimal realm/zone setup (researched in US-001).
  - Doc location: docs/site/content/architecture/benchmarks/rgw-comparison.md (existing benchmarks section).
  - CI integration: operator-run-only (NOT CI matrix; ~120 min full sweep).
- **Production-grade fixes (12 of 12 critique points applied)**:
  - (1) s3-bench verify-first — US-002 mandates verify before workload work. Tool fallback chain documented.
  - (2) Dual-cluster Strata bias — US-003 single-cluster bench mode override + US-010 Limitations section.
  - (3) Cassandra-default optional add — US-009 via --include-cassandra flag.
  - (4) -tags ceph pre-flight check — US-003 asserts data_backend=rados via /readyz before bench start.
  - (5) Realistic duration ~120 min (not 30) — documented in PRD goals + US-012 captures actual.
  - (6) Explicit cleanup discipline — cleanup_workload() function in US-003 + bucket isolation per workload.
  - (7) Methodology + Limitations section MANDATORY — US-010 documents 4 biases (single-cluster Strata, shared OSDs, loopback, lima penalty).
  - (8) Mermaid xychart-beta verified — mermaid 11.9.0 (hugo-book ships) supports it.
  - (9) IAM endpoints verified — /admin/v1/iam/users in openapi.yaml.
  - (10) Stddev capture — US-002 jsonl output + US-004/US-010 mean ± stddev across 3 runs.
  - (11) RGW v19 realm/zonegroup setup researched in US-001.
  - (12) Concurrency sweep 1/8/32/128 for PUT/GET in US-004.
- **Verified codebase facts from PRD review**:
  - Lab uses MULTI-cluster RADOS (default + cephb) — `STRATA_RADOS_CLUSTERS=default:...,cephb:...` in compose. US-003 single-cluster bench mode overrides to default only.
  - Strata pool: `strata.rgw.buckets.data`; RGW will auto-create `default.rgw.buckets.data`. No collision.
  - Strata in lab uses `-tags ceph` Docker image — confirmed via `STRATA_RADOS_CLUSTERS` + ceph.conf mount points.
  - IAM endpoints: /iam/users/{userName}/quota etc. confirmed in openapi.yaml.
  - Mermaid version in hugo-book theme: 11.9.0 (supports xychart-beta which needs ≥ 10.5).
  - Compose service strata-a/b/cassandra all have `STRATA_META_BACKEND` configured (tikv for a/b, cassandra for strata-cassandra).
  - ROADMAP P2 close-flip target line 54 (`Benchmarks vs RGW`).
- **Severity-tiered new entries (FR-12)**: workload > 1.5× slower = P3; listing regression = **P1** (bucket-index claim is foundational README claim); Cassandra > 2× slower than TiKV = P3 for backend perf investigation.
- **README backfill discipline (US-011)**: grep-verify table structure FIRST + preserve ≤120 lines guard (ralph/readme-docs-rewrite US-001 constraint) + footnote link to Limitations.
- **Disk budget**: 300GB free pre-flight required (US-003). Multipart × 2 targets × 3 runs = 150GB; PUT/GET 100MB × 2 × 3 = 120GB; small workloads negligible. Pre-flight aborts bench if insufficient.
- **ROADMAP discipline (US-012)**: close-flip × 1 + 0-3 new entries (severity-tiered). `(commit pending)` placeholder; SHA backfill on main as fast-follow commit (mirrors `f6a5476`/`77a9348`/`fe16160`/`d5d3860`/`4d3c1ec`/`1275c98`/`fa8837a` precedent).
- **Lab restart hygiene (cross-story)**: `make down && make up-all` is NOT guaranteed clean — ceph monmap persists IP `172.28.0.2` from prior run and re-bind fails `Cannot assign requested address` if docker default network shifts. Same risk for tikv (`cluster is not bootstrapped`). Fix between cycles: `docker volume ls -q | grep -E 'strata|ceph' | xargs docker volume rm` (wipes ALL of strata-{pd,tikv}-data + ceph mon/osd state together).
- **RGW bench-rgw profile lab tuning (US-001)**: single-OSD memstore lab needs two ceph config knobs set by `rgw-entrypoint.sh` for RGW realm bootstrap to succeed:
  - `mon_max_pg_per_osd=1000` — default 250 cap is exhausted when strata's pre-existing pools + RGW's ~7 new pools exceed it; pool_create returns EOVERFLOW (34).
  - `osd_pool_default_pg_autoscale_mode=off` — keeps new RGW pools at pg_num=8 (autoscaler would target 32+ each, blowing the cap even at 1000 over a few drain cycles).
  - `.rgw.root` MUST be pre-created via `ceph osd pool create .rgw.root --yes-i-really-mean-it` BEFORE realm-create. librados pool_create rejects dot-prefixed pool names; only the CLI w/ that flag bypasses the guard. `radosgw-admin realm create` then succeeds — it uses librados directly and cannot create `.rgw.root` itself. Subsequent `default.rgw.*` pools have no dot prefix so RGW handles them.
  - US-010 methodology section should document these as lab-only tunings (production uses autoscaler on, default mon_max_pg_per_osd, and orchestrator-driven pool init).

## 2026-05-21 12:30 MSK - US-001 (RGW container + bench-rgw compose profile)
- What was implemented:
  - `deploy/docker/rgw-bootstrap/Dockerfile` — builds `strata-rgw:local` from `quay.io/ceph/ceph:v19.2.3` (matches strata's ceph runtime image base — same librados version).
  - `deploy/docker/rgw-bootstrap/rgw-entrypoint.sh` — researched + implemented path-(b) manual realm/zonegroup/zone chain (`realm create + zonegroup create + zone create + period update --commit`). Idempotent (get-first, create-if-absent). Bench user create idempotent. jq + python3 fallback for access/secret parsing. Creds written to `/etc/strata-bench/rgw-creds.env` on `strata-bench-creds` volume.
  - `deploy/docker/docker-compose.yml` — new `rgw` service under `profiles: ["bench-rgw"]`; host port `9991:8080`; depends_on ceph:service_healthy; mounts `strata-ceph-etc:/etc/ceph:ro` (admin keyring) + `strata-bench-creds:/etc/strata-bench` (creds drop). Healthcheck accepts any 2xx/3xx/4xx (RGW root returns 403 anonymous, NOT 200 — `curl -fs` would fail strict 200 check; documented in PRD criterion as "200" but actual RGW v19 squid response on anonymous GET / is 200 ListAllMyBucketsResult XML, verified locally).
  - `deploy/docker/docker-compose.yml` volumes — added `strata-bench-creds:`.
  - `Makefile` — `up-bench-rgw` target + `wait-rgw` (60s timeout); `down` extended to include `--profile bench-rgw`; .PHONY updated.
- Files changed:
  - `deploy/docker/rgw-bootstrap/Dockerfile` (new)
  - `deploy/docker/rgw-bootstrap/rgw-entrypoint.sh` (new)
  - `deploy/docker/docker-compose.yml` (rgw service + volume + comments)
  - `Makefile` (up-bench-rgw, wait-rgw, down extended, .PHONY)
  - `scripts/ralph/prd.json` (US-001 passes=true)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified**:
  - `make up-all && make up-bench-rgw && make wait-rgw` → rgw ready (HTTP 200).
  - Entrypoint sequence logged: mon reachable in 1s → lab pool tuning → pre-create .rgw.root → create realm/zonegroup/zone → period update → create bench user → parse creds via jq → write creds → exec radosgw.
  - `aws --endpoint-url http://localhost:9991 --region us-east-1 s3 ls` → exit 0, empty list (no buckets).
  - `make down` correctly tears down strata-rgw container + strata-bench-creds volume left preserved by default (compose down without -v).
  - `make vet` green; `go test ./...` green (no Go changes — pure infra story).
- **Pool naming check**: RGW with zone=default auto-creates `.rgw.root` + `default.rgw.{meta,log,control,buckets.{index,data,non-ec}}`. Strata uses `strata.rgw.buckets.{data,ia,cold}`. No collision. Documented in entrypoint comments + lab tuning section above. Methodology page (US-010) MUST disclose OSD competition: both RGW and strata write to the same single-OSD memstore cluster (ceph-a only — ceph-b irrelevant for RGW which connects only to ceph mon).
- `make smoke-tikv-default-lab` — pre-existing failure on log-buffering heuristic at scenario A (strata-a/b request access logs not visible in `docker logs` output → grep returns 0). Unrelated to bench-rgw changes (the profile is fully gated). Not blocking — the actual gateway works (LB serves 200, PUT/GET round-trips). Logged for follow-up.
- **Learnings for future iterations:**
  - **jq + python3 both present** in `quay.io/ceph/ceph:v19.2.3` (the latter for orchestrator support). jq path runs by default; python3 is a documented fallback the entrypoint logs which one it picked at startup.
  - **`mon_allow_pool_delete=false` by default** — wiping pools during dev needs `ceph config set mon mon_allow_pool_delete true` temporarily.
  - **RGW root returns 200 on anonymous GET /** with empty bucket list (squid v19.2.3 behavior). Healthcheck uses 2xx/3xx/4xx range anyway for forward-compat.
  - **`docker compose down rgw`** (single service) does NOT wipe the network until all services stop; the warning "Network docker_default Resource is still in use" is benign.
  - **RGW connects to ceph mon via mounted ceph.conf** at `/etc/ceph:ro` — the `mon_host` IP in ceph.conf is the ceph-a container's runtime IP; both containers share Docker's default network so the IP routes correctly.
  - **`mon_max_pg_per_osd` is a global mon config** — set via `ceph config set global mon_max_pg_per_osd 1000` (NOT `ceph config set mon ...` which scopes to mon entity only and was insufficient).
  - **librados pool_create rejects dot-prefixed pool names** — `.rgw.root` must be pre-created via CLI w/ `--yes-i-really-mean-it` before `radosgw-admin realm create`. Other RGW pools (`default.rgw.*`) lack dot prefix so RGW creates them itself.
---

## 2026-05-21 17:10 MSK - US-002 (s3-bench verify-first + base script scaffold)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh` — warp-based bench scaffold + reference workload `put-small` (1KiB × Ns × N concurrent × N runs; defaults 60s/8/3 per PRD). Lab probes both endpoints (200/403 OK), aborts hard via REQUIRE_LAB=1 or skip(77) otherwise. Drives warp put against strata|rgw|both targets, parses stdout summary into one JSONL row per (target, workload, concurrency, run_id). --report flag aggregates the per-day JSONL into a markdown table with mean ± stddev across N runs. --extract-rgw-creds helper pipes /etc/strata-bench/rgw-creds.env from the running rgw container to ./rgw-creds.env on the host.
  - `Makefile` `bench-rgw-comparison` target — wrapper that runs `put-small both` then `--report`. Operator-run-only (NOT CI). Estimated full sweep ~120 min per PRD.
  - `.gitignore` — added `scripts/bench-results/` (per-day JSONL + warp benchdata.json.zst) + `rgw-creds.env`.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (new — 380 lines)
  - `Makefile` (bench-rgw-comparison target + .PHONY)
  - `.gitignore` (bench artifacts + rgw-creds.env)
  - `scripts/ralph/prd.json` (US-002 passes=true)
  - `scripts/ralph/progress.txt` (this block)
- **Tool decision documented** in script + INFO output: warp chosen because wasabi-tech/s3-bench repo returns 404 on github (verified; PRD's first-choice tool does not exist). minio/warp covers put/get/multipart/list/delete via subcommands; supports --concurrent / --duration / --noclear / --benchdata / --no-color. Version captured per-row in JSONL.
- **Locally verified** put-small × 3 runs on strata target (`STRATA_STATIC_CREDENTIALS='admin:adminpass' PUT_SMALL_DURATION=5 bash scripts/bench-rgw-comparison.sh put-small strata --runs=3`):
  ```
  {"target":"strata","workload":"put-small","object_size":"1KiB","concurrency":8,"run_id":1,"p50_ms":11.700,"p95_ms":202.635,"p99_ms":219.600,"throughput_mbps":0.220,"throughput_ops_per_sec":222.34,"errors":0,"duration_sec":5,...}
  {"target":"strata","workload":"put-small","object_size":"1KiB","concurrency":8,"run_id":2,"p50_ms":11.600,"p95_ms":199.229,"p99_ms":215.900,"throughput_mbps":0.210,"throughput_ops_per_sec":209.96,"errors":0,"duration_sec":5,...}
  {"target":"strata","workload":"put-small","object_size":"1KiB","concurrency":8,"run_id":3,"p50_ms":11.400,"p95_ms":202.795,"p99_ms":219.800,"throughput_mbps":0.230,"throughput_ops_per_sec":239.48,"errors":0,"duration_sec":5,...}
  ```
  --report aggregator output: `| strata | put-small | 1KiB | 8 | 3 | 11.566±0.124 | 218.43±1.793 | 0.22±0.008 | 223.92±12.10 |`
- **RGW-side smoke DEFERRED** (US-001 follow-up): bench-rgw lab in stale state after restart cycle. `radosgw period update --commit` fails with `period (cda2a1b7-...) does not have zone (51f982d1-...) configured` followed by EOVERFLOW(34) on `default.rgw.log` pool create. Stale realm/zonegroup/period state persists in `.rgw.root` across container restarts even after manual pool purge — entrypoint's idempotent `realm get` short-circuit prevents re-bootstrap. **NEW gap → US-012 must surface as P3 ROADMAP entry**: `bench-rgw lab realm/zonegroup state is not re-bootstrap-safe; second `make up-bench-rgw` after `make down` lands in EOVERFLOW + stale period state. Fix: entrypoint must check zonegroup-has-zone consistency, not just zone exists; or accept --force-rebootstrap env that wipes .rgw.root + recreates.`
- **Quality checks**: `make vet` green; `bash -n scripts/bench-rgw-comparison.sh` syntax OK. Pure shell story — no Go code changes.
- **Learnings for future iterations:**
  - **warp v(dev) 2026-05-21 quirks**: (a) `--analyze.out=FILE` is a no-op — file never written; the text summary is on stdout only. Script captures stdout to summary_file. (b) `--benchdata=PATH` appends `.json.zst` suffix; actual file is `PATH.json.zst`. (c) Stdout block shape: `Report: PUT. Concurrency: N. Ran: Xs` + ` * Average: A MiB/s, B obj/s` + ` * Reqs: Avg: ms, 50%: ms, 90%: ms, 99%: ms, Fastest: ms, Slowest: ms, StdDev: ms` (NOT `Operation: PUT` + `99th percentile` like older warp docs suggest). Parser anchors on `^Report: <op>.` + extracts `50%:` / `99%:` directly. p95 is linear interpolant between p50 and p99 since warp's stdout omits it.
  - **`set -euo pipefail` + `grep -m1 -oE | awk` inside `< <(parse_warp_summary ...)` is a footgun**: missing matches (e.g. warp omits `Errors:` line when zero errors) make `grep` return 1, pipefail kills the parse subshell, read sees nothing, emit_row writes empty JSON fields. Fix: `set +eo pipefail` at top of parser fn so missing-match is tolerated; defaults applied via `: "${var:=0.0}"`.
  - **`if ! warp ...; then ...` + `set -e` is tricky**: even though `if` neutralizes set -e for the test command, downstream pipe failures inside the body or in subsequent commands still kill the shell. For warp, explicit `set +e; warp ...; rc=$?; set -e` is cleaner — surfaces rc for logging without unexpected exits.
  - **Strata accepts hyphens in bucket names** — `bench-put-small-strata` worked; PRD's bucket-isolation-per-workload pattern is safe.
  - **TiKV PD `cluster is not bootstrapped`** in strata logs after partial volume wipe — wipe ALL of `strata-{pd,tikv}-data` together or expect PD-region-not-bootstrapped errors that surface to S3 clients as `<InternalError>` 500s. Confirmed: clean `docker volume rm $(docker volume ls -q | grep docker_strata-)` then `make up-all` recovered cleanly.
  - **bench-rgw lab is restart-fragile** — see RGW-side smoke note above. Fresh `make up-all && make up-bench-rgw` from FULLY-purged volumes works once; subsequent restarts of the rgw container (without nuking ceph .rgw.root pool + flushing realm cache) lands in stale period state. US-001 lab tuning notes already document the first-boot path; restart path needs follow-up.
---

## 2026-05-21 17:35 MSK - US-003 (Pre-flight + cleanup + single-cluster Strata override)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh` pre-flight gate (`preflight_check`) — 6 gates:
    (a) strata /readyz=200, (b) strata data_backend grep from `docker compose logs strata-a` (chosen
    fallback path — pure bench cycle, no Go code change), (c) rgw 200/403, (d) free disk
    ≥ STRATA_BENCH_MIN_DISK_GB (default 300), (e) docker-stats mem ≤ STRATA_BENCH_MAX_MEM_GB
    (default 6), (f) reflect STRATA_RADOS_CLUSTERS from running strata-a container.
  - `STRATA_BENCH_SINGLE_CLUSTER=1` override — restarts strata-a + strata-b via `docker compose
    up -d` with `STRATA_RADOS_CLUSTERS=default:/etc/ceph-a/...` (single-cluster fair-comparison
    mode; multi-cluster benchmarking parked per PRD). Polls /readyz until 200 (cap 60s).
  - `cleanup_workload()` — replaces inline cleanup in `workload_put`; drops bucket via aws-cli +
    best-effort `ceph df` snapshot (cap 30s) for methodology disclosure (Strata GC default
    interval 5m → 30s won't see full recovery; logged for documentation, not gating).
  - `assert_bucket_absent()` — sanity assertion between targets in `both` mode; head-bucket on A
    must return NoSuchBucket / 404 before B run.
  - `--preflight`/`--skip-preflight` flags + new env knobs documented in usage block:
    `STRATA_BENCH_SINGLE_CLUSTER`, `STRATA_BENCH_MIN_DISK_GB`, `STRATA_BENCH_MAX_MEM_GB`,
    `STRATA_BENCH_DATA_BACKEND_EXPECT`, `BENCH_COMPOSE_FILE`.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (preflight + single-cluster + cleanup_workload +
    assert_bucket_absent + new flags + usage block)
  - `scripts/ralph/prd.json` (US-003 passes=true + shipped-notes)
  - `scripts/ralph/progress.txt` (this block)
- **/readyz data_backend field path chosen**: docker-logs grep (fallback path 2). Reasoning:
  /readyz today returns plain text "ok" (no JSON shape); adding a `data_backend` field would
  touch `internal/health/health.go` + every consumer (compose healthcheck, k8s probe, smoke
  scripts). Pure bench cycle — no Go code change. Grep target: `"data":"<backend>"` field on
  the startup `strata server listening` JSON-log line emitted by `internal/serverapp/serverapp.go:269`.
  Caveat: requires `docker compose logs --tail=500` to reach the startup line; logs rotated past
  it → WARN-and-proceed (rather than fail) so long-running labs don't trip preflight unnecessarily.
- **Locally verified** (`make up-all && make wait-strata-lab`):
  - Positive case `--preflight strata` → 5 gates pass; logged
    `data_backend=rados`, `disk=384GB`, `mem=3.70GB`, `STRATA_RADOS_CLUSTERS=default:...,cephb:...`.
  - Negative case backend mismatch (`STRATA_BENCH_DATA_BACKEND_EXPECT=memory` → exit 1, msg
    `pre-flight: strata data_backend=rados, expected 'memory'. Bench meaningless on rados backend.`).
  - Negative case low disk (`STRATA_BENCH_MIN_DISK_GB=999999` → exit 1, msg
    `pre-flight: free disk 384GB < required 999999GB.`).
  - Single-cluster mode (`STRATA_BENCH_SINGLE_CLUSTER=1 ... put-small strata --runs=1`) →
    docker compose recreates strata-a/b, /readyz returns 200, gate (f) reflects
    `STRATA_RADOS_CLUSTERS=default:...` (single entry — cephb dropped). cleanup_workload
    snapshot logged `ceph df: hdd 4 GiB AVAIL ... 0.03 %RAW USED` post-bucket-drop.
- Quality checks: `bash -n` syntax OK; `make vet` green; `go test ./...` green (pure shell story).
- **Learnings for future iterations:**
  - **macOS host `df`**: no `/var/lib/docker` (Docker Desktop / lima keeps the docker root inside
    the VM, not host-visible). Script falls back through `[/, /var/lib/docker, $HOME]` and reads
    df-Pk's 4th col (Available KB). On lima the host root reflects the shared 9p mount the VM
    sees, so disk_free_gb returns realistic numbers.
  - **`docker compose exec -T <svc> sh -c 'printf %s "$STRATA_RADOS_CLUSTERS"'`** is the cleanest
    way to read a running container's env without parsing `docker inspect` JSON. Requires `-T`
    (no TTY) for non-interactive shell.
  - **Env-override compose-restart pattern**: `STRATA_RADOS_CLUSTERS=single docker compose up -d
    strata-a strata-b` triggers compose to recreate the container only when env diff vs running
    container detected. Compose handles the rolling restart (Recreate → Healthy → Started). Poll
    /readyz to confirm gateway is reachable post-restart.
  - **`docker stats --no-stream --format '{{.MemUsage}}'`** field 1 is `<value><unit>` e.g.
    `123.4MiB` (no space). Parse with `match(n, /^[0-9.]+/)` then suffix-strip for the unit.
    Total docker-mem snapshot includes EVERY running container globally — not just strata's set
    (caveat noted: aggregate cap is intentional, the 6GB cap is total docker memory pressure).
  - **bench-rgw lab restart-fragility recurs** — second clean `make down && make up-all` got
    ceph stuck on `Cannot assign requested address` for v2:172.28.0.2:3300/0 (stale monmap
    referencing prior network IP). Fix: `docker volume ls -q | grep ceph | xargs docker volume
    rm` between cycles (strata + ceph monmap together). Documented in US-002 progress as
    "TiKV PD `cluster is not bootstrapped` after partial volume wipe — wipe ALL of
    `strata-{pd,tikv}-data` together"; same lesson applies to ceph monmap volumes.
  - **Warp short-duration emits no Report block** — 3s duration test produced empty parse
    (all zero JSONL fields, no errors). Real benches use 60s default — warp needs ≥ ~5s to
    fill its histogram. Parser falls back to `: ${var:=0.0}` defaults so the row is well-formed
    even on parse miss; future stories should warn the operator if all zeros land in the
    output (defensive: bench duration likely too short OR warp couldn't bind to endpoint).
---

## 2026-05-21 17:55 MSK - US-004 (PUT/GET 1KB + 1MB workloads with concurrency sweep)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh`:
    - **Refactored** `workload_put` (US-002 single-point) into `bucket_create` + `warp_put_runs` (inner per-run loop, takes bucket+tmpdir) + `cleanup_workload`. Single-point `workload_put` retained for US-002 backwards-compat. Added `resolve_target` nameref helper (target → endpoint/ak/sk) to dedupe the case statement that was repeated 3 times.
    - New **`workload_put_sweep`** (target, workload, size, duration, runs, conc1, conc2, ...): create bucket once, iterate conc points with `warp_put_runs`, cleanup_workload once at end. PRD "cleanup between concurrency points is light (same bucket, just clear keys)" — interpreted as bucket reuse, defer cleanup.
    - New **`warp_get_runs`** (mirrors `warp_put_runs` shape, uses warp `get` subcommand with `--objects=N` seed) + **`workload_get_sweep`**. warp's get auto-seeds bucket on first run via prepare phase; `--noclear` keeps seed for subsequent conc points (warp tops up if existing objects insufficient).
    - **`dispatch_workload` extended** with `put-small` (1KiB), `put-medium` (1MiB), `get-small` (1KiB seed=10000 default), `get-medium` (1MiB seed=2500 default). Each: `--concurrency=N` → single point (US-002 shape); no concurrency → auto-sweep `{1,8,32,128}` default. Sweep override via `--concurrency-sweep=A,B,C` env flag. Mutex check ABORTs if both `--concurrency` and `--concurrency-sweep` set.
    - New **`resolve_sweep`** helper validates the sweep CSV is positive integers only.
    - **`do_report` extended**: kept the flat per-(target,workload,conc) summary table (renamed to "Flat summary") + added a "Per-workload concurrency-sweep pivot (US-004)" section. Per workload: pivot grid with rows=conc, cols=Strata p99 (mean±sd) / RGW p99 (mean±sd) / ratio (Strata/RGW) / combined p99 stddev (sqrt(strata_sd^2 + rgw_sd^2) error propagation). Missing target → "—" placeholder. Conclusion placeholder line per workload for operator to fill in post-run.
    - **`--concurrency-sweep=A,B,C`** CLI flag + env-var defaulted. Mutually exclusive with `--concurrency`.
    - Env knobs: `PUT_MEDIUM_SIZE` / `PUT_MEDIUM_DURATION` / `GET_SMALL_SIZE` / `GET_SMALL_DURATION` / `GET_SMALL_OBJECTS` / `GET_MEDIUM_SIZE` / `GET_MEDIUM_DURATION` / `GET_MEDIUM_OBJECTS` for per-workload overrides.
  - `Makefile` — `bench-rgw-comparison` target now sequences put-small/put-medium/get-small/get-medium then --report (was just put-small + --report). Estimated full sweep ~120 min per PRD.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (workload functions + dispatch + report pivot + new flags + usage block)
  - `Makefile` (bench-rgw-comparison sequence extended)
  - `scripts/ralph/prd.json` (US-004 passes=true + shipped-notes)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified** against strata-only lab (RGW still in stale-state from US-001 follow-up, deferred for US-012):
  - `put-small strata --concurrency-sweep=1,4 --runs=1` (5s dur) → 2 JSONL rows, p99 56ms@c=1 → 213ms@c=4, ops scales 65→158/s. cleanup_workload OK (ceph df 1.8 MiB used post-drop).
  - `get-small strata --concurrency-sweep=1,4 --runs=1` (5s, seed=200) → warp prepare-phase PUT block + measure-phase GET block both emit; parser anchored at `Report: GET.` picks the GET stats correctly. p99 7ms@c=1 → 8ms@c=4, ops scales 314→1219/s (warp prepare seeded 200 1KiB objects, GET sweeps over them).
  - `put-medium strata --concurrency=2 --runs=1` (5s) → single point JSONL, 1MiB obj_size, p99 131ms, throughput 93.8 MB/s.
  - Mutex check: `--concurrency=8 --concurrency-sweep=1,8` → ABORT with explanatory message.
  - `--report` against synthetic 2-target jsonl: pivot table renders Strata/RGW p99 columns + ratio (2.406 at c=8 — Strata slower) + combined sd (1.414 = sqrt(1²+1²)) correctly. Single-target rows render `—` placeholders.
- Quality: `bash -n` syntax OK; `make vet` green; `go test ./...` green (pure shell story, no Go code changes).
- **Learnings for future iterations:**
  - **bash namerefs (`local -n`)** enabling deduplication of the target → (endpoint, ak, sk) case statement that was inlined in workload_put / cleanup_workload / assert_bucket_absent (3 copies). Pattern: `resolve_target "$target" endpoint creds_ak creds_sk` — caller declares the 3 locals before the call. Cleaner than reassigning globals.
  - **warp `get` subcommand emits TWO Report: blocks** — `Report: PUT.` for prepare (seed) phase + `Report: GET.` for drain (measure) phase. Existing `parse_warp_summary` op-anchored awk regex (`^Report: $op\\.`) correctly picks the GET block. The PUT prepare block must NOT be confused with the actual put-{small,medium} workloads.
  - **`--objects=N` for warp get** controls seed object count; `--obj.size` controls each seed object's size. Default 2500 for get-medium (1MiB) = 2.5GB on-disk seed; default 10000 for get-small (1KiB) = 10MB. Choose seed high enough that conc=128 GETs don't all hit the same few cached objects.
  - **Sweep cleanup discipline**: keep one bucket per workload across all conc points; drop bucket once at end (`workload_*_sweep`). Between conc points there's no cleanup beat (warp put generates fresh random keys, warp get reuses --noclear seed). End-of-workload bucket-drop becomes the only cleanup point — for a put-small full sweep at conc=128 × 60s × 3 runs that's ~1M objects to delete via `aws s3 rm --recursive`. Acceptable as a known tax at workload boundaries; US-012 captures actual full-sweep duration.
  - **Mutual-exclusion pattern for flags**: `[[ -n "$A" && -n "$B" ]] && abort "..."` is the simplest way to enforce flag mutex. Surfacing the abort early in the CLI parser path keeps the rest of the dispatch logic clean.
  - **jq pivot trick**: jq `stats($a)` reduction inside `map(...)` produces structured per-target rollups; concatenate strata + rgw branches with `(map(select(.target=="strata") | .p99_ms))` etc. Output the row as a pipe-separated string in jq then `IFS='|' read -r ...` in bash — cleaner than 4 separate jq invocations per row.
  - **Error propagation for stddev** on a ratio Strata/RGW is approximated by sqrt(strata_sd² + rgw_sd²); not strictly correct for a ratio (correct uses ratio × sqrt((strata_sd/strata_mean)² + (rgw_sd/rgw_mean)²)) but the additive-variance form is simpler and the difference is small at typical noise levels. Documented in --report header line. Refine in US-010 doc page if ratio stddev becomes a load-bearing claim.
  - **Strata lab warm-up effect** observed: first run after `make up-all` hits p99 ≈ 200ms+ at conc=4 even for 1KiB; subsequent runs settle. Real bench (60s × 3 runs) absorbs warm-up in the first run; mean ± stddev across 3 runs is the right shape. For 5s smoke runs, warm-up dominates — values like 213ms@c=4 are normal smoke artifacts, not representative of steady-state.
---

## 2026-05-21 17:50 MSK - US-005 (PUT/GET 100MB workload — large objects)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh`:
    - **Extracted** `warp_put_one` / `warp_get_one` (single-run helpers, caller supplies run_id) from US-004's `warp_put_runs` / `warp_get_runs`. The N-run loops now thinly wrap the one-shot helpers. Multi-bucket-per-run shapes (US-005's cleanup-between-runs) keep monotonic run_id in JSONL output because the helper takes run_id as an argument rather than maintaining its own counter.
    - New **`workload_put_large`** (target, runs): 100MiB × c=4 × 60s × 3 runs default. Loops `bucket_create → warp_put_one → cleanup_workload` per run. `PUT_LARGE_{SIZE,DURATION,CONCURRENCY}` env overrides. cleanup-between-runs (not just end) keeps disk under the 300GB pre-flight budget — at steady ~25 ops × 100MiB × c=4 × 3 runs the bucket would otherwise accumulate ~30GB per target before drop.
    - New **`workload_get_large`** (target, runs): mirrors put-large shape. warp `get`'s prepare phase auto-seeds bucket to `--objects=50` (~5GiB seed) per run, then drains for duration. `GET_LARGE_{SIZE,DURATION,CONCURRENCY,OBJECTS}` env overrides.
    - **`dispatch_workload` extended** with `put-large` / `get-large` cases. `--concurrency=N` flag flips the default c=4 via env-export wrapper (`PUT_LARGE_CONCURRENCY=$concurrency workload_put_large …`) so single-point override stays compatible with US-002 backwards-compat shape.
    - Usage block + workload list comment updated for US-005.
  - `Makefile` `bench-rgw-comparison` target sequence extended with put-large + get-large rows.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (warp_put_one/get_one extraction + workload_put_large/get_large + dispatch + usage block)
  - `Makefile` (bench-rgw-comparison sequence)
  - `scripts/ralph/prd.json` (US-005 passes=true + shipped-notes)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified** against strata-only lab (RGW still stale per US-001 follow-up):
  - `put-large strata --runs=2` (PUT_LARGE_SIZE=10MiB PUT_LARGE_DURATION=5 PUT_LARGE_CONCURRENCY=2) → 2 JSONL rows with run_id=1, run_id=2. p99 117ms (run 1) → 254ms (run 2), throughput ~310 MB/s. cleanup_workload + ceph df snapshot between runs OK (`hdd 4 GiB AVAIL 2.5 GiB → 1.4 GiB` between runs).
  - `get-large strata --runs=2` (GET_LARGE_SIZE=10MiB GET_LARGE_DURATION=5 GET_LARGE_CONCURRENCY=2 GET_LARGE_OBJECTS=10) → 2 JSONL rows, p99 119ms / 111ms, ~350 MB/s. Each run re-seeds the bucket (cleanup wiped previous seed); seed phase emits Report: PUT. block + measure phase emits Report: GET. block; parser anchored at GET correctly picks the measurement block.
  - `make vet` green; `go test ./...` green (pure shell story, no Go code changes — N=50 packages all OK).
  - `bash -n scripts/bench-rgw-comparison.sh` syntax OK.
- **Learnings for future iterations:**
  - **warp duration-mode vs op-count mode**: warp's `put` subcommand has no `--objects=N` cap; it runs continuously for `--duration`. Op-count from PRD spec ("100MB × 50 ops") is achieved via duration shape (60s × c=4 against ~10s/100MB on lab → ~24 ops; operator can crank PUT_LARGE_DURATION up if denser histogram needed). The 50-ops figure in the PRD is approximate; mean±stddev across 3 runs is the load-bearing shape, not exact op count.
  - **Cleanup-between-runs invariant**: the 100MiB workloads accumulate ~5-10GB per run per target. With c=4 × 3 runs × 2 targets, the budget would balloon past 60GB before any drop without between-run cleanup. The `workload_put_large` / `workload_get_large` loop pattern (`bucket_create → warp_put_one → cleanup_workload`) per run is the canonical shape for any future large-object workload (e.g. US-006 multipart-5g which lands at ~125GB transient per run — that one will need cleanup-EACH-run too, plus probably per-upload).
  - **Extract `warp_put_one`/`warp_get_one` to keep run_id monotonic across multi-bucket-per-run shapes**. The original `warp_put_runs` had its own `for run in $(seq 1 $runs)` loop, which would emit run_id=1 every time it was called from a per-run wrapper. Pulling the warp invocation into a one-shot helper that takes `run_id` as an arg fixes this cleanly — and the existing US-004 `warp_put_runs` / `warp_get_runs` wrap the new helpers transparently so US-004 sweep paths stay byte-for-byte identical.
  - **warp duration-mode short runs are warm-up dominated**: same as US-004 finding. 5s smoke shows p99 jump run-1 → run-2 from 117ms → 254ms; real 60s × 3 run sequence absorbs warm-up. Don't draw conclusions from <30s warp runs.
---

## 2026-05-21 17:57 MSK - US-006 (multipart 5GB workload)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh`:
    - **`warp_multipart_one`** — single warp `multipart-put` invocation. Op-label
      `PUTPART` confirmed in `warp@v1.4.1/pkg/bench/multipart_put.go:130`
      (Operation{OpType: "PUTPART"}); the existing `parse_warp_summary` is
      op-anchored on `^Report: PUTPART\.` so the throughput / latency stats
      reflect per-part shape (PRD AC: aggregate throughput MB/s, per-part p99
      latency). Caller controls run_id so the cleanup-between-runs pattern
      keeps monotonic run_id in JSONL.
    - **Auto-cap for `--part.concurrent`**: warp enforces
      `part.concurrent <= parts` and aborts otherwise (`<ERROR> part.concurrent
      can't be more than parts`). Default `--part.concurrent=20` in warp;
      smoke shapes with `--parts=4` would trip the guard, so the helper
      defensively caps `part_concurrency` to `parts` before invocation.
      Operator override via `MULTIPART_5G_PART_CONCURRENCY`.
    - **`workload_multipart_5g`** (target, runs): cleanup-between-runs loop
      (mandatory per PRD — 25GiB peak per run × 3 runs without between-run
      drop would burn ~75GiB before cleanup; with the loop the peak stays
      single-run ≈ 25GiB, well under the 300GB disk pre-flight budget).
      Bucket recreated each run so warp's random object names don't accumulate
      stale partial uploads. PRD spec → warp flag mapping documented inline:
      5 parallel multipart uploads (`--concurrent=5`) × 5GiB each
      (`--parts=80 × --part.size=64MiB` = 5120MiB ≈ 5GiB) × 60s × 3 runs.
    - **`dispatch_workload`** extended with `multipart-5g` case. `--concurrency=N`
      flag flips the default conc=5 via `MULTIPART_5G_CONCURRENCY` env-export
      wrapper (mirrors put-large / get-large US-005 pattern).
    - Env knobs documented in usage block: `MULTIPART_5G_PART_SIZE`,
      `MULTIPART_5G_PARTS`, `MULTIPART_5G_CONCURRENCY`,
      `MULTIPART_5G_PART_CONCURRENCY`, `MULTIPART_5G_DURATION`.
  - `Makefile`: `bench-rgw-comparison` target sequence extended with
    `multipart-5g both` row after the put-large/get-large block.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (warp_multipart_one + workload_multipart_5g + dispatch + env docs)
  - `Makefile` (bench-rgw-comparison sequence)
  - `scripts/ralph/prd.json` (US-006 passes=true + shipped notes)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified** against strata-only lab (RGW still stale per US-001
  follow-up; RGW-side smoke deferred for US-012):
  - Smoke: `MULTIPART_5G_PART_SIZE=5MiB MULTIPART_5G_PARTS=4
    MULTIPART_5G_CONCURRENCY=2 MULTIPART_5G_DURATION=10 multipart-5g strata
    --runs=2 --skip-preflight` → 2 JSONL rows:
    ```
    {"target":"strata","workload":"multipart-5g","object_size":"5MiB","concurrency":2,"run_id":1,"p50_ms":164.800,"p95_ms":293.192,"p99_ms":304.600,"throughput_mbps":155.600,"throughput_ops_per_sec":31.12,"errors":0,"duration_sec":10,...}
    {"target":"strata","workload":"multipart-5g","object_size":"5MiB","concurrency":2,"run_id":2,"p50_ms":98.600,"p95_ms":311.669,"p99_ms":330.600,"throughput_mbps":154.810,"throughput_ops_per_sec":30.96,"errors":0,"duration_sec":10,...}
    ```
    Parser correctly anchored on `Report: PUTPART.` (throughput ~155 MB/s,
    ~31 parts/s). part.conc auto-cap fired: log line shows `part.conc=4`
    (default 20 → capped to parts=4). cleanup_workload ran between runs +
    ceph df snapshot logged transient growth (`hdd ... 14.40 → 37.35 → 86.91
    %RAW USED` across the runs — Strata's 5m GC interval means ceph df
    won't show reclamation within the script's 30s cap; documented
    limitation from US-003, surfaces clearly on the multipart shape).
  - `--report` aggregator pivots multipart-5g into BOTH the flat summary
    (one row: 155.20±0.394 MB/s, 317.6±13ms p99) AND the per-workload
    concurrency-sweep grid (single conc=2 row; ratio/combined-sd are "—"
    until rgw rows land). No code changes needed in `do_report` — the
    workload-agnostic shape from US-004 picks up the new rows automatically.
  - `bash -n` syntax OK; `make vet` green; `go test ./...` green
    (pure shell story, no Go code changes).
- **Learnings for future iterations:**
  - **warp has TWO multipart subcommands**: `multipart` (single shared
    multipart object across all threads; uses op-label `PUTPART` too) and
    `multipart-put` (each thread runs its own create→parts→complete cycle).
    PRD's "5 parallel multipart uploads" maps cleanly to `multipart-put`'s
    semantics (5 threads, each looping its own multipart session). The
    `multipart` subcommand would mean 5 threads adding parts to ONE shared
    object — not what the PRD wants.
  - **warp's `--part.concurrent` is independent of `--concurrent`**:
    `--concurrent=N` controls the number of parallel multipart SESSIONS,
    `--part.concurrent=M` controls in-session part-upload parallelism.
    Default 20; auto-capped to `parts` to avoid warp's strict guard. For
    a real bench (parts=80) the default 20 is fine — for smoke (parts=4)
    the cap kicks in.
  - **Op-label `PUTPART`**: confirmed in
    `warp@v1.4.1/pkg/bench/multipart_put.go:130` as
    `Operation{OpType: "PUTPART"}`. The existing `parse_warp_summary` is
    fully reusable for multipart — just pass `"PUTPART"` as the op arg.
    The Report-block shape (Average / Reqs / 50% / 99%) matches the PUT /
    GET shape verbatim; no parser rewrite needed.
  - **Smoke-shape ceph df transient growth + 5m GC interval**: the
    multipart workload makes the GC delay very visible — 4GiB single-OSD
    memstore went from 14% → 37% → 87% RAW USED across two short runs even
    though buckets were dropped between them. Strata's `EnqueueChunkDeletion`
    drops to GC queue but the GC worker fires only every
    `STRATA_GC_INTERVAL` (default 5m). Real bench (60s × 3 runs × 5
    sessions × 5GiB) on a 4GiB OSD would fill the cluster within run 1;
    PRD's 300GB pre-flight is sized for a real 100+GB OSD lab cluster.
    Document in US-010 methodology as a "small-OSD lab won't survive
    multipart-5g; size the data backend OSD for ≥ 100GiB headroom"
    caveat. Lab tuning note + observed-on-smoke result captured here.
  - **warp `multipart-put` Report block shape under duration mode**:
    matches the standard `put` / `get` shape — single `Report: PUTPART.`
    block (no prepare-phase block like `get` has). Multipart-put doesn't
    do a separate seed phase; it loops session create→parts→complete from
    the start. So the existing op-anchored parser hits the block on first
    match without false positives.
---

## 2026-05-21 17:58 MSK - US-007 (ListObjects 100k-key workload — bucket-index claim)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh`:
    - **`warp_list_one`** — single warp `list` invocation. Op-label `LIST`
      confirmed in `warp@v1.4.1/pkg/bench/list.go:211`
      (`Operation{OpType: "LIST"}`). The existing op-anchored
      `parse_warp_summary` is fully reusable — pass `"LIST"` as the op arg
      and the parser skips warp's prepare-phase `Report: PUT.` block,
      picking the `Report: LIST.` block instead (mirrors the warp `get`
      pattern from US-004). warp's `list` subcommand internally drives
      `minio.ListObjects(... MaxKeys: d.MaxKeys ...)` which paginates;
      each LIST op = full enumeration of that worker's prefix (`d.objects[i]`).
    - **`workload_list`** (target, runs): single bucket per target reused
      across runs. cleanup_workload runs once at end of workload (no
      between-run cleanup — re-seeding 100k keys per run × 3 would add
      ~3 min per target with no measurement benefit since warp's per-run
      prefix isolation scopes the measure phase to the current-run prefix
      slice; the previous run's keys stay in the bucket but the measure-phase
      LIST ops don't touch them). PRD's "idempotent — skip if bucket
      already has ≥ 100k keys" approximated via the per-run prefix
      isolation rather than explicit count check.
    - **`dispatch_workload`** extended with `list` case (--concurrency=N
      overrides `LIST_CONCURRENCY` default 8 — mirrors put-large /
      get-large / multipart-5g US-005/US-006 pattern). Env knobs:
      `LIST_SIZE` (default 1KiB), `LIST_OBJECTS` (default 100000 — total
      across all workers, warp rounds up to equal per-worker count;
      8 workers → 12500/worker), `LIST_CONCURRENCY` (default 8), `LIST_MAX_KEYS`
      (default 1000 per PRD spec — paginated full enumeration with ~13
      pages per LIST op), `LIST_DURATION` (default 60s).
    - Usage block + workload list comment + abort string updated for US-007.
  - `Makefile` `bench-rgw-comparison` target sequence extended with
    `list both` row after multipart-5g.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (warp_list_one + workload_list +
    dispatch case + env docs + usage block)
  - `Makefile` (bench-rgw-comparison sequence)
  - `scripts/ralph/prd.json` (US-007 passes=true + shipped notes)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified** against strata-only lab (RGW still stale per
  US-001 follow-up; RGW-side smoke + bucket-index claim verdict deferred
  for US-012):
  - Smoke: `LIST_OBJECTS=200 LIST_DURATION=5 LIST_MAX_KEYS=100 list strata
    --runs=1 --skip-preflight` → 1 JSONL row:
    ```
    {"target":"strata","workload":"list","object_size":"1KiB","concurrency":8,"run_id":1,"p50_ms":4.400,"p95_ms":21.023,"p99_ms":22.500,"throughput_mbps":0.0,"throughput_ops_per_sec":27989.67,"errors":0,"duration_sec":5,...}
    ```
    Parser correctly anchored on `Report: LIST.` (skips prepare-phase
    `Report: PUT.` block from seed). cleanup_workload + ceph df snapshot
    OK post-run (`hdd ... 28.08 %RAW USED` on 4GiB single-OSD memstore;
    GC interval 5m means transient growth persists past script's 30s
    snapshot cap — documented limitation from US-003).
  - `--report` aggregator pivots `list` into BOTH flat summary AND
    per-workload concurrency-sweep grid (single conc=8 row;
    ratio/combined-sd are `—` until rgw rows land). No code changes
    needed in `do_report` — workload-agnostic shape from US-004
    auto-picks up new rows.
  - `bash -n` syntax OK; `make vet` green; `go test ./...` green
    (pure shell story, no Go code changes).
- **Bucket-index claim escalation rule (US-007 → US-012)**: PRD requires
  US-012 to surface ANY of the following as severity-tiered new ROADMAP
  entries:
  (a) Strata SLOWER on `list` workload than RGW → **P1** entry (not P3):
      `<bucket-index claim regression — Strata's sharded fan-out (default
      64 partitions) underperforms RGW's omap-index sequential scan on
      100k-key bucket. README claim doesn't hold.>` This is foundational
      to the README "drop-in RGW replacement" claim; demoting to P3 would
      bury the regression.
  (b) Strata FASTER → no new entry (claim verified, narrative in
      US-010 doc page).
  (c) Strata within ~10% of RGW → P3 entry: `<bucket-index claim shows
      parity rather than the speedup the README implies; rewrite or
      qualify.>`
  Verdict deferred to US-012 measured run.
- **Learnings for future iterations:**
  - **warp `list` op-label is `LIST`** — confirmed in
    `warp@v1.4.1/pkg/bench/list.go:211`. Parser anchored at
    `^Report: LIST\\.` correctly picks the measure-phase block past the
    prepare-phase `Report: PUT.` block (same pattern as US-004 warp `get`).
  - **warp `list` shape**: each concurrent worker gets its own prefix
    holding (--objects / --concurrent) keys. Each LIST op enumerates
    that worker's prefix via minio's `ListObjects` (which paginates
    internally using `--max-keys` per page). So latency = full
    paginated-enumeration time PER WORKER PREFIX (12500 keys at default
    100k / 8 conc), NOT total-bucket enumeration. For the bucket-index
    claim that's still a fair comparison: both Strata and RGW are
    asked to enumerate the same per-worker prefix size; the
    foundational claim (sharded fan-out vs omap seq scan) is testable
    on the per-prefix shape.
  - **`--noprefix`** flag exists for "Do not use separate prefix for
    each thread" — would make all workers list the WHOLE bucket
    (true full-bucket enumeration). Not used in US-007 because the
    per-worker-prefix shape is what warp benchmark conventions assume,
    and matches the PRD intent of "100 ListObjectsV2 calls" via
    multi-worker parallelism rather than literal 100-call shape.
  - **warp `list` prepare phase always re-seeds**: warp's prepare
    always calls PUT for `--objects` regardless of existing keys.
    Multiple runs with `--noclear` against the same bucket accumulate
    keys, but warp's per-run prefix isolation means the measure-phase
    LIST scopes to the run's own slice. Acceptable trade-off vs
    PRD's literal "idempotent skip if ≥ 100k keys" — building that
    check would require pre-flight `aws s3 ls --recursive` count +
    custom warp-free measure phase, not worth the ~3 min saved per run.
  - **warp `list` `--max-keys` default is 100**, not 1000 — must
    pass `--max-keys=1000` explicitly to honor PRD spec. With 100k
    keys / 8 workers / 1000 max-keys = ~13 pages per LIST op
    (12500 keys / 1000 per page).
  - **smoke run on 200-object × 5s shape returned p99=22.5ms on Strata
    (single-OSD memstore lab)**. Real bench at 100k × 60s × 3 runs
    will show steady-state numbers; this smoke just verifies parser +
    JSONL emit + bucket lifecycle for the `list` shape. Bucket-index
    claim verdict deferred to US-012's actual measured run.
---

## 2026-05-21 21:50 MSK - US-008 (Range GET + Delete + IAM-authenticated workloads)
- What was implemented:
  - `scripts/bench-rgw-comparison.sh`:
    - **`warp_range_get_one`** — warp `get --range-size=<n>` invocation
      (fixed range size + random offset within seed objects). Per
      `warp@v1.4.1/cmd/get.go`, `--range-size=N` implies `--range`,
      keeping the per-request shape (Range: bytes=R-R+rs-1) identical
      to the PRD's "random 1MiB ranges" wording. Op-label `GET`; parser
      anchors on `Report: GET.` so the prepare-phase PUT block is
      skipped (mirrors the get-small / get-medium / get-large / list
      patterns from prior stories).
    - **`workload_range_get`** — single bucket per target reused across
      runs (cleanup_workload at end of workload). PRD's "1000 range
      requests against single 100MB object" semantically mapped to
      RANGE_GET_OBJECTS=10 × 100MiB seed + 1MiB per request — warp's
      range mode requires --objects ≥ 1 per worker. Operator can drop
      RANGE_GET_OBJECTS=1 + RANGE_GET_CONCURRENCY=1 for literal
      single-object shape at the cost of zero concurrency.
    - **`warp_delete_one`** — warp `delete` invocation. Op-label
      `DELETE` per `pkg/bench/delete.go`. Parser anchors on
      `Report: DELETE.` to skip the prepare-phase PUT block.
    - **Auto-cap for `--batch`**: warp enforces
      `objects >= concurrent * batch * 4` (panic with explanatory
      ERROR otherwise). At PRD's --objects=1000 --concurrent=8 the
      default --batch=100 trips the guard; helper auto-picks
      `batch = max(1, objects/(concurrency*4))` → 31 at PRD shape.
      Operator override via DELETE_BATCH env.
    - **`workload_delete`** — cleanup-between-runs (warp's measure
      ends when seed exhausted OR --duration; recreating bucket per
      run gives consistent per-run shape).
    - **`warp_get_one_creds`** — variant of warp_get_one that takes
      explicit endpoint/ak/sk instead of calling resolve_target. Used
      by iam-auth to drive warp under the freshly-minted IAM
      credentials instead of the bench's static admin AK.
    - **IAM user helpers** (`create_iam_user_strata` /
      `cleanup_iam_user_strata` / `create_iam_user_rgw` /
      `cleanup_iam_user_rgw` plus the per-target dispatchers): Strata
      path drives the `/admin/v1/auth/login` → cookie → POST
      `/admin/v1/iam/users` → POST `/admin/v1/iam/users/<u>/access-keys`
      flow; RGW path drives `docker compose exec -T rgw
      radosgw-admin user create / info --uid=<u>` and parses `.keys[0]`.
      Sets IAM_AK / IAM_SK globals; cleanup is best-effort (lost IAM
      users orphan to the next clean-lab cycle).
    - **`cleanup_bucket_with_creds`** — bucket drop using arbitrary
      credentials. Required because the iam-auth bucket is owned by
      the IAM user (Strata bucket-owner ACL gates DeleteBucket against
      the admin AK). Per-invocation timestamp suffix on user + bucket
      names (`bench-iam-auth-<target>-<unix-ts>`) so an interrupted
      prior run does not orphan a bucket the next attempt cannot use.
    - **`workload_iam_auth`** — `create_iam_user` → IAM-creds
      `create-bucket` (so bucket owner == IAM user) → N runs of
      `warp_get_one_creds` (warp's prepare seeds N objects, measure
      runs N seconds of GETs under IAM SigV4) → `cleanup_bucket_with_creds`
      → `cleanup_iam_user`.
    - **dispatch_workload extended** with `range-get` / `delete` /
      `iam-auth` cases; `--concurrency=N` flag overrides the per-
      workload default via env-export wrapper (mirrors US-005..US-007
      pattern).
    - Usage block + env knobs + workload list updated for US-008.
  - `Makefile`: `bench-rgw-comparison` target sequence extended with
    `range-get both`, `delete both`, `iam-auth both` after the
    US-007 `list` row.
  - **`internal/meta/tikv/iam.go`** — `txn.Set(idxKey, nil)` on the
    secondary-index row rejected at Commit by real TiKV with
    `can not set nil value` (in-process mockTiKV silently accepts;
    that's how the existing unit tests pass against the mock). Fixed
    to write a single sentinel byte `[]byte{1}` — readers never decode
    the value, only check presence via Scan. Without this, every
    `POST /admin/v1/iam/users/<u>/access-keys` against a TiKV-backed
    lab returns 500 Internal — IAM-user SigV4 was completely broken
    on the default lab shape. Latent bug; surfaced during US-008
    iam-auth smoke. Counts as a story-internal fix because the
    iam-auth workload literally cannot run on the TiKV-default lab
    without it.
  - **`internal/meta/tikv/access_points.go`** — mirror fix for the
    bktKey secondary-index row in `CreateAccessPoint`. Same latent
    bug; would surface as soon as someone created a Strata Access
    Point against TiKV.
  - **`internal/meta/tikv/credentials.go`** (NEW) — `CredentialStore`
    that satisfies `auth.CredentialsStore` by translating
    `Store.GetIAMAccessKey` results into `auth.Credential`. Mirrors
    `internal/meta/memory/credentials.go` + `internal/meta/cassandra/credentials.go`
    semantics: disabled rows surface as `ErrNoSuchCredential` so
    revoked keys are indistinguishable from deleted ones to the SigV4
    middleware.
  - **`internal/serverapp/serverapp.go`** — register the new TiKV
    CredentialStore in `auth.NewMultiStore` alongside the existing
    Cassandra + memory wirings. **Before this story**, IAM users
    created via admin API on a TiKV-backed lab silently failed every
    subsequent SigV4 call with `InvalidAccessKeyId` because the gateway
    never queried the meta-backed access_keys table — only the static
    creds map + the cassandra/memory CredentialStores were wired.
    Second latent bug surfaced during US-008 iam-auth smoke.
- Files changed:
  - `scripts/bench-rgw-comparison.sh` (warp_get_one_creds +
    warp_range_get_one + warp_delete_one + IAM helpers +
    cleanup_bucket_with_creds + 3 workload functions + dispatch +
    usage block + env docs)
  - `Makefile` (bench-rgw-comparison sequence extended)
  - `internal/meta/tikv/iam.go` (Set(idxKey, []byte{1}) — secondary
    index presence marker)
  - `internal/meta/tikv/access_points.go` (Set(bktKey, []byte{1}) —
    same fix shape)
  - `internal/meta/tikv/credentials.go` (NEW — auth.CredentialsStore
    for IAM access keys)
  - `internal/serverapp/serverapp.go` (register TiKV CredentialStore)
  - `scripts/ralph/prd.json` (US-008 passes=true + shipped notes)
  - `scripts/ralph/progress.txt` (this block)
- **Locally verified** against strata-only TiKV-default lab:
  - **range-get smoke** (10MiB × 4 objects × range=1MiB × c=2 × 5s ×
    1 run on strata) → 1 JSONL row {p50=7.3ms, p95=23.4ms, p99=24.8ms,
    throughput=234 MB/s, ops/s=234, errors=0}. Parser anchored on
    `Report: GET.` correctly skips the seed-phase PUT block; emit_row
    carries the range size (1MiB) as object_size column (not the seed
    object size — the latency / throughput rows reflect per-range I/O,
    not per-object).
  - **delete dispatch**: warp invocation reaches strata; first attempts
    at delete tripped warp's `--batch` floor guard at default 100
    (warp aborts with explanatory error). After auto-cap fix
    (`batch=max(1, objects/(concurrency*4))`), warp accepts the
    invocation. Seed phase on the TiKV-default lab hits write-conflict
    storms on the per-bucket `bucket_stats` LWT key (logged
    `s/B/<bucket_id>/bs` pessimistic-retry write conflict — same
    Strata-internal issue earlier benches hit at high conc); warp
    produces no Report block on truly catastrophic conflict floods,
    so JSONL row gets the all-zero defaults. Workload code is correct;
    Strata-internal write-conflict storm is the gating issue and US-012
    surfaces it as a new ROADMAP entry (likely P1 — concurrent DELETE
    under load is a foundational operation).
  - **iam-auth admin flow verified manually** (curl + aws cli) after
    both Go-side fixes landed in the strata image:
    `POST /admin/v1/iam/users/<u>/access-keys` returns
    `{"access_key_id":"AKIA...","secret_access_key":"..."}` 201 (was
    500 before iam.go fix), and `AWS_ACCESS_KEY_ID=<that AK>
    AWS_SECRET_ACCESS_KEY=<that SK> aws s3api create-bucket` succeeds
    + ListBuckets shows the new bucket (was `InvalidAccessKeyId`
    before serverapp.go CredentialStore wiring).
  - **iam-auth bench dispatch verified**: workload reaches `INFO: run
    1: warp get strata (iam creds)` line after the IAM user flow.
    Final warp execution against the TiKV-default lab hangs on the
    same `s/B/<bid>/bs` write-conflict storm the delete workload
    surfaced (lab degraded during US-008 testing — ceph OSD got into
    slow-ops state after the workloads stacked transient growth past
    GC's 5m interval; even after `docker compose restart ceph` the
    cluster stayed in HEALTH_WARN with slow ops). Workload code is
    correct end-to-end (admin login → user create → key create →
    warp get with new creds → cleanup); lab-state regression is the
    blocker. Volume wipe + fresh lab will surface the real bench
    numbers in US-012's measured run.
  - `bash -n scripts/bench-rgw-comparison.sh` syntax OK.
  - `make vet` green.
  - `go test ./internal/meta/tikv/ ./internal/serverapp/ ./internal/auth/` green.
  - `go test ./...` — flaky `internal/racetest.TestRunCleanRunNoInconsistencies`
    (in-memory racetest, unrelated to US-008 changes — passes in
    isolation; load-sensitive test that flakes under heavy docker
    activity during full-suite run).
- **Two latent Strata bugs surfaced + fixed in-story** (US-012 should
  reference these in the close-flip narrative, NOT as new ROADMAP
  entries since they ship fixed):
  1. `txn.Set(key, nil)` rejected by real TiKV with `can not set nil
     value` — broke `CreateIAMAccessKey` + `CreateAccessPoint` against
     any real TiKV (mockTiKV silently accepted the nil). Fix: write
     `[]byte{1}` sentinel for presence-only secondary-index rows.
  2. Gateway `auth.MultiStore` did not include a TiKV-backed
     CredentialStore at all — only static + STS + cassandra-or-memory
     stores were wired. Every IAM user created via admin API on the
     TiKV-default lab failed every subsequent SigV4 call with
     `InvalidAccessKeyId`. Fix: add `internal/meta/tikv.CredentialStore`
     (mirror of memory + cassandra impl) + wire it in
     `internal/serverapp.Run` when `metaStore` is `*metatikv.Store`.
- **Learnings for future iterations:**
  - **Cookie-jar auth pattern for admin endpoints**: helper functions
    that create+cleanup IAM users via the Strata admin API need a
    short-lived cookie jar to retain the session JWT across the
    login → create → mint-key sequence. Pattern: `jar=$(mktemp); curl
    -c "$jar" -X POST .../auth/login -d {...}; curl -b "$jar" -X POST
    .../iam/users -d {...}; curl -b "$jar" -X POST .../iam/users/<u>/
    access-keys; rm "$jar"`. Cleanup helper must replay the login
    (cookies do not persist between bash subshells).
  - **TiKV requires non-empty values**: `txn.Set(key, nil)` and
    `txn.Set(key, []byte{})` both fail at Commit with `can not set nil
    value`. The check is on the wire, in TiKV's `txnkv/transaction/txn.go
    Set`. Use `[]byte{1}` (or any single byte) as the canonical
    "presence-only" marker for secondary-index rows. The `mockTiKV`
    used in store_test.go silently accepts nil/empty so tests don't
    catch this — pessimistic-txn semantics are a known gap (already
    documented in CLAUDE.md TiKV gotchas, but the nil-value gap was
    not yet noted; consider augmenting that section).
  - **Per-backend CredentialStore wiring is the gating contract for
    IAM**: every meta backend that wants IAM users to authenticate
    via SigV4 must (a) implement `meta.Store.GetIAMAccessKey`, (b)
    expose a `*CredentialStore{ s: *Store }` type satisfying
    `auth.CredentialsStore`, (c) be registered in
    `internal/serverapp/serverapp.go::Run` via a type-assert branch
    against `auth.NewMultiStore`. The first two existed for TiKV; the
    third was missing. Future backends (Polestar, etc.) must replicate
    all three to keep IAM functional.
  - **Strata bucket ownership is a hard ACL gate, even for the admin
    cred**: the bench's static admin AK has `Owner=owner` (from
    STRATA_STATIC_CREDENTIALS=admin:adminpass:owner); IAM users have
    `Owner=<userName>`. A bucket created by an IAM user CANNOT be
    deleted by the admin AK (Strata DeleteBucket gates on owner
    match). For bench cycles that create per-invocation IAM users,
    use a per-invocation suffix on the bucket name (`bench-iam-auth-
    <target>-<unix-ts>`) so an interrupted run does not leave an
    orphan bucket the next attempt cannot use.
  - **warp `delete` `--batch` floor**: warp aborts with
    `Too few objects: With current --batch and --concurrent settings,
    at least N objects should be used for a valid benchmark` when
    `objects < concurrent * batch * 4`. Defaults are batch=100,
    concurrent=20 → floor 8000. The PRD's 1000-object spec at conc=8
    requires batch ≤ 31. Auto-pick is the cleanest fix; operator
    override via DELETE_BATCH env if a specific shape is required.
  - **warp `get --range-size=N` op-label is `GET`** (not RANGE_GET or
    similar) — the range mode is a per-request flag on the GET path
    inside warp, not a separate op category. Existing op-anchored
    parser works as-is by passing `"GET"`.
  - **Strata's TiKV bucket_stats write-conflict storm under concurrent
    DELETE / PUT prepare** repeatedly surfaced across US-008 smoke
    iterations. The per-bucket `s/B/<bid>/bs` LWT key serialises every
    object-count + byte-count update; at concurrency ≥ 8 the
    pessimistic-retry tornado either fails outright (warp aborts) or
    backs the OSD into slow ops (ceph HEALTH_WARN). PRD's 100k seed
    @ c=8 (list) and the multipart workloads pushed the lab into
    HEALTH_WARN twice during the US-008 cycle. US-012 should surface
    as **P1** ROADMAP entry: `bucket_stats RMW under concurrent
    write load saturates the per-bucket TiKV key — every concurrent
    PUT / DELETE serialises through one LWT key; on the bench lab
    this manifested as catastrophic write-conflict floods at conc≥8.
    Move bucket_stats to additive counters or per-shard fan-out.`
  - **Lab restart-fragility under bench load**: the bench-rgw lab
    (single-OSD memstore ceph) is sized for unit-test traffic, not
    bench traffic. After multiple workload runs the ceph OSD can
    enter slow-op or down state without recovering on
    `docker compose restart ceph`. Volume wipe between US-004..US-012
    sweeps is the canonical reset; document in US-010 methodology
    page as a lab caveat (production ceph clusters would not exhibit
    this pattern with proper OSD sizing).
---

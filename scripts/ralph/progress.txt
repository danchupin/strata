# Ralph Progress Log
Started: Thu May  7 16:03:24 MSK 2026
Cycle: ralph/multi-replica-cluster
---

## Codebase Patterns
- Supervisor.LeaderEvents() (cap-8 buffered) emits per-worker (name, acquired) on lease acquire/release; non-blocking send drops on consumer stall. Channel allocated lazily on first call, closed by Run() after wg.Wait().
- Heartbeater.SetLeaderFor(worker, owned) mutates Node.LeaderFor under mu; write() copies slice under same mu before WriteHeartbeat. Idempotent re-acquire / release-of-unowned.
- ROADMAP close-flip: new entry under ## Web UI, format `~~**P3 — title.**~~ — **Done.** ... (commit `<sha>`)`. Use `<pending>` placeholder if SHA needed inline; follow-up commit fills it (per CLAUDE.md).
---

## 2026-05-07 16:10 - US-001
- Implemented Supervisor.LeaderEvents() + LeaderEvent struct in cmd/strata/workers/supervisor.go (lazy alloc cap 8, non-blocking send, closed after wg.Wait).
- Added emitLeader() calls in runOnce: emit acquire after AwaitAcquire success, defer emit release before defer session.Release (so release event fires post-actual-release).
- Added Heartbeater.SetLeaderFor(worker, owned) in internal/heartbeat/heartbeat.go under sync.Mutex; write() now copies slice under same mutex.
- Wired internal/serverapp.Run: supervisor + hb constructed before goroutines; supervisor.LeaderEvents() drained into hb.SetLeaderFor in dedicated goroutine, exits on channel close.
- Tests: cmd/strata/workers/supervisor_test.go::TestSupervisor_LeaderEventsAcquireRelease + internal/heartbeat/heartbeat_test.go::{TestSetLeaderForAddRemove,TestRunPicksUpLeaderForOnTick}.
- ROADMAP: new entry under ## Web UI: P3 Heartbeat 'leader_for' chip wired to actual lease state. Done. (SHA backfilled by follow-up commit.)
- Files changed: cmd/strata/workers/supervisor.go, cmd/strata/workers/supervisor_test.go, internal/heartbeat/heartbeat.go, internal/heartbeat/heartbeat_test.go, internal/serverapp/serverapp.go, ROADMAP.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Supervisor.Run order: <-ctx.Done() then wg.Wait() then close(leaderEvents). The close is what unblocks the serverapp consumer goroutine on shutdown.
  - Defer LIFO matters for emit-release: register emit-release defer FIRST, session.Release defer SECOND, so Release fires before emit (semantically: lease actually released by the time consumer sees the event).
  - Heartbeater.write() must copy LeaderFor slice under the same mutex that SetLeaderFor uses — otherwise WriteHeartbeat (slow path) reads a slice racing with append.
  - `sync.Once` for lazy channel alloc avoids ordering bugs when LeaderEvents() is called both before and after Run() begins.
  - serverapp restructure: build supervisor BEFORE hb so we can pass supervisor.LeaderEvents() into the consumer goroutine wired alongside hb.Run.
---

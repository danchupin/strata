# Ralph Progress Log
Started: Tue May 19 19:00:00 MSK 2026
Cycle: ralph/polish-dx

## Codebase Patterns
- 4-story polish cycle bundling 3 small fixes (no new feature surface, no UI, no admin endpoint) + 1 smoke story. Module-tags cleanup dropped after fact-check — default `go build ./...` is already hermetic (only internal/data/rados/{backend,rebalance}.go import go-ceph, both already `//go:build ceph`; stub.go covers default tag). `go.mod` retains direct go-ceph require because Go module graph is not tag-aware; truly moving to `// indirect` requires a separate Go module split which is out of scope here.
- **Story ordering rationale**: US-001 (gc.Worker ENOENT-ack) lands first because it introduces the backend-agnostic sentinel `data.ErrChunkNotFound` that US-003 (lifecycle retry) terminal classifier consumes. US-002 (TestThreeReplicaDistribution determinism) is independent and can land in any slot.
- **ENOENT lift pattern (US-001)**: Currently `internal/data/rados/backend.go:373` uses `goceph.ErrNotFound` directly. Lift to a backend-agnostic sentinel: declare `data.ErrChunkNotFound = errors.New("chunk not found")` in `internal/data/errors.go` (build-tag-free); each backend `Delete` impl wraps its native ENOENT into the sentinel before returning. RADOS wraps `goceph.ErrNotFound`; S3 wraps `NoSuchKey`; memory backend already returns nil on missing (no-op). gc.Worker `internal/gc/worker.go:222-228` (the `eg.Go` body inside `drainCount` — NOT line 123 which is RunOnce body) checks `errors.Is(err, data.ErrChunkNotFound)` and acks via the same `w.Meta.AckGCEntry(delCtx, w.Region, e)` call used on success path. Counter `strata_gc_terminal_ack_total{reason="enoent"}` registered in internal/metrics/gc.go.
- **gc.Worker Delete signature** is `w.Data.Delete(delCtx, manifest)` where `manifest = &data.Manifest{Chunks: []data.ChunkRef{e.Chunk}}` — NOT `Data.Delete(ctx, cluster, pool, OID)` (the earlier draft of this PRD had the wrong signature). The chunk identity sits inside the manifest's single-entry Chunks slice.
- **Deterministic UUID (US-002)**: The flake source is `seedExpiringBucket` at `internal/lifecycle/distribute_test.go:54` calling `uuid.New().String()` — NOT the test body (line 111 is where TestThreeReplicaDistribution starts). `seedExpiringBucket` is shared with 4 other test functions (lines 76, 204, 243 + the line-120 site in TestThreeReplicaDistribution itself). Modifying its UUID gen cascades to all 4 callers — preserve those by adding a `seedExpiringBucketWithID(t, ..., name, id)` variant; the original wraps it with `uuid.New()`. TestThreeReplicaDistribution switches to the new variant with `uuid.NewSHA1(uuid.Nil, []byte(fmt.Sprintf("bucket-%d", i)))`. Flake reproduced empirically: `-count=20` produced 4/20 fail (~30% rate) with random-UUID seeding.
- **Lifecycle retry classifier (US-003)**: `internal/lifecycle/retry.go` (or fold into worker.go if < 30 LoC). Transient predicate: `context.DeadlineExceeded`, `syscall.ECONNRESET`, `syscall.ETIMEDOUT`, any `interface{ Temporary() bool }` returning true. Terminal: `meta.ErrNotFound`, `data.ErrChunkNotFound`, `data.ErrNoSuchKey`, `data.ErrNoSuchBucket`, anything not matching transient. 3 attempts total, backoff `1s → 3s → 10s`, sleep BEFORE attempts 2 and 3, ctx-aware (race `time.After` against `ctx.Done()`). Injectable `sleep func(time.Duration)` for unit testability. Counter `strata_lifecycle_retry_total{outcome=ok|exhausted|terminal}` in internal/metrics/lifecycle.go. Wrap each per-action call site (`expireNoncurrent` at line 393 calls `Meta.DeleteObject`; grep `w.Meta.DeleteObject\|w.Data.Delete` for the full enumeration) in `retryAction(ctx, func() error { return ... })`.
- **ROADMAP discipline (US-004)**: 3 entries flip in one commit — 2 pre-existing (gc.Worker latent bug, TestThreeReplicaDistribution latent bug) + 1 new-and-closed for `Lifecycle worker no-retry on transient failures` per CLAUDE.md "Discovering a new gap" rule (US-003 is not pre-roadmapped). Each entry carries `(commit <pending>)` placeholder; SHA backfill lands on main as fast-follow commit (mirrors prior cycle's f6a5476-backfill pattern).
- **No UI in this cycle** — none of the 3 fixes surfaces in the operator console. Per memory rule `Cycle end-to-end`, normally e2e smoke via UI walkthrough is required. This cycle's final-story validation is `go test -race -count=5 -run TestThreeReplicaDistribution` (proves US-002 stable) + full `go test -race ./...` + ROADMAP close-flip + PRD removal — substitutes the UI-smoke discipline because the value is backend-only.
- **US-001 sentinel-import gotcha (S3 backend)**: `deleteFromCluster` must match BOTH `*s3types.NoSuchKey` (typed) and the generic `smithy.APIError` whose `ErrorCode()` returns `"NoSuchKey"` — MinIO + some emulators surface NoSuchKey as a generic api error (mirrors the `rebalance.go:140` precedent comment). Add `smithy "github.com/aws/smithy-go"` import. `DeleteObject` (the legacy admin/test helper) keeps the idempotent contract by swallowing `data.ErrChunkNotFound` to nil at the boundary; only the multi-cluster `Delete(m)` surface bubbles the sentinel to the gc worker.
- **gc.Worker test pattern**: `metrics.GCTerminalAck` is a `prometheus.NewCounterVec` package-level var — tests can call `testutil.ToFloat64(metrics.GCTerminalAck.WithLabelValues("enoent"))` directly. Do NOT call `metrics.Register()` from tests: it uses `prometheus.MustRegister` which panics on duplicate registration if any other test (or the binary itself) already registered the counters.
- **Memory-only test helpers**: when a test needs to pin internal state that the meta.Store interface does not expose (e.g. caller-supplied bucket UUID), add a thin helper on `memory.Store` only — Cassandra/TiKV generate their IDs server-side and do not need the hook. Pattern in US-002: `memory.Store.CreateBucketWithID(ctx, name, owner, class, id)` plus a shared private `createBucketWithID` so the random-UUID `CreateBucket` and the deterministic variant share one body. Avoids reflection / unsafe field writes and keeps the meta.Store interface clean.
- **Bounded-retry pattern (US-003)**: `internal/lifecycle/retry.go` ships `retryAction(ctx, fn)` (production) + `retryActionWith(ctx, fn, sleep)` (test seam). 3 attempts, sleeps `[1s, 3s]` consumed before attempts 2 and 3 (PRD's `1s→3s→10s` notation is forward-compatible — third slot reserved but never consumed today). Transient classifier covers `context.DeadlineExceeded` + `syscall.ECONNRESET` + `syscall.ETIMEDOUT` + any `interface{Temporary() bool}` returning true; terminals are `meta.ErrObjectNotFound`/`meta.ErrBucketNotFound`/`data.ErrChunkNotFound`/`data.ErrNotFound`. Note: PRD wrote `meta.ErrNotFound`/`data.ErrNoSuchKey`/`data.ErrNoSuchBucket` which don't exist in the code today — actual sentinels are listed above. Ctx-aware sleep via `ctxSleep` racing `time.After` vs `ctx.Done`. Counter `strata_lifecycle_retry_total{outcome=ok|terminal|exhausted}` registered in `internal/metrics/metrics.go`. Wired around the two `w.Meta.DeleteObject` sites in `worker.go` (`expire` line ~593, `expireNoncurrent` line ~393); transition's GetChunks/PutChunks/SetObjectStorage chain is intentionally **not** retried (a transient mid-transition would require unwinding multi-step manifest state — out of scope).

## 2026-05-19 21:00 MSK - US-003
- Lifecycle worker bounded-retry helper for transient `Meta.DeleteObject` failures.
- Files changed:
  - internal/lifecycle/retry.go (new — `retryAction` production seam, `retryActionWith` test seam taking injectable sleeper, `isTransient` classifier, `ctxSleep` ctx-aware sleeper, `retryBackoffs` package-level `[1s, 3s, 10s]` array)
  - internal/lifecycle/retry_test.go (new — table-driven `TestIsTransientClassifier`, plus the AC matrix: TestRetryActionSuccessFirstAttempt / TransientThenSuccess / Exhausted / TerminalFirstAttempt / CtxCancelMidBackoff / CtxSleepCancelImmediate)
  - internal/lifecycle/worker.go (`expire` line ~593 + `expireNoncurrent` line ~393 wrap their `Meta.DeleteObject` call in `retryAction`; `removed *meta.Object` declared outside the closure to escape the loop scope cleanly)
  - internal/metrics/metrics.go (registered `LifecycleRetryTotal CounterVec{outcome}` + added to `Register()` MustRegister bundle)
- Quality: `go test -race -run 'TestIsTransientClassifier|TestRetryAction|TestCtxSleep' ./internal/lifecycle/...` clean. Full `go test -race ./internal/lifecycle/...` clean. `go vet ./...` clean. `go build ./...` clean.
- **Learnings for future iterations:**
  - PRD's "1s→3s→10s backoff" + "sleep BEFORE the 2nd + 3rd attempt" is internally inconsistent — the AC tests only assert 2 sleeps. I kept the 10s slot in `retryBackoffs[2]` so a future 4th-attempt extension does not need a fresh PRD line, but the loop today consumes `[0]` and `[1]` only.
  - PRD listed sentinels that don't exist in the codebase today: `meta.ErrNotFound`, `data.ErrNoSuchKey`, `data.ErrNoSuchBucket`. Actual sentinels are `meta.ErrObjectNotFound`, `meta.ErrBucketNotFound`, `data.ErrChunkNotFound`, `data.ErrNotFound`. Grep first when wiring a new classifier — PRDs drift from reality.
  - For testable retry helpers with closures over outer-scope vars, the cleanest pattern is `var x T; err := retryAction(ctx, func() error { var rerr error; x, rerr = call(...); return rerr })`. Avoids changing the helper signature with generics or adapter types.
  - Wrapping only `Meta.DeleteObject` (and not the transition pipeline `GetChunks → PutChunks → SetObjectStorage`) was intentional — mid-transition retry would have to unwind partial manifest state, far beyond a polish-cycle fix.
  - `prometheus/client_golang/prometheus/testutil.ToFloat64` reads counter labels by `WithLabelValues(...)`. Tests should capture the pre-test value and assert delta — every other test in the binary that runs first might bump the same vector. Don't assume the counter starts at 0.
---

## 2026-05-19 20:15 MSK - US-002
- Deterministic UUID seeding for TestThreeReplicaDistribution. Flake fix: 20/20 pass at `-race -count=20` (previously ~14/20).
- Files changed:
  - internal/meta/memory/store.go (new `CreateBucketWithID` test-helper; existing `CreateBucket` delegates via shared `createBucketWithID` to avoid duplication; method comment notes it's memory-only because Cassandra/TiKV server-side UUIDs don't need this hook)
  - internal/lifecycle/distribute_test.go (`seedExpiringBucket` becomes a 1-line wrapper around new `seedExpiringBucketWithID(..., uuid.UUID)`; `TestThreeReplicaDistribution` derives each bucket UUID via `uuid.NewSHA1(uuid.Nil, fmt.Appendf(nil, "bucket-%d", i))`; replaced the `n < 1 || n > 5` distribution-skew guard with an exact `[3]int{4, 1, 4}` equality check on the pre-run hash split)
- Quality: `go test -race -count=20 -run TestThreeReplicaDistribution ./internal/lifecycle/...` → 20/20 OK. `go test -race ./internal/lifecycle/... ./internal/meta/memory/...` clean. `go vet ./...` clean. `go build ./...` clean.
- **Learnings for future iterations:**
  - The memory.Store package does NOT expose a UUID override path for buckets — `CreateBucket` always self-allocates. When a test needs reproducible bucket.ID values, add a thin `CreateBucketWithID(ctx, name, owner, class, id)` helper backed by a shared private constructor; cleaner than reflection or unsafe field writes.
  - The Cassandra/TiKV backends generate bucket IDs server-side; the new helper is intentionally memory-only and is not part of the meta.Store interface (no behavioural drift, no Cassandra DDL).
  - For FNV-32a-mod-N hash determinism in tests, prefer `uuid.NewSHA1(uuid.Nil, ...)` over `uuid.New()` — version-5 UUIDs are byte-stable across go versions; v4 randomness is not seedable.
  - When swapping a tolerance-band assertion for an exact-value assertion, compute the deterministic value via a throwaway program at PRD-prep time (not from a partial run inside CI) and pin it in a comment near the assertion so future drift surfaces immediately.
---

## 2026-05-19 19:30 MSK - US-001
- Implemented backend-agnostic `data.ErrChunkNotFound` sentinel + RADOS / S3 backend lifts + gc.Worker terminal-ack classifier + `strata_gc_terminal_ack_total{reason}` counter.
- Files changed:
  - internal/data/errors.go (added ErrChunkNotFound)
  - internal/data/rados/backend.go (Delete wraps goceph.ErrNotFound, behind `//go:build ceph`)
  - internal/data/s3/backend.go (deleteFromCluster lifts NoSuchKey via both s3types.NoSuchKey + smithy.APIError; DeleteObject swallows sentinel; smithy import added)
  - internal/gc/worker.go (eg.Go body checks errors.Is(err, data.ErrChunkNotFound) → bump counter + ack; non-ENOENT keeps loop-fail)
  - internal/metrics/metrics.go (GCTerminalAck CounterVec registered)
  - internal/gc/worker_test.go (TestWorker_TerminalAckOnChunkNotFound — X ack'd, Y retries, Z ack'd; counter delta=1)
  - internal/data/s3/backend_test.go (TestDeleteLiftsNoSuchKeyToErrChunkNotFound + TestDeleteObjectSwallowsNoSuchKey)
  - internal/data/rados/backend_integration_test.go (second-Delete asserts errors.Is(err, data.ErrChunkNotFound); `//go:build ceph && integration`)
- Quality: `go vet ./...` clean, `go build ./...` clean, full `go test ./...` green (race-mode on gc/data subset: green). `go vet -tags ceph ./...` requires librados headers — not present on dev host; gate runs in CI Docker container.
- **Learnings for future iterations:**
  - The s3 backend's `DeleteObject` (legacy admin/test surface) must stay idempotent, so any new sentinel lifted in `deleteFromCluster` must be swallowed there — otherwise admin/integration tests that expect nil-on-missing break (see `TestDeleteObjectSwallowsNoSuchKey` for the pin).
  - When wrapping native AWS SDK errors, match BOTH the typed `*s3types.<Code>` AND `smithy.APIError` by `ErrorCode()` — MinIO + some emulators surface protocol errors as generic api errors. The `rebalance.go:140` precedent already documents this.
  - `prometheus.MustRegister` panics on duplicate. Tests reading counter values must NOT call `metrics.Register()` — read the package-level CounterVec leaf directly via `testutil.ToFloat64(metric.WithLabelValues(...))`.
  - gc.Worker shape: per-entry decision lives in the inner `eg.Go` body inside `drainCount` (~line 222 today). The outer `RunOnce` only counts processed and stamps the iteration span. New terminal classifiers plug into the same place.
---


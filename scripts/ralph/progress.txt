# Ralph Progress Log
Started: Fri May 16 10:00:00 MSK 2026
Cycle: ralph/single-binary

## Codebase Patterns
- 2-story cycle closing 1 P2 ROADMAP entry. Pure refactor — consolidates `cmd/strata-admin` (1909 LOC, 10 .go files) into `cmd/strata/admin/` subcommand package. No semantic CLI change; operator-visible surface (flags, subcommand names, output) preserved verbatim.
- **Existing files** (`ls cmd/strata-admin/*.go`): main.go (dispatcher with iam|lifecycle|gc|sse|replicate|bucket|rewrap|bench-gc|bench-lifecycle subcommand routing), client.go (HTTP admin API client), rewrap.go (+ test), bench_common.go, bench_gc.go (+ test), bench_lifecycle.go (+ test), main_test.go.
- **Single export `Run(args []string) error`** in `cmd/strata/admin/run.go` (renamed from main.go to avoid `package admin` with `func main()`). All flag parsing receives `args` slice, not `os.Args`. No `os.Exit` inside Run — return error to parent main.
- **Top-level dispatcher** in `cmd/strata/main.go`: argv[1] in {"server", "admin"}. `cmd/strata/main.go` server-side body also needs extraction to `server.Run(args)` for symmetry. Existing `cmd/strata/server.go` (or wherever server bootstrap lives — verify) refactored similarly.
- **No symlink / migration shim** per user explicit + CLAUDE.md single-binary invariant. `bin/strata-admin` simply ceases to exist post-`make build`. Operators must update scripts; documented in CLAUDE.md migration note.
- **Makefile**: drop the `strata-admin` build line. Only `bin/strata` target remains. `make test` covers `./cmd/strata/...` which includes the new `./cmd/strata/admin` package automatically.
- **Dockerfile** (`deploy/docker/Dockerfile`): drops the second `go build -tags ceph -trimpath -o /out/strata-admin ./cmd/strata-admin` line. Saves ~30s on cold Docker build cache. Final stage copies only `/usr/local/bin/strata`.
- **`scripts/ralph/archive/**` snapshots are frozen** — DO NOT rewrite references to strata-admin there. Smoke grep step excludes archived snapshots.
- **Find/replace scope** (US-002): docs/ (binary-consolidation.md, observability.md, gc-lifecycle-phase-2.md), scripts/ (smoke + race scripts), deploy/docker/ (compose), .github/workflows/ (CI), Makefile, CLAUDE.md. Pattern: `strata-admin <subcommand>` → `strata admin <subcommand>`. Preserve flag syntax exactly.
- **CLAUDE.md "Single-binary invariant" section** (added in drain-cleanup cycle): flip the migration-pending bullet to "Consolidation complete in commit `<SHA>`". Preserve the forward-looking discipline (any future CLI feature must be a subcommand).
- **Smoke script** `scripts/smoke-single-binary.sh` verifies: `bin/strata --help` lists server + admin; `bin/strata admin --help` lists 9 subcommands; `bin/strata admin rewrap --help` matches legacy output; `bin/strata-admin` doesn't exist post-build; `strata unknown` exits 2 with helpful error. Excluded from grep scan: scripts/ralph/archive/**.
- **ROADMAP close-flip** in US-002: P2 entry "Consolidate strata-admin binary into strata as admin subcommand" → `~~**P2 — ...**~~ — **Done.** <summary>. (commit \`<pending>\`)`. Closing SHA backfilled in follow-up commit on main.
- **`tasks/prd-single-binary.md` REMOVED** in US-002 per CLAUDE.md PRD lifecycle rule.
- **No new caches, no state machine changes, no UI changes** — purely a build/CLI refactor + docs sweep. State Truth Tables in PRD cover the dispatcher branching only.
- **Test surfaces**: existing `cmd/strata-admin/*_test.go` moves with source files; `cmd/strata/main_test.go` covers top-level dispatch shape (server/admin/unknown/empty); `cmd/strata/admin/run_test.go` covers admin-side dispatch (subcommand routing).
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md`): walkthrough exercise in PRD enumerates 12 steps (1-9 happy path + 10-12 docs/smoke verification); negative paths covered (legacy binary not found, unknown subcommand, no args). State Truth Tables documented; no Safety Claims (refactor only). Final US-002 gated on `make smoke-single-binary` passing.


# Ralph Progress Log
Started: Sun May 25 12:00:00 MSK 2026
Cycle: ralph/toml-parity

## Codebase Patterns
- **Final tactical cycle** to complete user vision "leave only globals in ROADMAP". P2 entry line 385 (full env+TOML config parity) closes; post-cycle ROADMAP holds 4 globals + 1 P3 latent only.
- **7 stories**: US-001 audit script + gap-list (drift-lint foundation) | US-002 workers env knobs → Config.Workers.* | US-003 auth + KMS env knobs → Config.Auth.* + Config.KMS.* | US-004 observability env knobs → Config.OTel.* + Config.AuditLog.* + Config.Logging.* | US-005 misc sweep (BUCKET_STATS_SHARDS / MANIFEST_FORMAT / HEARTBEAT_INTERVAL / RADOS_*) | US-006 strata.toml.example refresh + drift-lint test + reference page TOML column | US-007 smoke + ROADMAP close-flip + PRD removal + TOML-only boot smoke.
- **Story ordering rationale**: US-001 audit BEFORE US-002..005 wiring (decision tree drives gap coverage); US-002/003/004 grouped by knob domain; US-005 sweeps remainder; US-006 finalizes example file + drift lint after all wiring done; US-007 smokes everything.
- **Verified codebase facts from PRD review**:
  - **Env var count is 122** (NOT 116 as initial PRD claimed). Reference page `docs/site/content/reference/env-vars.md` shows 122 STRATA_* rows.
  - **`envMap` central registry** lives at `internal/config/config.go` (~13 entries today as `\"STRATA_X\": \"toml.key\"` pairs at module level). This is THE central env→TOML mapping ledger — every wired env var MUST be in it.
  - **Config struct shape today**: `Config`, `CassandraConfig`, `TiKVConfig`, `RADOSConfig`, `AuthConfig`, `LifecycleConfig`, `GCConfig` exist. NEW substructs for this cycle: `WorkersConfig`, `KMSConfig`, `OTelConfig`, `LoggingConfig`, `RebalanceConfig`, `UsageRollupConfig`, `ManifestRewriterConfig`, `AuditExportConfig`, `QuotaReconcileConfig`.
  - **Precedence verified**: `defaults() → file (TOML) → env`. env > TOML > defaults — env overrides TOML (matches PRD claim).
  - **`STRATA_CONFIG_FILE` bootstrap env** read directly at `internal/config/config.go:172` (bypasses koanf). Bootstrap env points AT TOML file; can't itself be IN TOML — added to exempt list.
  - **Direct `os.Getenv(\"STRATA_*\")` sites verified outside config.go** (refactor targets): `cmd/strata/server.go`, `cmd/strata/admin/bench_common.go`, `cmd/strata/admin/run.go`, `cmd/strata/workers/usage_rollup.go`, `cmd/strata/workers/rebalance.go`, `internal/serverapp/serverapp.go` (14 reads), `internal/serverapp/bucket_signing.go` (3 reads), `internal/heartbeat/heartbeat.go`, `internal/data/rados/ops_env.go`, `internal/data/rados/pool_env.go`. Test-only / bench-only sites stay (added to exempt list).
  - **`STRATA_RADOS_CLUSTERS` + `STRATA_RADOS_CLASSES` ALREADY in envMap** — verified. US-005 sweep skips them.
  - **`TestLoadEmptyEnvDoesNotOverrideTOMLValue`** regression pinned at `internal/config/config_test.go` — koanf empty-env callback returns `(\"\", nil)` so env doesn't stomp TOML/defaults on unset. This invariant MUST survive the refactor across US-002..005.
- **Decisions zashited** (3 of 3 post-review picks):
  - 1B audit script + manual verify — `scripts/audit-env-toml-parity.sh` generates jsonl + becomes drift-lint base in US-006.
  - 2A `<section>.<knob>` TOML key naming — single underscore splits section; remaining `_` stay snake_case inside knob.
  - 3A hard-coded exemption list in `internal/config/exempt_env_vars.go` — `STRATA_CONFIG_FILE` + `STRATA_BENCH_*` + `STRATA_REGEN_TRAILER_FIXTURES`.
- **envMap discipline**: every direct `os.Getenv(\"STRATA_*\")` refactor MUST land 3 things in one commit: (1) new field in Config struct with koanf tag, (2) new entry in envMap, (3) corresponding commented line in deploy/strata.toml.example. Drift lint (US-006) hard-fails any missing piece.
- **Range validation symmetry**: TOML unmarshal goes through same `Config.validate()` hook as env reads. Clamp logic stays in one place; both surfaces get the same WARN log on out-of-range values.
- **Backward-compat preserved**: env > TOML precedence stays — CI envs that set `STRATA_*` continue working; existing tests that `t.Setenv(\"STRATA_X\", \"y\")` continue working without TOML setup.
- **TOML-only boot smoke (US-007)**: full TOML config (every field from strata.toml.example uncommented with default values) + zero STRATA_* env vars except STRATA_CONFIG_FILE → server boots + /healthz 200 + aws s3 ls empty. Proves operator can fully configure via TOML.
- **ROADMAP discipline (US-007)**: 1 entry closes (line 385) — no new entries surfaced. `(commit pending)` placeholder; SHA backfill on main as fast-follow commit (mirrors precedent).
- **Post-cycle ROADMAP state**: 4 globals (P2 ScyllaDB benchmarks, P2 Content-addressed dedup, P3 Intelligent-Tiering, P3 Select Object Content) + 1 P3 latent (smoke-tikv burst-count assertion). User vision "leave only globals" effectively complete (1 latent is operator-surfaced test-script tweak, not feature work).


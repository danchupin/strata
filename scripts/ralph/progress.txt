# Ralph Progress Log
Started: Sat May 30 2026
Cycle: ralph/qa-production-readiness
---

## Codebase Patterns
- CI on Linux is the source of truth. Many failures (cgo/Xcode, gosec cephimpl, Cassandra Paxos starvation, SSE-KMS, drain/rebalance) do NOT reproduce on a local macOS/lima box. Verify every story by pushing the cycle branch and reading the actual run log (`gh run list --branch ralph/qa-production-readiness`, `gh run view <id>`, `gh run view --job <jobid> --log`), not local repro.
- `internal/s3api` test build needs cgo; on a macOS box without the Xcode license it fails `build failed: Xcode license` — env issue, not a code bug. Take s3api coverage from CI; locally `CGO_ENABLED=0`-skip with a logged note.
- Test harness entry points to REUSE (do not fork parallel harnesses — FR-2 no-duplication): `internal/s3api/testutil_test.go` (`newHarness(t)`, `h.doString`, `h.mustStatus`); `internal/meta/storetest` (`Run(t, factory)` contract on TiKV + memory); `internal/racetest/workload.go` + `tracker.go` (RecordIntent before Complete for multipart etag); the `*_integration_test.go` MinIO pattern (`startMinio`, `MINIO_KMS_SECRET_KEY=strata-test-key:OSMM+vkKUTCvQs9YL/CVMIMt43HFhkUpqJxTmGl6rYw=` for SSE).
- Cassandra is NOT on the per-PR CI gate (JVM-heavy Paxos starves the runner — known latent P2). If a story needs the Cassandra contract, use the existing gated/integration path, not the PR gate. Cassandra stays first-class in code.
- FR-8 split rule: if a story can't land in one iteration, SPLIT into US-00N + a trailing story rather than half-finishing — and record the split here. US-009/US-010 (drain vs mover) already split. US-005 (auth matrix), US-011 (multi-backend corruption) are next likely split candidates.
- The cycle's single tracked artifact is `tasks/qa-readiness-report.md` — updated incrementally each story, finalized in US-013. Verdict stays PENDING until US-013.
- Verified-correct premises (checked against code at PRD time): `conditional.go` = etagMatches+checkConditional, 0 direct tests; memory backend HAS `CorruptFirstChunk()` at `internal/data/memory/backend.go:148` (the `sse_object_test.go:207` skip is stale); `ErrKeyIDMismatch` in kms/aws.go+classify.go; `ListMultipartUploadsByCluster` in store.go+tikv/queues.go; `strata_bucket_stats_shard_writes_total` in metrics.go.
- s3-tests is NOT a CI job — re-baseline (`scripts/s3-tests/run.sh`) is manual/local against the compose stack. Current baseline 92.7% (165/178); the 13 misses are deliberate SigV2/boto3-prefix/anonymous-list gaps, not real bugs.
- Branch-protection edits are a MANUAL post-merge step (headless Ralph lacks admin-API auth). Stories prepare the `gh api` command + new check context in the report; a human applies it after merge.
---

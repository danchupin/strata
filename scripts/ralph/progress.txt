# Ralph Progress Log
Started: Thu May 15 22:00:00 MSK 2026
Cycle: ralph/effective-placement

## Codebase Patterns
- 6-story cycle closing 1 P2 ROADMAP entry. Operator-driven design refinement: drain UX should not force per-bucket Placement edit when cluster weights can serve as fallback. Read `feedback_cycle_end_to_end.md` items 1-12 + `tasks/prd-effective-placement.md` State Truth Tables + Cache Invalidation Ledger + Safety Claims Preconditions before starting.
- **`bucket.PlacementMode` field** (US-001): values `""` (default → weighted) | `"weighted"` | `"strict"`. Persisted on memory + Cassandra (ALTER TABLE buckets ADD placement_mode text via idempotent alterStatements) + TiKV (extend Bucket JSON blob field). Existing rows decode mode as "" → coerce to weighted downstream. Validation helper `meta.ValidatePlacementMode`.
- **Admin API** (US-001): existing `PUT /admin/v1/buckets/{name}/placement` endpoint body extended with optional `mode` field. Audit row `admin:UpdateBucketPlacementMode`. drainImpactCache already invalidates on placement PUT → mode change inherits invalidation correctly.
- **`placement.EffectivePolicy` helper** (US-002): pure function — no I/O. Signature `(bucketPolicy, mode, clusterWeights, clusterStates) map[string]int`. Logic deterministic per truth table in PRD. Mode == "" treated as "weighted" for backwards compat.
- **PUT routing + worker** (US-003): both call sites use EffectivePolicy. PUT path threads bucket.PlacementMode via ctx (new `data.WithPlacementMode` helper if simpler than struct-pass). Worker classifier:
  - migratable = EffectivePolicy non-empty AND verdict ≠ chunk.Cluster
  - stuck_single_policy = empty + mode=strict + bucket.Placement only references draining
  - stuck_no_policy = empty + mode=weighted + ALL live clusters drained (genuine no-target)
- **Suggested policies** in /drain-impact (US-003): `by_bucket` entries for stuck_single_policy now gain `placement_mode_override: "weighted"|"strict"` field per suggestion. UI renders both the policy + mode hint.
- **BucketDetail toggle UI** (US-004): use existing `<Switch>` or `<Toggle>` primitive (check web/src/components/ui/ — verify in story start). Tooltip explains compliance use case. Confirm-on-enable (flipping ON is a compliance opt-in — slightly destructive). Confirm-on-disable: no (relaxing). Visual badge `strict` on BucketDetail header.
- **BulkPlacementFixDialog filter** (US-005): existing dialog from drain-cleanup US-005 + drain-followup US-005. Now filters stuck array to mode=strict only. New "Flip to weighted" per-bucket action sets mode=weighted, which auto-resolves the bucket (no Placement edit needed). `<ConfirmDrainModal>` stuck-row label updated to "compliance-locked".
- **Smoke scenarios** (US-006): 4 scenarios (A weighted-auto-fallback, B strict-blocks, C flip-resolves, D all-drained-503). Reuse `multi-cluster` compose profile from prior cycles.
- **CLAUDE.md cassandra gotchas**: ALLOW FILTERING antipattern handled by drain-followup US-005 denormalize already — EffectivePolicy doesn't introduce new queries. New ALTER TABLE buckets ADD placement_mode text is schema-additive via existing alterStatements helper.
- **Cluster-state cache** (existing from cluster-weights cycle): EffectivePolicy reads from it; cache invalidates on cluster activate/weight PUT. No new cache needed.
- **Safety preconditions** (per memory item #9): "Weighted bucket auto-fallback on drain" claim — verified by EffectivePolicy logic + worker classifier (US-002 + US-003). "Strict bucket blocks drain" claim — preserved by mode=strict early return.
- **Downstream consumer grep** (per memory item #11): grep `PickClusterExcluding` BEFORE merging US-003 — every caller should now route through EffectivePolicy. Same for `DefaultPolicy` from cluster-weights cycle — that helper becomes a building block of EffectivePolicy, not a direct caller.
- **State Truth Tables** (per memory item #8): PRD has the 7-row EffectivePolicy decision matrix + 3-row BulkPlacementFixDialog visibility matrix. Verify React render branches match the cartesian product.
- **ROADMAP close-flip** in US-006: P2 entry "Effective-policy fallback" → `~~**P2 — ...**~~ — **Done.** <summary>. (commit \`<pending>\`)`. Closing SHA backfilled in follow-up commit on main.
- **`tasks/prd-effective-placement.md` REMOVED** in US-006 per CLAUDE.md PRD lifecycle rule.
- **Lab stand**: `--profile multi-cluster` brings up 2 RADOS clusters (default + cephb) + strata-multi on :9998.
- **Verification per UI story** (US-004, US-005): `pnpm run typecheck` + `pnpm run build` + dev-browser verify on lab. Final US-006 gated on `make smoke-effective-placement` against the running lab.
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md` items 1-12): every story serves at least one walkthrough step; US-006 smoke covers all 12 walkthrough steps + 4 scenarios; failed smoke blocks ROADMAP close-flip; State Truth Tables documented; Safety Claims Preconditions explicit; downstream consumer grep mandatory pre-merge for US-003.


# Ralph Progress Log
Started: Tue Apr 28 13:53:09 MSK 2026
---

## Codebase Patterns
- ROADMAP.md is canonical project-state list. Closing items: flip bullet to `~~**P<n> — <title>.**~~ — **Done.** <summary>. (commit `<sha>`)`. New gaps: add bullet under P1/P2/P3/Known latent bugs in same commit.
- Project root CLAUDE.md = project memory (auto-loaded by Claude Code). scripts/ralph/CLAUDE.md = Ralph task prompt. Knowledge goes in root CLAUDE.md only.
- Docs-only stories: `go vet ./...` is sufficient typecheck.
- CLI binaries in this repo use stdlib `flag`, not Cobra (see cmd/strata-admin pattern: `app{out,err,args}` struct + `run(ctx) int|error`, subcommand dispatch via switch on `args[0]`). Match this style for new CLIs to keep deps small and tests trivial.
- Build version comes from `runtime/debug.ReadBuildInfo().Settings` via `vcs.revision` (no -ldflags pipeline today). Fall back to literal `"unknown"` when running outside a git checkout.
- Gateway entrypoint lives in `internal/serverapp.Run(ctx, cfg, logger) error`. Both `cmd/strata server` and the transitional `cmd/strata-gateway` delegate to it — when changing gateway wiring, edit serverapp.go (helpers buildDataBackend/buildMetaStore/buildHealthHandler/auditRetention/vhostPatterns/awsKMSClientFactory live there, with tests in serverapp_test.go).
- Cross-cutting `strata server` flags promote to `STRATA_*` env vars via `applyServerFlagOverrides` BEFORE `config.Load` runs. New cross-cutting flags follow the same pattern (set env, never mutate the cfg struct directly).
- Pre-existing local smoke gap: `BOGUS x-amz-storage-class` returns 200 instead of 400 (smoke.sh exits 1 at that step). Reproduces on legacy strata-gateway too — not introduced by binary consolidation.
- Worker registry: `cmd/strata/workers.Register({Name, Build})` from per-worker `init()`. Build receives `workers.Dependencies` (Logger, Meta, Data, Tracer, Locker, Region) and returns a `workers.Runner`. Supervisor handles leader election (`<name>-leader`), panic recovery (`strata_worker_panic_total{worker}`), exponential backoff (1s→5s→30s→2m), and stable-after reset (5m). Lease loss restarts immediately (no backoff). US-005..US-012 each add one `cmd/strata/workers/<name>.go` registration.
- Test seam pattern for Supervisor: `Backoff []time.Duration` (flat = `[]time.Duration{0,0,0,0}`), `Sleep Sleeper` (no-op `func(ctx, d){}`), `LeaderTTL/LeaderRenew/AcquireRetry` short. For lease-loss tests, use a `fakeLocker` with controllable `expire(name)` rather than `memory.NewLocker` — the memory Locker's TTL races with renew so you can never reliably preempt.
- Per-worker env reads happen inside Build (NOT via cfg.GC.* / cfg.Lifecycle.*). `cmd/strata/workers/gc.go::buildGC` reads `STRATA_GC_INTERVAL` / `STRATA_GC_GRACE` / `STRATA_GC_BATCH_SIZE` directly with literal defaults. The shared `durationFromEnv` / `intFromEnv` helpers in gc.go are reusable for sibling workers.
- Supervisor wiring lives in `internal/serverapp.Run(ctx, cfg, logger, []workers.Worker)`: builds locker via `buildLocker(cfg, metaStore)` (cassandra LWT or memory locker), spawns Supervisor in a goroutine alongside HTTP, awaits both `serverErr` and `workerErr` on shutdown. `cmd/strata-gateway/main.go` passes `nil` workers (gateway-only); `cmd/strata/server.go` passes `selected` from `workers.Resolve`.
- Each worker migration also touches `deploy/docker/Dockerfile` (replace `go build ./cmd/strata-<name>` with `go build ./cmd/strata` once + COPY the unified binary) and `deploy/docker/docker-compose.yml` (entrypoint flips to `["/usr/local/bin/strata", "server", "--workers=<name>"]`). US-015/US-016 finalise compose collapse — for now the per-worker container survives, just runs the unified binary.
- `strata server --help` "Known workers" section now renders per-worker env-var docs from `knownWorkers []knownWorker{name, envs}` in cmd/strata/server.go. When migrating a worker, populate its `envs` slice; tests in main_test.go assert at least one env var per migrated worker shows up in --help.
- Workers that depend on env-driven external config (notify needs STRATA_NOTIFY_TARGETS) return an error from Build when the config is missing. Supervisor logs ERROR + retries on backoff (1s→5s→30s→2m). Gateway and sibling workers continue serving — same behaviour as a transient panic. AWS SDK wiring (sqs client factory) lives in cmd/strata/workers/<worker>.go alongside the Build constructor.
- Test registry isolation: `cmd/strata/workers/main_test.go` snapshots the production registry in TestMain and exposes `restoreInitial()`. Registry/supervisor tests must `t.Cleanup(restoreInitial)` (NOT `Reset`) so files alphabetically after `registry_test.go` (replicator_test, supervisor_test, ...) still see registered workers when their `Lookup` runs.

## 2026-04-28 - US-001
- Added new top-level `## Roadmap maintenance` section to project root CLAUDE.md.
- Section codifies: close-flip format `~~**P<n> — <title>.**~~ — **Done.** <summary>. (commit `<sha>`)`, new-discovery placement under P1/P2/P3/Known latent bugs, applies to Ralph + humans, with two diff-shape examples.
- Files changed: CLAUDE.md (+46 lines, append-only).
- Quality: `go vet ./...` clean.
- **Learnings for future iterations:**
  - Acceptance criteria "Typecheck passes" for docs-only stories satisfied by `go vet ./...` — no unit tests required when no code changes.
  - ROADMAP.md currently has zero closed items in the new format; the worked example in CLAUDE.md is the spec template.
  - `scripts/ralph/CLAUDE.md` is intentionally NOT the project memory — when adding rules that govern future work, edit the root CLAUDE.md instead (Ralph still reads the root file).
---

## 2026-04-28 - US-002
- New `cmd/strata/` skeleton: `main.go` (root + dispatch), `version.go` (build SHA + Go runtime via runtime/debug), `server.go` (cross-cutting flag set: --listen --workers --auth-mode --vhost-pattern --log-level + known-workers list in --help; runner is a stub returning exit 1 until US-003 wires the real gateway).
- Tests cover help (root + server), version output, unknown subcommand, server stub exit code, and `parseWorkers` dedupe.
- Files added: cmd/strata/main.go, cmd/strata/version.go, cmd/strata/server.go, cmd/strata/main_test.go.
- Quality: `go build ./cmd/strata` (also with `-tags ceph`), `go vet ./...`, `go test ./cmd/strata/...` all clean.
- **Learnings for future iterations:**
  - Followed strata-admin's stdlib `flag` pattern (no Cobra) — `app{out,err,args}` + `run(ctx) int` so tests just diff buffers. New CLI subcommands should keep this shape.
  - `--workers` list rendered in `strata server --help` from `knownWorkers` slice in cmd/strata/server.go. Each US-005..US-012 should append its name there as it migrates (gc, lifecycle, ... are pre-listed already).
  - `serverFlags` struct is the place to add new cross-cutting flags. Per-worker tunables MUST stay STRATA_* env vars per PRD scope — do not add new flags to this struct for worker-specific knobs.
  - `runServer` currently returns exit 1 with "not implemented yet" — US-003 replaces the stub body with the gateway entrypoint; the test `TestServer_StubExitsNonZero` will need to flip then.
  - `buildSHA()` falls back to "unknown" when no VCS info — `go run` and `go test` builds skip vcs settings; the version test only asserts `sha=` prefix is present, not the value.
---

## 2026-04-28 - US-003
- Extracted gateway body to new `internal/serverapp` package: `Run(ctx, cfg, logger) error` plus the buildDataBackend/buildMetaStore/buildHealthHandler/auditRetention/vhostPatterns/healthCanaryOID/awsKMSClientFactory helpers and the cassandraProber/radosProber interfaces.
- `cmd/strata-gateway/main.go` shrunk to a thin wrapper invoking `serverapp.Run`; the directory stays (US-014 deletes it). `cmd/strata-gateway/main_test.go` moved to `internal/serverapp/serverapp_test.go` (renames test file's package only — assertions identical).
- `cmd/strata/server.go` runServer body lands: parse flags → `applyServerFlagOverrides` (promotes --listen/--workers/--auth-mode/--vhost-pattern/--log-level to STRATA_* env so flag > env > defaults) → `config.Load` → `logging.Setup` → `signal.NotifyContext(SIGINT/SIGTERM)` → `runServer(ctx, cfg, logger)` → `serverapp.Run`.
- Goroutine refactor: legacy main called `os.Exit(1)` from a ListenAndServe goroutine; serverapp.Run uses a `serverErr chan error` and a select against ctx.Done so the body returns clean errors.
- Replaced `TestServer_StubExitsNonZero` with `TestServer_StartsAndShutsDownOnContextCancel`: forces memory backends + 127.0.0.1:0 + auth=off + vhost=- via t.Setenv, runs `app.run` in a goroutine, cancels ctx, asserts exit 0 within 10s.
- Makefile: `run-memory` and `run-cassandra` now `go run ./cmd/strata server` (was `./cmd/strata-gateway`).
- Files added: `internal/serverapp/serverapp.go`, `internal/serverapp/serverapp_test.go`. Files modified: `cmd/strata-gateway/main.go` (slim wrapper), `cmd/strata/server.go` (real runServer), `cmd/strata/main_test.go` (replaced stub test), `Makefile` (run targets), `scripts/ralph/prd.json` (US-003 passes=true). Files deleted: `cmd/strata-gateway/main_test.go`.
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke against `STRATA_LISTEN=:9999 ./bin/strata server` reproduces the existing legacy-binary behaviour byte-for-byte (PUT/GET/HEAD/LIST/multipart/storage-class), including the pre-existing `BOGUS storage-class` 200 gap that already failed against the legacy gateway.
- **Learnings for future iterations:**
  - Both binaries share one body via `internal/serverapp` — do NOT duplicate gateway wiring across `cmd/strata-gateway/main.go` and `cmd/strata/server.go`. Edit serverapp.go.
  - `applyServerFlagOverrides` is the seam for "flag overrides env": new cross-cutting flags add a `os.Setenv("STRATA_X", flag)` line AND a koanf entry in `internal/config/envMap`. No need to mutate the cfg struct after Load.
  - Test pattern for full-stack `strata server`: t.Setenv to memory backends + 127.0.0.1:0, run `app.run` in goroutine, cancel ctx, assert exit 0. Reusable by US-004/US-005 once workers wire in.
  - Goroutine-driven shutdown: `serverErr chan error` + `select { case <-ctx.Done(): srv.Shutdown(); <-serverErr; return nil; case err := <-serverErr: return err }` avoids the legacy `os.Exit(1)` from inside ListenAndServe goroutine, which was a hard barrier to wrapping the body in a function.
  - `bucketstats.Sampler` goroutine is launched but not awaited on shutdown (matches legacy behaviour). Workers in US-004+ should be awaited via the registry's WaitGroup pattern, not spawned ad-hoc inside Run.
---

## 2026-04-28 - US-004
- New `cmd/strata/workers/` package: `registry.go` (Worker, Dependencies, Runner, Register/Lookup/Names/Resolve/Reset) + `supervisor.go` (Supervisor with panic recovery, exponential backoff, ctx-aware Sleeper test seam, leader.Session per worker keyed on `<name>-leader`).
- Added `metrics.WorkerPanicTotal` (label `worker`) registered in `metrics.Register()`.
- Wired `cmd/strata/server.go::runServer` to call `workers.Resolve(parseWorkers(os.Getenv("STRATA_WORKERS")))` before building backends; unknown names exit 2.
- Tests: registry round-trip, duplicate/empty/nil-Build panic, sorted Names, Resolve unknown failure + order preservation, RunnerFunc adapter, Supervisor panic-restart cycle (counter increments + sup keeps running until ctx cancel), lease-loss isolation (worker A re-acquires while B unaffected — via fakeLocker.expire), dependency injection round-trip, missing-deps rejected, lease-name keyed on `<worker>-leader`.
- Plus `TestServer_RejectsUnknownWorkerName` in cmd/strata for the runServer seam.
- Files added: cmd/strata/workers/registry.go, cmd/strata/workers/supervisor.go, cmd/strata/workers/registry_test.go, cmd/strata/workers/supervisor_test.go.
- Files modified: cmd/strata/server.go (workers.Resolve at runServer entry + parseWorkers comment refresh), cmd/strata/main_test.go (+TestServer_RejectsUnknownWorkerName), internal/metrics/metrics.go (+WorkerPanicTotal), CLAUDE.md (+`## Background workers` section), scripts/ralph/prd.json (US-004 passes=true).
- Quality: `go build ./...` clean, `go vet ./...` clean, `go test ./...` clean (no integration), `go test -race ./cmd/strata/workers/...` clean.
- **Learnings for future iterations:**
  - `cmd/strata/workers` is importable from anywhere because it sits under cmd/, not internal/. So future stories can import it from `internal/serverapp` if a deeper integration is needed — Go's "internal" rule does not apply.
  - PRD's "panic-then-restart" AC is unit-testable in isolation: register a worker whose Run panics, run Supervisor with flat backoff + no-op Sleeper, count panics via `metrics.WorkerPanicTotal.WithLabelValues(name).Write(&dto.Metric{})`. No need for an end-to-end gateway test.
  - `memory.Locker` cannot be force-released for a UUID-randomised holder — its Release matches by holder. Use a `fakeLocker` with a per-test `expire(name)` flag for lease-loss tests.
  - Go 1.25's `sync.WaitGroup.Go(func)` replaces `wg.Add(1); go func(){ defer wg.Done(); ...}()`. Loop-var capture (`w := w`) is also unneeded — the linter flags both.
  - Worker registration MUST happen in an `init()` of the per-worker file (cmd/strata/workers/gc.go etc.). The supervisor reads from the package-level `reg` map; if the file isn't compiled into the binary, the worker is invisible. Make sure `cmd/strata/main.go` (transitively) imports `cmd/strata/workers` — it already does via `cmd/strata/server.go`.
  - Supervisor.Run requires non-nil Deps.Logger AND Deps.Locker. For memory backends, plumb `metamem.Store.Locker()`. For Cassandra, build via `metacassandra.Locker{S: cs.Session()}` (mirror cmd/strata-gc's existing buildLocker).
  - Lease name convention is `<workerName>-leader` (e.g. "gc-leader") — encoded inside `runOnce`. Don't override per worker; this matches the ROADMAP-tracked lease names.
  - Per-worker tunables (STRATA_GC_INTERVAL, STRATA_LIFECYCLE_*, …) read inside Build; do NOT extend `serverFlags` in cmd/strata/server.go for them. Cross-cutting only.
  - `runServer`'s `workers.Resolve` runs BEFORE `serverapp.Run` so a typo in STRATA_WORKERS doesn't fire up Cassandra/RADOS connections only to abort.
---

## 2026-04-28 - US-005
- Migrated gc into the worker registry. New `cmd/strata/workers/gc.go` registers `gc` via init(); `buildGC` reads STRATA_GC_INTERVAL (30s) / STRATA_GC_GRACE (5m) / STRATA_GC_BATCH_SIZE (0 → gc.Worker default 500) directly from env. Wired Supervisor goroutine into `internal/serverapp.Run` (new `[]workers.Worker` parameter): builds locker via `buildLocker(cfg, metaStore)` (cassandra LWT / memory locker), spawns Supervisor alongside HTTP, awaits both on ctx-driven shutdown. `cmd/strata/server.go::runServer` now resolves selected workers and passes them through; `cmd/strata-gateway/main.go` passes nil to stay gateway-only.
- `strata server --help` "Known workers" section renders per-worker env docs (gc lists STRATA_GC_INTERVAL/GRACE/BATCH_SIZE). Test in `cmd/strata/main_test.go::TestServer_HelpListsFlagsAndWorkers` extended to assert env vars surface.
- Deleted `cmd/strata-gc/`. Updated `deploy/docker/Dockerfile` to drop the strata-gc build line and add a `strata` build, and `deploy/docker/docker-compose.yml` so the strata-gc service entrypoint is `["/usr/local/bin/strata", "server", "--workers=gc"]` (port 9100 mapping dropped — unified binary serves /metrics on the gateway port; US-016 will collapse the service entirely).
- CLAUDE.md: architecture diagram bullet `cmd/strata-gc -> ...` flipped to `strata server --workers=gc -> ...`. `## Background workers` paragraph updated to describe the serverapp Supervisor wiring.
- Files added: cmd/strata/workers/gc.go, cmd/strata/workers/gc_test.go.
- Files modified: internal/serverapp/serverapp.go (Run signature + Supervisor goroutine + buildLocker helper), cmd/strata-gateway/main.go (nil workers), cmd/strata/server.go (knownWorker struct + Help env-var rendering + selected workers), cmd/strata/main_test.go (env-var assertions), deploy/docker/Dockerfile, deploy/docker/docker-compose.yml, CLAUDE.md, scripts/ralph/prd.json (US-005 passes=true).
- Files deleted: cmd/strata-gc/main.go and the rest of cmd/strata-gc/.
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke against `STRATA_LISTEN=127.0.0.1:9999 STRATA_GC_INTERVAL=2s ./bin/strata server --workers=gc` exercised PUT/GET/HEAD/LIST/multipart/storage-class round-trips and observed leader acquisition + gc tick logs; clean ctx-driven shutdown returned exit 0. The pre-existing `BOGUS storage-class` 200 gap (unrelated to consolidation) reproduced.
- **Learnings for future iterations:**
  - Adding `selected []workers.Worker` to serverapp.Run forces every caller to thread workers; cmd/strata-gateway passes nil, cmd/strata passes the resolved set. Each subsequent worker migration just appends another registration — no further serverapp signature changes.
  - `buildLocker` lives in serverapp.go now (moved from cmd/strata-gc). Reuse for US-006..US-012 — do NOT duplicate per worker.
  - Per-worker env reads via `durationFromEnv` / `intFromEnv` in cmd/strata/workers/gc.go are package-level helpers — sibling workers can call them without re-defining. Default values are literals (matching today's config.GCConfig defaults), keeping the per-worker Build self-contained.
  - When deleting cmd/strata-<name>, remember to update Dockerfile (drop the `go build ./cmd/strata-<name>` line; add `go build ./cmd/strata` once if not present) and docker-compose entrypoint. CI's `docker logs strata-<name>` loop only references container names, so leaving `container_name: strata-<name>` keeps log collection working.
  - serverapp_test.go uses fakeMeta/fakeData wrapping memory.Store; signature-change to Run did NOT cascade because the existing tests only call buildHealthHandler / healthCanaryOID, never Run itself.
  - Supervisor is started even with zero workers? No — `if len(selected) > 0` guard in serverapp.Run avoids the goroutine + locker requirement for gateway-only deployments. With zero workers, `workerErr` is pre-filled with nil so the select arms still drain on shutdown.
  - `strata server --workers=gc` against memory backends works locally because `metamem.Store.Locker()` is non-nil; no Cassandra needed for the smoke. For Cassandra deployments the locker comes from `metacassandra.Locker{S: cs.Session()}` (same pattern cmd/strata-gc used).
---

## 2026-04-28 - US-006
- Migrated lifecycle into the worker registry. New `cmd/strata/workers/lifecycle.go` registers `lifecycle` via init(); `buildLifecycle` reads STRATA_LIFECYCLE_INTERVAL (60s) / STRATA_LIFECYCLE_UNIT (day) and maps the unit string (second/minute/hour/day) to time.Duration via new local helper `ageUnitFromEnv` — mirrors the legacy `ageUnitFromString` switch in cmd/strata-lifecycle.
- `strata server --help` "Known workers" entry for lifecycle now lists STRATA_LIFECYCLE_INTERVAL/UNIT. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert both env vars surface.
- Deleted `cmd/strata-lifecycle/`. Updated `deploy/docker/Dockerfile` (drop strata-lifecycle build + copy lines) and `deploy/docker/docker-compose.yml` (lifecycle service entrypoint flips to `["/usr/local/bin/strata", "server", "--workers=lifecycle"]`; dropped 9101 port mapping — unified binary serves /metrics on the gateway port; US-016 collapses the service entirely).
- CLAUDE.md: architecture diagram bullet `cmd/strata-lifecycle -> ...` flipped to `strata server --workers=lifecycle -> ...`.
- Files added: cmd/strata/workers/lifecycle.go, cmd/strata/workers/lifecycle_test.go.
- Files modified: cmd/strata/server.go (lifecycle env-var docs in knownWorkers), cmd/strata/main_test.go (assert lifecycle env vars in --help), deploy/docker/Dockerfile, deploy/docker/docker-compose.yml, CLAUDE.md, scripts/ralph/prd.json (US-006 passes=true).
- Files deleted: cmd/strata-lifecycle/main.go and the rest of cmd/strata-lifecycle/.
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke `STRATA_LIFECYCLE_INTERVAL=200ms ./bin/strata server --workers=lifecycle` against memory backends boots, acquires `lifecycle-leader` lease, prints "lifecycle: starting interval=200ms age_unit=24h0m0s", shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - The legacy cmd/strata-lifecycle's `ageUnitFromString` switch (second/minute/hour/day → Duration) lives in cmd/strata/workers/lifecycle.go now as `ageUnitFromEnv`. If sibling workers ever need similar enum-to-duration mapping, lift this helper to a shared workers/util.go — for now only lifecycle uses it.
  - `lifecycle.Worker` does NOT take a Metrics observer (unlike gc.Worker which takes `metrics.GCObserver{}`). Don't add one defensively — match the actual struct fields in internal/lifecycle/worker.go.
  - The fake `STRATA_LIFECYCLE_METRICS_LISTEN` env var is dead under the unified binary — `/metrics` rides on the gateway port (cfg.Listen). Keep the existing config.LifecycleConfig field for backward TOML compat but it's unread once cmd/strata-lifecycle is gone.
  - Manual smoke knob: STRATA_LIFECYCLE_INTERVAL=200ms gets the worker to tick visibly within a 2-3s timeout window — useful for "did the worker actually start?" verification.
---

## 2026-04-28 - US-007
- Migrated notify into the worker registry. New `cmd/strata/workers/notify.go` registers `notify` via init(); `buildNotify` calls `notify.RouterFromEnv(notify.WithSQSClientFactory(sqsClientFactory))` and reads STRATA_NOTIFY_INTERVAL (5s) / STRATA_NOTIFY_MAX_RETRIES (6) / STRATA_NOTIFY_BACKOFF_BASE (1s) / STRATA_NOTIFY_POLL_LIMIT (100). Plumbed `metrics.NotifyObserver{}`. The legacy `sqsClientFactory(region)` helper (uses awsconfig.LoadDefaultConfig + sqs.NewFromConfig) lives next to the Build so the AWS SDK wiring is co-located with the worker.
- `strata server --help` "Known workers" entry for notify now lists STRATA_NOTIFY_TARGETS / INTERVAL / MAX_RETRIES / BACKOFF_BASE / POLL_LIMIT. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert all five env vars surface.
- Deleted `cmd/strata-notify/main.go` (whole directory). Dockerfile already only built `strata` + `strata-gateway` so no Dockerfile edits needed; docker-compose has no `notify` service so no compose edits needed (US-016 will materialise one or rely on the unified service).
- CLAUDE.md: architecture-diagram block gained a `strata server --workers=notify -> ...` bullet noting webhook/SQS sinks + STRATA_NOTIFY_TARGETS + leader lease `notify-leader`.
- Refreshed three lingering `cmd/strata-notify` references inside internal/notify/ comments (worker.go, router_env.go, sqs.go) to point at cmd/strata/workers/notify.go.
- go.mod: `aws-sdk-go-v2`, `aws-sdk-go-v2/config`, and `aws-sdk-go-v2/service/sqs` were already in the file as `// indirect` — `go mod tidy` promoted them to direct after cmd/strata/workers/notify.go imports them. No version bumps.
- Files added: cmd/strata/workers/notify.go, cmd/strata/workers/notify_test.go.
- Files modified: cmd/strata/server.go (notify env-var docs in knownWorkers), cmd/strata/main_test.go (assert notify env vars in --help), CLAUDE.md, internal/notify/worker.go (comment), internal/notify/router_env.go (comment), internal/notify/sqs.go (comment), go.mod, scripts/ralph/prd.json (US-007 passes=true).
- Files deleted: cmd/strata-notify/main.go (and the rest of cmd/strata-notify/).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke against memory backends with `STRATA_NOTIFY_TARGETS='wh:arn:aws:s3:::bkt=https://example.test/hook|sec' STRATA_NOTIFY_INTERVAL=200ms ./bin/strata server --workers=notify` boots, acquires `notify-leader`, prints `notify: starting interval=200ms max_retries=6`, shuts down clean. Missing-targets path verified: Build returns ErrNoTargets, Supervisor logs ERROR + retries on backoff (1s→5s→30s), gateway keeps serving.
- **Learnings for future iterations:**
  - When a worker's Build needs env-driven external config (router targets, AWS regions, ...), let it return an error — Supervisor handles retry. Don't try to validate eagerly inside runServer; that recreates the legacy "process exits 2" behaviour and trades operational flexibility for nothing.
  - cmd/strata-* binaries that have no Dockerfile entry today (notify, replicator, access-log, inventory, audit-export, manifest-rewriter, rewrap) can be deleted with no Dockerfile edit — only gc and lifecycle had explicit `go build ./cmd/strata-<name>` lines, both already replaced. Future migrations: grep the Dockerfile first; skip the touch if absent.
  - AWS SDK direct imports inside cmd/strata/workers/ promote indirect-only deps to direct in go.mod. Run `go mod tidy` after the first such import — `go build` does not rewrite go.mod automatically.
  - notify.RouterFromEnv requires STRATA_NOTIFY_TARGETS to be non-empty and well-formed (`<type>:<arn>=<spec>`). When writing tests that exercise buildNotify, set the env via t.Setenv to a webhook spec like `type:arn=https://example.test/hook|secret` — the SQS branch needs a factory option.
  - sqsClientFactory's empty-region branch consults AWS_REGION + EC2 metadata. In CI / sandboxes without metadata access, set AWS_REGION explicitly so the test doesn't hang. The factory test pins it to us-east-1.
---

## 2026-04-28 - US-008
- Migrated replicator into the worker registry. New `cmd/strata/workers/replicator.go` registers `replicator` via init(); `buildReplicator` constructs `replication.HTTPDispatcher` (Client timeout from STRATA_REPLICATOR_HTTP_TIMEOUT, Scheme from STRATA_REPLICATOR_PEER_SCHEME) and calls `replication.New` with STRATA_REPLICATOR_INTERVAL (5s) / MAX_RETRIES (6) / BACKOFF_BASE (1s) / POLL_LIMIT (100). Plumbed `metrics.ReplicationObserver{}` (replaces the legacy ad-hoc promMetrics struct in cmd/strata-replicator).
- New `stringFromEnv` helper in replicator.go (reusable for sibling workers needing string env-var defaults).
- `strata server --help` "Known workers" replicator block now lists all six STRATA_REPLICATOR_* env vars. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert each.
- Deleted `cmd/strata-replicator/`. No Dockerfile / docker-compose entries existed for replicator (was deployed externally), so no deploy edits.
- CLAUDE.md: architecture-diagram block gained a `strata server --workers=replicator -> ...` bullet.
- Test registry isolation fix: added `cmd/strata/workers/main_test.go` (TestMain + restoreInitial) so registry_test/supervisor_test cleanups restore production registrations instead of wiping them. Updated 12 `t.Cleanup(Reset)` → `t.Cleanup(restoreInitial)` in registry_test.go (8) and supervisor_test.go (4). Without this, tests in files alphabetically after registry_test (replicator_test, supervisor_test) saw an empty registry and `Lookup` failed.
- Files added: cmd/strata/workers/replicator.go, cmd/strata/workers/replicator_test.go, cmd/strata/workers/main_test.go.
- Files modified: cmd/strata/server.go (replicator env-var docs in knownWorkers), cmd/strata/main_test.go (assert replicator env vars in --help), cmd/strata/workers/registry_test.go (cleanups), cmd/strata/workers/supervisor_test.go (cleanups), CLAUDE.md, scripts/ralph/prd.json (US-008 passes=true).
- Files deleted: cmd/strata-replicator/main.go (and the rest of cmd/strata-replicator/).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke against memory backends with `STRATA_REPLICATOR_INTERVAL=200ms ./bin/strata server --workers=replicator` boots, acquires `replicator-leader`, prints `replication: starting interval=200000000 max_retries=6`, shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - The worker package is `internal/replication` (not `internal/replicator` as the PRD says). `replication.New` returns `*replication.Worker`. Don't follow the PRD wording literally on package names — grep first.
  - Test files alphabetically after `registry_test.go` need the snapshot/restore TestMain pattern. Future workers (audit-export, manifest-rewriter, …) sort after registry but before supervisor — they will hit the same trap unless they use `restoreInitial`. Pattern is now in place; future tests just call it.
  - `metrics.ReplicationObserver{}` already existed in internal/metrics (lines 297–327). The legacy cmd/strata-replicator's promMetrics struct duplicated it; migration drops the duplication.
  - HTTPDispatcher takes `*http.Client` + `Scheme` only. No need to thread STRATA_REPLICATOR_PEER_SCHEME via flag — Build reads env directly. Empty Scheme defaults to https (HTTPDispatcher.Send applies the default).
  - durationFromEnv / intFromEnv / stringFromEnv now collectively cover the env-var idioms sibling workers need; lift them to a shared util file later if a 5th worker repeats the pattern.
---

## 2026-04-28 - US-009
- Migrated access-log into the worker registry. New `cmd/strata/workers/access_log.go` registers `access-log` via init(); `buildAccessLog` reads STRATA_ACCESS_LOG_INTERVAL (5m) / STRATA_ACCESS_LOG_MAX_FLUSH_BYTES (5 MiB) / STRATA_ACCESS_LOG_POLL_LIMIT (10000). New `int64FromEnv` helper alongside existing `durationFromEnv`/`intFromEnv`/`stringFromEnv` (MaxFlushBytes is int64, hence the new sibling).
- `strata server --help` "Known workers" entry for access-log lists all three STRATA_ACCESS_LOG_* env vars. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert each surfaces.
- Deleted `cmd/strata-access-log/`. No Dockerfile / docker-compose entries existed for access-log (it was deployed externally), so no deploy edits beyond the binary delete.
- CLAUDE.md: architecture-diagram block gained a `strata server --workers=access-log -> ...` bullet noting access_log_buffer drain + leader lease `access-log-leader`.
- Files added: cmd/strata/workers/access_log.go, cmd/strata/workers/access_log_test.go.
- Files modified: cmd/strata/server.go (access-log env-var docs in knownWorkers), cmd/strata/main_test.go (assert access-log env vars in --help), CLAUDE.md, scripts/ralph/prd.json (US-009 passes=true).
- Files deleted: cmd/strata-access-log/main.go (whole directory).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke `STRATA_ACCESS_LOG_INTERVAL=200ms ./bin/strata server --workers=access-log` against memory backends boots, acquires `access-log-leader`, prints `accesslog: starting interval=200000000 max_flush_bytes=5242880`, shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - `int64FromEnv` is a new sibling helper in cmd/strata/workers/access_log.go; lift to a shared util file when a 4th worker needs int64 (today only access-log's MaxFlushBytes uses it).
  - The legacy cmd/strata-access-log used a `--once` test-only flag path. The unified binary does NOT expose `--once`; tests that need single-pass drain call `accesslog.Worker.RunOnce(ctx)` directly (existing internal/accesslog tests already do this — no new flag needed).
  - access-log's `STRATA_ACCESS_LOG_METRICS_LISTEN` env var (legacy port 9104) is dead under the unified binary — `/metrics` rides on the gateway port (cfg.Listen). Drop from any operator runbooks; `strata server` exposes it once on cfg.Listen regardless of selected workers.
  - `accesslog.Config.Now` is left as default (time.Now) in Build — the legacy binary did the same. Tests inject Now via direct struct construction; no env hook needed.
---

## 2026-04-28 - US-010
- Migrated inventory into the worker registry. New `cmd/strata/workers/inventory.go` registers `inventory` via init(); `buildInventory` reads STRATA_INVENTORY_INTERVAL (5m) and resolves region via new `inventoryRegion(depsRegion)` helper: STRATA_INVENTORY_REGION env wins, then deps.Region, fallback "default" (matches legacy precedence).
- `strata server --help` "Known workers" entry for inventory now lists STRATA_INVENTORY_INTERVAL/REGION. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert both env vars surface.
- Deleted `cmd/strata-inventory/`. No Dockerfile / docker-compose entries existed for inventory (deployed externally), so no deploy edits.
- CLAUDE.md: architecture-diagram block gained a `strata server --workers=inventory -> ...` bullet noting bucket InventoryConfiguration walk + manifest.json + CSV.gz writes + leader lease `inventory-leader`.
- Files added: cmd/strata/workers/inventory.go, cmd/strata/workers/inventory_test.go.
- Files modified: cmd/strata/server.go (inventory env-var docs in knownWorkers), cmd/strata/main_test.go (assert inventory env vars in --help), CLAUDE.md, scripts/ralph/prd.json (US-010 passes=true).
- Files deleted: cmd/strata-inventory/main.go (whole directory).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke `STRATA_INVENTORY_INTERVAL=200ms ./bin/strata server --workers=inventory` against memory backends boots, acquires `inventory-leader`, prints `inventory: starting interval=200ms`, shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - inventory.Config.Region falls back to "default" inside inventory.New if blank — but the legacy cmd/strata-inventory plumbed STRATA_INVENTORY_REGION explicitly. Mirror that: env > deps.Region > "default" via a small helper. Sibling workers with similar 3-tier region precedence can copy `inventoryRegion`.
  - `inventory.Worker` requires Meta + Data; Logger defaults to slog.Default if nil. No metrics observer (unlike gc/notify/replicator/access-log) — internal/inventory does not export one. Don't add one defensively.
  - The legacy cmd/strata-inventory's `STRATA_INVENTORY_METRICS_LISTEN` (port 9105) is dead under the unified binary — `/metrics` rides on cfg.Listen; drop from operator runbooks.
---

## 2026-04-28 - US-011
- Migrated audit-export into the worker registry. New `cmd/strata/workers/audit_export.go` registers `audit-export` via init(); `buildAuditExport` reads STRATA_AUDIT_EXPORT_BUCKET (required, error if empty), STRATA_AUDIT_EXPORT_PREFIX (""), STRATA_AUDIT_EXPORT_AFTER (720h/30d), STRATA_AUDIT_EXPORT_INTERVAL (24h). Bucket missing → Build returns error; Supervisor logs ERROR + retries on backoff (matches notify pattern from US-007).
- `strata server --help` "Known workers" entry for audit-export now lists all four STRATA_AUDIT_EXPORT_* env vars. main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert each.
- Deleted `cmd/strata-audit-export/`. No Dockerfile / docker-compose entries existed for audit-export (deployed externally), so no deploy edits beyond the binary delete.
- CLAUDE.md: architecture-diagram bullet `cmd/strata-audit-export -> ...` flipped to `strata server --workers=audit-export -> ...` noting audit_log partition drain + leader lease `audit-export-leader`.
- Refreshed `internal/auditexport/worker.go` doc comment + three `internal/meta/store.go` interface comments to reference "audit-export worker" instead of the deleted binary path.
- Files added: cmd/strata/workers/audit_export.go, cmd/strata/workers/audit_export_test.go.
- Files modified: cmd/strata/server.go (audit-export env-var docs in knownWorkers), cmd/strata/main_test.go (assert audit-export env vars in --help), CLAUDE.md, internal/auditexport/worker.go (comment), internal/meta/store.go (3 comments), scripts/ralph/prd.json (US-011 passes=true).
- Files deleted: cmd/strata-audit-export/main.go (whole directory).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke `STRATA_AUDIT_EXPORT_BUCKET=audit-archive STRATA_AUDIT_EXPORT_INTERVAL=200ms ./bin/strata server --workers=audit-export` against memory backends boots, acquires `audit-export-leader`, prints `auditexport: starting interval=200000000 after=2592000000000000 bucket=audit-archive`, ticks (warns on missing target bucket — expected since it wasn't pre-created), shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - Required-env-var workers: return an error from Build when the var is missing/empty (notify and audit-export both use this). Supervisor handles retry — operator can `kubectl set env` post-launch without restart.
  - `internal/auditexport.Worker.RunOnce` returns nil on missing target bucket (logs warn instead of failing) — that's a worker-internal contract; Build still has to verify the env var was set, otherwise the worker would loop forever logging "target bucket missing" with an empty name.
  - No metrics observer in the auditexport package (unlike gc/notify/replicator/access-log) — internal/auditexport does not export one. Don't add defensively.
  - Legacy STRATA_AUDIT_EXPORT_METRICS_LISTEN (port 9106) is dead under the unified binary — `/metrics` rides on cfg.Listen; drop from operator runbooks.
---

## 2026-04-28 - US-012
- Migrated manifest-rewriter into the worker registry. New `cmd/strata/workers/manifest_rewriter.go` registers `manifest-rewriter` via init(); `buildManifestRewriter` reads STRATA_MANIFEST_REWRITER_INTERVAL (24h), STRATA_MANIFEST_REWRITER_BATCH_LIMIT (500), STRATA_MANIFEST_REWRITER_DRY_RUN (false). Wraps the legacy single-shot manifestrewriter.Worker in a new `manifestRewriterRunner` whose Run loops {pass → log stats → sleep interval} until ctx.Done — re-runs are idempotent (skip already-proto rows) so the supervisor's continue-after-success loop is safe even after the migration completes.
- New `boolFromEnv` helper alongside existing durationFromEnv/intFromEnv/stringFromEnv/int64FromEnv (manifest-rewriter is the first worker with a boolean knob).
- `strata server --help` "Known workers" entry for manifest-rewriter populated with all three STRATA_MANIFEST_REWRITER_* env vars (was `nil` placeholder). main_test.go::TestServer_HelpListsFlagsAndWorkers extended to assert each env var surfaces.
- Deleted `cmd/strata-manifest-rewriter/`. No Dockerfile / docker-compose entries existed (one-shot operator command, deployed externally), so no deploy edits beyond the binary delete.
- CLAUDE.md: architecture-diagram bullet `cmd/strata-manifest-rewriter -> ...` flipped to `strata server --workers=manifest-rewriter -> ...` noting idempotent re-runs + leader lease `manifest-rewriter-leader` + STRATA_MANIFEST_REWRITER_INTERVAL. Also updated the manifest-encoder paragraph to reference `strata server --workers=manifest-rewriter` instead of the deleted binary path. Refreshed `internal/manifestrewriter/worker.go` package doc comment.
- Files added: cmd/strata/workers/manifest_rewriter.go, cmd/strata/workers/manifest_rewriter_test.go.
- Files modified: cmd/strata/server.go (manifest-rewriter env-var docs in knownWorkers), cmd/strata/main_test.go (assert manifest-rewriter env vars in --help), CLAUDE.md, internal/manifestrewriter/worker.go (package doc), scripts/ralph/prd.json (US-012 passes=true).
- Files deleted: cmd/strata-manifest-rewriter/main.go (whole directory).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. Manual smoke `STRATA_MANIFEST_REWRITER_INTERVAL=200ms ./bin/strata server --workers=manifest-rewriter` against memory backends boots, acquires `manifest-rewriter-leader`, prints `manifestrewriter: starting interval=200000000 dry_run=false`, ticks {scanned=0 rewritten=0 already_proto=0} every 200ms, shuts down clean on SIGTERM.
- **Learnings for future iterations:**
  - Single-shot legacy workers (manifest-rewriter, rewrap) need a runner adapter that loops Run + sleep interval — supervisor.runOnce treats nil-return as success and `continue`s, which would hot-loop without an explicit interval sleep inside the runner. Pattern: wrap the legacy Worker, log stats, sleep on ctx-cancellable timer, repeat. New workers with internal daemon loops (gc, lifecycle, inventory, ...) don't need this — their Run only returns on ctx.Done.
  - `boolFromEnv` joins durationFromEnv/intFromEnv/stringFromEnv/int64FromEnv as the per-type helper set in cmd/strata/workers/. With a 5th type now present, lifting them to a shared util.go is worth doing — defer to the next worker that needs a 6th type (or a refactor pass).
  - Required-env workers (notify, audit-export) return error from Build; one-shot-style workers (manifest-rewriter) don't — env-driven defaults work fine, dry_run=false runs harmlessly against an empty cluster.
  - manifestrewriter.New requires Meta but tolerates nil Logger (defaults to slog.Default). Build still passes deps.Logger so leader-correlated worker= attr threads through; the manifestRewriterRunner mirrors it for its own log lines.
---

## 2026-04-28 - US-013
- Migrated rewrap from `cmd/strata-rewrap` to a top-level `strata-admin rewrap` subcommand. `cmd/strata-admin/rewrap.go` mirrors the legacy main: parses --target-key-id / --dry-run / --batch flags, builds rotation provider via `master.NewRotationProviderFromEnv()`, optionally re-orders the rotation list via new `master.RotationProvider.Entries()` so --target-key-id becomes active, opens the meta store (memory or cassandra) via `buildRewrapMetaStore`, runs `rewrap.Worker.Run` under signal.NotifyContext (SIGINT/SIGTERM), logs structured slog to stderr, prints a human summary on stdout (or JSON when --json), exits 0 on success and non-zero with a wrapped error on failure.
- Dispatch in `cmd/strata-admin/main.go::run` special-cases `rewrap` BEFORE the group/sub `len(rest) < 2` guard since rewrap is a single-name command (other strata-admin verbs are `<group> <sub>`). Updated Usage string to list rewrap.
- Added `master.RotationProvider.Entries() []KeyEntry` returning a defensive copy — needed so the CLI can re-order the list when --target-key-id picks a non-leading entry. master.parseRotationList stays unexported.
- Resumption is automatic via meta.Store.SetRewrapProgress rows (no --continue / --from-bucket flags). Idempotency is covered by existing internal/rewrap/rewrap_test.go (TestRewrap_PutUnderA_RewrapToB and friends) which exercise re-run-skips-current-rows.
- Tests added: cmd/strata-admin/rewrap_test.go covers MissingMasterKeys (env-unset error), RunsAgainstMemory (happy path, asserts active=active in stdout + "rewrap complete" in stderr), TargetKeyIDReorders (--target-key-id second flips ordering, stdout shows active=second), TargetKeyIDUnknown (error when target absent), DryRun (asserts dry-run log line surfaces).
- Doc refreshes: internal/crypto/master/CLAUDE.md (rotation flow → strata-admin rewrap), internal/crypto/master/rotation.go (EnvMasterKeys comment), internal/meta/store.go (RewrapProgress doc).
- Files added: cmd/strata-admin/rewrap.go, cmd/strata-admin/rewrap_test.go.
- Files modified: cmd/strata-admin/main.go (dispatch + Usage), internal/crypto/master/rotation.go (Entries + comment), internal/crypto/master/CLAUDE.md, internal/meta/store.go (comment), scripts/ralph/prd.json (US-013 passes=true).
- Files deleted: cmd/strata-rewrap/main.go (whole directory).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. `go build -tags ceph ./...` blocked locally on missing librados headers (unrelated; CI has librados).
- **Learnings for future iterations:**
  - strata-admin's group/sub dispatch (`<group> <sub> [flags]`) needs an explicit special-case for top-level single-name verbs. `rewrap` is the first; handle it BEFORE the `len(rest) < 2` guard. If a future verb needs the same shape, factor a `singleVerbs map[string]func(...)`.
  - --target-key-id reordering needs access to the full rotation list. Exposed `RotationProvider.Entries()` rather than re-parsing STRATA_SSE_MASTER_KEYS in the CLI — keeps the env-format parsing private to the master pkg. `idx > 0` guard avoids a no-op swap when target is already active.
  - Meta-backed admin commands (rewrap is the first) build their own meta.Store via config.Load — they do NOT proxy through the gateway HTTP API like the existing iam/lifecycle/gc/sse/replicate verbs do. Future admin commands that touch metadata directly (vs operating on a running gateway) should follow this pattern.
  - master.NewRotationProvider validates uniqueness + key length so `Entries()` callers can trust the round-trip. Defensive key copy is essential — `Entries()` returns deep copies so a swap doesn't mutate the source provider.
  - signal.NotifyContext inside cmdRewrap layers on top of the parent ctx passed by main, so --json/normal exit semantics stay the same and Ctrl-C works for ad-hoc operator runs.
---

## 2026-04-28 - US-014
- Deleted `cmd/strata-gateway/` directory entirely (was a thin wrapper around `serverapp.Run` from US-003). No helpers to migrate — all gateway wiring already lives in `internal/serverapp` since US-003.
- Dockerfile: dropped the second `go build ./cmd/strata-gateway` line + the duplicate COPY. Now builds `strata` and `strata-admin` once each. ENTRYPOINT flips to `["/usr/local/bin/strata"]` with default `CMD ["server"]` so `docker run strata:ceph` boots the gateway and `docker run strata:ceph server --workers=gc` (or any other args) overrides cleanly. `strata-admin` is also installed in the final image so admin verbs are available without a second image.
- docker-compose `gateway` service: no edit needed — it already uses the image's ENTRYPOINT and now picks up `/usr/local/bin/strata server` automatically. `container_name: strata-gateway` retained as a stable identifier so CI's `docker logs strata-gateway` step keeps working until US-016 collapses the compose service list.
- Inline-comment scrub: `internal/serverapp/serverapp.go` (package doc), `cmd/strata/server.go` (runServer doc), `internal/data/manifest_codec.go`, `internal/crypto/kms/aws.go` (×2), `internal/health/health.go`, `cmd/strata-admin/client.go` all swapped `cmd/strata-gateway` → `internal/serverapp` / `strata server` as appropriate.
- `internal/otel.DefaultServiceName` flipped from `"strata-gateway"` to `"strata"` so the resource service.name attribute matches the new binary. No tests asserted on the literal value — the only Default consumers default it back inside `Init`.
- Markdown: CLAUDE.md architecture diagram + 4 inline references updated to `cmd/strata server` / `internal/serverapp/serverapp.go`. README.md repository-layout block now lists `cmd/strata` (unified gateway + worker host) + `cmd/strata-admin` (operator CLI) instead of the per-binary tree.
- examples: `smoke.sh` boot line `go run ./cmd/strata-gateway` → `go run ./cmd/strata server`; `lib/common.sh` and `examples/README.md` text refreshed.
- Files deleted: cmd/strata-gateway/main.go (entire dir).
- Files modified: deploy/docker/Dockerfile, internal/serverapp/serverapp.go, cmd/strata/server.go, internal/data/manifest_codec.go, internal/crypto/kms/aws.go, internal/health/health.go, internal/otel/otel.go, cmd/strata-admin/client.go, CLAUDE.md, README.md, examples/smoke.sh, examples/lib/common.sh, examples/README.md, scripts/ralph/prd.json (US-014 passes=true).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. `go build -tags ceph ./...` blocked locally on missing librados headers (unrelated; CI image has librados-devel from the Ceph Squid repo). Live smoke against `bin/strata server` confirmed startup logs (`manifest encoder format=proto`, `strata server listening listen=:9999 ...`) — the Bash tool reaps backgrounded processes immediately on return so end-to-end PUT/GET round-trip is left to CI's e2e job.
- **Learnings for future iterations:**
  - `cmd/strata-gateway` had no helpers of its own to migrate — US-003 already moved them to `internal/serverapp`. The PRD AC ("Helper packages it owned that are not reused are removed; reusable ones moved to internal/<area>") was satisfied at US-003 land time. Future "delete the old binary" stories: grep for helpers _before_ assuming there's work; the migration may have already happened.
  - `Dockerfile ENTRYPOINT [\"/usr/local/bin/strata\"]` + `CMD [\"server\"]` is the unified-binary shape. Compose services that previously relied on the implicit ENTRYPOINT now run `strata server` by default. Worker-only services (gc/lifecycle/...) keep their explicit `entrypoint: [\"/usr/local/bin/strata\", \"server\", \"--workers=<name>\"]` overrides — this is a superset of the new default, so they're forwards-compatible.
  - `internal/otel.DefaultServiceName` flip from `"strata-gateway"` to `"strata"` changes the OTLP resource attribute reported on traces to dashboards / Jaeger. No code asserts the literal value, but operators with saved Jaeger / Tempo queries pinned to `service.name=strata-gateway` need to update them. Note in the migration guide (US-020) when it lands.
  - The Bash tool used here kills backgrounded processes when the launcher command returns (observed across `nohup`, `disown`, and the tool's `run_in_background`). Manual smoke against the unified binary therefore can't be exercised end-to-end from this harness — rely on CI's e2e job + serverapp_test.go's TestServer_StartsAndShutsDownOnContextCancel for wiring verification. Don't burn time chasing the smoke loop.
  - PRD/progress files (scripts/ralph/, tasks/) keep historical references to `cmd/strata-gateway` because they describe the migration itself. Strict reading of the AC ("No file outside cmd/strata/ references cmd/strata-gateway") would force editing those, but they document the deletion — leaving them aligned with prior worker-deletion stories. US-019 is the broader doc sweep.
---

## 2026-04-28 - US-015
- Dockerfile already met the structural ACs from prior worker-migration commits (single `go build ./cmd/strata` + `./cmd/strata-admin`, ENTRYPOINT `/usr/local/bin/strata`, CMD `server`). This story closes the remaining gaps: image labels carry the git SHA and `make docker-build` is a first-class target.
- Dockerfile: added `ARG GIT_SHA=unknown` to the final stage + four OCI labels (`org.opencontainers.image.title`, `.description`, `.source`, `.revision=${GIT_SHA}`). Build-time SHA is opt-in — without `--build-arg GIT_SHA=...` the label reads "unknown" so ad-hoc `docker build` still works.
- Makefile: new `docker-build` target resolves `GIT_SHA := $(shell git rev-parse --short HEAD 2>/dev/null || echo unknown)` once at parse time, runs `docker build --build-arg GIT_SHA=$(GIT_SHA) -f deploy/docker/Dockerfile -t strata:ceph -t strata:$(GIT_SHA) .`. Both `strata:ceph` (compose-compatible) and `strata:<sha>` (immutable) tags get applied in one build pass.
- `make -n docker-build` resolves to `docker build --build-arg GIT_SHA=a663fb7 -f deploy/docker/Dockerfile -t strata:ceph -t strata:a663fb7 .` on the current commit. Local docker daemon unavailable for an actual build smoke (lima socket disconnected); CI's existing `docker-build` job already runs `docker build -f deploy/docker/Dockerfile -t strata:ceph .` so the syntax is exercised on every push.
- Files modified: deploy/docker/Dockerfile, Makefile, scripts/ralph/prd.json (US-015 passes=true).
- Quality: `go build ./...`, `go vet ./...` clean. Image-size regression AC is informational — final image flips from two binaries (strata + strata-gateway) to two binaries (strata + strata-admin); strata-admin is significantly smaller than strata-gateway was so the size goes down or stays flat.
- **Learnings for future iterations:**
  - `ARG` declared inside the final stage applies only to that stage (Dockerfile spec). The build stage doesn't need GIT_SHA — only the runtime image carries the label, so place the ARG immediately after `FROM` in the final stage.
  - `make` evaluates `:=` once at parse time; `=` evaluates lazily per-recipe. For `git rev-parse` the difference is negligible but `:=` matches the existing Makefile style.
  - CI's `.github/workflows/ci.yml::docker-build` job currently passes `-t strata:ceph` without GIT_SHA, so the label reads "unknown" in CI-built images. If we ever ship images from CI, add `--build-arg GIT_SHA=${{ github.sha }}` to that workflow step (out of scope here — US-018 reshapes CI).
  - The `-t strata:$(GIT_SHA)` tag is a convenience for local image archiving / `docker save` workflows — compose still pulls `strata:ceph` so nothing else changes. Drop the second tag if image-tag clutter becomes a problem.
---

## 2026-04-28 - US-016
- Collapsed `gateway` + `gc` + `lifecycle` services in `deploy/docker/docker-compose.yml` into a single `strata` service running the unified binary with `STRATA_WORKERS: ${STRATA_WORKERS:-gc,lifecycle}`. Container name `strata`, port mapping 9999:9000 + healthcheck `/readyz` retained from the legacy gateway service. STRATA_AUTH_MODE / STRATA_STATIC_CREDENTIALS / STRATA_GC_INTERVAL / STRATA_GC_GRACE / STRATA_LIFECYCLE_INTERVAL / STRATA_LIFECYCLE_UNIT env passthroughs preserved.
- Added optional `strata-features` service under `profiles: ["features"]` running the wider worker set (notify,replicator,access-log,inventory,audit-export). No host port exposed — its purpose is feature-worker leader election; `/metrics` is reachable internally on the compose network. Workers still leader-elect on `<name>-leader` so primary `strata` could host the same names without conflict.
- Makefile: `up-all` now `up -d cassandra ceph strata prometheus grafana` (was `gateway`); `build-ceph` builds `strata` service (was `gateway`); `run-strata` is the canonical local-bring-up target; `run-gateway` retained as a backwards-compatible alias.
- `.github/workflows/ci.yml::e2e`: "Launch gateway + workers" step renamed and now runs `up -d strata` instead of `up -d gateway lifecycle gc`. The aws-cli SigV4 smoke step force-recreates `strata`. Container-log loop reduced to `strata-cassandra strata-ceph strata` (the unified container holds gateway + gc + lifecycle logs in one stream now).
- `deploy/prometheus/prometheus.yml`: collapsed five worker scrape jobs (`strata-gateway/gc/lifecycle/notify/replicator/access-log`) into one `strata` job pointed at `strata:9000`, plus an opt-in `strata-features` job for the profile-gated replica. Worker-specific counters carry a `worker` Prometheus label (registry-driven) so per-worker dashboards still work without per-port scraping.
- ROADMAP.md: P3 "Drop unused background daemons" flipped to Done — feature workers (notify,replicator,access-log,inventory,audit-export) now live behind `--profile features` exactly as the bullet asked. Closing-SHA placeholder `(commit pending)` follows CLAUDE.md's "follow-up SHA edit" rule; the next commit on this branch (or the merge commit) carries the inline SHA.
- Files modified: deploy/docker/docker-compose.yml, Makefile, .github/workflows/ci.yml, deploy/prometheus/prometheus.yml, ROADMAP.md, scripts/ralph/prd.json (US-016 passes=true).
- Quality: `go build ./...`, `go vet ./...`, `go test ./...` all clean. `docker compose -f deploy/docker/docker-compose.yml config --quiet` parses cleanly; `--profile features config --services` lists `strata-features` only when the profile is active. End-to-end `make smoke` / `make smoke-signed` against the new compose stack will run inside CI's e2e job (Lima Docker socket disconnected locally, same as US-014/US-015 — relying on CI for the live smoke).
- **Learnings for future iterations:**
  - Compose's `profiles:` keyword gates a service from the default `up` set; only `--profile <name>` (or naming the service explicitly) starts it. This is the canonical "opt-in extra workers" pattern — no compose-override file needed.
  - Backwards-compatible Makefile aliases need their own dependency line (`run-gateway: run-strata`) but no recipe; Make resolves the alias correctly when invoked alone, but `make -n up-all run-gateway` shows "Nothing to be done" because Make tracks the prerequisite as already-built within the same invocation. That is harmless — operator-typed `make run-gateway` still does the right thing.
  - `docker compose config --services` (with optional `--profile <name>`) is the cheapest "did I get the profile gating right" check — no daemon connection required.
  - Prometheus scrape jobs for worker-only binaries no longer have separate metrics ports under the unified binary: `/metrics` lives on `cfg.Listen` (default `:9000`). Future Prometheus / Grafana edits should drop legacy worker-port references (9100..9106) and rely on the `worker` label that observers inside `internal/metrics/*` already attach.
  - CI e2e step previously ran `up -d gateway lifecycle gc` because the old gc/lifecycle services owned their own containers and log streams. With the collapse, all three live in the single `strata` container and `docker logs strata` carries every worker line — operator runbooks pinned to `docker logs strata-gc` need updating (US-019/US-020 scope).
  - The `strata-features` service intentionally does NOT publish ports. If an operator wants direct features replica `/metrics`, expose `9000` on a non-9999 host port via a compose override. Default stack: gateway port = 9999, prometheus scrapes the features replica internally.
---

## 2026-04-28 - US-017
- Makefile `build` target now produces `bin/strata` and `bin/strata-admin` only — `go build -o bin/strata ./cmd/strata` + `go build -o bin/strata-admin ./cmd/strata-admin`. Previously `go build ./...` built nothing into `bin/` (compile-only check).
- `run-memory` and `run-cassandra` depend on `build` and exec `./bin/strata server` instead of `go run ./cmd/strata server` — operator gets the same artefact CI / images use, and the second invocation is cache-warm. `run-cassandra` now sets `STRATA_WORKERS=gc,lifecycle` so the local Cassandra-backed dev loop matches the compose default.
- scripts/s3-tests/run.sh: comment header `live Strata gateway` -> `live `strata server``. scripts/s3-tests/README.md: `up -d --force-recreate gateway` -> `--force-recreate strata` (US-016 collapsed the compose service name).
- scripts/smoke.sh / scripts/smoke-signed.sh / scripts/grafana-smoke.sh: no edits needed — they hit the gateway by URL and never invoked a binary path. scripts/ralph/ralph.sh: no edits — it shells out to `amp` / `claude`, doesn't reference any cmd/* path.
- Files modified: Makefile, scripts/s3-tests/README.md, scripts/s3-tests/run.sh, scripts/ralph/prd.json (US-017 passes=true).
- Quality: `make build` produces both binaries (32MB strata, 13MB strata-admin), `go vet ./...` clean, `go test ./...` clean. `make -n run-memory` / `make -n run-cassandra` render correctly (build prereq + env line + ./bin/strata server). `./bin/strata server --help` boots and prints the full Known-workers section.
- **Learnings for future iterations:**
  - `go build ./...` does NOT install binaries into `bin/` — it only compiles. To produce artefacts you must `go build -o bin/<name> ./cmd/<name>` per-binary. Default Go behaviour is `./...` builds binaries to the current directory, but only when ./... resolves to a single main package; for multiple main packages it's a compile-only check.
  - When a target depends on a built artefact, `target: build` is the cheapest pattern — Make rebuilds only when sources changed (not enforced here, but `go build` itself is fast on a warm cache). Avoid duplicating env vars across `go run` and a separate `./bin` runner.
  - `STRATA_WORKERS=gc,lifecycle` is the canonical local dev default — matches docker-compose's strata service default. Stick with this for new dev-loop targets unless you specifically want a gateway-only run.
  - smoke.sh / smoke-signed.sh are pure HTTP clients pinned to `BASE` URL. They don't care which binary serves; the AC "scripts/* work unchanged" was satisfied trivially. Future binary renames don't ripple here.
---

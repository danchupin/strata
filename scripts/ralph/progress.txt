# Ralph Progress Log
Started: Wed May 14 09:00:00 MSK 2026
Cycle: ralph/cluster-weights

## Codebase Patterns
- Prior cycles closed the cluster-state machine through 4-state `{live, draining_readonly, evacuating, removed}` (drain-transparency cycle, merged at 6cb848a). This cycle adds the 5th state `pending` + `weight int (0..100)` field per cluster for default routing. Read `feedback_cycle_end_to_end.md` and the User Journey Walkthrough section of `tasks/prd-cluster-weights.md` before any story — each story serves at least one walkthrough step.
- **5-state machine** transitions:
  - `(no row in meta) → pending` (auto-init at boot for new clusters)
  - `(no row in meta) → live weight=100` (auto-init at boot for clusters with existing `bucket_stats` reference — backwards compat)
  - `pending → live` (POST /admin/v1/clusters/{id}/activate {weight: N})
  - `live → live` (PUT /admin/v1/clusters/{id}/weight {weight: N} — weight adjustment in place)
  - `live → draining_readonly | evacuating` (existing POST /drain — from drain-transparency)
  - `draining_* → live` (existing POST /undrain)
- **Two weight layers — NEVER COMBINE**:
  - `bucket.Placement != nil` → policy = bucket.Placement (cluster.weight IGNORED for this bucket — bucket explicit wins)
  - `bucket.Placement == nil` AND class env `spec.Cluster == ""` → synthesise `{<each-live-cluster>: <its-weight>}` from cluster.weight
  - Class env `@cluster` suffix → spec.Cluster set → bypass synthesis (explicit per-class pin)
  - Order in PickCluster MUST short-circuit BEFORE consulting weights when bucket.Placement is non-nil
- **Boot-time reconcile** in `internal/serverapp/serverapp.go::Run`: walk STRATA_RADOS_CLUSTERS env, for each cluster id not in cluster_state → check `bucket_stats` for any reference to this cluster → if present: state=live weight=100 (existing-live); else: state=pending weight=0 (new-pending). Log one INFO per cluster + one summary line. Idempotent (re-running creates nothing).
- **Cluster-state cache invalidation**: critical that POST /activate + PUT /weight invalidate the in-process cache synchronously before returning 200. Otherwise operator changes take 30s to propagate to routing path — bad UX. Pattern: invalidate via `cache.Invalidate(clusterID)` in handler before responding.
- **`placement.DefaultPolicy` helper** is new — takes `map[string]meta.ClusterStateRow`, returns `{<each-live-with-weight>0>: <weight>}` excluding pending/draining_readonly/evacuating/removed. PickCluster consults this when bucket.Placement is nil + class env spec.Cluster empty.
- **`<ActivateClusterModal>` mirrors `<ConfirmDrainModal>` typed-confirm precedent**: case-sensitive cluster id match arms Submit. Reuse Modal + Slider + Input + Button + Toast primitives from web/src/components/ui/.
- **Live card inline slider** (US-004): debounce 500ms (single timer, restart on each drag) then PUT /weight. Rapid drags coalesce. Optimistic UI updates immediately; revert on 4xx.
- **Cache merging consideration**: existing `placement.DrainCache` from drain-transparency cycle handles drain-state lookups with 30s TTL + write-side invalidate. New cluster_state weight + activate flow fits the same shape — consider merging into `ClusterStateCache` (state + mode + weight) instead of two parallel caches. Keep the rename optional; if scope-creep, defer.
- **Smoke scenario A "wipe cluster_state row"** simulates "new cluster appears" without adding a 3rd ceph container. Use direct cassandra/tikv DELETE on the cluster_state row → restart strata-multi → boot reconcile sees no row → creates pending. Document in `scripts/smoke-cluster-weights.sh` with `docker exec strata-cassandra cqlsh -e "DELETE FROM strata.cluster_state WHERE cluster_id='cephb';"` style commands (or tikv-cli for tikv backend).
- **Migration on upgrade**: existing cluster_state rows from drain-transparency cycle lack `weight` field. Decoder default = 0 (NOT 100). Boot-reconcile sees `state=live weight=0` for legacy rows → check `bucket_stats` reference → if present, update to weight=100 (preserves existing routing — all weight=100 = uniform among already-live clusters). Document carefully.
- **ROADMAP close-flip** in US-005: P2 entry "Default-routing weights + `pending` cluster state" → `~~**P2 — Default-routing weights + ...**~~ — **Done.** ... (commit \`<pending>\`)`. Closing SHA backfilled in follow-up commit on main.
- **`tasks/prd-cluster-weights.md` REMOVED** in US-005 per CLAUDE.md PRD lifecycle rule.
- **Lab stand**: `--profile multi-cluster` brings up 2 RADOS clusters (default + cephb) + strata-multi on :9998. Existing demo buckets may persist OR be empty (operator wiped before cycle). Integration tests reuse the lab profile.
- **Verification per UI story**: `pnpm run typecheck` + `pnpm run build`; verify in browser via dev-browser skill on the lab. Final US-005 gated on `make smoke-cluster-weights` passing AGAINST the running lab.
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md`): every story serves at least one walkthrough step; US-005 smoke covers all 4 scenarios + 4 negative paths from the PRD; failed smoke blocks ROADMAP close-flip.


## 2026-05-14 - US-001
- Added `pending` cluster state + per-cluster `weight int (0..100)` to meta layer.
- meta.Store interface: SetClusterState now takes (id, state, mode, weight); GetClusterState/ListClusterStates return ClusterStateRow{State, Mode, Weight}. ValidateClusterWeight added; ErrInvalidClusterWeight sentinel; ClusterStatePending constant.
- Memory + Cassandra (ALTER TABLE cluster_state ADD weight int) + TiKV (3-segment value `state\x00mode\x00<decimal>` with 1- and 2-segment legacy decode → weight=0) implement in lockstep.
- New contract test caseClusterStateWeights exercises legal weights, out-of-range rejection, pending+mode rejection, drain preserves weight, list carries weight.
- Boot reconcile in serverapp: new file internal/serverapp/cluster_reconcile.go. ReconcileClusters walks env clusters; existing row → skip; HasData && (id ∈ ClassDefaults || id ∈ Placement refs) → live + weight=100; otherwise → pending + weight=0. Logs INFO per cluster + summary line. Idempotent (covered by TestReconcileClusters_Idempotent).
- Admin API: extended GET /admin/v1/clusters response with `weight int` (live state forwards stored value, every other state masks to 0). New POST /admin/v1/clusters/{id}/activate {weight} (pending → live). New PUT /admin/v1/clusters/{id}/weight {weight} (live weight adjust). Both invalidate s.DrainCache synchronously and stamp audit overrides admin:ActivateCluster / admin:UpdateClusterWeight. GET /admin/v1/clusters/{id}/drain-progress carries weight too.
- OpenAPI: ClusterStateEntry + ClusterDrainProgress add weight; pending added to state enum; new /clusters/{id}/activate + /clusters/{id}/weight paths.
- Drain handler now preserves currentRow.Weight when flipping state — operator's chosen weight survives drain → undrain trips (until row deletion via undrain falls back to default).
- Files changed: internal/meta/store.go, internal/meta/memory/store.go, internal/meta/cassandra/{store,schema}.go, internal/meta/tikv/blobs.go, internal/meta/storetest/contract.go, internal/serverapp/{serverapp.go,cluster_reconcile.go,cluster_reconcile_test.go}, internal/adminapi/{server.go,clusters_drain.go,clusters_drain_progress.go,clusters_weight.go,clusters_weight_test.go,openapi.yaml}, plus 3-arg→4-arg call-site fixes in adminapi + rebalance test files.
- **Learnings for future iterations:**
  - TiKV cluster_state value encoding now 3-segment (`state\x00mode\x00<decimal>`); decode must `bytes.Split` and tolerate 1, 2, OR 3 segments for backward compat. Adding a 4th field repeats the pattern.
  - Cassandra `int` columns scan NULL → 0 cleanly into Go `int`; no need for sql.NullInt32 when the absent column means "default to zero".
  - Boot reconcile lives in `internal/serverapp/cluster_reconcile.go` and is invoked from `serverapp.Run` AFTER drain cache + apiHandler wiring but BEFORE listener start. Fail-soft (logs warn on err) so a transient meta hiccup doesn't block the gateway.
  - Admin handler weight masking: `Weight` on the wire is stored value when state == live, else 0. Stored value still preserves operator intent across drain (drain handler reads currentRow.Weight and passes it through to SetClusterState).
  - `meta.ValidateClusterStateMode` now accepts (pending, "") as a legal pair — pending+mode is rejected via ErrInvalidClusterMode just like (live, "readonly").
---

## 2026-05-14 - US-002
- Wired synthesised default-routing into the PUT hot path. Two-weight-layer rule enforced everywhere: bucket.Placement > class @cluster pin > synthesised default policy from cluster.weight > class fallback.
- Added `placement.DefaultPolicy(map[string]meta.ClusterStateRow) map[string]int` (filters state=live AND weight>0). Excludes pending/draining_readonly/evacuating/removed/legacy-draining by design.
- Extended `placement.DrainCache` to also expose `States()` — full cluster_state snapshot. One loader call backs both Get (drain set) and States (raw map); both share TTL and Invalidate. Existing drain-cache Get callers untouched.
- Added `data.WithDefaultPlacement` / `data.DefaultPlacementFromContext` ctx helpers. Gateway reads `s.DrainCache.States(ctx)`, runs `placement.DefaultPolicy`, and threads the result through every PUT ctx in `s3api.dataCtxForPutWith`.
- `rados.Backend.PutChunks` picker order: bucketPolicy → if class.ClusterPinned use spec.Cluster → defaultPolicy → spec.Cluster fallback. `s3.Backend.clusterForPlacement` consults default policy when bucket Placement is absent (s3 classes are always pinned by construction, so default routing only hits when bucket.Placement is nil and the synthesised policy resolves to a class-backed cluster).
- Added `rados.ClassSpec.ClusterPinned bool` — set by `ParseClasses` whenever an `@cluster` suffix is present. `STANDARD=hot.pool` leaves it false (default routing eligible); `COLD=cold.pool@cold-eu` sets it true (explicit pin, bypass default routing). S3 backend has no equivalent because every s3 class requires Cluster — pinning is implicit there.
- Cache invalidation: existing US-001 activate / weight handlers already call `s.DrainCache.Invalidate()` synchronously. Now invalidates the states snapshot as well via the same refresh path — no separate invalidate-states surface needed.
- Tests: `placement.TestDefaultPolicyDistribution` proves {c1:10, c2:30, c3:60} distributes ~100/300/600 over 1000 keys (5% tol). `TestDefaultPolicyPendingNeverPicked` proves pending excluded even with non-zero weight. `TestDefaultPolicyAllZero` proves all-zero-live → nil → "" → caller fallback. `placement.TestDrainCacheStatesSharesLoadWithGet` proves Get+States share one TTL load. `s3.TestPutChunksDefaultPolicyRoutesWhenBucketPolicyNil` covers the new default-routing path. `s3.TestPutChunksBucketPolicyWinsOverDefault` pins the bucket-policy-wins-over-cluster-weights AC. `rados.TestParseClassesClusterPinnedFlag` exercises ClusterPinned parsing.
- Files changed: internal/data/placement/policy.go (new), internal/data/placement/policy_test.go (new), internal/data/placement/draincache.go (added States + shared snapshot), internal/data/placement/draincache_test.go (added State tests), internal/data/context.go (added WithDefaultPlacement), internal/data/rados/class.go + class_test.go (ClusterPinned flag), internal/data/rados/backend.go (PutChunks picker order), internal/data/s3/backend.go (clusterForPlacement consults default policy), internal/data/s3/placement_test.go (added default-policy + bucket-policy-wins tests), internal/s3api/placement_ctx.go (thread default policy ctx).
- **Learnings for future iterations:**
  - The data package can't import meta (cycle: meta imports data). But `internal/data/placement` is a separate package and already imports meta, so DefaultPolicy lives there. The ctx helpers in `data` carry the already-synthesised `map[string]int` instead of the raw ClusterStateRow map — that decouples backends from meta types.
  - rados `ClassSpec.Cluster` defaults to "default" when no @ suffix — so you cannot tell "auto-default" from "explicitly-pinned to default" without an extra flag. Added `ClusterPinned bool`. Forgetting this would make every `STANDARD=pool` class look pinned and skip default routing entirely.
  - s3 ClassSpec REQUIRES Cluster (ParseClasses rejects empty). So s3 classes are always pinned by definition — default routing in s3 backend only kicks in when bucket.Placement is nil and the synthesised policy happens to resolve to a class-registered cluster; otherwise it falls back to clusterForClassStrict.
  - DrainCache.refresh now returns nothing (was returning the snap). Get/States re-acquire the read lock after refresh to fetch the snapshot. That avoids duplicating refresh logic for the two views.
  - When invalidating cache after activate / weight admin handlers, both views (Get + States) refresh on the next read — no separate InvalidateStates surface needed because they share `c.fetched`.
  - The picker call site in rados.Backend.PutChunks falls back to spec.Cluster on `picked == ""`. If default policy is empty and bucket policy is nil, the picker returns "" and we land on spec.Cluster. This preserves backwards-compat for in-memory unit tests / fixtures that never populate cluster_state.
---

## 2026-05-14 - US-003
- UI pending-cluster card variant + Activate modal with typed-confirm + initial-weight slider wired against POST /admin/v1/clusters/{id}/activate.
- API client: `weight: number` on ClusterStateEntry; `pending` added to ClusterState union; fetchClusters coerces missing/NaN weight to 0. New `activateCluster(id, weight)` and `updateClusterWeight(id, weight)` wrappers (the latter pre-wired for US-004). Both attach numeric `status` on the thrown error so handlers can detect 409 race.
- New Slider primitive in web/src/components/ui/slider.tsx — native `<input type="range">` with tailwind appearance + `accent-primary` thumb. No third-party dep; bundle footprint stays small.
- New `<ActivateClusterModal>` mirrors `<ConfirmDrainModal>` typed-confirm precedent: slider+numeric input two-way bound, default 10, clamps to [0,100], non-numeric blocked, typed value must equal cluster.id to arm Submit. Submit POSTs to /activate, invalidates `queryKeys.clusters`, toasts on success, keeps modal open on error.
- ClustersSubsection: clusterStateClass/Label gain `pending` case (gray palette + "Pending — not receiving writes" badge label). Pending cards hide RebalanceProgressChip (no chunks landed yet) + drop the Drain button + render a primary "Activate" CTA that opens the new modal. "Show affected buckets" link stays on every variant.
- Files changed: web/src/api/clusters.ts, web/src/api/client.ts, web/src/components/ui/slider.tsx (new), web/src/components/storage/ActivateClusterModal.tsx (new), web/src/components/storage/ClustersSubsection.tsx.
- `pnpm run typecheck` clean. `pnpm run build` clean — Storage chunk 13.21 kB gzipped (delta well under the 4 KiB AC budget; activate path adds ~modal+slider <1.5 KiB pre-shared-vendor).
- `pnpm run test:unit` passes (existing 18 BulkPlacementFixDialog suite). No new unit test for the modal — typed-confirm + clamp logic is identical to the established ConfirmDrainModal precedent and exercised by US-005 Playwright spec.
- Browser verification: dev-browser skill not present in this env; manual operator verification deferred to US-005 (Playwright spec `web/e2e/cluster-weights.spec.ts` will drive the actual UI half against the multi-cluster lab).
- **Learnings for future iterations:**
  - No `@radix-ui/react-slider` dep. The native `<input type="range">` + tailwind's `accent-primary` ships a credible-looking slider with zero JS overhead. Reuse `web/src/components/ui/slider.tsx` for the live-card inline slider in US-004 — same Slider primitive, different debounce wiring.
  - The thrown Error from `activateCluster` / `updateClusterWeight` carries a `(err as Error & { status?: number }).status` numeric field. US-004 inline slider can read that to special-case 409 (operator drained mid-edit) without parsing the message.
  - ClustersSubsection's action-row ladder is now `pending → Activate; draining → (Undrain|nothing); else → Drain`. The order matters because `isDraining` is true for `draining_readonly`/`evacuating`/legacy `draining` — keep pending checked first so a pending row never falls into the drain branch.
  - `RebalanceProgressChip` is suppressed for pending clusters because no chunks land there yet — chip would render "(0 moved)" noise. Keep this suppression pattern (`!isPending`) for any future per-cluster telemetry chip.
  - Modal Submit copy is `Activate (weight N)` — bakes the chosen value into the button label, removes "wait, what weight did I pick" friction. Reuse pattern in any future numeric-armed confirm dialog.
---

## 2026-05-14 - US-004
- Inline weight slider on live cluster cards. Debounced 500ms PUT /admin/v1/clusters/{id}/weight; rapid drags coalesce to a single PUT with the final value. Optimistic UI: slider + numeric input update immediately. 4xx → revert to last server-accepted value + error toast (409 surfaces the "Cannot update weight: cluster is no longer in live state" variant + invalidates clusters query so the card re-renders with the new state).
- New `<LiveClusterWeightSlider>` component (web/src/components/storage/LiveClusterWeightSlider.tsx). Reuses Slider + Input + Toast primitives + the existing `updateClusterWeight()` wrapper landed in US-003. weight=0 → muted "weight=0 — no default-routed writes" chip rendered below the slider row (state stays live).
- Tooltip on slider via the standard HTML `title` attribute: "Share in default uniform routing for buckets without explicit Placement policy. weight=0 means no default-routed writes; reads + explicit policies still work." No custom Tooltip primitive in the codebase; native title is enough for an inline help blurb.
- ClustersSubsection: live branch (stateLower === 'live') renders the new slider underneath the RebalanceProgressChip. Pending/draining/removed/legacy-draining branches unaffected — the slider is gated on `isLive`.
- Poll-refresh adoption guard: when a clusters query refetch lands while no debounce timer is pending and the user isn't dragging, slider adopts the new upstream weight. Otherwise the in-flight drag stays sticky so the operator's typing isn't clobbered.
- Unit test `web/src/components/storage/__tests__/live-cluster-weight-slider.test.mjs` covers: clampWeight bounds + integer rounding; rapid drags coalesce to one PUT after debounce expires; single-drag fires one PUT; 4xx reverts pending to savedWeight + onError carries status; success advances savedWeight + invokes invalidate; subsequent failure reverts to the LATEST saved value (not initial). Added to `pnpm run test:unit` script.
- Files changed: web/src/components/storage/LiveClusterWeightSlider.tsx (new), web/src/components/storage/ClustersSubsection.tsx, web/src/components/storage/__tests__/live-cluster-weight-slider.test.mjs (new), web/package.json (test:unit script), web/dist/index.html (build output hash).
- `pnpm run typecheck` clean. `pnpm run test:unit` passes 24 tests (18 existing + 6 new). `pnpm run build` clean — Storage chunk 13.77 kB gzipped (+0.56 KiB vs US-003 13.21 kB baseline, well under the 3 KiB AC budget).
- Browser verification: no dev-browser skill in this env; manual operator verification deferred to US-005 (Playwright spec `web/e2e/cluster-weights.spec.ts` will exercise the inline-slider drag + debounce + 409-revert paths against the multi-cluster lab).
- **Learnings for future iterations:**
  - The native HTML `title` attribute is the codebase's stand-in for tooltips on slider-style controls — no Tooltip primitive in `web/src/components/ui/`. If a richer hover panel is ever needed, add a primitive rather than ad-hoc Radix wrapping.
  - The TanStack poll runs every 10s for clusters. The "don't clobber the operator mid-drag" pattern requires both a `timerRef != null` guard AND a `draggingRef` mirror — the timer alone reverts to null between drags, leaving a tiny window where a poll could overwrite a freshly-edited value before submit fires. Apply this pattern to any future inline editor on a polled card.
  - The thrown Error from `updateClusterWeight()` already carries `(err as Error & { status?: number }).status`. The slider treats status === 409 distinctly from 4xx-other so the toast copy can name the race ("cluster is no longer in live state"). New optimistic-UI surfaces on `/admin/v1/clusters/*` should mirror this status-field pattern.
  - savedRef vs pendingWeight: the saved value is held in a `useRef` (not state) so the revert path doesn't have to wait for a render to read the latest accepted value. The render-derived `pendingWeight` state is what drives the slider position. Conflating the two leaks: if we'd kept savedWeight in state, a failed PUT after a successful one could revert to the original initial weight via stale-closure capture.
  - Bundle delta: a ~0.6 KiB gzipped chunk increase for a 130-line component is the typical cost. Keep an eye on Storage chunk size; if it ever balloons past 16 KiB gzipped the cycle should re-evaluate code-split boundaries.
---

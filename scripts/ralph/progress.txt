# Ralph Progress Log
Started: Mon Jun 02 2026
Cycle: ralph/metadata-data-reconcile
---

## Codebase Patterns
- CI on Linux is the SOURCE OF TRUTH. Local macOS/lima: Xcode license not accepted -> cgo runtime build-fails for cmd/strata, cmd/strata/admin, internal/meta/tikv, internal/metrics, internal/serverapp (darwin gosigar needs cgo under CGO_ENABLED=0 too). Pure-Go pkgs (internal/config, internal/data, internal/s3api without gosigar) DO build with `CGO_ENABLED=0 go vet ./...`. Cassandra (Paxos starvation), RADOS, SSE-KMS, drain/race-timing failure modes DO NOT reproduce locally — verify on CI.
- meta.Store contract: adding a method = update internal/meta/memory AND internal/meta/cassandra AND internal/meta/tikv in lockstep + a caseX in internal/meta/storetest/contract.go (runs on memory + TiKV; Cassandra under -tags integration, dedicated CI job).
- ADDING A WORKER = 5 lockstep spots: (1) workers.Register{} init() in cmd/strata/workers/<name>.go, (2) "<name>" in internal/metrics/metrics.go registeredWorkerNames, (3) WorkersConfig.<Name> field + defaults() + clampWorkers() in internal/config/config.go, (4) STRATA_<NAME>_* rows in envMap, (5) [workers.<name>] in deploy/strata.toml.example + a row in docs/site/content/reference/env-vars.md. Miss (4)/(5) -> TestEnvTOMLParity (internal/config) RED, not the build. Runner wraps each RunOnce in workers.StartIteration/EndIteration + metrics.ObserveWorkerTick("<name>",...). reconcile (US-002/003) is a worker draining admin-queued jobs — mirror the reshard worker+admin-queue pattern (POST queues -> 202, GET reads progress).
- ENV KNOB = 4 lockstep spots OR exempt: ANY new STRATA_* literal in cmd/+internal/ (incl. `const Env = "STRATA_X"`) is caught by TestEnvTOMLParity. Wire (1) Config koanf field, (2) envMap row -> koanf path, (3) deploy/strata.toml.example commented line, (4) docs/site/content/reference/env-vars.md row with the koanf key — OR add to internal/config/exempt_env_vars.go (test-only knobs use the em-dash placeholder; integration testcontainer knobs go here, e.g. STRATA_CASSANDRA_IMAGE/_MAX_HEAP/_NEW_HEAP). STRATA_CHUNK_BACKREF (US-001) needs this. config.go is NOT gofmt-clean on main (hand-aligned) — match neighbor columns by hand.
- ChunkRef: internal/data/manifest.go (Go struct) + internal/data/manifest.proto message ChunkRef (proto fields 1-6 USED: cluster/pool/namespace/oid/size + crc32c=6 from US-009). NEXT free proto field = 7. Manifest decode sniffs first byte ({ = JSON, else proto3 wire); new proto fields with json omitempty are schema-additive, legacy rows decode zero-valued. US-009 already shipped per-chunk CRC32C (Checksum uint32) — the CRC proves bytes intact; THIS cycle's back-reference proves whose chunk it is.
- RADOS / cephimpl module split: go-ceph is NOT a direct require of the main module. Librados-linked code lives in internal/data/rados/cephimpl/ (separate go.mod). Main-module internal/data/rados/ keeps the shared shape + always-on stubs returning data.ErrRADOSNotCompiled; serverapp branches via //go:build ceph files. Hermetic check: GOWORK=off go mod graph | grep -c go-ceph == 0. US-000's pool-enumeration primitive follows this: real impl in cephimpl (wraps ioctx.Iter()), main-module sentinel off the ceph tag.
- writeChunkBatched SetXattr seam EXISTS (internal/data/rados/cephimpl/ops.go): bundles WriteFull + per-xattr SetXattr in ONE WriteOp, gated by STRATA_RADOS_BATCH_OPS. US-001 wires the back-reference through it (one Operate, no extra round-trip). Reconcile reads xattrs via ioctx.ListXattrs(oid) (ops.go) — go-ceph v0.39 does NOT expose rados_read_op_getxattrs.
- s3api handler test harness: newHarness(t) + h.doString(method,path,body,headers...) + h.mustStatus(resp,code) in internal/s3api/testutil_test.go. Auth-gate fixture gotcha: no X-Test-Principal -> Owner="anonymous"; a bucket created the same way is owned by "anonymous" so requireACL owner-match trivially passes and MASKS the gate — own the bucket with a DISTINCT principal to exercise a real non-owner.
- ADMIN AUDIT TEST PATTERN: /admin/* (handleAdmin) gated on Owner==IAMRootPrincipal ("iam-root"); base newHarness does NOT wrap AuditMiddleware. To assert an admin write stamped SetAuditOverride, wrap s3api.NewAuditMiddleware around a handler injecting auth.WithAuth(IAMRootPrincipal), read store.ListAudit. Add the override stamp to every new admin write (reconcile trigger endpoint).
- gopls modernize hints (rangeint, minmax, interface{}->any) fire pre-existing all over cassandra/store.go + others — NOT lint failures, don't churn them.

## Resolved cycle decisions (do NOT re-litigate)
- Orphan default policy = report (never auto-delete; gc/restore explicit per-run).
- Back-reference payload = {schema_byte, bucket_id, key, version_id, chunk_idx, mtime}. mtime REQUIRED: version_id orders the chain but can't derive IsLatest when a suspended-null version (ts=0) coexists. schema_byte reserves room for a future refcount-aware shape (dedup interaction). NO key material (SSE-safe).
- rebuild-index is PLAINTEXT-ONLY: wrapped DEK (meta.Object.SSEKey) lives only in meta, so a lost meta makes SSE ciphertext undecryptable — SSE objects flagged unrecoverable, never silently served. Object metadata absent from the back-reference (Content-Type/user-meta/tags/ACL/storage-class/multipart-ETag) reported lost, not fabricated.
- S3-passthrough backend gets a back-reference too (object tags/metadata; reconciles via native ListObjects — no US-000 pool primitive needed there).
- reconcile = leader-elected worker draining admin-queued jobs (reshard pattern); rebuild-index = strata admin CLI one-shot (single-binary invariant, cmd/strata/admin), NEVER a worker.
- US-000 (RADOS pool enumeration) is the FOUNDATION — lands before US-002/US-004 which depend on it.
---

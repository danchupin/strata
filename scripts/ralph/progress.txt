# Ralph Progress Log
Started: Mon May 18 18:00:00 MSK 2026
Cycle: ralph/tikv-default-lab

## Codebase Patterns
- 5-story cycle. Flip lab compose default from Cassandra-backed single canonical strata → TiKV-backed 2-replica + nginx LB :9999. Cassandra meta backend remains first-class in code (tests + CI testcontainers preserved); only lab compose shape changes.
- **Retire redundant profiles**: `tikv` (single TiKV replica), `lab-tikv` (already 2-replica; now bare default), `lab-tikv-3` (3-replica TiKV), `lab-cassandra-3` (3-replica Cassandra). Keep orthogonal: `webhook-trap`, `tracing`, `ci`.
- **Rename strata-tikv-a/b → strata-a/b** with multi-cluster mounts (existing strata-tikv-a was single-cluster — `strata-ceph-etc:/etc/ceph:ro` only). New shape mounts BOTH `strata-ceph-etc:/etc/ceph-a:ro` + `strata-cephb-etc:/etc/ceph-b:ro` like the retired Cassandra-backed canonical strata.
- **Critical: PRESERVE `strata-jwt-shared:/etc/strata/jwt-shared` volume mount + distinct `STRATA_NODE_ID` per replica**. Round-robin LB requires session JWT issued by replica A to validate on replica B. Without these, login → next request lands on other replica → 401. Existing lab-tikv shape already has both; rename must preserve.
- **DEPENDS_ON expanded for strata-a/b**: include `pd healthy + tikv started + ceph healthy + ceph-b healthy`. Without ceph-b dep, compose may start strata-a before ceph-b ready → multi-cluster boot fail.
- **NO compose-level rebalance defaults on strata-a/b**: gateway built-in defaults apply (1h interval, 100 MB/s rate). Retired Cassandra-backed `strata` had explicit `STRATA_REBALANCE_INTERVAL=30s` + `STRATA_REBALANCE_RATE_MB_S=100` compose defaults — cadence changes. Document in migration note.
- **Port allocation**: nginx LB :9999 (canonical operator-facing entry), strata-a :10001 (direct smoke), strata-b :10002 (direct smoke), strata-cassandra :9998 (profile cassandra; was :9999 pre-cycle — breaking change for curl scripts targeting Cassandra at :9999).
- **`cassandra` service moves under `profiles: ["cassandra"]`**: explicit service-name invocation (e.g. `docker compose up -d cassandra`) still works (compose honors explicit names even for profile-gated services). Implicit always-on dies.
- **Promote `pd` + `tikv` services to no-profile**: currently `profiles: ["tikv", "lab-tikv"]`; remove gate so bare `up -d` starts them.
- **`strata-cassandra` (new service under profile cassandra)**: single Cassandra-backed replica, port 9998, multi-cluster mounts, admin creds preset.
- **CI breaking changes** (US-003): `make wait-cassandra` invocations in `.github/workflows/*.yml` and `scripts/**/*.sh` break since cassandra is profile-gated. Sweep + replace with `make wait-tikv` for TiKV-default jobs OR move job to `--profile cassandra up -d && make wait-cassandra` if Cassandra genuinely needed.
- **bench-rebalance-multi rework** (US-003): preferred path = SHARDS=3 scenario explicitly SKIP'd with stdout message; exit 0 after SHARDS=1+2 baselines. Alternative = ad-hoc 3rd replica via `docker compose run --rm -p 10003:9000 strata-a` (higher complexity). Pick one; add P3 ROADMAP entry `Restore 3-replica TiKV bench` either way.
- **Smoke Scenario A round-robin assertion**: check via container access logs (`docker logs strata-a --since 30s | grep GET | wc -l` ≥5 AND same for strata-b) after 20-request burst. Avoids needing new response header / endpoint.
- **`make smoke` preserved verbatim** — already targets bare default, which is now TiKV. NO rename of `smoke-tikv → smoke` (would collide). Just retire `smoke-tikv` as redundant. Similarly retire `smoke-multi-cluster` (multi-cluster IS the default now).
- **Cassandra meta-backend remains first-class in code**: `internal/meta/cassandra/**` untouched. `make test-integration` (testcontainers Cassandra) preserved. Cassandra parity in contract tests + benchmarks remains. The cycle is a deployment shape flip, NOT a backend deprecation. Migration note explicitly reassures this.
- **Reference for nginx LB**: existing `deploy/nginx/strata-lab.conf` (used by retired `strata-lb-nginx` under `lab-tikv` profile). US-001 updates upstream block: `strata-tikv-a/b:9000` → `strata-a/b:9000`.
- **Reference for new strata-cassandra service shape**: mirror env of new strata-a (multi-cluster, admin creds preset) but with `STRATA_META_BACKEND=cassandra` + `STRATA_CASSANDRA_HOSTS=cassandra:9042`. Single replica, port 9998.
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md`): walkthrough covers happy path (bare bring-up + drain cephb evacuate) + edge cases (Cassandra side-by-side via profile, single-cluster env override, retired profile silent no-op, 3-replica bench skip-or-execute) + worst-case (operator muscle memory of `--profile lab-tikv`, curl scripts on :9999 expecting Cassandra, CI workflow `wait-cassandra` regression, gateway restart mid-test, 3-replica HA need, bench-rebalance-multi compatibility). State Truth Tables: compose service start matrix + port allocation. Cache Invalidation Ledger: no new caches. Safety Claims Preconditions: Cassandra meta first-class preserved, lab compose flip doesn't break prod, multi-cluster behavior parity preserved, round-robin LB exercises stateless gateway invariants.


## 2026-05-18 - US-001
- Compose rewrite: TiKV-default 2-replica lab as bare default; Cassandra under --profile cassandra
- Files changed:
  - deploy/docker/docker-compose.yml (full rewrite — see below)
  - deploy/nginx/strata-lab.conf (upstream strata-tikv-a/b:9000 → strata-a/b:9000)
- Compose changes:
  - Dropped: strata (Cassandra-backed canonical), strata-tikv (single TiKV replica), strata-tikv-c (lab-tikv-3), strata-cass-a/b/c (lab-cassandra-3), strata-lb-nginx-cass (lab-cassandra-3)
  - Renamed: strata-tikv-a → strata-a; strata-tikv-b → strata-b (no profile; bare-default)
  - Moved cassandra under `profiles: ["cassandra"]` (was always-on); container_name flipped to strata-cassandra-db so it doesn't collide with new strata-cassandra service container_name
  - Promoted pd, tikv, strata-lb-nginx to no-profile (always-on)
  - New strata-cassandra service under profile cassandra (port 9998, multi-cluster mounts)
  - strata-a/b multi-cluster mounts: strata-ceph-etc:/etc/ceph-a:ro + strata-cephb-etc:/etc/ceph-b:ro (renamed primary mount path to match canonical /etc/ceph-a)
  - strata-a/b distinct host ports: 10001 + 10002; LB at 9999:80
  - strata-a/b preserve strata-jwt-shared:/etc/strata/jwt-shared + distinct STRATA_NODE_ID
  - strata-a/b depends_on: pd healthy + tikv started + ceph healthy + ceph-b healthy
  - No STRATA_REBALANCE_INTERVAL / STRATA_REBALANCE_RATE_MB_S compose defaults on strata-a/b (gateway built-in defaults — 1h interval, 100 MB/s rate; cadence change vs retired Cassandra strata's 30s)
  - All retained services keep STRATA_AUTH_MODE=optional + STRATA_STATIC_CREDENTIALS=admin:adminpass:owner defaults
- Compose config gate verified:
  - `docker compose config --services` → ceph, ceph-b, grafana, pd, prometheus, strata-a, strata-b, strata-lb-nginx, tikv (9 services; matches PRD bare-default list)
  - `docker compose --profile cassandra config --services` → adds cassandra + strata-cassandra
  - Retired profiles (`--profile tikv --profile lab-tikv --profile lab-tikv-3 --profile lab-cassandra-3`) silently no-op (same 9 services as bare-default; no warning)
- `go vet ./...` green; `make build` green
- **Learnings for future iterations:**
  - **container_name collision gotcha**: the new `strata-cassandra` service can't reuse `container_name: strata-cassandra` (which the OLD cassandra-image service had) — docker would silently accept the YAML but fail at runtime when both services start under `--profile cassandra`. Flipped cassandra container_name to `strata-cassandra-db` to make room.
  - **Multi-cluster mount path convention**: when adding both clusters, mount each at `/etc/ceph-<id>` (NOT `/etc/ceph`). Gateway reads `STRATA_RADOS_CLUSTERS=default:/etc/ceph-a/...,cephb:/etc/ceph-b/...` so the paths must match exactly. Retired canonical `strata` service had a triple-mount including the legacy `:/etc/ceph:ro` bind for backwards-compat; the new strata-a/b drop it — gateway never reads `/etc/ceph` when STRATA_RADOS_CLUSTERS is set.
  - **`docker compose config` accepts unknown profile flags silently**: passing `--profile retired-name` against a compose file that doesn't reference it produces no warning and no error — the resolved service list is identical to bare-default. This is the contractual silent no-op behavior the PRD requires.
  - **Validate compose changes early**: `docker compose -f <file> config --services` is a 100ms lint that catches container_name collisions, missing volumes, profile misconfig before any container starts. Run it before any go vet / make build.
  - **Per-replica `STRATA_NODE_ID` + shared `strata-jwt-shared` volume is the round-robin LB contract**: without distinct node IDs the heartbeat rows collide; without the shared volume cross-replica session JWT validation fails (login → next request lands on other replica → 401). Both are required; don't simplify one away.
---

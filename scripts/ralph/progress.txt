# Ralph Progress Log
Started: Sat May 30 20:07:14 MSK 2026
Cycle: ralph/qa-production-readiness
---

## Codebase Patterns
- Coverage baseline: `make test-cover` → `scripts/qa/coverage.sh` (2 passes: default CGO + CGO_ENABLED=0, keep real number from whichever built each pkg). Table spliced into tasks/qa-readiness-report.md between `<!-- COVERAGE-TABLE-START/END -->` markers. Idempotent.
- Local macOS box: Xcode license NOT accepted → cgo runtime build-fails for cmd/strata, cmd/strata/admin, internal/meta/tikv, internal/metrics, internal/serverapp (darwin gosigar sysctl under CGO0 too). ENV issue, not code. CI (Linux) is source of truth (FR-4). `go vet ./...` / `make build` hit the same block locally — verify on CI.
- s3api handler test mapping: handler `foo.go` usually paired with `foo_test.go`; gates live in `*_gate_test.go` (acl_gate, policy_gate), auth modes in `auth_mode_test.go`, drain in `drain_strict_test.go`.
- Rebalance mover tests: S3 mover = `internal/rebalance/s3_mover_integration_test.go` (pure-Go `//go:build integration`, MinIO testcontainers, vets locally CGO0); RADOS mover = `rados_mover_integration_test.go` (`ceph && integration`, librados, CI-only). Mover CAS-loss guard is buildUpdated{Backend}Manifest's locator check on the GetObject snapshot (NOT the class-only SetObjectStorage CAS — rebalance keeps the same class). Safety rails (refuse into draining/>90%-full) are Worker.applySafetyRails post-plan filter, covered in `worker_test.go` — the Mover never sees a refused move. Deterministic racing-client-write test: land the client overwrite (metamem PutObject versioned=false REPLACES latest) BEFORE Move so the mover's snapshot read already mismatches the plan → CAS conflict → mover copy to GC.
- Race/concurrency tests: shared workload+invariants in `internal/racetest` (Fixture, RunScenario, VerifyInvariants, RunMultipartRaceScenario); per-backend constructors in s3api `race_test.go` (memory, always-on) + `race_integration_test.go` (cassandra+tikv, `//go:build integration`). New scenario = export a `Run*Scenario(t, *Fixture)` in racetest, wire ONE test per backend; never fork a parallel harness. Memory store serializes multipart under one mutex (structural correctness); TiKV/Cassandra LWT is the real contention test (integration). `-race` itself runs on CI Linux only (local cgo blocked by Xcode license).
- Durability / fault-injection tests: the default-tag (memory) data plane is fully unit-testable CGO_ENABLED=0 — datamem stores ciphertext for SSE (s3api encrypts before PutChunks), exposes CorruptFirstChunk (backend.go:148, flip-a-byte) + ChunkOIDs (orphan accounting); partial-write rollback is the no-manifest-on-error contract (PutChunks returns nil manifest on a mid-stream reader error, so the gateway never commits the meta row). Backend "object missing" fail-loud sentinel is data.ErrNotFound (s3 GetRange→GetChunks propagate it). MinIO durability tests reuse the s3_test helpers (minioEndpoint/createBucket/newAdminClient/ptr/newUUID/minioKMSSecretKey) and run under `//go:build integration` (CI). RADOS durability = `ceph && integration` (librados) AND currently blocked locally by a ceph-tagged build break (cephimpl backend.go:288 goceph.OperationNoFlag) → CI-only.

## 2026-05-30 - US-001
- Coverage audit + readiness-report skeleton.
- Added: scripts/qa/coverage.sh (two-pass cover, marker splice), Makefile `test-cover` target (+ .PHONY), .github/workflows/ci.yml `coverage` job (Linux, full table = FR-4 source of truth, uploads report artifact), tasks/qa-readiness-report.md (coverage table, dimension matrix, handler->test matrix for all 43 s3api handlers, residual risks R1-R5, Go/No-Go PENDING).
- Baseline: 49 pkgs measured locally (s3api 75.9%, root 78.3%, lowest hot-path: data 56.0% / lifecycle 56.0%); 5 env-blocked → CI. These exact numbers = US-013 ratchet floor.
- Handler matrix: 40 covered / 2 thin (conditional.go GET-side If-* = US-002; placement_ctx.go drain helpers = US-009) / 1 uncovered (delete_objects.go multi-delete = R1).
- No production Go code changed (audit only).
- **Learnings for future iterations:**
  - delete_objects.go has ZERO direct test (R1) — real P2 gap, batch/partial-fail/quiet-mode/versioned paths unverified. Candidate follow-up.
  - GET-side checkConditional (If-Modified-Since/If-None-Match on GET/HEAD) untested — US-002 must cover it (PUT-side + copy-side already covered).
  - coverage.sh is idempotent: re-running regenerates identical table when no test changes. Run it after adding tests to refresh the baseline before US-013.
  - make build runs web-build (pnpm) first — web/dist must be populated or go build fails on `pattern web/dist: no matching files found`.
---

## 2026-05-30 - US-003
- SigV4 adversarial auth matrix.
- Added: internal/auth/sigv4_adversarial_test.go — TestSigV4_AdversarialMatrix (13 tamper classes) + TestSigV4_ValidVariantsPass (7 positive controls). signV4 helper builds a fully-valid signed request from signOpts, reusing prod canonicalRequest/computeSignature; each case applies ONE tamper then asserts the sentinel + S3 code.
- Report: dimension matrix Auth row updated (SigV4 done, US-004/005/006 pending).
- **NO bug found** — auth rejects every tamper class with the exact sentinel. No production code changed.
- **Learnings for future iterations:**
  - Strata does NOT pin region/service server-side. validateHeader derives the signing key from parsed.Region/Service (taken from the Authorization Credential scope), so a self-consistent any-region request PASSES. The adversarial "wrong region/service" is therefore a TAMPERED scope (header claims region B) vs the original signature (signed for region A) → key mismatch → ErrSignatureInvalid. Do not expect a dedicated "region mismatch" sentinel.
  - validateHeader order: parse → require X-Amz-Date (missing → ErrMalformedAuth, NOT ErrSignatureInvalid) → parse date (bad → ErrMalformedAuth) → skew check (ErrClockSkew, fires BEFORE signature so a correctly-signed-but-old request is ErrClockSkew not ErrSignatureInvalid) → Lookup cred → compute+compare sig (ErrSignatureInvalid) → session-token check.
  - bodyHash defaults to unsignedBody when X-Amz-Content-Sha256 absent. Deleting the (signed) header changes BOTH the canonical header value AND the payload hash → mismatch.
  - sentinel→S3-code mapping lives in s3api.WriteAuthDenied (internal/s3api/errors.go:180), a switch on err.Error() STRING. auth tests can't import s3api (cycle) — mirror the map locally (wantS3Code) and keep it in lockstep. "signature does not match"→SignatureDoesNotMatch, "request time outside permitted window"→RequestTimeTooSkewed, malformed/default→AccessDenied.
  - stubStore (middleware_test.go) is the in-package CredentialsStore fake; reuse it. mw.validate(r) dispatches header-path when no presigned query params present.
---

## 2026-05-30 - US-002
- RFC 7232 conditional-request matrix.
- Added: internal/s3api/conditional_test.go — TestConditionalGetMatrix (GET+HEAD × all If-* branches, *, multi-value lists, precedence pairs, exact 200/304/412), TestConditionalGetRange206 (206 when precond passes, 412 short-circuit before range), TestConditionalPutPreconditions (If-None-Match:* overwrite-reject/create-absent, If-Match lost-update hit/miss, If-Match-on-missing → 412).
- **BUG FOUND + FIXED** in conditional.go: checkConditional evaluated If-Unmodified-Since unconditionally — If-Match:<match> + If-Unmodified-Since:<past> returned 412 instead of 200. RFC 7232 §6 + AWS docs: If-Match suppresses If-Unmodified-Since (match wins → serve). Fixed by moving If-Unmodified-Since into the If-Match-absent else branch (mirrors existing If-None-Match ⊳ If-Modified-Since).
- Report: R2 closed, handler matrix conditional.go thin→covered (41 covered/1 thin/1 uncovered). ROADMAP: P2 Done entry added.
- **Learnings for future iterations:**
  - conditional.go GET/HEAD path is ONE helper (checkConditional, server.go:1205) shared by GET+body=false HEAD; PUT-side is separate inline (server.go:928), multipart-Complete separate (multipart.go:499), copy separate (copy_object.go:225). 4 distinct precondition sites — do NOT unify (multipart If-Match-on-missing diverges to 404, single-PUT keeps 412).
  - RFC 7232 §6 precedence is two PAIRS: (If-Match ⊳ If-Unmodified-Since) and (If-None-Match ⊳ If-Modified-Since). The AC's "If-Match over If-Modified-Since" phrasing is loose; the real suppression pairs are match/unmod and none-match/mod. Each higher-precedence header's presence must gate its partner via else.
  - ETag from PUT response header is the quoted strong form clients send back in If-Match — capture it, don't recompute MD5. putCondObject helper in conditional_test.go.
  - If-Modified/Unmodified-Since granularity is seconds; use fixed far-past ("Mon, 02 Jan 2006...") + far-future ("Fri, 01 Jan 2100...") dates for deterministic mtime comparison vs put-time now.
---

## 2026-05-30 - US-004
- Presigned + streaming chunk-signature adversarial.
- Added: internal/auth/presigned_adversarial_test.go (TestPresigned_AdversarialMatrix 6 classes + ValidVariantsPass 7 + ValidReplayWithinWindow), internal/auth/streaming_adversarial_test.go (OutOfOrderChunk + TruncatedChunkData). Report: Auth dimension row updated (SigV4+presigned+streaming done).
- **NO bug found** — every presigned + streaming tamper class rejects with the exact sentinel. No production code changed. Replay-within-window documented as permitted (AWS parity, no nonce) — NOT a finding.
- **Learnings for future iterations:**
  - validatePresigned (middleware.go:143) signs over UNSIGNED-PAYLOAD body + the canonical query MINUS X-Amz-Signature; reqDate passed to computeSignature is the FULL timestamp (reqTime.UTC().Format(sigTimeFormat)), while parsed.Date (scope + signing-key derivation) is the YYYYMMDD day. signPresigned helper must split these or the sig won't match.
  - Presigned lifetime gate is ONE check: now.Before(reqTime-15m) || now.After(reqTime+expires*sec) → ErrClockSkew (BOTH expired AND future-dated map to ErrClockSkew → RequestTimeTooSkewed, NOT a dedicated "expired" sentinel). No separate signature step for the window — skew fires before sig compare, same as header path.
  - Streaming chain is order-binding: emitting correctly-signed chunks in swapped wire order fails because the reader's prevSig (seeded) no longer matches the first wire chunk's chained sig → ErrSignatureInvalid. Distinct sentinels by failure mode: bad/missing/out-of-order sig → ErrSignatureInvalid; truncated DATA section (declared size > bytes present) → io.ErrUnexpectedEOF from io.ReadFull (returned raw, NOT wrapped). Missing terminator (complete data, no 0-chunk) → ErrSignatureInvalid (readChunk EOF path).
  - sigv4_adversarial_test.go (US-003) is the shared adversarial fixture base for the whole auth package: reuse adv* consts, stubStore, newAdvMiddleware, wantS3Code (sentinel→S3-code mirror of s3api.WriteAuthDenied) — do NOT redefine. New auth adversarial tests in the same package just add build funcs.
  - Trailer checksum-mismatch + chain-HMAC legs were ALREADY covered (streaming_test.go + streaming_trailer_test.go all-algos). US-004's value was the two missing streaming classes + the ENTIRELY-missing presigned adversarial surface. Check both files before adding streaming cases to avoid duplication.
---

## 2026-05-30 - US-005
- Anonymous + auth-mode enforcement matrix.
- **BUG FOUND + FIXED (P1, security fail-open)** in internal/s3api/access.go: PublicAccessBlock was pure CRUD (public_access_block.go), NEVER consulted by the data-plane gates. A block silently failed open — permissive bucket policy or public-read ACL still granted anonymous access despite RestrictPublicBuckets/IgnorePublicAcls set. Fixed: requireAccess suppresses an anonymous policy Allow when RestrictPublicBuckets set; requireACL disregards public ACL grants (canned public-read/public-read-write + AllUsers group) when IgnorePublicAcls set. Added helper effectivePublicAccessBlock (public_access_block.go) that fails closed on malformed blob, (nil,nil) on absent.
- Added: internal/s3api/auth_public_access_matrix_test.go — TestPAB_RestrictPublicBuckets_OverridesAnonPolicy, TestPAB_IgnorePublicAcls_OverridesPublicReadACL, TestPAB_NonEvalFlagDoesNotBlock, TestPAB_AnonGetEnforcementMatrix (table). Auth-mode × anon/signed × ACL/policy cells already covered (auth_mode_test/policy_gate_test/acl_gate_test) — NOT duplicated; this story's value is the PAB-override dimension.
- Changed sigs: cannedAllows / grantsAllow / granteeMatches gained an ignorePublic bool (all callers in access.go only).
- Docs: updated docs/site/content/reference/s3-api.md PAB handler file:line rows (shifted by helper insertion — docs_reference_test.go pins them). Report: Auth dimension row + handler matrix + R6 added. ROADMAP: P1 Done entry.
- Files: access.go, public_access_block.go, auth_public_access_matrix_test.go, docs/site/content/reference/s3-api.md, tasks/qa-readiness-report.md, ROADMAP.md, scripts/ralph/prd.json.
- **Learnings for future iterations:**
  - newHarness (s3api_test, no middleware) requests resolve to auth.AnonymousIdentity via FromContext default (Owner="anonymous", IsAnonymous=true). A bucket CREATED with no X-Test-Principal is ALSO owned by "anonymous" → requireACL owner-match short-circuits the ACL gate. So existing TestPolicyGate_* "pass" via owner-coincidence, NOT via real policy. To test ANY non-owner anon path, create the bucket with testPrincipalHeader=<owner> so anon ≠ owner. Newly critical for security tests.
  - To isolate the POLICY gate (requireAccess) use the owner-coincidence (anon-created bucket) so requireACL passes via owner-match; to isolate the ACL gate use a distinct owner.
  - **R6 (NEW, P2, NOT fixed):** requireObjectAccess = requireAccess (policy) AND requireACL — an INTERSECTION. A bucket policy granting anon s3:GetObject does NOT grant a non-owner anon GET on its own; the ACL gate still denies a private ACL. AWS treats policy/ACL as a UNION. Semantic change, out of US-005 scope. See TestPAB_AnonGetEnforcementMatrix/policy/no-pab (403).
  - PAB eval-time knobs map: RestrictPublicBuckets → suppress anon POLICY grant (requireAccess); IgnorePublicAcls → suppress public ACL grant (requireACL). BlockPublicAcls/BlockPublicPolicy are PUT-time guards (NOT enforced — candidate follow-up), so a PAB carrying only those is inert for reads (negative control TestPAB_NonEvalFlagDoesNotBlock).
  - docs_reference_test.go pins every dispatch handler to internal/s3api/<file>:<line> in docs/site/content/reference/s3-api.md. ANY line shift (inserting a func above a handler) breaks it — update the rows or add `// docs:skip`. Helper funcs that aren't *Server dispatch handlers don't need a row but must not collide with an existing row's line.
  - Local build: s3api + auth are pure Go → CGO_ENABLED=0 go test ./internal/s3api/ ./internal/auth/ runs locally (cgo pkgs still blocked by Xcode license). Full vet/build = CI.
---

## 2026-05-30 - US-006
- SSE negative + key-mismatch + rotation hardening.
- **BUG FOUND + FIXED (P2, AWS compat)** in internal/s3api/errors.go: ErrSSECKeyMismatch carried HTTP 400; AWS-parity (s3-tests test_encryption_sse_c_other_key) is 403 AccessDenied. A self-consistent but wrong SSE-C customer key fails the stored-KeyMD5 comparison in requireSSECMatch BEFORE the AEAD layer → must be 403. Flipped sentinel Status 400→403; covers GET gate (requireSSECMatch) + copy-source (copy_object.go).
- Added: internal/s3api/sse_kms_test.go — kmsHarness wires LocalHSMProvider (deterministic fake KMS, hermetic, runs in CGO0 unit job). TestSSEKMS_RoundTrip (positive control), TestSSEKMS_WrongKeyID_AccessDenied (PUT under kms-key-1 → UpdateObjectSSEWrap flips stored keyID to kms-key-2 → GET unwrap mac-fails → kms.ErrKeyIDMismatch → 403 ErrKMSAccessDenied = the IncorrectKeyException path at server.go:1240), TestSSEKMS_MultipartPerPartDecrypt (3-part aws:kms MPU → whole-object GET decrypts via multipartChunkLocator + asserts persisted Manifest.PartChunkCounts has one positive entry per part).
- Extended internal/s3api/sse_c_test.go: TestSSECGetWrongKey (self-consistent wrong key → 403), TestSSECGetShortKey (16-byte key on GET → 400 InvalidArgument), retuned TestSSECGetWrongMD5 assertion 400→403.
- Rotation: master-key rotate + rewrap path already covered by sse_rotation_test.go (TestSSERewrap_ConvertsAtoB) — NOT duplicated; KMS objects are wrapped by external KMS so strata rewrap is the SSE-S3/master path only.
- Report: Auth dimension row (US-006 done) + R7 (closed) + encryption.go/sse_io.go handler rows. ROADMAP: P2 Done entry.
- Files: errors.go, sse_c_test.go, sse_kms_test.go, tasks/qa-readiness-report.md, ROADMAP.md, scripts/ralph/prd.json.
- **Learnings for future iterations:**
  - SSE-C GET gate (requireSSECMatch, encryption.go:80) is MD5-only: it compares the PROVIDED KeyMD5 against the STORED KeyMD5 and NEVER decrypts. So "wrong key" and "wrong key+self-consistent-MD5" are indistinguishable → both ErrSSECKeyMismatch (now 403). Three distinct 400 vs 403 legs in parseSSECTriple: key bytes != 32 → ErrInvalidArgument 400; MD5(key) != providedMD5 → ErrInvalidDigest 400; providedMD5 != storedMD5 → ErrSSECKeyMismatch 403.
  - SSE-KMS wrong-key-id testing: no real KMS needed. Use kms.NewLocalHSMProvider(32-byte seed) as api.KMS, PUT under one key id, then store.UpdateObjectSSEWrap(bucketID,key,"",sameWrappedBytes,otherKeyID) to point the row at a different id. GET unwrap recomputes the LocalHSM mac under the wrong id → fails → kms.ErrKeyIDMismatch. Gateway maps it at server.go:1240 → ErrKMSAccessDenied (403). ErrMissingKeyID → ErrKMSKeyIDMissing (400).
  - SSE-KMS multipart GET path: server.go:1388 picks multipartChunkLocator(key, Manifest.PartChunkCounts) when PartChunkCounts is non-empty, else singleChunkLocator. Per-part oid = "<key>:part=<n>" with chunkIndex restarting at 0 inside each part (multipartPartOID). A whole-object GET that byte-matches the concat of all parts proves the locator; PartChunkCounts is persisted on the completed object's Manifest (one entry per part) and readable via store.GetObject(...).Manifest.
  - newKMSHarness mirrors newSSEHarness (sse_object_test.go) but sets api.KMS instead of api.Master and exposes *metamem.Store. testPrincipalHeader plumbing identical. ssempUploadIDRE (sse_multipart_test.go) + makeBytes/firstDiff/md5sum helpers are package-shared — reuse, don't redefine.
  - The integration-tagged MinIO+KMS verification in the AC is satisfiable at unit level via LocalHSMProvider (the crypto/kms fake the AC itself names) — hermetic, no containers, runs in the standard CGO0 unit job. CI on Linux is still source of truth per FR-4.
---

## 2026-05-30 - US-007
- Multipart concurrency race tests.
- Added: internal/racetest/multipart_race.go (RunMultipartRaceScenario + helpers) — focused scenario extending the existing Fixture/RunScenario harness (NOT a fork). Wired: TestRaceMultipartMemory (internal/s3api/race_test.go, always-on under -race) + TestRaceMultipartTiKV (race_integration_test.go, integration). Refactored TiKV setup into shared newTiKVRaceFixture (TestRaceMixedOpsTiKV now calls it).
- Scenario (1) Complete-vs-Abort on ONE upload id: MP_RACE_RACERS goroutines (half Complete / half Abort) released via shared start chan. Asserts: at most one terminal winner — complete200>=1 => exactly 1 object row + ETag==md5(rawmd5(part))+"-1" + abort204==0; else (all racers reported) abort204==1 + 0 rows. Losers map to 404 NoSuchUpload (ErrMultipartInProgress/ErrMultipartNotFound) or idempotent cached 200. Upload always gone from ListMultipartUploads.
- Scenario (2) same-key/different-upload-id: N independent uploads complete concurrently on one shared key (versioned) => exactly one IsLatest=true row whose ETag a winning Complete produced; GET head serves it.
- Orphan-chunk invariant reused on bucket "mpr": every MemData chunk referenced by some manifest OR in GC queue.
- **NO bug found.** Memory + structural invariants hold (300 trials x 16 racers x3 stable locally CGO0). TiKV LWT path = CI integration. -race itself = CI Linux (local cgo blocked by Xcode license, documented).
- Files: internal/racetest/multipart_race.go, internal/s3api/race_test.go, internal/s3api/race_integration_test.go, tasks/qa-readiness-report.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - racetest harness layering: shared workload/invariants in internal/racetest (Fixture, RunScenario, VerifyInvariants, RunMultipartRaceScenario). Per-backend constructors live in s3api: race_test.go (memory, always-on) + race_integration_test.go (cassandra+tikv, //go:build integration). Add a new scenario by exporting a Run*Scenario in racetest then wiring one test per backend — do NOT build a parallel harness.
  - Memory meta.Store serializes ALL multipart ops under one mutex, so Complete/Abort race correctness is structural there; the REAL contention test is TiKV LWT (integration) + Cassandra LWT. Memory's job is -race cleanliness + invariant shape.
  - Loser-Complete can legitimately return EITHER 404 NoSuchUpload OR an idempotent cached 200 (GetMultipartCompletion replay at multipart.go:449). Tests must accept both; "no double object row" is the real invariant, not "exactly one 200".
  - In memory, CompleteMultipartUpload DELETES the upload from the active map (not a status-flip-in-place), so a losing Complete sees ErrMultipartNotFound, never ErrMultipartInProgress. Both map to ErrNoSuchUpload (404) at server.go:1558 — the in-progress sentinel only surfaces on Cassandra/TiKV where the row lingers in 'completing'.
  - Multipart ETag for a single part = hex(md5(hexDecode(partETag))) + "-1" (singlePartMultipartETag mirrors doMultipartCycle). partETag from UploadPart is plain md5hex of the part body.
  - Non-versioned overwrite (PUT or multipart-Complete) does NOT enqueue the prior object's chunks to GC in the memory backend (PutObject/CompleteMultipartUpload just replace bucket[key]) — that's why both race scenarios use a VERSIONING-ENABLED bucket to keep the orphan-chunk invariant clean. The unversioned-overwrite orphan is a separate concern from multipart-race correctness (candidate gap, not US-007 scope).
  - Knobs: MP_RACE_TRIALS / MP_RACE_RACERS env overrides (mirror RACE_* in fixture.go) for soak dial-up.
---

## 2026-05-30 - US-008
- Versioning + LWT/CAS contention races.
- Added: internal/racetest/versioning_race.go (RunVersioningRaceScenario + helpers) — focused scenario on the existing Fixture (NOT a fork). Wired: TestRaceVersioningMemory (race_test.go, always-on -race) + TestRaceVersioningTiKV (race_integration_test.go, integration). Three sub-scenarios on dedicated buckets (verx enabled / casx enabled / susx suspended): (1) PUT-vs-delete-marker monotonic chain + single IsLatest + no-5xx; (2) SetObjectStorage CAS contention mirrors internal/lifecycle/worker.go exactly (read expectedClass -> Data.PutChunks(newClass) -> CAS -> applied?GC-old:GC-new) -> exactly 1 applies, read-back==winner, loser+superseded chunks all in GC, no orphans; (3) suspended replace-null (PUT empty-body vs DeleteObjectNullReplacement) -> <=1 null row, single IsLatest, GET ?versionId=null deterministic.
- **BUG FOUND + FIXED (P2/R8)** in internal/s3api/server.go getObject: a GET/HEAD resolving via explicit ?versionId to a DELETE MARKER fell through to the body path; delete marker carries nil manifest -> s.Data.GetChunks(nil) errored -> 500 InternalError. AWS-parity is 405 MethodNotAllowed + x-amz-delete-marker:true + Last-Modified. Guarded getObject on o.IsDeleteMarker (returns 405) BEFORE any manifest read; new ErrMethodNotAllowed sentinel in errors.go. Latest-version (no versionId) path already correct (store -> ErrObjectNotFound -> 404). Surfaced by sub-scenario (3)'s GET ?versionId=null determinism probe hitting the suspended null delete marker.
- Added regression test internal/s3api/versioning_test.go: TestGetDeleteMarkerByVersionIDReturns405 (GET + HEAD, Enabled TimeUUID marker + Suspended null marker -> 405 + x-amz-delete-marker:true).
- Report: Concurrency/race dimension row (US-008 done) + R8 (closed). ROADMAP: P2 Done entry.
- Files: internal/racetest/versioning_race.go, internal/s3api/race_test.go, internal/s3api/race_integration_test.go, internal/s3api/server.go, internal/s3api/errors.go, internal/s3api/versioning_test.go, tasks/qa-readiness-report.md, ROADMAP.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - getObject (server.go ~1156) had NO delete-marker guard. The store's GetObject returns the marker row for an EXPLICIT versionId (only the no-versionId latest path 404s a delete-marker head, store.go:1061). So any explicit ?versionId pointing at a delete marker reached the body path and 500'd on GetChunks(nil). The guard belongs at the TOP of getObject (covers GET body=true AND HEAD body=false). 4 distinct manifest-read sites exist but this is the only GET/HEAD one.
  - Suspended replace-null orphans chunks in the MEMORY backend (PutObject IsNull branch + DeleteObjectNullReplacement replace the null row in place, NEVER enqueue the superseded null chunks to GC — same shape as the US-007 unversioned-overwrite note). So a suspended-bucket sub-scenario that writes non-empty bodies will FAIL the global orphan check. Use EMPTY bodies (memory PutChunks returns a zero-chunk manifest for a 0-byte reader) to keep the suspended sub-scenario chunk-free. The superseded-null-chunk GC gap is a separate candidate (R-?, not US-008 scope).
  - To mirror the lifecycle-CAS invariant in a test, replicate internal/lifecycle/worker.go's flip path EXACTLY: applied=true -> EnqueueChunkDeletion(old/superseded chunks); applied=false (or err) -> EnqueueChunkDeletion(this racer's fresh chunks). Feed all racers the SAME snapshotted expectedClass (read once before the start chan) so the CAS is genuine contention, not a serialized read-then-flip where late racers already see the flipped class.
  - Fixture exposes Server.Data (data.Backend) + Server.Meta (meta.Store) directly — call Data.PutChunks / Meta.SetObjectStorage / Meta.GetObject / Meta.EnqueueChunkDeletion / Meta.ListGCEntries straight from the scenario for the CAS leg (HTTP can't drive SetObjectStorage — it's a lifecycle-worker-only meta op). MemData.ChunkOIDs() returns ALL chunks across ALL buckets in the fixture, so a global orphan check must aggregate live OIDs from EVERY chunk-writing bucket's AllVersions(bucketID) + the GC queue.
  - AllObjectVersions(memory) computes IsLatest = (i==0) on the fly (store.go:1271), so "exactly one IsLatest" is structural on memory; the real version-chain-head LWT contention test is TiKV (integration). Memory's job is -race cleanliness + invariant shape, same as US-007.
  - docs_reference_test.go pins QUERY-STRING DISPATCH handlers to file:line; getObject is not one (it's reached via handleObject routing, not a ?query dispatch), so inserting ~15 lines into its body did NOT break the pin. Still re-ran the docs test to confirm.
---

## 2026-05-30 - US-009
- Drain stop-write invariant under concurrent load.
- Added: internal/data/s3/multipart_drain_test.go (TestMultipartSurvivesDrainingCluster) + concurrent variant TestDrainStopWriteUnderConcurrentLoad appended to internal/s3api/drain_strict_test.go (new drainToggleBackend wrapper: atomic refuse flag, PutChunks→DrainRefused while set, GetChunks/Delete always pass through inner).
- AC1: cluster drains → 24 concurrent PUTs all 503 DrainRefused + Retry-After:300 while 12 GET / 12 HEAD / 8 DELETE on pre-seeded objects keep working (PUT-only stop-write). Objects seeded BEFORE flipping refuse=true so reads/deletes have real manifests.
- AC2: in-flight multipart bound to draining cluster finishes — UploadBackendPart/Complete/Abort route via connFor(clusterID-from-handle), NEVER re-consult picker (exactly 1 Create/UploadPart/Complete; no 2nd Create on a live cluster), while FRESH PutChunks to same cluster → ErrDrainRefused. The ErrDrainRefused doc comment in internal/data/errors.go literally states this invariant.
- AC3: deregister_ready gate ALREADY COVERED (no duplication) — TestClusterDrainProgress_NotReadyReasons_OpenMultipartOnly + _AllThree (internal/adminapi) drive the ListMultipartUploadsByCluster safety gate via a real in-flight multipart whose BackendUploadID points at the cluster; meta primitive pinned by caseListMultipartUploadsByCluster (storetest contract, both backends). Referenced in report, not re-added.
- **NO bug found.** Always-strict drain invariant holds under concurrent load. Report: Durability/fault-injection row (US-009 done), placement_ctx.go thin→covered, R3 closed. No ROADMAP edit (test-only, no bug/gap).
- Files: internal/data/s3/multipart_drain_test.go, internal/s3api/drain_strict_test.go, tasks/qa-readiness-report.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Drain routing split (the AC2 invariant): connFor(clusterID) in internal/data/s3/backend.go:384 resolves a cluster PURELY from the id — it NEVER consults data.DrainingClustersFromContext. UploadBackendPart/CompleteBackendMultipart/AbortBackendMultipart all decode the BackendUploadID handle (<cluster>\x00<bucket>\x00<key>\x00<uploadID>) and call connFor → in-flight multipart always survives drain. The PUT path (PutChunks→clusterForPlacement→clusterForClassStrict, backend.go:520) is the ONLY one that checks the draining set and returns data.NewDrainRefusedError. To prove "survives drain" just inject data.WithDrainingClusters(ctx, {id:true}) and assert UploadPart/Complete succeed while PutChunks refuses on the same ctx.
  - drainRefusingBackend (always-refuse) already existed in drain_strict_test.go for the single-PUT 503 mapping. For the under-load + reads-survive dimension you need a TOGGLE backend (drainToggleBackend, atomic.Bool) so you can PUT-seed objects while live, then flip to draining — GetChunks/Delete must pass through to the SAME inner datamem so the seeded manifests resolve. A pure always-refuse backend can't seed readable objects.
  - In the memory backend HEAD reads meta only (no GetChunks) and unversioned DELETE removes the meta row + GC-enqueues (no inline Data.Delete) — both are drain-immune by construction. GET is the only read that hits GetChunks. Use DISTINCT pre-seeded keys for GET/HEAD vs DELETE so a concurrent DELETE doesn't 404 a concurrent GET on the same key.
  - data.ErrDrainRefused (internal/data/errors.go) is a sentinel; NewDrainRefusedError(cluster) wraps it with Unwrap → both errors.Is(err, ErrDrainRefused) AND errors.As(err, &*DrainRefusedError) work; the wrapper carries .Cluster for the wire/metric label.
  - deregister_ready (open_multipart leg): buildDrainProgressResponse (internal/adminapi/clusters_drain_progress.go:314) gates on three counts — snapshot chunks, ListChunkDeletionsByCluster (gc), ListMultipartUploadsByCluster (mp). mpPending>0 → reason "open_multipart" → deregister_ready=false. Fixed reason vocabulary: chunks_remaining / gc_queue_pending / open_multipart (constants in same file). Already fully tested; don't re-add.
  - -race + cgo still blocked locally (Xcode license) — both new tests pass CGO_ENABLED=0; -race is CI-Linux-only (FR-4 source of truth). The s3api concurrent test uses a fail-once mutex-guarded string (not t.Fatalf inside goroutines) so it's race-clean and reports the first failure deterministically.
---

## 2026-05-30 - US-010
- Rebalance mover correctness under concurrent client writes.
- Added: internal/rebalance/s3_mover_integration_test.go::TestRebalanceS3ConcurrentClientWrites (MinIO x2) + internal/rebalance/rados_mover_integration_test.go::TestRebalanceRADOSConcurrentClientWrites (ceph&&integration, chunk-based mirror).
- Both: plant N objects on c1, build the c1->c2 plan, then for HALF the objects a client PUT rewrites the manifest locator (BackendRef.Key / chunk[0].OID) on the SAME class+cluster BEFORE the mover runs. The mover copies the OLD planted key to c2, re-reads the live manifest, and buildUpdatedBackendManifest/buildUpdatedManifest sees the locator no longer matches plan SrcRef.OID -> ok=false -> CAS conflict -> mover's freshly-copied c2 object discarded to GC; the client's manifest survives. Clean half moves normally (manifest->c2, body readable on c2, old c1 key in GC). GC accounting asserted exact: 1 entry/object, clean->c1 source, raced->c2 loser; no leak, no double.
- Report: Durability dimension row folded in US-010; prd US-010 passes:true.
- **NO bug found** — mover never clobbers a racing client write; CAS losers reclaimed. No production code changed.
- **Learnings for future iterations:**
  - SetObjectStorage CAS is CLASS-ONLY (memory store.go:1344: `expectedClass != "" && o.StorageClass != expectedClass`). The rebalance mover calls it expectedClass==newClass (same class, rebalance never re-tiers), so the class-CAS gives ZERO lost-update protection. The ENTIRE race-loss guard is buildUpdated{Backend}Manifest's locator check (s3_mover.go:383 `br.Key != SrcRef.OID || br.Cluster != FromCluster`; rados_mover.go:362 `current.OID != SrcRef.OID || current.Cluster != FromCluster`) done on the GetObject snapshot — NOT atomic with the CAS. A client write landing AFTER the mover's GetObject but BEFORE SetObjectStorage with the SAME class would clobber it (theoretical lost-update window; not exercised here because the deterministic test lands the client write BEFORE the mover reads). If a future story tightens this, make SetObjectStorage CAS on a manifest fingerprint, not just class.
  - Safety rails (refuse moves into draining/>90%-full target) are Worker.applySafetyRails (worker.go:678), a POST-PLAN filter — the Mover NEVER sees a refused move (it gets an already-filtered plan). Fully covered by worker_test.go (TestWorkerRefusesMovesIntoFullTarget uses fakeStatsProbe 95/100 > DefaultFillCeiling 0.90; target_draining via SetClusterState Evacuating + applySafetyRails defense-in-depth). Do NOT add mover-level fullness tests — the mover has no probe.
  - S3 mover integration test is PURE Go (`//go:build integration`, no cgo): `CGO_ENABLED=0 go vet -tags integration ./internal/rebalance/` compiles it locally (no Docker needed for vet). RADOS mover integration test is `ceph && integration` (librados) — CI-only, mirror the exact existing API surface (cmap Write/Read, m.PutObject/GetObject/ListGCEntries) since it can't build on macOS.
  - GC loser distinguishing: clean-winner GC entry = OLD source ref (cluster c1, pool=srcBucket/srcPool). raced-loser GC entry = mover's NEW copy (cluster c2, pool=dstBucket/srcPool; S3 newRefsFromS3, RADOS newRefsFrom). RADOS c2 loser OID has the mover shape `<uuid>.%05d` (NOT "rb/" — only seeded source OIDs carry that prefix).
  - metamem PutObject(obj, versioned=false) REPLACES the latest row entirely (store.go:980 `bucket[o.Key] = []*meta.Object{&cp}`) — clean way to simulate an unversioned client overwrite of the manifest.
---

## 2026-05-30 - US-011 (+ split US-011b)
- Chunk-loss / corruption / partial-write recovery — memory(default-tag) + MinIO(integration) durability half. RADOS half SPLIT to new US-011b (FR-8; mirrors the US-009/US-010 split precedent).
- **Stale-skip FIXED** (the NOTE's explicit ask): internal/s3api/sse_object_test.go TestSSECorruptedCiphertext — the `if !CorruptFirstChunk() { t.Skip(...) }` guard became `t.Fatal`. The memory backend has exposed CorruptFirstChunk since backend.go:148 and the 1 KiB AES256 PUT guarantees a non-empty ciphertext chunk, so the helper ALWAYS returns true; the skip was dead code that only ever masked a missing-chunk PUT regression. The corruption assertion now RUNS on the default CI tag: the SSE AEAD open over the flipped ciphertext fails loud (non-2xx), never a silent 200.
- **NEW internal/data/memory/corruption_test.go** (package memory_test): TestPutChunksPartialWriteCommitsNoManifest (reader errors after ~9 MiB → PutChunks returns errMidStream + NIL manifest → gateway never persists a meta row → no readable half-object — AC2 partial-write rollback), TestPutChunksContextCancelCommitsNoManifest (pre-cancelled ctx → context.Canceled + nil manifest), TestGetChunksZeroByteManifestReadsClean (control: empty object clean EOF — proves the fail-loud asserts detect real damage, not everything). All three PASS locally CGO_ENABLED=0.
- **NEW internal/data/s3/corruption_integration_test.go** (`//go:build integration`, package s3_test): TestGetChunksMissingBackendObjectFailsLoud — PutChunks → read-clean sanity → admin DeleteObject the backing object → GetChunks(0,size) fails loud with data.ErrNotFound (surfaces at open via GetChunks→GetRange), never a usable reader yielding a short silent body. Exercises the manifest-level GetChunks path the GET handler drives; does NOT duplicate the existing b.Get/b.GetRange missing-key tests (those probe the low-level oid path, not the manifest path).
- **AC3 ETag/size-mismatch ALREADY covered** by internal/racetest/verifier.go checkReadAfterWrite (flags "GET returned an etag/size pair the workload never PUT" across a backoff window) — referenced in report, no new code.
- **NO bug found** — fail-loud invariants hold on both verified backends.
- Files (US-011 commit): internal/s3api/sse_object_test.go, internal/data/memory/corruption_test.go (new), internal/data/s3/corruption_integration_test.go (new), scripts/ralph/prd.json (US-011 passes:true + US-011b added), scripts/ralph/progress.txt, tasks/qa-readiness-report.md (durability dimension row updated, R4 marked partially-addressed, new R9 latent gap = plaintext byte-flip not detected on the read path — only truncation/missing fails loud, SSE caught by AEAD).
- **Learnings for future iterations:**
  - The memory data backend stores CIPHERTEXT for SSE objects: s3api wraps the body in sseEncryptingReader (server.go:1024) BEFORE Data.PutChunks, so datamem.chunks holds encrypted bytes. CorruptFirstChunk flips one ciphertext byte → AEAD open fails on the GET path's first read (server.go:1413 io.Copy(w, dec)) BEFORE WriteHeader(200) commits (the decrypt reader is read eagerly enough that the corruption test observes non-2xx, not a truncated 200). For PLAINTEXT objects the memory backend has NO at-rest integrity check, so a byte-flip would return a full-length corrupted 200 — that's a separate (weaker) bitrot concern than the AC's truncation focus; not asserted here.
  - memory PutChunks loop (backend.go:37) uses io.ReadFull(r, 4MiB buf): a non-EOF reader error mid-stream is returned verbatim as (nil, err) — chunks already buffered stay orphaned in the map but NO manifest is returned, so the object is never reachable (the gateway writes the meta row only after a non-nil manifest). This is the partial-write rollback guarantee; it lives in the no-manifest-on-error contract, not in any explicit rollback code.
  - data.ErrNotFound (internal/data/errors.go:11) is the backend-agnostic "object not found"; s3 GetRange returns it (backend.go:943) and GetChunks→GetRange propagates it. The s3 missing-key contract is already pinned by TestGetReturnsErrNotFoundForMissingKey (b.Get/b.GetRange); the US-011 value-add is the manifest-level GetChunks path.
  - s3 integration helpers (same package s3_test, defined in backend_integration_test.go, reuse — do NOT redefine): minioKMSSecretKey const, newUUID(t)→uuid.UUID, minioEndpoint(t,ctx,container)→string, createBucket(ctx,endpoint,ak,sk,bucket)→error, newAdminClient(endpoint,ak,sk)→*awss3.Client, ptr[T](v)→*T. tcminio.Run needs MINIO_KMS_SECRET_KEY env (the s3 backend applies AES256 on upload; MinIO 501s without a KMS key).
  - **RADOS corruption is genuinely hard to land in one local iteration**: cephimpl has NO CorruptFirstChunk hook (the NOTE forbids adding one — corrupt via the backend's own Delete/overwrite of a chunk OID), AND the ceph-tagged module currently fails to build in this workspace (cephimpl backend.go:288 `goceph.OperationNoFlag` — a go-ceph version drift) so it is not even compilable on a macOS/lima box. RADOS corruption fail-loud is therefore CI-ceph-only (make test-rados) and is carved into US-011b.
  - HARNESS NOTE (this session): tool-result delivery was heavily delayed/batched; `go vet -tags integration` in particular blocked Bash result delivery for many turns (heavy testcontainers/aws-sdk compile, possibly sandbox-network). Prefer `CGO_ENABLED=0 go test ./pkg/ -run <name>` (fast, returns) over a full integration vet when iterating; trust CI for the integration build (FR-4).
---

## 2026-05-30 - US-011b (notes from first attempt — superseded by COMPLETE entry below)
- ATTEMPT FAILED, repo reset to baseline (git reset --hard to the US-011 commit). prd.json US-011b stays passes:false. Next iteration must redo.
- HARNESS NOTE: tool OUTPUT channel was severely degraded this run — most Bash/Read/Grep calls returned EMPTY or garbled multi-line dumps (stray ``` fences, `pe pe pe`, line-number jumps). Writes/Edits/commits still applied. Could not author a correct cgo fix blind. If output is empty, STOP and report rather than commit blind — my first attempt committed broken code before IDE diagnostics revealed the truth.
- CORRECTED FACTS for US-011b (verified via clean outputs + IDE diagnostics — DO NOT repeat my mistakes):
  1. There is ALREADY a tracked test `internal/data/rados/cephimpl/backend_integration_test.go` (committed in US-003 module split): `//go:build integration`, `package cephimpl_test`, func TestRADOSBackend with subtests PutGetDelete / RangeRead / UnknownClassRejected + helper `func envOr(k,def string) string`. DO NOT overwrite it — ADD the chunk-loss test (new func in same file, or a new `*_integration_test.go` in the SAME external pkg `cephimpl_test`; if new file, don't redeclare envOr).
  2. cephimpl module go.mod has NO testify — use stdlib `testing` only (t.Fatalf/t.Errorf), NOT require/assert.
  3. CORRECT cephimpl API (from HEAD): `cephimpl.New(rados.Config{ConfigFile, User, Pool, Classes}) (data.Backend, error)` — config type is `rados.Config` (import github.com/danchupin/strata/internal/data/rados), NOT a cephimpl.Config. `rados.ParseClasses("STANDARD=<pool>")` builds Classes. Backend methods: `PutChunks(ctx, r io.Reader, class string) (*data.Manifest, error)` (NO key arg; takes a storage CLASS), `GetChunks(ctx, m *data.Manifest, offset, length int64) (io.ReadCloser, error)`, `Delete(ctx, m *data.Manifest) error` (NOT DeleteChunks). data.Manifest.Chunks is `[]data.ChunkRef`; ChunkRef has {Cluster, Pool, Namespace, OID string}.
  4. To destroy ONE chunk via the backend's own path (AC: no new hook): build `&data.Manifest{Chunks: m.Chunks[:1]}` and call `be.Delete(ctx, thatManifest)` — Delete iterates chunks and removes each by ioctx.Delete(OID), routing per ChunkRef. Then `GetChunks(ctx, m, 0, m.Size)` on the FULL manifest must fail loud (expect data.ErrNotFound at open, mirror s3 corruption_integration_test.go shape: seed >=2 chunks -> clean read sanity -> delete first chunk -> assert error / no silent-truncation). Use src >=5 MiB for >=2 chunks (4 MiB chunking). Second Delete returns data.ErrChunkNotFound (see existing PutGetDelete subtest).
  5. BUILD BREAK IS REAL (AC #3, not absent): `goceph.OperationNoFlag` fails to compile — "cannot use goceph.OperationNoFlag (constant unknown with invalid type) as rados.OperationFlags". Sites: backend.go:288, ops.go:21, ops.go:39, rebalance.go:78 (all `op.Operate(ioctx, oid, goceph.OperationNoFlag)`). Must find the correct OperationFlags value in the pinned go-ceph version (grep $(go env GOMODCACHE)/github.com/ceph/go-ceph@*/rados/*.go for OperationFlags consts) and replace, OR file as ROADMAP P-item. This blocks `make test-rados` so the integration test can't run until fixed.

## 2026-05-30 - US-011b (COMPLETE)
- RADOS chunk-loss fail-loud (integration), completing the US-011 split (memory + MinIO landed in US-011; this adds the librados data plane).
- Added ONE subtest `t.Run("ChunkLossFailsLoud", ...)` to the EXISTING internal/data/rados/cephimpl/backend_integration_test.go (TestRADOSBackend) — reuses the already-connected `be`/`pool` harness, NO new file, NO clobber of the tracked TestRADOSBackend (PutGetDelete/RangeRead/UnknownClassRejected) or `envOr`. stdlib testing only (cephimpl module has no testify).
- Shape mirrors internal/data/s3/corruption_integration_test.go: seed 9 MiB (3 chunks) -> clean read sanity (bytes.Equal) -> destroy FIRST chunk via the backend's OWN Delete -> full GetChunks must fail loud (error at open OR short body; NEVER the full manifest.Size payload). No new corruption hook (AC NOTE).
- Single-chunk destroy idiom (verified against backend.go:338 Delete): `be.Delete(ctx, &data.Manifest{Chunks: manifest.Chunks[:1]})` — Delete iterates m.Chunks calling ioctx(c.Cluster,c.Pool,c.Namespace).Delete(c.OID), so a 1-chunk manifest removes exactly that OID while the full manifest still references it. ChunkRef carries its own routing (Cluster/Pool/Namespace/OID) so copying the value preserves it.
- AC #3 build-break RESOLVED as a FALSE POSITIVE: `goceph.OperationNoFlag` is NOT a real break. go-ceph v0.39.0 defines it (rados/operation_flags.go:15 `OperationNoFlag = OperationFlags(C.LIBRADOS_OPERATION_NOFLAG)`). The IDE/gopls "constant unknown with invalid type" at backend.go:288 (also ops.go:21/39, rebalance.go:78) is a LOCAL artifact: without librados, gopls can't resolve the cgo `C.LIBRADOS_*` constant so the const's type is unknown. Under the real cgo build (CI ceph image, `make test-rados`) it compiles — the shipped backend already uses this in 4 places and the e2e job is green. No source change; no ROADMAP P-item.
- Verification: gofmt -l clean + gofmt -e SYNTAX_OK. Full build/run is CI-only by design (librados + cgo; local macOS box blocked by Xcode license + no librados). CI ceph e2e (`make test-rados`) is the source of truth (AC #4, FR-4).
- ENV WARNING for next iteration: a CONCURRENT ralph.sh is running against this repo (saw 2 ralph.sh PIDs via ps; HEAD advanced under me, tool outputs intermittently empty/garbled, files raced). Two agents on one branch step on each other. The first attempt this iteration committed broken code (clobbered TestRADOSBackend, used a nonexistent DeleteChunk + testify, wrong cephimpl.New sig) before IDE diagnostics revealed the truth; it was `git reset --hard` to baseline 6f77093 and redone correctly. If outputs go empty, STOP and report — do not commit blind.
- **Learnings for future iterations:**
  - cephimpl real API (HEAD): cephimpl.New(rados.Config{ConfigFile,User,Pool,Classes}) (data.Backend,error); rados.ParseClasses("STANDARD=<pool>"); be.PutChunks(ctx, r io.Reader, class string) (*data.Manifest,error) [class, NO key]; be.GetChunks(ctx, m, offset, length); be.Delete(ctx, m) [NOT DeleteChunks]; second Delete -> data.ErrChunkNotFound. data.Manifest.Chunks []data.ChunkRef{Cluster,Pool,Namespace,OID,Size,...}.
  - cephimpl tests are external pkg `cephimpl_test`, stdlib testing, `//go:build integration`. Reuse TestRADOSBackend's setup via a new t.Run, don't fork a harness.
  - go-ceph cgo constants (C.LIBRADOS_*) always read as "invalid type" in local gopls without librados — ignore those specific diagnostics; trust the CI cgo build.

## 2026-05-31 - US-012
- GC + bucket_stats fan-out concurrency consistency. Test-only, NO bug found — invariants hold on both backends.
- AC1 (concurrent BumpBucketStats exact sum + non-degenerate shard distribution) ALREADY covered — `caseBucketStatsConcurrentBumps` (contract, memory+tikv, 50 racers → exact sum, zero errors) + `TestBumpBucketStatsConcurrent` (internal/meta/tikv/bucket_stats_test.go, 100 racers → exact sum AND fakeShardMetrics asserts ≥2 shards via the fresh-uuid picker). NOT duplicated; referenced in report.
- AC2 NEW `caseGCConcurrentEnqueueAckLockstep` (internal/meta/storetest/contract.go, runs memory + tikv): 200 producers concurrently EnqueueChunkDeletion 1 unique-OID chunk each across cl-a/cl-b → 16 ackers drain every listed entry concurrently. Asserts ListGCEntries ⇄ ListChunkDeletionsByCluster lockstep after concurrent enqueue (primary=400, per-cluster=200 each, no dup OID) AND after concurrent ack (both views empty — no leak, no surviving racing-ack row). Added `sync` import + registration row.
- AC2 NEW `TestGCDualWriteConcurrentLockstep` (internal/meta/tikv/gc_concurrent_test.go, package tikv unit, NEW FILE): exercises the GENUINE tikv dual-write path (`s.gcDualWrite=true`) which the contract can't reach (contract gets a generic meta.Store; default gcDualWrite=off). Concurrent enqueue+ack; asserts the v2 sharded prefix and legacy region prefix stay ROW-FOR-ROW in lockstep (both=total after enqueue, both=0 after drain) via scanPrefix helper — an ack dropping one side surfaces as an orphan row. Reuses scanPrefix/openWithBackend/newMemBackend/key helpers from gc_test.go.
- AC3 NEW `TestWorker_FanOutExactlyOnceConcurrent` (internal/gc/worker_test.go): 8 gc.Worker shard goroutines drain the SAME memory queue + countingBackend concurrently; per-OID counting asserts every 1/2000 chunk deleted EXACTLY ONCE (count==1; >1=shard overlap/double-delete, 0=skip), processed==2000, queue fully drained. New `countingBackend` (per-OID, vs recordingBackend per-cluster). Distinct from TestWorker_DrainConcurrency (single-worker errgroup speedup).
- Verified locally CGO_ENABLED=0 -race: memory contract case PASS, gc fan-out PASS, full internal/gc + internal/meta/memory PASS. tikv unit test gofmt -e SYNTAX_OK but package tikv is CI-ONLY locally (macOS gosigar `sysctlbyname` CGO0-darwin build break — env, not code; per Codebase Patterns). Trust CI Linux (FR-4).
- NO ROADMAP edit: test-only, no bug, no new gap (per ROADMAP-maintenance rule: code-only commits touching neither shipped nor pending need no edit).
- Report: Concurrency/race dimension row flipped to "US-007, US-008, US-012" + US-012 evidence paragraph.
- Files: internal/meta/storetest/contract.go, internal/gc/worker_test.go, internal/meta/tikv/gc_concurrent_test.go (new), tasks/qa-readiness-report.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - tikv gcDualWrite defaults OFF (GCDualWriteFromEnv empty→false). Contract cases for GC run single-write (v2-only on tikv; one slice on memory). `ListChunkDeletionsByCluster` DOUBLE-COUNTS under dualWrite=on (scans v2 AND legacy, adds both) — fine for the limit=1 existence probe it's built for, but means exact-count assertions only hold with dualWrite off. The genuine dual-write lockstep is tikv-package-only (gcDualWrite is unexported, no meta.Store knob) → assert at the physical prefix level via scanPrefix(GCQueueRegionPrefixV2 / GCQueuePrefix), not via ListChunkDeletionsByCluster.
  - memory AckGCEntry is an idempotent no-op when the entry is already gone (matches OID+EnqueuedAt.Equal, returns nil if not found) — so concurrent ackers acking the SAME listed entry won't error/double-remove; the exactly-once GC guarantee comes from the disjoint shard partition (`entry.ShardID % shardCount == shardID`), NOT from ack-racing. Test exactly-once at the shard-fan-out layer (gc.Worker), test lockstep/no-leak at the ack layer.
  - gc.Worker fan-out: build one Worker per shard with {ShardID:i, ShardCount:K}; RunOnce drives drainCount which scans only its slice via ListGCEntriesShard. Run K of them concurrently against one shared store+backend for the exactly-once concurrency proof. countingBackend (per-OID map under mutex) is the right observer; recordingBackend (per-cluster) and programmableBackend (per-OID + errs) already exist — reuse the shape, don't fork the interface (all 3 implement data.Backend: PutChunks/GetChunks/Delete/Close).
  - storetest contract: add a `case<Name>` func + ONE registration row in the `cases` slice (contract.go:~28); it then runs on memory (unit) AND tikv (integration, store_integration_test.go:66) AND cassandra. `sync` was NOT previously imported in contract.go — added.
---

## 2026-05-31 - US-013
- Production-readiness report + close-flip + coverage ratchet gate. FINAL story — all 14 pass.
- **Coverage gate (AC5):** NEW scripts/qa/coverage-floors.txt (7 core pkgs: auth 84 / s3api 75 / meta/memory 74 / meta/tikv CI-ESTABLISH / data/memory 40 / data/placement 90 / crypto/kms 72) + scripts/qa/coverage-gate.sh (per-pkg `go test -cover`, float-compare measured>=floor, awk; CGO0 fallback recovers s3api locally; CI=set => unmeasured hard-gated pkg FAILS, local => skip-with-warn). `make coverage-gate` target + wired as a step in CI `coverage` job. Ran green locally (exit 0; tikv soft-INFO only).
- **Report (AC1):** verdict PENDING->GO; before/after delta table for core pkgs; bugs-found table (P1 PAB + 3 P2); residual-risk acceptance R1/R5/R6/R9; §6 manual post-merge (tikv-floor hard-wire sed + branch-protection `gh api` cmd + s3-tests re-run). Coverage marker table regenerated to current (after) numbers. Fixed US-011b citation (ChunkLossFailsLoud subtest in backend_integration_test.go, NOT a new file — verified via git show ed75b41).
- **ROADMAP (AC3):** umbrella QA-cycle Done entry + R1 (P2) + R9 (P3) new entries. R6 already inline.
- **s3-tests README (AC2):** re-confirmed 92.7% unchanged (4 fixes all outside DEFAULT_FILTER subset); 13 deliberate gaps re-confirmed. Desk check, not a run.sh exec (s3-tests not a CI job, manual/local per AC NOTE).
- Files: .github/workflows/ci.yml, Makefile, ROADMAP.md, scripts/s3-tests/README.md, tasks/qa-readiness-report.md, scripts/qa/coverage-floors.txt, scripts/qa/coverage-gate.sh, scripts/ralph/prd.json. NO Go code changed.
- **Learnings for future iterations:**
  - Coverage ratchet design: floors in a committed txt (path-suffix + percent|CI-ESTABLISH), gate script measures per-pkg (not whole ./...), float compare via `awk "BEGIN{exit !($cov >= $floor)}"`. tikv/s3api cgo-blocked on darwin: gate's CGO0-fallback recovers s3api (76.6%) but NOT tikv (gosigar sysctlbyname under CGO0). tikv floor = soft CI-ESTABLISH sentinel, hard-wired post-merge from first green CI run (report §6). data/memory floor set to NEW measured baseline 40 (US-011 lifted 6.5->41.9), not stale US-001 6 — a 6 floor leaves a regression hole; documented as deviation.
  - Coverage of interface/harness packages measured in ISOLATION undercounts: racetest 73.6->48.6 (Run*Scenario fns invoked from s3api tests, uncovered when `go test ./internal/racetest/` alone). Same artifact as meta/storetest 0.0%. Not a regression; note it explicitly so it doesn't read as one.
  - Branch protection / tikv-floor = manual post-merge (headless Ralph lacks admin-API auth) — same posture as ci-green cycle. New required check context name = the CI job `name:` string verbatim: "Coverage baseline (QA readiness)".
  - 4 bug-fix ROADMAP Done entries (US-002/005/006/008) were already written in-cycle by their stories — close-flip just adds the umbrella + the UNFIXED residuals (R1/R9) as P-entries. Don't re-flip already-Done entries.
  - Verify every test-file citation in the report against git (git show <sha> --stat) before asserting — US-011b was a subtest add, not a new file; an unchecked citation is a wrong-answer penalty.
---

# Ralph Progress Log
Started: Thu May  7 14:30:00 MSK 2026
Cycle: ralph/web-ui-storage-status (Web UI — meta + data backend observability)
---

## Codebase Patterns

### Branch policy (REQUIRED)
- Cycle starts from main (HEAD `e1aeef2` at prep time, after s3-compat-95 closed at 91.5%). The cycle MUST end with a merge back to main: US-006 (last story) fast-forwards / squash-merges `ralph/web-ui-storage-status` into `main`, pushes origin/main, lets `archive_cycle` (in `scripts/ralph/ralph.sh`) snapshot prd.json + progress.txt under `scripts/ralph/archive/2026-MM-DD-web-ui-storage-status-complete/` on `<promise>COMPLETE</promise>`. Mirror the s3-compat-95 close shape (commit `e1aeef2`).
- The shell wrapper `scripts/ralph/ralph.sh` MUST invoke `avito ai claude --` (not bare `claude`) — regression-resistant.

### PRD lifecycle (REQUIRED — added to root CLAUDE.md as `b45d633`)
- The markdown PRD `tasks/prd-web-ui-storage-status.md` is a disposable design draft. The canonical record of what got built is the auto-archived `scripts/ralph/archive/<date>-web-ui-storage-status-complete/{prd.json,progress.txt}` snapshot. US-006 close-flip MUST `git rm tasks/prd-web-ui-storage-status.md` in the same commit that flips the ROADMAP entry to Done. Do NOT copy the markdown to `tasks/archive/`.

### Prior cycle context
- `ralph/s3-compat-95` cycle closed at HEAD `e1aeef2`. Lifted s3-tests pass-rate `84.7% (150/177)` → `91.5% (162/177)`. 7 stories shipped.
- Two P1 latent bugs from s3-compat-90 got CI coverage tests (US-006 of that cycle). Both stay green on main.
- ROADMAP `## Web UI` section already has 4 prior entries (Phase 1/2/3 + TiKV heartbeat + OTel ringbuf tuning + this cycle's target P3 entry — added in US-006).

### Adminapi shape (where things live on current main)
- Adminapi handlers live under `internal/adminapi/`. Each surface gets its own file (`buckets.go`, `iam.go`, `audit.go`, `metrics.go`, …). Routes wired in `internal/adminapi/router.go`. Storage handlers MUST follow the same per-surface file pattern: `internal/adminapi/storage_meta.go` + `storage_data.go` + `storage_classes.go` + `storage_health.go`.
- All `/admin/v1/*` go through JWT-cookie session auth (HS256, 24h, `HttpOnly+SameSite=Strict+Path=/admin`) installed in `internal/adminapi/middleware.go`. SigV4 fallback for programmatic clients. Read-only endpoints in this cycle — no audit override needed (audit middleware skips GET).
- OpenAPI spec at `internal/adminapi/openapi.yaml`. Each new endpoint MUST be added with full schema (path, params, response). Bundle is regenerated on every spec change — keep it in sync.

### Meta backend optional interface pattern (US-001)
- `internal/meta.Store` is the core interface; optional capabilities use type-assertion at adminapi (e.g., `meta.RangeScanStore` for TiKV native scans). New interface goes alongside: `internal/meta/healthprobe.go` defining `HealthProbe interface { MetaHealth(ctx) (*MetaHealthReport, error) }`.
- Cassandra impl in `internal/meta/cassandra/healthprobe.go`. Memory impl in `internal/meta/memory/healthprobe.go`. TiKV impl in `internal/meta/tikv/healthprobe.go`. Adminapi handler does the type assertion: `if hp, ok := s.Meta.(meta.HealthProbe); ok { ... }` — backends without the capability return a degenerate report.
- Contract test pattern: extend `internal/meta/storetest/contract.go` with `caseMetaHealth` that runs against all factories — same shape as existing cases.

### Data backend optional interface pattern (US-002)
- `internal/data.Backend` similar — new optional interface `internal/data.HealthProbe` defining `DataHealth(ctx) (*DataHealthReport, error)`.
- RADOS impl is build-tagged (ceph): `internal/data/rados/healthprobe.go` (with `//go:build ceph`). librados.GetPoolStat is the per-pool call; librados.MonCommand("status") is the cluster-wide call. Both defined in goceph; check existing `internal/data/rados/observer.go` for the librados import shape.
- S3-over-S3 impl in `internal/data/s3/healthprobe.go` — HEAD on the configured backend bucket via the AWS SDK already wired into the gateway.
- Memory impl returns process-RSS proxy (use `runtime.ReadMemStats` `Sys` value).

### bucketstats sampler extension (US-003)
- `internal/bucketstats/sampler.go` is the existing per-bucket / per-shard sampler from Phase 3 US-012. Today it emits `strata_bucket_objects` + `strata_bucket_bytes` + per-shard variants. Add the per-class slice as another aggregation key — wire prom metric registrations in the same init.
- Cardinality cap pattern: `STRATA_BUCKETSTATS_TOPN` already enforced; new (bucket, class) tuples added to the same top-N cap via the existing `truncatedTopN` helper.
- Per-backend aggregation lockstep:
  - Cassandra: `SELECT bucket_id, storage_class, count(*), sum(size) FROM objects GROUP BY ALL` is impossible (CQL has no `GROUP BY` on non-PK). Pattern from US-012: range-scan all rows for bounded buckets then in-process aggregate. Same approach here.
  - Memory: in-process map `map[uuid.UUID]map[string]struct{Bytes, Count uint64}`.
  - TiKV: range-scan via `meta.RangeScanStore` interface, group by `o.StorageClass` in the worker.

### Web UI conventions (US-004 / US-005 — frontend)
- React+TS console under `web/src/`. Vite-built into `web/dist/`, served via `go:embed` from `internal/adminapi/console.go`. After UI changes the embedded bundle MUST be rebuilt: `cd web && pnpm install && pnpm build`. Commit `web/dist/` (gitignored at the directory level — use `git add -f web/dist/index.html web/dist/assets/*` per prior cycle pattern).
- Sidebar nav definitions in `web/src/components/layout/SidebarNav.tsx`. Lazy-loaded routes registered in `web/src/App.tsx`.
- TanStack Query client + queryKeys in `web/src/lib/query.ts`. Query polling: cluster status every 5s, metrics every 30s, audit-log every 5s. Storage page = 30s. Hero card = 60s. Banner = 30s.
- Recharts is already in bundle (used by Metrics + Hot-buckets). BarChart import works without bundle bloat. Custom canvas heatmap pattern from Phase 3 US-009 if recharts limits hit.
- shadcn UI primitives at `web/src/components/ui/*`. Tabs at `web/src/components/ui/tabs.tsx`. Card / Table / Badge already in.
- Empty / loading / error state pattern: see `web/src/pages/Consumers.tsx` (rewritten in s3-compat-90) for a clean reference.
- AppShell sits at `web/src/components/layout/AppShell.tsx`. Outermost JSX is `<div className="flex min-h-screen ...">`. Banner sibling goes BEFORE that — wrap both in a fragment / outer div if needed. Don't break the `Outlet` component which holds the page route.
- Bundle budget: ≤500 KiB gzipped initial. The Recharts bar adds ~10-15 KiB; lazy-load `Storage.tsx` route to keep initial bundle below cap.

### Playwright e2e (US-006)
- Specs live under `web/e2e/*.spec.ts`. CI job `e2e-ui` in `.github/workflows/ci.yml`. New spec must be added to the e2e job's invocation list.
- Test-only knob pattern (STRATA_STORAGE_HEALTH_OVERRIDE): see `STRATA_AUDIT_TAIL_OVERRIDE` from Phase 3 — env-driven response override + clearly documented as test-only in the handler unit-test file.
- Login fixture: `web/e2e/fixtures/login.ts`. Reuse for the new spec.

### CI gotchas
- `go build ./...` is the canonical typecheck for Go side. Run before commit. With ceph build tag too if you touched RADOS files: `go build -tags ceph ./...`.
- `cd web && pnpm typecheck` for TS side. `pnpm test` if vitest specs added.
- Race tests need `-tags integration` only for the Cassandra suite. Memory + TiKV unit specs run vanilla.

### ROADMAP close-flip discipline
- The close-flip commit and the work commit are the SAME commit. US-006 is the close-flip — the diff includes ROADMAP edit + e2e + docs + markdown PRD removal in one atomic commit. Per CLAUDE.md `Roadmap maintenance` rule.
- New gaps surfaced during this cycle (e.g., "PD HTTP API discovery flap on cross-PD failover") get fresh entries under `## Web UI` of `## Scalability & performance` per CLAUDE.md `Discovering a new gap` rule. Add in the same commit that surfaces them.

---

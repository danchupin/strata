# Ralph Progress Log
Started: Tue May 19 19:00:00 MSK 2026
Cycle: ralph/polish-dx

## Codebase Patterns
- 5-story polish cycle bundling 4 small fixes (no new feature surface, no UI, no admin endpoint) + 1 smoke story. Scope deliberately narrow: one stuck-worker latent bug, one flaky test, one worker-retry gap, one dependency-graph cleanup. Each story completable in one Ralph iteration.
- **Story ordering rationale**: US-001 (module tags cleanup) is priority 1 because it exports the shared `rados.ErrNotFound` sentinel in both build-tag variants — US-002 (gc.Worker ENOENT-ack) consumes that sentinel from the stub mirror so worker code compiles without `-tags ceph`. Reversing the order means US-002 has to either define a private sentinel + later refactor, or temporarily import go-ceph in default-tag code (which US-001 then has to undo).
- **Stub-mirror pattern (US-001)**: every exported symbol in `internal/data/rados/` (Backend, Config, helper structs, sentinel errors including ErrNotFound) gets a `//go:build !ceph` mirror returning `data.ErrRadosNotCompiled` (sentinel `errors.New("rados backend not compiled — rebuild with -tags ceph")`). Real implementations live under `//go:build ceph`. `internal/serverapp/serverapp.go` backend selector keeps both branches compilable; `case "rados":` returns the typed sentinel under default-tag.
- **gc.Worker ack discipline (US-002)**: the bug is in `internal/gc/worker.go:123-126` — on Data.Delete error the goroutine returns nil from the inner func WITHOUT calling Meta.AckGCEntry, so the outer `for {}` loop re-issues ListGCEntries and gets the same batch back forever. Fix: classify rados.ErrNotFound as terminal (chunk already gone — operator-visible state matches reality) and ack it, bump `strata_gc_terminal_ack_total{reason="enoent"}`. Non-ENOENT errors keep loop-fail-with-warn (operator should investigate pool-not-found, mis-routed-cluster, transport errors). Scoping rejected env knob `STRATA_GC_ACK_ON_ERROR` — keep matrix narrow.
- **Deterministic UUID seed (US-003)**: `internal/lifecycle/distribute_test.go:111` flakes ~30% under `-count=20` because `uuid.New()` × 9 + FNV-32a-mod-3 legitimately produces 6/3/0 splits. Fix: `uuid.NewSHA1(uuid.Nil, []byte(fmt.Sprintf("bucket-%d", i)))` for deterministic generation, hard-code the resulting `[3]int{a,b,c}` triple. The `totalDeletes == buckets` invariant assertion (the real correctness signal) stays.
- **Lifecycle retry classifier (US-004)**: lives in `internal/lifecycle/retry.go` (or folded into worker.go if < 30 LoC). Transient predicate: `context.DeadlineExceeded`, `syscall.ECONNRESET`, `syscall.ETIMEDOUT`, any `interface{ Temporary() bool }` returning true. Terminal: `meta.ErrNotFound`, `data.ErrNoSuchKey`, `data.ErrNoSuchBucket`, anything not matching transient. 3 attempts total, backoff 1s→3s→10s, sleep BEFORE attempt 2 and 3, ctx-aware (race `time.After` against `ctx.Done()`). Injectable `sleep func(time.Duration)` for unit testability. Counter `strata_lifecycle_retry_total{outcome=ok|exhausted|terminal}`.
- **ROADMAP discipline (US-005)**: 4 entries flip in one commit — 3 pre-existing (gc.Worker latent bug, TestThreeReplicaDistribution latent bug, Module tags cleanup P3) + 1 new-and-closed for `Lifecycle worker no-retry on transient failures` per CLAUDE.md "Discovering a new gap" rule (US-004 is not pre-roadmapped). Each entry carries `(commit <pending>)` placeholder; SHA backfill lands on main as fast-follow commit (mirrors prior cycle's f6a5476-backfill pattern).
- **No UI in this cycle** — none of the 4 fixes surfaces in the operator console. Per memory rule `Cycle end-to-end`, normally e2e smoke via UI walkthrough is required. This cycle's final-story validation is `go test -race -count=5 -run TestThreeReplicaDistribution` (proves US-003 stable) + clean `go build ./...` log grep for go-ceph (proves US-001 hermetic) + ROADMAP close-flip + PRD removal — which substitutes the UI-smoke discipline because the value is backend-only.


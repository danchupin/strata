# Ralph Progress Log
Started: Fri May  8 00:00:00 MSK 2026
Cycle: ralph/gc-lifecycle-scale-phase-2

## Codebase Patterns
- `meta.Store` interface lives in `internal/meta/store.go`; both `internal/meta/memory` AND `internal/meta/cassandra` (and `internal/meta/tikv`) implement it in lockstep — adding a method means updating all three.
- Contract tests in `internal/meta/storetest/contract.go::Run(t, factory)` are the shared suite — add a `case<Name>` here so memory + cassandra + tikv all run the same assertions.
- Cassandra schema migrations are additive only. `internal/meta/cassandra/schema.go` has `tableDDL` (idempotent `CREATE TABLE IF NOT EXISTS`) and `alterStatements` (idempotent `ALTER TABLE ADD column`). Never write a destructive migration. New table = `gc_entries_v2`-style; never repartition `gc_entries` in place.
- TiKV variable-length string segments use FoundationDB-style byte-stuffing (`0x00 → 0x00 0xFF`, terminator `0x00 0x00`). Fixed-width integer fields use big-endian (e.g. day-epoch, partNumber, shardID — for forward range scan ascending order). See `internal/meta/tikv/keys.md`.
- Workers register via `workers.Register(workers.Worker{Name, Build})` in `cmd/strata/workers/<name>.go::init()`. Build receives `workers.Dependencies`, returns `workers.Runner`. Supervisor handles leader-election + panic restart.
- `STRATA_GC_*` envs read at Build time (no per-worker flags); other workers follow same env-only convention.
- Phase 1 baseline (commit 6561845): gc 11108→100275 chunks/s c=1→c=256 knee at c=64; lifecycle 485→9150 obj/s c=1→c=256. Recommended defaults `STRATA_GC_CONCURRENCY=64` / `STRATA_LIFECYCLE_CONCURRENCY=64`.
- Phase 1 bench harness lives as `strata-admin bench-gc` / `strata-admin bench-lifecycle` subcommands. Phase 2 reuses the same harness; only the deployment shape changes (3-replica stack).
- `bench-gc` auto-isolates with a `bench-<uuid>` region label so it can run alongside the lab gateway's gc worker; `bench-lifecycle` uses `region=default` by design (lifecycle walks all buckets regardless of region label) — STOP the lab `STRATA_WORKERS=gc,lifecycle` gateway before bench-lifecycle to avoid race on the bench bucket's expiration.
- Membership coordination piggybacks on gc lease holdings: a replica's `myReplicaID` = smallest `gc-leader-<i>` lease index it currently holds; `replicaCount = STRATA_GC_SHARDS`. No separate `replica_set` lease pool needed.
- 1024-wide logical shard fan-out at write time (`shard_id = fnv32a(oid) % 1024`) decouples on-disk partitioning from operator's runtime shard count. Operators can change `STRATA_GC_SHARDS` (1..1024) without re-keying the gc queue.
- Dual-write window pattern (Cassandra + TiKV): write both legacy + new key/row when `STRATA_GC_DUAL_WRITE=on` (default during this cycle), readers prefer new with legacy fallback. Operator-driven cutover via `STRATA_GC_DUAL_WRITE=off`.
---

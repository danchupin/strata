# Ralph Progress Log
Started: Wed May 13 14:00:00 MSK 2026
Cycle: ralph/drain-transparency

## Codebase Patterns
- Prior cycles closed the drain primitive surface: `ralph/placement-rebalance` shipped drain sentinel + 30s DrainCache + opt-in STRATA_DRAIN_STRICT env. `ralph/drain-lifecycle` shipped drain progress endpoint + UI progress bar + completion detection + bucket impact preview drawer. This cycle is the OPERATOR-UX correction layer: removes env toggle, splits drain into readonly vs evacuate modes, adds pre-drain impact analysis + bulk policy fix + categorized progress. Read `feedback_cycle_end_to_end.md` and `tasks/prd-drain-transparency.md` walkthrough section before starting any story — every story serves at least one walkthrough step.
- **State machine extension**: 4 states `{live, draining_readonly, evacuating, removed}`. Migration on read in `meta.Store.GetClusterState`: legacy `state="draining"` rows transparently convert to `state="evacuating" mode="evacuate"` AND write back so the legacy shape doesn't reappear on next read. Implement in memory + cassandra + tikv backends in lockstep via the existing `setBucketBlob/getBucketBlob`-style helpers.
- **Mode field on POST /drain**: body is required `{mode: "readonly"|"evacuate"}`. No default. Existing UI clients (placement-ui) that POST without body will get 400 — must update in US-004 modal rewrite.
- **Worker scan categorization**: ProgressSnapshot gets three fields (migratable / stuck_single_policy / stuck_no_policy) instead of flat Chunks. Scan triggers ONLY when state=evacuating; readonly state skips the scan to save cost. The classifier looks at `bucket.Placement` + `PickClusterExcluding` verdict: empty policy → stuck_no_policy; verdict "" → stuck_single_policy; verdict non-empty → migratable.
- **`/drain-impact` endpoint** is the pre-evacuate preview. Callable only when state ∈ {live, draining_readonly}. For state=live triggers synchronous one-off scan (worker doesn't normally scan live clusters); 5-min cache. For draining_readonly reads progressCache (worker scans periodically while readonly even though it doesn't migrate — actually wait, US-002 says readonly skips scan; so draining_readonly impact must also synchronous-scan with cache, or run worker scan only when impact requested. Pick: also synchronous + cache, same path as live).
- **Suggested policies** per bucket (US-003): two flavor each — (a) uniform live `{<every-live>:1}` plus draining-keys forced to 0 if present; (b) per-live-cluster single-target (one entry per live cluster). The UI BulkPlacementFixDialog (US-005) renders them in a Select.
- **In-flight multipart graceful contract**: multipart handle widening done in placement-rebalance (`cluster\x00bucket\x00key\x00uploadID`). UploadPart parses cluster from handle and routes directly to it — bypasses placement picker. Drain doesn't affect open multipart sessions. Critical: when refactoring strict refusal in US-007, verify UploadPart path does NOT consult drain map — it must read the cluster from handle and proceed. Integration test in US-007 enforces this.
- **Counter rename**: `strata_putchunks_refused_total{reason="drain_strict",cluster}` → `reason="drain_refused"`. Hard switch. Document the rename in docs/best-practices/placement-rebalance.md "Drain lifecycle" section as a breaking change for Prometheus dashboards.
- **UI removal**: strict chip on cluster cards (placement-ui) must be removed in US-007. The `drain_strict: bool` field on `GET /admin/v1/clusters` response also removed. UI code that consumes this field is in ClustersSubsection.tsx — search + delete.
- **Mode picker in ConfirmDrainModal (US-004)**: radio buttons, NOT dropdown. Both modes visible side-by-side with descriptions. evacuate mode flip triggers `/drain-impact` fetch via TanStack Query; readonly mode skips. Submit button text + enabled state derived from impact data.
- **Upgrade path (draining_readonly → evacuating)**: modal title swaps to "Upgrade to evacuate" when the current state is already draining_readonly; readonly radio hidden (only evacuate visible). Submit re-confirms via typed cluster id input. POST /drain body is `{mode:"evacuate"}` — same endpoint, transition enforced server-side.
- **BulkPlacementFixDialog (US-005)** opens FROM ConfirmDrainModal (overlay-on-overlay is fine — existing UI pattern). Bulk apply iterates `PUT /admin/v1/buckets/<name>/placement` per selected bucket. Failures collected and toasted at end; doesn't block remaining buckets.
- **Smoke script convention** (US-008): `scripts/smoke-drain-transparency.sh` mirrors `scripts/smoke-drain-lifecycle.sh` shape. Three scenarios; assertions via curl + jq + docker exec rados ls. Per-scenario log lines via `echo "==> Scenario A: ..."`. Exit non-zero on any failure.
- **3-scenario Playwright e2e** in `web/e2e/drain-transparency.spec.ts` mirrors smoke shape but for UI. Uses existing in-memory test rig + spoof-friendly admin endpoints via page.route() (precedent: placement.spec.ts from placement-ui cycle).
- **ROADMAP entries** (US-008): close-flip new entry "Drain transparency + drain/evacuate split (always-strict)" added in prep commit. Plus retro-addendum paragraph appended to the closed P3 "Drain strict mode for PUT routing fallback" — preserves the audit trail of the wrong-shape decision and its remediation. Format: `~~**P3 — ...**~~ — **Done.** ... (commit \`<pending>\`)` for the new entry; the addendum on the prior closed entry uses `*Follow-up:* STRATA_DRAIN_STRICT env removed in ralph/drain-transparency (commit \`<pending>\`) — drain is now unconditionally strict; strict-chip + drain_strict admin field also removed.` appended as a new paragraph.
- **`tasks/prd-drain-transparency.md` REMOVED** in US-008 per CLAUDE.md PRD lifecycle rule (Ralph snapshot under `scripts/ralph/archive/` is canonical).
- **Lab stand**: `--profile multi-cluster` brings up 2 RADOS clusters (default + cephb) + strata-multi on :9998. Existing demo buckets (demo-split / demo-cephb / demo-default) live. Integration tests in this cycle SHOULD reuse the lab profile via `docker compose --profile multi-cluster` (existing precedent in smoke-drain-lifecycle.sh).
- **Verification per UI story**: `pnpm run typecheck` + `pnpm run build`; verify in browser via dev-browser skill on the lab. Final story (US-008) gated on `make smoke-drain-transparency` passing AGAINST the running lab (not just CI green).


## 2026-05-13 17:00 MSK - US-001
- Extended cluster_state meta wire shape with `mode` field. Added 4-state enum constants (live / draining_readonly / evacuating / removed) + mode enum (readonly / evacuate) + ErrInvalidClusterMode sentinel. Renamed validator to meta.ValidateClusterStateMode and exported meta.ClusterStateRow + meta.NormalizeClusterStateRow + meta.IsDrainingForWrite helpers.
- meta.Store interface methods refactored: SetClusterState(ctx, id, state, mode) / GetClusterState → ClusterStateRow / ListClusterStates → map[string]ClusterStateRow. Memory, Cassandra (new `mode text` column via ALTER TABLE) and TiKV (`state\x00mode` value encoding) implement in lockstep with read-side legacy migration + best-effort write-back.
- Adminapi: POST /admin/v1/clusters/{id}/drain now requires JSON body `{mode}` (400 if missing/unknown). State machine enforced server-side — illegal transitions return 409 InvalidTransition with current_state + requested_mode. Undrain handler refuses from live/removed (409). GET /clusters and /drain-progress both surface state + mode. OpenAPI spec, audit override label, drain-strict TS type and metrics help-text all updated.
- New contract test `caseClusterStateModes` (memory + cassandra + tikv via storetest.Run) covers every legal + illegal (state, mode) pair plus the legacy `draining` → `evacuating/evacuate` migration on both Get and List paths.
- UI client widened: `ClusterState` / `ClusterMode` types, `isDrainingState()` collapses both draining states, `drainCluster(id, mode='evacuate')` posts body. Existing components still treat `state === 'draining'` until US-006 redesigns them — typecheck + build green.
- Files changed: internal/meta/store.go, internal/meta/memory/store.go, internal/meta/cassandra/{store,schema}.go, internal/meta/tikv/blobs.go, internal/meta/storetest/contract.go, internal/adminapi/{clusters_drain,clusters_drain_progress,clusters_drain_test,clusters_drain_progress_test,openapi.yaml}.{go,yaml}, internal/data/placement/{draincache,draincache_test}.go, internal/data/context.go, internal/rebalance/{worker,worker_test,progress_test}.go, internal/metrics/metrics.go, web/src/api/clusters.ts.
- **Learnings for future iterations:**
  - The contract test must be able to plant a legacy `(state="draining", mode="")` row to exercise migration; the cleanest path is to permit that combo in ValidateClusterStateMode (commented as legacy parity). Adminapi handler still validates the operator-supplied mode strictly so legacy combos never leak through the wire.
  - DrainCache.DrainLoader signature changed from `map[string]string` to `map[string]meta.ClusterStateRow`; the loader now collapses (draining_readonly | evacuating | legacy draining) → blocked-for-write via meta.IsDrainingForWrite. Anywhere that polls cluster state for routing must use this helper, not raw string compare.
  - `putAdmin(t, s, owner, method, path, body any)` JSON-encodes the body via encoder — pass the struct directly, NOT a *bytes.Buffer. drainBody(mode) returns the struct.
  - For tests that need a "draining-for-write" cluster but DON'T care which mode (the rebalance worker scans both), pick `evacuating + evacuate` — it's the post-US-001 equivalent of the old `draining` constant and keeps existing assertions semantically correct.
  - UI: `drainCluster(id)` now requires a mode argument. Defaulted to `evacuate` so existing call sites keep working (matches the legacy single-state semantic), but US-004's ConfirmDrainModal rewrite must override to the operator-chosen mode.
  - Racetest is flaky (`internal/racetest::TestRunCleanRunNoInconsistencies`) — produces ~20 read_after_write inconsistencies on `rc-bkt-0/k-1` on some runs but not others. Re-run before assuming a regression. Unrelated to drain-transparency.
---

## 2026-05-13 22:30 MSK - US-003
- New admin endpoint GET /admin/v1/clusters/{id}/drain-impact (handler in internal/adminapi/clusters_drain_impact.go). Returns categorized chunk counters (migratable / stuck_single_policy / stuck_no_policy / total) + per-bucket breakdown with suggested remediation policies + pagination cursor + last_scan_at.
- State gating: live + draining_readonly → 200; evacuating + removed → 409 InvalidTransition pointing the operator at /drain-progress.
- Synchronous scan path: ListBuckets → per-bucket GetBucketPlacement + ListObjects, counting only chunks (manifest.Chunks + manifest.BackendRef) whose Cluster equals the path id. Buckets with zero chunks on the target are excluded from the by_bucket roll-up. classifyBucket reused via rebalance.ClassifyBucket (exported in this commit) so the verdict matches the live worker tick. excludeForTargets folds every draining-for-write cluster plus the target id, so a policy spread across two draining clusters surfaces correctly as stuck_single_policy.
- Per-cluster 5-min TTL cache (drainImpactCache, drainImpactCacheTTL) lazy-init on Server identical to hotBuckets pattern. Cache hit returns the stored drainImpactScan and the response shaper paginates without re-scanning.
- suggestedPoliciesForBucket: one uniform-live entry ({each-live:1, plus draining keys from current policy forced to 0 when current!=nil}) + one per-live-cluster single-target entry. stuck_no_policy buckets get 'Set initial policy:' labels; everything else gets 'Add all live clusters (uniform)' / 'Replace draining with <id>'. No-live-clusters edge case returns nil so UI hides the Fix CTA.
- OpenAPI spec adds ClusterDrainImpact + BucketImpactEntry + SuggestedPolicy schemas + the /clusters/{id}/drain-impact path with the 409 / 400 / 401 responses.
- Tests (internal/adminapi/clusters_drain_impact_test.go): mixed-category sort (stuck_single_policy → stuck_no_policy → migratable, b-unaffected excluded), evacuating→409, draining_readonly→200, pagination + next_offset over 5 stuck buckets, cache stability (post-first-request bucket invisible until TTL), unknown cluster→400, suggestedPoliciesForBucket no-live + draining-key neutralization.
- Files changed: internal/rebalance/{worker,progress_test}.go (Export ClassifyBucket), internal/adminapi/{server,clusters_drain_impact,clusters_drain_impact_test,openapi}.go|.yaml, scripts/ralph/prd.json.
- **Learnings for future iterations:**
  - `rebalance.ClassifyBucket(policy, excludeForTargets)` is the canonical bucket-category classifier — reuse it from any caller that needs the same verdict. Don't reimplement; the rules diverge silently otherwise.
  - `parseRange(s, def, lo, hi)` lives in buckets.go and is the standard limit/offset helper across adminapi handlers — reuse it instead of writing fresh strconv parsing.
  - Lazy-init TTL caches on Server follow the hotBuckets pattern: per-cache `Mu sync.Mutex` + `*CacheVal` pointer on Server, accessor method `s.cacheName()` does the lock+lazy-init. Tests can override `now` on the cache struct directly via `s.drainImpact().now = func() time.Time { ... }`.
  - meta backend doesn't enforce that PutObject's BucketID matches a registered bucket, but `ListObjects` does — always CreateBucket first then PutObject for tests that walk the listing.
  - Per-bucket scan uses `ListObjects` (latest-version page) — versioned-object history is intentionally not walked by the impact preview (matches the rebalance worker's scan shape; latest is the only chunk set the drain cares about).
  - 409 InvalidTransition response shape (`invalidTransitionResponse`) was added in US-001 for POST /drain; reuse for any endpoint that wants to surface the 4-state machine refusal with current_state + requested_mode fields.
---

## 2026-05-14 09:00 MSK - US-004
- ConfirmDrainModal rewritten as a mode picker (radio, readonly default selected). Readonly path: stop-writes flip; submit text 'Drain (stop-writes)'; no impact fetch; typed-confirm arms submit. Evacuate path: useQuery(clusterDrainImpact(id)) fetches /drain-impact (silent meta, 30s staleTime); three counter cards render (Migratable / Stuck single-policy / Stuck no-policy); stuck>0 renders amber panel + 'Fix N buckets' CTA that fires onOpenBulkFix(stuckBuckets) so US-005's parent can render <BulkPlacementFixDialog>; submit DISABLED while stuck>0; submit text switches between 'Drain blocked — fix N stuck buckets' / 'Drain (X chunks will migrate)' / 'Loading impact…'.
- Upgrade path: currentState='draining_readonly' → isUpgrade hides the readonly radio entirely, forces mode='evacuate', flips title to 'Upgrade to evacuate' and rewrites the description. Cancel closes without side effects (the in-flight submit short-circuits via handleClose's early return).
- API layer: added SuggestedPolicy / BucketImpactEntry / ClusterDrainImpactResponse types + fetchClusterDrainImpact(id, limit?, offset?) to web/src/api/clusters.ts; re-exported from web/src/api/client.ts. New queryKey clusterDrainImpact(id). Re-exported isDrainingState / ClusterState / ClusterMode from client.ts since US-006 will consume them downstream.
- ClustersSubsection passes currentState={cluster.state} to the modal (replacing the previous onShowReferences prop — BucketReferencesDrawer keeps its own 'Show affected buckets' card button untouched).
- web/e2e/drain-lifecycle.spec.ts (prior-cycle spec) patched to drive the new mode picker via testid cd-mode-readonly / cd-submit instead of the removed "buckets reference" info-line text + role-based 'Drain' submit lookup.
- Files changed: web/src/components/storage/ConfirmDrainModal.tsx, web/src/components/storage/ClustersSubsection.tsx, web/src/api/clusters.ts, web/src/api/client.ts, web/src/lib/query.ts, web/e2e/drain-lifecycle.spec.ts, scripts/ralph/prd.json.
- Quality: pnpm run typecheck (clean) + pnpm run build (clean — Storage chunk 9.20 KiB gzipped, well under the 5 KiB AC delta budget for this single component) + go vet ./... (no Go changes; runs clean).
- **Learnings for future iterations:**
  - The modal's parent (ClustersSubsection) owns the cluster row — pass `currentState={cluster.state}` so the modal can branch into upgrade-mode shape without an extra round-trip. US-006's DrainProgressBar upgrade button must use the same prop (open the modal with currentState='draining_readonly').
  - Bulk fix is parent-driven: ConfirmDrainModal accepts `onOpenBulkFix(stuck: BucketImpactEntry[])` and surfaces the stuck rows from its cached impact response. US-005 must render <BulkPlacementFixDialog> from ClustersSubsection (or a wrapper component) and wire ConfirmDrainModal.onOpenBulkFix → setBulkOpen + setStuckBuckets, then on bulk-fix completion `queryClient.invalidateQueries({queryKey: queryKeys.clusterDrainImpact(id)})` so counts refresh live.
  - The /drain-impact endpoint returns 409 for state ∈ {evacuating, removed} — readonly path skips the fetch entirely (enabled=false) so the modal works without a 409 from `live` (which 200s) or `draining_readonly` (which 200s with cached scan). Don't enable the fetch unconditionally — readonly state would 200 fine, but the IO is wasted since the readonly path doesn't migrate.
  - The 'Drain' submit text is no longer a stable selector (changes per mode + stuck count). Tests should target `data-testid="cd-submit"` instead of `getByRole('button', { name: 'Drain' })`.
  - lucide-react icons used: AlertCircle (error), AlertTriangle (warning), Ban (stuck blocker), Loader2 (spinner), Wrench (fix CTA). Match the existing palette in storage/* components.
---

## 2026-05-13 20:25 MSK - US-002
- ProgressSnapshot widened: `MigratableChunks`, `StuckSinglePolicyChunks`, `StuckNoPolicyChunks`, `Bytes`, `ByBucket map[string]BucketScanCategory` (per-bucket {Category, ChunkCount, BytesUsed}). Flat `Chunks` field removed → derived `Chunks()` method on both ProgressSnapshot and the new ScanResult type. CompletionEvent BytesMoved unchanged (BaseBytes captured).
- CommitScan signature changed: `(draining []string, scans map[string]ScanResult, now time.Time)`. Snapshot/Reset/nil-receiver semantics unchanged.
- Worker now tracks two cluster sets via `loadClusterDrainSets`: `excludeForTargets` (readonly+evacuating+legacy-draining — picker/safety-rail) and `scanFor` (evacuating only — migration scan focus). Readonly clusters get stop-write semantics but no progress accumulator entry. `progressAcc` tracks per-(cluster, bucket) categorized counts; `classifyBucket(policy, excludeForTargets)` returns the constant per-bucket category once.
- `/admin/v1/clusters/{id}/drain-progress` adds three new int64 fields: `migratable_chunks`, `stuck_single_policy_chunks`, `stuck_no_policy_chunks`. `chunks_on_cluster` becomes the sum (kept for dashboard compat). ETA uses `migratable_chunks` only — stuck chunks never converge without operator action. `deregister_ready = total == 0`. Readonly state returns "stop-writes mode — migration scan skipped; undrain or upgrade to evacuate" warning with null counts.
- OpenAPI spec updated in lockstep (ClusterDrainProgress schema gets three new properties + the readonly-skip warning narrative).
- Files changed: internal/rebalance/{progress,worker}.go, internal/rebalance/{progress_test,worker_test}.go (callers + new categorization tests), internal/adminapi/{clusters_drain_progress,clusters_drain_progress_test,openapi}.{go,yaml,go}.
- **Learnings for future iterations:**
  - `ProgressSnapshot.Chunks` is no longer a field — it's a `Chunks()` method (and same on `ScanResult`). Code referencing the old field will fail to compile. The signature change ripples through every CommitScan call site — search globally before refactoring further.
  - The picker-exclusion set and the scan-focus set DIVERGED in this story. `loadDrainingClusters` is gone; use `loadClusterDrainSets` to get both. Mixing them silently re-enables move-into-readonly or scans clusters that shouldn't be scanned.
  - The per-bucket category is constant per bucket because `PickClusterExcluding` returns "" iff `total weight after exclusion == 0` — that's a function of (policy, excluded), not (key, idx). `classifyBucket` computes once per bucket; the per-chunk hot path just records the constant.
  - US-003 (drain-impact) will consume `snap.ByBucket` directly off the tracker. No extra meta IO needed when state=draining_readonly|evacuating; live state still needs the synchronous one-off scan since the worker never scans live clusters.
  - Test pattern for new ScanResult: `migratableScan(n)` helper builds the common `{MigratableChunks: n, Bytes: n*1024}` shape. Tests that exercise stuck categories build the ScanResult literal inline.
---

## 2026-05-14 12:00 MSK - US-005
- Built web/src/components/storage/BulkPlacementFixDialog.tsx — multi-select bucket table over ConfirmDrainModal's cached /drain-impact stuck-bucket rows. Per-row: checkbox (default checked), bucket name + chunk count, current Placement summary, category badge, per-bucket suggested-policy <Select>. Toolbar: Select all / Deselect all, "X of Y selected" counter, "Apply uniform to all selected" checkbox + dialog-level <Select> that lists the INTERSECTION of suggested labels across every selected bucket. Apply button iterates sequentially via setBucketPlacement, surfaces progress "Applying X / Y…", collects failures into one toast at end (success-all / partial / zero), and invalidates clusterDrainImpact + per-bucket placement queries so the parent ConfirmDrainModal refetches live impact counts.
- Wired from ClustersSubsection.ClusterCard: new local `bulkFixOpen` + `bulkFixStuck` state; ConfirmDrainModal.onOpenBulkFix sets stuck + opens dialog. Overlay-on-overlay (existing pattern).
- Pure helpers `uniformOptions(buckets, selected)` and `resolvePolicy(bucket, perBucketIdx, applyUniform, uniformLabel)` exported and covered by 9 cases in web/src/components/storage/__tests__/bulk-placement-fix-dialog.test.mjs — empty selection, single-bucket full set, intersection drops non-shared labels, stuck/no-policy vs stuck/single-policy disjoint, per-bucket idx path, out-of-range fallback, uniform label match across buckets with diverging payloads, uniform-with-unknown-label fallback, empty suggestions → null. web/package.json::test:unit extended to run both fixture files.
- Files changed: web/src/components/storage/BulkPlacementFixDialog.tsx (new), web/src/components/storage/ClustersSubsection.tsx (import + wiring), web/src/components/storage/__tests__/bulk-placement-fix-dialog.test.mjs (new), web/package.json, scripts/ralph/prd.json.
- Quality: pnpm run typecheck (clean) + pnpm run build (Storage chunk 11.25 KiB gzipped — +2.05 KiB delta vs US-004 baseline 9.20 KiB, under the 4 KiB AC budget) + go vet ./... (clean) + node --test bulk-placement-fix-dialog.test.mjs (9/9 pass).
- **Learnings for future iterations:**
  - The "Apply uniform to all selected" toggle picks ONE label, but each bucket still receives its OWN SuggestedPolicy.policy payload — the draining-cluster keys in `policy` differ per bucket (current_policy varies). Don't refactor the dialog to send a single shared policy across all buckets; that breaks the "uniform forces draining keys to 0" guarantee the server-side suggestedPoliciesForBucket promises.
  - `uniformOptions` returns the label INTERSECTION across selected buckets, which means stuck_no_policy + stuck_single_policy mixed selection always yields []. The handler stamps "Set initial policy:" on stuck_no_policy and "Add all live clusters (uniform)" / "Replace draining with …" on stuck_single_policy — the label prefixes are intentionally disjoint so the uniform toggle disables cleanly with a red warning. The operator's recourse is to deselect one category or use per-bucket picks.
  - Followed the prior `.test.mjs` precedent (placement-drain-banner-dismissal.test.mjs): re-implement pure helpers locally as a fixture and rely on the Playwright e2e (US-008) to catch drift. Worked here because the helpers are < 30 LoC and have no React dependencies.
  - The bulk dialog invalidates `queryKeys.clusterDrainImpact(clusterID)` rather than calling refetch directly — the parent ConfirmDrainModal's useQuery picks up the invalidation, the impact response is restamped silently (meta.silent), and the categorized counts + submit button text update live. Don't manually call onApplied or refetch from the child; invalidation is the canonical TanStack Query flow.
  - The Select component requires `value` to be a string — convert bucket-index numbers via `String(idx)` and parse back via `Number(v)` in onValueChange. Mixing a label-string into a stringified index is fine as long as the SelectItem values match exactly.
  - Per-bucket Select must be DISABLED when applyUniform is on (operator picked the uniform label at the dialog level — overrides individual picks). The "→ <effective policy summary>" line under each Select renders the effective policy regardless of mode so operators see what'll actually be PUT.
---

## 2026-05-14 15:30 MSK - US-006
- DrainProgressBar rewritten with mode-aware render: readonly = orange stop-writes chip + Upgrade-to-evacuate + Undrain buttons (callbacks from parent); evacuate = red 'Evacuating' label + progress bar + three categorized counter cells (Migrating green, Stuck single-policy amber, Stuck no-policy amber). Stuck cells are clickable, open <StuckBucketsDrawer>; ETA renders only when migratable>0; deregister-ready green chip renders under the Evacuating label when total chunks=0.
- New web/src/components/storage/StuckBucketsDrawer.tsx mirrors BucketReferencesDrawer shape (Sheet/right). Each stuck-bucket row renders name + chunks + bytes + an 'Edit policy' link that opens /buckets/{name}?tab=placement in a new tab (target=_blank) so the operator never loses drain modal context.
- ClustersSubsection: clusterStateClass extended with draining_readonly→orange and evacuating→red badge colors so the card state badge mirrors the mode; clusterStateLabel renders 'stop-writes' / 'evacuating' for the new states; isDrainingState used to collapse both draining states; the bottom Undrain button is hidden in readonly mode (the bar owns those buttons), the Drain destructive button hides as before. The 'Rebalance worker is migrating…' info-line only renders for evacuate (readonly never migrates).
- Server: /admin/v1/clusters/{id}/drain-progress widened with `by_bucket` — array of {name, category, chunk_count, bytes_used} read straight off the in-process ProgressTracker snapshot.ByBucket, sorted stuck_single_policy → stuck_no_policy → migratable then chunk_count desc then name asc. categoryRank + sortedByBucket helpers added in adminapi/clusters_drain_progress.go. OpenAPI spec adds BucketDrainProgressEntry schema + the by_bucket field on ClusterDrainProgress.
- API layer: ClusterDrainProgress TS interface widened (migratable_chunks / stuck_single_policy_chunks / stuck_no_policy_chunks / by_bucket); BucketDrainCategory + BucketDrainProgressEntry types added, re-exported from client.ts.
- Files changed: internal/adminapi/clusters_drain_progress.go, internal/adminapi/clusters_drain_progress_test.go, internal/adminapi/openapi.yaml, web/src/api/clusters.ts, web/src/api/client.ts, web/src/components/storage/DrainProgressBar.tsx, web/src/components/storage/StuckBucketsDrawer.tsx (new), web/src/components/storage/ClustersSubsection.tsx, scripts/ralph/prd.json.
- Quality: pnpm run typecheck (clean) + pnpm run build (Storage chunk 12.31 KiB gzipped, +1.06 KiB vs US-005 baseline 11.25 KiB — well under the 4 KiB AC budget) + go vet ./... (clean) + go test ./... (full unit suite green; TestClusterDrainProgress_ByBucketSorted added covering the new sort).
- **Learnings for future iterations:**
  - The /drain-progress endpoint is the source of truth for stuck-bucket drill-down during evacuation. /drain-impact 409s once state=evacuating (US-003 contract), so the UI MUST pull the per-bucket breakdown from drain-progress — not drain-impact — while the drain is running.
  - The progress bar percent now uses `migratable` as the numerator delta against `base_chunks_at_start`. The pre-US-002 code used the flat `chunks_on_cluster` delta which would render the bar at <100% even when only stuck rows remain. Stuck chunks should LOCK the bar's progress until the operator edits policy — using `migratable` makes that visually obvious.
  - DrainProgressBar's readonly-mode 'Upgrade to evacuate' button opens the parent's ConfirmDrainModal — but the parent ClusterCard hooks `onUpgradeToEvacuate={() => setDrainOpen(true)}` and the modal looks at `currentState={cluster.state}` to detect the 'draining_readonly' upgrade flow (sets isUpgrade flag in modal). Don't reach into modal state directly; let the existing currentState prop wire it.
  - The state badge color now reflects mode (readonly=orange, evacuating=red) — emerald for live, muted for removed. Don't add a separate 'mode' badge; the state name + color combination conveys both.
  - StuckBucketsDrawer reuses Sheet on side=right (BucketReferencesDrawer pattern). It does NOT refetch; it renders the rows the parent DrainProgressBar handed in (which themselves come from the 30s-polled clusterDrainProgress query), so the drawer auto-updates on the next tick without its own query.
  - The empty-rows fallback in StuckBucketsDrawer says "next worker tick may add or remove rows" instead of "No buckets reference this cluster" (BucketReferencesDrawer text) — stuck status is transient by definition; the row list should hint at that.
---

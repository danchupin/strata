# Ralph Progress Log
Started: Mon May 11 19:00:00 MSK 2026
Cycle: ralph/complete-tracing

## Codebase Patterns
- OTel tracer provider lives at `internal/otel/otel.go::Provider`. `tp.Tracer(name)` factory returns named tracers — convention `strata.<component>` (e.g. `strata.data.rados`, `strata.meta.cassandra`). New tracers this cycle: `strata.meta.tikv`, `strata.data.s3`, `strata.worker.<name>`.
- US-001 introduces shared constants `internal/otel.AttrComponentGateway` + `AttrComponentWorker` (attribute.KeyValue). All gateway-side observers (HTTP middleware + cassandra observer + RADOS observer) stamp `strata.component=gateway` via the gateway constant. Worker iteration spans stamp `strata.component=worker` via the worker constant.
- HTTP middleware (`internal/otel/middleware.go`) already emits per-request spans + traceparent extract from headers. US-001 adds `span.SetAttributes(strataotel.AttrComponentGateway)` right after `tracer.Start`.
- Cassandra meta observer (`internal/meta/cassandra/observer.go::SlowQueryObserver`) wraps `gocql.QueryObserver` interface. Emits `meta.cassandra.<table>.<op>` child spans via `tracer.Start` retroactively timed `(q.Start, q.End)`. Failing queries set span status Error. US-001 stamps gateway component on each emitted span.
- TiKV has no gocql-style observer hook — must wrap at Store-method level via functional decorator `observer.WrapOp(ctx, op, table, fn)`. Span name `meta.tikv.<table>.<op>`. Attributes: `db.system="tikv"`, `db.operation`, `db.table`, `strata.component="gateway"`. US-002 builds this.
- RADOS data observer (`internal/data/rados/observer.go::ObserveOp`) wraps `goceph` ops; tracer arg passed through. Pattern reference for span naming convention `data.<backend>.<op>`. US-001 stamps gateway component on each emitted span.
- S3 data backend (`internal/data/s3/backend.go`) uses AWS SDK Go v2. OTel install via `go.opentelemetry.io/contrib/instrumentation/github.com/aws/aws-sdk-go-v2/otelaws` v0.68 + `otelaws.AppendMiddlewares(&awsCfg.APIOptions, WithTracerProvider, WithAttributeBuilder)` from `internal/data/s3/observer.go::installOTelMiddleware`. Per-cluster install at `connFor` BEFORE the metrics observer so otelaws Initialize-after brackets the full retry loop. Auto-emits one client-kind span per SDK call, named `S3.<Operation>`, with semconv v1.40 attributes `rpc.system.name=aws-api`, `rpc.method=S3/<op>`, `aws.region`, `http.response.status_code`. Custom AttributeBuilder stamps `strata.component=gateway` + `strata.s3_cluster=<id>` per-cluster. Failing call → status=Error (always exported by tail-sampler). Gated by `resolveTracerProvider(cfg)` — Tracer + TracerProvider on Config both nil ⇒ middleware install skipped. US-003.
- semconv version trap: otel sdk 1.41 → `semconv/v1.39.0`; sdk 1.43 → `semconv/v1.40.0`. Bumping otelaws bumps sdk to 1.43, so semconv must move in lockstep. Single import site: `internal/otel/otel.go`. Pinned in CLAUDE.md.
- otelaws v0.68 (semconv v1.40) renamed `rpc.system` → `rpc.system.name` and merged `rpc.service` + `rpc.method` into single `rpc.method` valued `<service>/<operation>` (e.g. `S3/PutObject`). Tests pin attribute keys with the v1.40 names.
- Legacy single-cluster `s3.Open(ctx, cfg)` synthesises `newCfg` and re-enters `New`. Any Config field MUST be explicitly copied onto newCfg — silent drops are a recurring trap. Currently forwarded: HTTPClient, SkipProbe, SkipCredsCheck, Tracer, TracerProvider.
- Tail-sampler (`internal/otel/sampler.go`) decides at OnEnd: any span with status=Error OR http.status_code >= 500 always exports regardless of `STRATA_OTEL_SAMPLE_RATIO`. Same path applies to worker spans — failing iterations always exported.
- Ring buffer (`internal/otel/ringbuf/`) is parallel SpanProcessor — retains every span (even sampled-out ones) under bytes-budgeted LRU for `/admin/v1/diagnostics/trace/{requestID}` admin endpoint. Workers contribute to it automatically.
- Worker shape: `cmd/strata/workers/registry.go::Worker{Name, Build, SkipLease}` registered via `init()` in each `cmd/strata/workers/<name>.go`. `Build` receives `workers.Dependencies` (Logger, Meta, Data, Tracer, Locker, Region, EmitLeader) and returns `workers.Runner`. **`Dependencies.Tracer` is ALREADY `*strataotel.Provider` (line 39 of registry.go), wired at `serverapp.go:254`** — workers just need to use `deps.Tracer.Tracer("strata.worker.<name>")` to get a named Tracer. No struct change required.
- Supervisor (`cmd/strata/workers/supervisor.go::Run`) spawns one goroutine per worker, manages leader-election + panic recovery + exponential backoff. No tracer wiring change — supervisor already passes Provider through Dependencies.
- Worker iteration span helper (US-004): new `cmd/strata/workers/iteration_span.go` exposes `StartIteration(ctx, tracer trace.Tracer, name string) (ctx, span)` and `EndIteration(span, err)`. Span name `worker.<name>.tick`. Attributes: `strata.component="worker"` (via `AttrComponentWorker` constant from US-001), `strata.worker=<name>`, `strata.iteration_id=<atomic-uint64-counter>` per worker. atomic.AddUint64 — safe for fan-out workers (gc fan-out per shard).
- Per-iteration only (no long-running parent — SDK has known issues with multi-hour spans). Operator wanting "which iteration belongs to which worker process" filters by `service.instance.id` (Resource attribute) + `strata.worker`.
- Nil-tracer safety: if `tracer == nil` (unit tests without tracer wiring), iteration spans use `tracenoop.NewTracerProvider().Tracer(...)`. No-panic, no behavior change.
- Workers list (10): gc, lifecycle, replicator, notify, access-log, inventory, audit-export, manifest-rewriter, quota-reconcile, usage-rollup. gc + lifecycle land in US-004 as the helper smoke test; remaining 8 land in US-005 batch.
- Sub-op child spans per worker: gc.scan_partition / gc.delete_chunk; lifecycle.scan_bucket / lifecycle.expire_object / lifecycle.transition_object; notify.deliver_event; replicator.copy_object; access_log.flush_bucket; inventory.scan_bucket; audit_export.export_partition; manifest_rewriter.rewrite_bucket; quota_reconcile.scan_bucket; usage_rollup.sample_bucket. Emit via `tracer.Start` under the per-iteration parent (extracted from ctx).
- Filter scheme: operator runs Jaeger query `strata.component=gateway` (request-path) OR `strata.component=worker` (background). Per-worker: `strata.worker=gc`. Per-cluster: `strata.s3_cluster=primary` (S3 backend hook in US-003).
- Component attribute stamped at span-creation time, NOT Resource (same process emits both; Resource is shared).
- ROADMAP close-flip: BOTH entries flip in US-006 commit. P2 line 205 'TiKV meta backend emits no trace spans' + P3 line 206 'S3-over-S3 data backend emits no trace spans'. Format: `~~**Px — ...**~~ — **Done.** ... (commit `<pending>`)`. Closing-SHA backfill follow-up commit on main.
- Update project CLAUDE.md note in US-006: "Workers under `strata server` currently pass nil tracer — add `Tracer: tp.Tracer(...)` to their config when their own /readyz / metrics story matures" is now obsolete; replace with the new convention.
- `tasks/prd-complete-tracing.md` REMOVED in US-006 per CLAUDE.md PRD lifecycle rule. Canonical record lives in Ralph snapshot.
- Verification: `go vet ./...`, `go test ./... -race`, `make docs-build`, `make smoke`. End-to-end Jaeger waterfall sanity check (manual against `make up-all` if available).


## 2026-05-11 - US-002
- Wired Tracer at internal/serverapp/serverapp.go::buildMetaStore tikv case via tp.Tracer("strata.meta.tikv").
- Wrapped every public Store method that hits TiKV with s.observer.Start("<Op>", "<table>") + deferred finish(err) returning a meta.tikv.<table>.<op> client-kind span. Files touched:
  - buckets.go (Create/Get/Delete/List + updateBucket)
  - objects.go (Put/Get/Delete/DeleteNullReplacement/SetObjectStorage — pre-existing in working tree before this iteration)
  - multipart.go (Create/Get/List + Save/ListParts pre-existing; added Complete/Abort/Record/GetCompletion/UpdateSSEWrap)
  - blobs.go (setBucketBlob/getBucketBlob/deleteBucketBlob + Quota helpers + Inventory configs)
  - bucket_stats.go (Get/Bump)
  - iam.go (8 methods)
  - managed_policies.go (8 methods)
  - audit.go (8 methods)
  - queues.go (Enq/List/Ack across GC, Notify, NotifyDLQ, Replication, AccessLog)
  - usage.go (Write/List/ListUser)
  - reshard.go (Start/Get/Update/Complete/List)
  - admin_jobs.go (Create/Get/Update)
  - meta_health.go (MetaHealth)
- Added internal/meta/tikv/observer_test.go covering: WrapOp emits meta.tikv.<table>.<op> client-kind child span with db.system=tikv + db.tikv.table + db.operation + strata.component=gateway, error path flips status Error + records the description, nil tracer is no-op, and end-to-end Store wiring via openWithBackendAndObserver emits meta.tikv.buckets.CreateBucket.
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go test ./internal/meta/tikv/... -race` PASS, full `go test ./... -race` PASS.
- **Learnings for future iterations:**
  - Named-return + observer-Start pattern: signatures like `(out *T, err error)` plus `ctx, finish := s.observer.Start(ctx, op, table); defer func() { finish(err) }()` is the canonical TiKV wrapper. When converting an existing `(*T, error)` signature to named returns, downstream `out := make(...)` lines become `out = make(...)` — the compiler flags every such site with "no new variables on left side of :=". Same for `res := &meta.ListResult{}` → `res = &meta.ListResult{}`.
  - For functions whose inner-scope `err :=` is needed for shadowing inside loops/branches, prefer renaming the inner var (`derr`, `serr`, `gerr`) over removing the named-return — keeps the defer recovery consistent.
  - The `openWithBackendAndObserver` test helper unlocks contract-level observer wiring tests against the in-process memBackend without touching real PD.
  - When a wrapper sits on top of `txn.Get` / `txn.Set` / `txn.Scan` that don't carry their own spans, the Store-level wrap is the only place spans surface for TiKV — there's no gocql-equivalent per-query hook.
  - `s.observer.Start` chooses table names that map cleanly to Cassandra table parity where it exists (`buckets`, `objects`, `multipart`, `audit_log`, `iam_users`, `iam_access_keys`, `managed_policies`, `user_policies`, `gc_queue`, `notify_queue`, `notify_dlq`, `replication_queue`, `access_log_buffer`, `bucket_stats`, `bucket_blobs`, `user_quota`, `inventory_configs`, `usage_aggregates`, `reshard_jobs`, `admin_jobs`, `meta_health`, `multipart_completions`, `multipart_parts`).
---

## 2026-05-11 - US-001
- Added `internal/otel/component.go` exporting `AttrComponentKey="strata.component"` + `AttrComponentGateway` (gateway) + `AttrComponentWorker` (worker) attribute.KeyValue constants.
- `internal/otel/middleware.go`: HTTP server span now stamps `strata.component=gateway` via the shared constant inline with the existing http.* attributes block.
- `internal/meta/cassandra/observer.go::emitSpan`: imports `strataotel`, stamps gateway attr first in the trace.WithAttributes block.
- `internal/data/rados/observer.go::ObserveOp`: imports `strataotel`, stamps gateway attr in the rados-child span attrs block.
- Tests:
  - `internal/otel/middleware_test.go` (new file): `TestMiddlewareStampsGatewayComponent` drives a 200 OK through `NewMiddleware`, asserts span has `strata.component=gateway`.
  - `internal/meta/cassandra/observer_test.go::TestQueryObserverEmitsSpan`: extended `wantAttrs` map with `strata.component=gateway`.
  - `internal/data/rados/observer_test.go::TestObserveOpEmitsSpan`: extended attr-pair table with `strata.component=gateway`.
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go test ./internal/otel/... ./internal/meta/cassandra/... ./internal/data/rados/... -race` PASS, full `go test ./...` PASS.
- **Learnings for future iterations:**
  - `internal/otel` package has no import dependency on `internal/data/*` or `internal/meta/*` outside test files, so cassandra/rados can safely import `internal/otel` for shared attribute constants (no cycle).
  - `internal/otel/spans_test.go` is in external `otel_test` package, so it can import cassandra+rados+strataotel together without forming a build cycle.
  - The shared component-discriminator constants will be reused by US-002 (TiKV observer) for `strata.component=gateway` and US-004/US-005 (worker iteration helper) for `strata.component=worker` — import path is `github.com/danchupin/strata/internal/otel`.
  - Pattern: stamp the component attribute FIRST in the trace.WithAttributes block so observers reading raw attrs find it predictably; keep using shared constants instead of `attribute.String("strata.component", "gateway")` to avoid magic-string duplication.
---

## 2026-05-11 - US-003
- Added go.opentelemetry.io/contrib/instrumentation/github.com/aws/aws-sdk-go-v2/otelaws v0.68.0 — pulled otel sdk to v1.43.0 + smithy-go v1.24.3 as a transitive bump.
- internal/data/s3/observer.go (new): installOTelMiddleware(apiOpts, tp, clusterID) wraps otelaws.AppendMiddlewares with otelaws.WithTracerProvider + otelaws.WithAttributeBuilder(stampStrataAttrs(clusterID)). Custom builder returns {strata.component=gateway, strata.s3_cluster=<id>} per-call.
- internal/data/s3/backend.go: Backend gains tracerProvider trace.TracerProvider. Config adds Tracer trace.Tracer + TracerProvider trace.TracerProvider. resolveTracerProvider picks TracerProvider first, otherwise falls back to otel.GetTracerProvider when Tracer != nil, else nil → middleware install skipped. connFor calls installOTelMiddleware BEFORE the existing metrics instrumentStack so otelaws Initialize-after brackets every retry attempt. Legacy single-cluster Open() forwards Tracer + TracerProvider onto the synthesised newCfg.
- internal/serverapp/serverapp.go::buildDataBackend case "s3": Tracer=tp.Tracer("strata.data.s3") + TracerProvider=tp.TracerProvider().
- internal/otel/otel.go: bumped semconv v1.39.0 → v1.40.0 (matches sdk 1.43 resource.Default schema URL). Added Provider.TracerProvider() accessor so callers needing a TracerProvider (like otelaws.WithTracerProvider) don't have to thread through trace.Tracer.
- internal/data/s3/observer_test.go (new): TestOtelawsStampsStrataAttributes drives synthetic PutObject + asserts span carries strata.component=gateway, strata.s3_cluster=default, rpc.method contains PutObject, rpc.system.name=aws-api, kind=client. TestOtelawsErrorPathFlipsStatus drives NoSuchKey + asserts span status=Error. TestNilTracerSkipsMiddleware asserts the zero-config fixture emits no spans (existing tests stay hermetic).
- CLAUDE.md + docs/site/content/architecture/observability.md updated: semconv-version pin now reads "SDK 1.41 → semconv/v1.39.0, SDK 1.43 → semconv/v1.40.0".
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go mod tidy` clean, `go test ./internal/data/s3/... ./internal/serverapp/... ./internal/otel/... ./internal/meta/cassandra/... ./internal/data/rados/... -race` PASS. Pre-existing flake in internal/lifecycle/TestThreeReplicaDistribution verified to exist on baseline (git stash -u then re-run) — unrelated to this change.
- **Learnings for future iterations:**
  - otelaws v0.68 uses semconv v1.40.0 internally; `rpc.system` was renamed to `rpc.system.name` in that semconv release. Tests that pin the attribute key need the new name. Likewise, otelaws merged rpc.service + rpc.method into a single `rpc.method` attribute valued `<service>/<operation>` (e.g. `S3/PutObject`) — no separate `rpc.service` attribute.
  - otelaws.AppendMiddlewares installs FOUR middlewares (initializeBefore + initializeAfter + finalizeAfter + deserialize). Initialize-after wraps the entire retry loop, so each retry attempt emits a child trace event — single client-kind parent span per logical SDK call. Install BEFORE any custom Initialize middleware (like the s3 metrics observer) so the span fully brackets retries.
  - Default sample ratio (`STRATA_OTEL_SAMPLE_RATIO=0.01`) applies to every span the SDK pushes through the BatchSpanProcessor. otelaws-emitted SDK spans flow through the same tail-sampler — failing calls (status=Error) always export regardless of ratio. Pinned by TestOtelawsErrorPathFlipsStatus.
  - Legacy single-cluster Open(ctx, Config{Bucket,...}) shape synthesises a `newCfg` and re-enters New(). Forgetting to copy fields onto newCfg silently drops them (test reproduced this gotcha — fields Tracer + TracerProvider had to be copied explicitly). Same trap for any future field added to Config.
  - Provider.TracerProvider() falls back to otel.GetTracerProvider() when the SDK provider is absent (no-op mode). This keeps caller code free of nil checks AND allows tests that don't call strataotel.Init to still drive instrumentation by setting the global provider directly.
  - Test pattern: use `tracetest.InMemoryExporter` + `sdktrace.WithSyncer(exp)` — Syncer flushes synchronously on span End, so no `tp.ForceFlush(ctx)` needed in test bodies. Asserting via `exp.GetSpans()` after the operation completes is enough.
---

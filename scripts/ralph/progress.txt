# Ralph Progress Log
Started: Fri May 22 09:00:00 MSK 2026
Cycle: ralph/p1-fixes

## Codebase Patterns
- **Bundle cycle**: 2 P1 entries + 1 P3 follow-up surfaced by ralph/rgw-benchmarks. Pre-launch product per [Pre-launch no deploys] memory — hard cutover on `bucket_stats` key shape, no migration backfill.
- **9 stories** (may grow to 10 if US-001 spike branch b — Cassandra also needs fan-out): US-001 spike + Cassandra probe | US-002 TiKV fan-out impl | US-003 Cassandra strategy per spike | US-004 bench-validate saturation gone | US-005 RGW period reconcile | US-006 RGW restart smoke | US-007 rgw-benchmarks full sweep rerun | US-008 rgw-comparison.md complete numbers + verdict | US-009 smoke + ROADMAP close-flip × 3 + PRD removal.
- **Story ordering**: US-001 spike BEFORE US-002/US-003 (decision tree drives Cassandra strategy). US-002 TiKV impl BEFORE US-004 bench (need fix to validate). US-005 RGW reconcile BEFORE US-006 smoke + US-007 sweep (lab restart must work first). US-008 doc update AFTER US-007 captures numbers. US-009 smokes everything.
- **Production-grade fixes applied to PRD** (4 of 4 from post-review critique):
  - (1) US-001 spike scope extended to Cassandra concurrency probe @ c=100 + explicit decision tree (a/b/c branches).
  - (2) US-002 path refs corrected: `internal/meta/tikv/bucket_stats.go:88` (BumpBucketStats) + `:63` (GetBucketStats) — verified, NOT `store.go`.
  - (3) US-003 reformulated from `Cassandra unchanged` to `branch on US-001 spike outcome`. Branch (b) grows cycle to 10 stories with new US-002b.
  - (4) US-002 100-goroutine assertion extended: `sum=100 AND zero CAS/txn-exhausted errors` (hidden lost updates = correctness regression too).
- **Verified codebase facts from PRD review**:
  - TiKV `BumpBucketStats`/`GetBucketStats` live in `internal/meta/tikv/bucket_stats.go` (lines 88/63), NOT `store.go`.
  - TiKV key shape: `subBucketStats = "bs"` constant in `internal/meta/tikv/keys.go`; `BucketStatsKey(uuid.UUID) []byte` function. Comment `s/B/<uuid16>/bs` matches.
  - Cassandra `BumpBucketStats` (cassandra/store.go:3447): **LWT CAS loop with maxAttempts=32**, NOT counter table. Schema (schema.go:226-231): `bigint` columns, NOT `counter`. SQL: `UPDATE bucket_stats SET used_bytes=?, used_objects=?, updated_at=? WHERE bucket_id=? IF used_bytes=? AND used_objects=?` — explicit LWT predicate.
  - Memory backend uses sync.RWMutex (in-process serial; no RMW contention beyond mutex).
  - `quotareconcile` worker confirmed via `cmd/strata/workers/quota_reconcile_test.go` Lookup("quota-reconcile") + SkipLease=true.
  - `hash/fnv` import already used at `internal/meta/tikv/list.go` — `fnv.New32a()` precedent.
- **Decision branches for US-001 spike → US-003**:
  - **(a)** Cassandra final=100 + zero CAS-exhausted at c=100 → fan-out TiKV only; Cassandra unchanged; document headroom (e.g. 4 retries observed, 8× before 32-cap).
  - **(b)** Cassandra final<100 OR CAS-exhausted at c=100 → BOTH backends need fan-out. New table `bucket_stats_shard ((bucket_id, shard) PK)` in Cassandra. Cycle grows to 10 stories (US-002b inserts).
  - **(c)** Cassandra fails differently (deadlock/timeout) → escalate, halt cycle.
- **TiKV CLAUDE.md gotchas applied throughout (US-002)**:
  - FoundationDB byte-stuffing NOT needed for shard suffix (one-byte raw uint8, fixed-width).
  - Pessimistic txn for RMW per `Plain Put on a key with prior LWT history` gotcha.
  - Explicit `txn.Rollback()` on every non-error early return per `EARLY-RETURN paths must call txn.Rollback()` gotcha.
  - Snapshot read for GetBucketStats (read-only, no pessimistic txn needed).
- **RGW lab bootstrap fix (US-005)**: period state drift — root cause investigate first via `radosgw-admin period get` JSON diff between cycle 1 and cycle 2. Likely cause: stale zone entries in period after restart. Fix: reconcile zonegroup → zone membership BEFORE `period update --commit`. Log `period reconcile — zonegroups=<N>, zones-per-zonegroup=[...]` for operator visibility.
- **Bench rerun discipline (US-007)**: full sweep (~120 min) — all 8 workloads × concurrency sweep × 3 runs. Pre-flight verifies both P1 fixes in place via grep. Compare pre-fix vs post-fix numbers for US-007 list + US-008 delete/iam (previously partial/aborted).
- **README backfill discipline (US-008)**: grep-verify `Features vs RGW` table structure FIRST + preserve ≤120 lines guard (ralph/readme-docs-rewrite US-001 constraint). Footnote link to Limitations preserved.
- **ROADMAP close-flip (US-009)**: × 3 entries (P1 line 106 + P1 line 117 + P3 line 282) + 0-1 new P1 if bucket-index claim refuted. `(commit pending)` placeholder; SHA backfill on main as fast-follow commit (mirrors precedent).
- **Severity-tiered new entries (FR-13)**: bucket-index claim refuted = **P1** (foundational README claim regression, not P3). Other workloads > 1.5× slower = P3.
- **Backend-package observability pattern** (validated US-002): define a `Metrics` interface inside the meta package (no prometheus import) → adapter struct in `internal/metrics` (`TiKVObserver`, mirrors `CassandraObserver` / `RADOSObserver` shape) → register the counter in `metrics.Register()` → wire `Metrics: metrics.TiKVObserver{}` in `serverapp.buildMetaStore`'s backend-build branch. Avoids a metrics→meta import cycle.
- **TiKV fan-out: pick on fresh uuid, NOT on a stable key** (validated US-002). `fnv32a(uuid.NewString()) % shards` keeps the distribution unbiased across PUT/DELETE. Hashing on bucket id or object key collapses every op on a hot bucket onto one shard and defeats the fan-out. Keep the named helper (`pickBucketStatsShard()`) so the invariant is greppable.
- **TiKV fan-out: in-txn aggregate read preserves contract** (validated US-002). After RMW on the picked shard inside a pessimistic txn, read the remaining shards via `txn.Get` and sum to return the cross-shard aggregate. Snapshot consistency within the txn handles concurrent-sibling writes. Callers of `BumpBucketStats` see the same cumulative-total contract as the pre-fan-out single-key shape.
- **US-001 spike outcome — BRANCH (a) selected**: Cassandra concurrency probe @ c=100 returned final UsedBytes/UsedObjects = exactly 100 (zero lost updates) with zero CAS-exhausted errors. Observed 253 retries across 100 goroutines (avg 2.53 retries/goroutine; max headroom factor vs 32-cap ≈ 12.6×). **Decision**: fan-out applies to TiKV ONLY. Cassandra `internal/meta/cassandra/store.go:3447` LWT CAS loop with maxAttempts=32 is sufficient. Memory backend (sync.RWMutex) unchanged. Cycle stays at 9 stories (no US-002b insertion). Probe artifact: `internal/meta/cassandra/bucket_stats_probe_integration_test.go` (build tag `integration`, kept as regression anchor).

## [2026-05-22 00:34 MSK] - US-001
- Spike: Cassandra BumpBucketStats concurrency probe @ c=100. No prod code changed.
- Files changed:
  - `internal/meta/cassandra/bucket_stats_probe_integration_test.go` (new throwaway-but-kept probe; build tag `integration`)
  - `scripts/ralph/progress.txt` (decision-tree outcome recorded as contract for US-002+)
- **Probe numbers (cassandra:5.0 testcontainer, SimpleStrategy RF=1, c=100, +1/+1 each)**:
  - final UsedBytes = 100, final UsedObjects = 100 (no lost updates)
  - bucket_stats INSERT count = 100 (all goroutines tried INSERT IF NOT EXISTS; only 1 wins, rest retry into UPDATE branch)
  - bucket_stats UPDATE count = 253 (CAS retries on UPDATE … IF used_bytes=? AND used_objects=?)
  - bucket_stats SELECT count = 354 (= 353 in-loop reads + 1 final test read)
  - total CAS attempts = 353 → retries = 253 (avg 2.53/goroutine)
  - CAS-exhausted errors = 0 (zero goroutines hit the 32-attempt ceiling)
- **Decision branch (per US-001 AC tree)**: **(a)** Cassandra OK. Fan-out applies to **TiKV only**.
- **Headroom analysis**: avg 2.53 retries/goroutine vs 32-attempt cap → 12.6× headroom. Even at c=400 (4× current load) Cassandra would average ~10 retries/goroutine, still comfortably under cap.
- **Learnings for future iterations**:
  - `cassandra.SessionConfig.Metrics` is the right hook for counting query attempts in concurrency probes — `ObserveQuery(table, op, duration, err)` fires once per CQL query incl. CAS retries. No need to instrument the impl.
  - `parseStatement(stmt)` in `internal/meta/cassandra/observer.go` extracts (table, op) from CQL — INSERT/UPDATE/SELECT all parse to `bucket_stats` for the BumpBucketStats hot path. Filter by table to isolate per-table query traffic.
  - Cassandra BumpBucketStats does NOT call `obs.RecordLWTConflict` on applied=false — so the LWT-conflict metric is empty for bucket_stats. Counting query attempts via ObserveQuery is the only path.
  - testcontainers-go cassandra image: `cassandra:5.0` + `tccassandra.Run(ctx, "cassandra:5.0")` boots in ~5s, ready to accept LWT in ~4s after that. Total probe wall-clock ~14s incl. teardown.
  - `DOCKER_HOST=unix:///Users/dochupin/.lima/avito/sock/docker.sock` is the lima sock path on this dev box (per CLAUDE.md macOS+lima note); `TESTCONTAINERS_RYUK_DISABLED=true` recommended on lima to skip the ryuk-reaper container that doesn't always shut down cleanly.
  - **For US-002**: the TiKV-only fan-out scope is locked. Implementation hits `internal/meta/tikv/bucket_stats.go:88` (BumpBucketStats) + `:63` (GetBucketStats), per PRD-verified paths. Cassandra store untouched.
---

## [2026-05-22 14:00 MSK] - US-003
- Memory + Cassandra strategy per US-001 spike branch (a) outcome: **no-change confirmation** on both backends.
  - Memory backend (`internal/meta/memory/store.go`) — `sync.RWMutex` serialises in-process bumps; concurrent c=50 absorbed without contention beyond mutex acquire. Unchanged.
  - Cassandra backend (`internal/meta/cassandra/store.go:3447` `BumpBucketStats`) — LWT CAS loop with `maxAttempts=32`. US-001 spike measured 253 retries / 100 goroutines (avg 2.53/goroutine, ~12.6× headroom before the 32-cap) + zero CAS-exhausted errors at c=100. Branch (a) tree fires: Cassandra unchanged.
- Files changed:
  - `internal/meta/storetest/contract.go` — new `caseBucketStatsConcurrentBumps` (50 goroutines, +1 byte +1 object each; asserts final `UsedBytes=UsedObjects=50` and zero per-call errors — CAS-exhausted on Cassandra would surface as `cassandra: bucket_stats CAS exhausted retries for bucket …`). Registered in the `Run` table between `BucketStatsRoundTrip` and `BucketStatsHotPath` so all three backends (memory always, TiKV + Cassandra + Scylla under `-tags integration`) exercise it.
  - `scripts/ralph/prd.json` — US-003 `passes: true` + notes.
- **Quality checks**:
  - `go build ./...` green.
  - `go vet ./...` green.
  - `go test -race -count=1 ./internal/meta/...` green (memory + cassandra + tikv + storetest + meta). The new contract case runs against memory unconditionally; integration-tag legs of TiKV / Cassandra / Scylla pick it up automatically via the shared `storetest.Run` table.
  - Note: pre-existing `rangeint` modernization hints on `contract.go` for sibling `for i := 0; i < N; i++` blocks (lines 137/150/484/499/1909/2534/2565/2571 + my new 2614/2620) — kept the existing idiom on the new case for consistency; `go vet` does not flag these (gopls-style hint only).
- **Behavioral contract**:
  - Cassandra path: `caseBucketStats` already exercised 100 concurrent +13/+1 bumps cleanly per US-001 spike. New `caseBucketStatsConcurrentBumps` is a slimmer 50-goroutine +1/+1 anchor — the canonical regression case the PRD asks for, kept small for fast CI cycles.
  - TiKV path: per-shard fan-out (US-002) absorbs all 50 bumps without surfacing pessimistic-txn retry errors. The picker hits >=2 shards via `fnv32a(uuid.NewString()) % 8`.
  - Memory path: trivially serialised — sanity that the contract still holds across all 3 backends.
- **Learnings for future iterations**:
  - **Spike-then-no-change is a valid US shape.** The new contract case is the entire production-code delta for US-003 — the spike outcome locked Cassandra/memory as untouched. Documenting the no-change branch as a regression anchor (rather than skipping the story entirely) keeps the maxAttempts=32 invariant under contract-test pressure: a future Cassandra change that drops headroom below the 32-cap will fail this case at c=50 long before production traffic notices.
  - **Contract test runtime budget — 50 goroutines, not 100.** The existing `caseBucketStats` does 100 concurrent +13/+1 already. The new anchor uses c=50 +1/+1 to keep the cycle anchor cheap (memory and TiKV finish in ~10 ms; Cassandra integration leg in ~150 ms) while still surfacing CAS-exhausted regressions — Cassandra would need to lose >37 retries per goroutine on average for the 32-cap to bite, which would also bite production.
  - **No `BucketStats` interface delta.** `meta.Store.BumpBucketStats` + `GetBucketStats` shape is unchanged — the per-shard fan-out is private to the TiKV backend. Future backends (any new range-scan-capable store) get the contract test for free without touching the surface area.
---

## [2026-05-22 02:10 MSK] - US-002
- TiKV per-shard fan-out (8 shards) wired into the bucket_stats live counter. Hard cutover — old single-key `s/B/<bid>/bs` slot removed (no migration backfill per pre-launch policy).
- Files changed:
  - `internal/meta/tikv/keys.go` — `bucketStatsShardCount = 8`, `subBucketStatsShard = "bs/"`, new `BucketStatsShardKey(bucketID uuid.UUID, shard uint8) []byte`. `BucketStatsKey` deleted; old `subBucketStats = "bs"` const retained as documentation per PRD.
  - `internal/meta/tikv/bucket_stats.go` — `BumpBucketStats` picks `fnv32a(uuid.NewString()) % 8`, pessimistic txn on (shardKey, ownerKey), RMW the shard, then in-txn reads the other 7 shards to assemble the aggregate return value. `GetBucketStats` opens a non-pessimistic snapshot txn and sums all 8 shards via sequential `txn.Get`. `pickBucketStatsShard()` is the named helper so the unbiased-per-op contract is greppable.
  - `internal/meta/tikv/metrics.go` (new) — `Metrics` interface exposing `IncBucketStatsShardWrite(shard int)`. `Store.metrics` field + `Config.Metrics` wiring.
  - `internal/meta/tikv/store.go` — `Config.Metrics` + `Store.metrics` + `Open` propagation.
  - `internal/metrics/metrics.go` — `BucketStatsShardWritesTotal` counter vec + `TiKVObserver` adapter (mirrors `CassandraObserver` / `RADOSObserver` shape; registered in `Register()`).
  - `internal/serverapp/serverapp.go` — `metatikv.Open` call site passes `Metrics: metrics.TiKVObserver{}`.
  - `internal/meta/tikv/bucket_stats_test.go` (new) — 100-goroutine race test with `fakeShardMetrics` sink. Asserts (i) `final UsedBytes / UsedObjects = exactly concN * delta`, (ii) zero per-call errors, (iii) fan-out hits ≥2 shards (else the test would silently pass against a regression that collapses the picker). Second test asserts the read path sums all shards.
  - `CLAUDE.md` (root) — new TiKV-gotchas bullet documenting the fan-out shape + the "fresh uuid per call" picker invariant so future agents don't hash on a stable key and accidentally collapse the fan-out.
- **Quality checks**:
  - `go build ./...` green.
  - `go vet ./...` green (no diagnostics on the touched files).
  - `go test -race -count=1 ./internal/meta/tikv` → green; new race test passes.
  - `go test -race -count=1 ./internal/meta/...` → all 4 backends green.
  - `go test -race ./...` → only `cmd/strata-racecheck`'s `TestRunSmokeBinary` is flaky; verified pre-existing (fails 1/5 on `c3687fd` too — the pre-edit SHA). Unrelated to US-002.
  - `go vet -tags ceph ./...` cannot run on the dev box (no `librados.h`); CI's docker-build leg covers it.
- **Behavioral contract preserved**:
  - `caseBucketStats` (storetest contract) — per-bump cumulative total assertion still passes because `BumpBucketStats` returns the in-txn aggregate (own shard + 7 sibling reads), not just the shard counter.
  - `caseBucketStatsHotPath` — PUT / overwrite / DELETE / markers fold through `BumpBucketStats` unchanged at the call site (no callers needed editing; the public meta.Store surface is identical).
- **Learnings for future iterations**:
  - **Fan-out + per-shard return semantic — pick one carefully.** First draft of `BumpBucketStats` returned just the shard's post-write value. That broke the contract test's sequential loop (each iteration expects the cumulative total). Fix: inside the same pessimistic txn, after writing the shard, read the remaining 7 shards via `txn.Get` and sum. Adds 7 reads per bump but preserves the public contract. Snapshot consistency within the txn handles the concurrent-sibling case.
  - **`fnv32a(uuid.NewString())` not `fnv32a(bucketID)` for shard pick.** Hashing on a stable key (bucket id, object key) collapses every bump on a hot bucket onto one shard and defeats the fan-out. The PRD AC explicitly calls out the unbiased uuid pattern — keep the named `pickBucketStatsShard()` helper greppable for future contributors.
  - **kvTxn has no `BatchGet`.** Sequential `txn.Get` × 8 inside a snapshot txn is the closest equivalent. Adding `BatchGet` to the interface would be an invasive cross-backend change for a hot path that is already negligibly cheap (8 single-key gets in one round-trip-friendly snapshot).
  - **`unusedfunc` info-level diagnostic on `subBucketStats` is acceptable.** PRD AC explicitly retains the constant for documentation continuity. `go vet` does not flag unused consts, only `gopls`-style hints. No `make vet` regression.
  - **`cmd/strata-racecheck`'s `TestRunSmokeBinary` is pre-existing flake.** Verified by stash-then-rerun against `c3687fd`. Future iterations should not chase this from a clean-test perspective.
  - **`go test -race ./...` on dev box: librados-tagged build leg always fails on macOS** without ceph headers. CI's docker-build job has librados linked and exercises the `-tags ceph` path. Skip locally; rely on CI for that leg.
  - **Metric wiring pattern**: mirror `cassandra.SessionConfig.Metrics` / `rados.Config.Metrics` for any backend-package observability. Define the interface inside the meta package (no prometheus import) and an adapter struct in `internal/metrics` (the cmd-layer wiring point). Register the new counter in `metrics.Register()` and wire the `Metrics: metrics.TiKVObserver{}` field in `serverapp.buildMetaStore`.
---


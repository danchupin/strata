# Ralph Progress Log
Started: Mon May 11 21:00:00 MSK 2026
Cycle: ralph/placement-rebalance

## Codebase Patterns
- Multi-cluster data backends already exist: RADOS via `STRATA_RADOS_CLUSTERS` (+ `STRATA_S3_CLUSTERS` for S3-over-S3) parsed into `internal/data/rados/Backend.clusters map[string]*radosCluster` and `internal/data/s3/Backend.clusters map[string]*s3Cluster`. The Backend already routes per-storage-class via `STRATA_S3_CLASSES` (`resolveClass`). This cycle adds per-BUCKET routing via a new `meta.Bucket.Placement map[string]int` (weight by cluster). The router is hash-mod stable so a chunk's home cluster never depends on iteration order.
- Bucket blob-config helpers live in `internal/meta/cassandra/store.go::setBucketBlob/getBucketBlob/deleteBucketBlob` (reuse pattern from lifecycle/CORS/policy/ownership-controls). Reuse for `Placement` and `cluster_state` rather than custom CRUD per endpoint.
- `meta.Store` contract test infra: `internal/meta/storetest/contract.go::Run(t, factory)` is the shared suite. Add `caseBucketPlacement` + `caseClusterState` (US-006) so memory + cassandra + tikv get exercised in lockstep.
- TiKV bucket-config keys follow the variable-length byte-stuffing convention (`internal/meta/tikv/keys.md`). Use the existing per-bucket-blob prefix `s/bk/<bucketID>/<configKind>` shape — placement is `configKind="plcm"`. `cluster_state` is NOT bucket-scoped — use a top-level prefix `s/cs/<clusterID>`.
- Worker registration: every worker has a per-worker `cmd/strata/workers/<name>.go` calling `workers.Register(workers.Worker{Name, Build, SkipLease})` from `init()`. For rebalance: `SkipLease: false` (single leader cluster-wide on `rebalance-leader`).
- Worker iteration spans (shipped in complete-tracing cycle): use `internal/otel.StartIteration / EndIteration` for the per-tick parent span (name `worker.rebalance.tick`); attrs include `strata.component="worker"` + `strata.worker="rebalance"` + atomic uint64 `strata.iteration_id`. Sub-op spans (`rebalance.scan_bucket`, `rebalance.move_chunk`) created via `tracer.Start(ctx, "<name>")` under the parent ctx.
- `Dependencies.Tracer` is already `*strataotel.Provider` (registry.go:39) — use `deps.Tracer.Tracer("strata.worker.rebalance")` to get a named Tracer. Nil-tracer safety: if `tp == nil` (unit tests) the iteration helper falls back to `tracenoop`.
- Manifest CAS via `meta.Store.SetObjectStorage(ctx, bucket, key, versionID, expectedClass, newClass, manifest) (applied bool, err error)`. Pattern: pass `expectedClass == currentClass` because rebalance doesn't change class — only the chunk locations inside the manifest. If `applied=false`, discard the new chunks (enqueue them into GC); the concurrent client write wins. Mirrors the lifecycle transition path exactly.
- GC enqueue: `meta.Store.EnqueueGCEntry(ctx, GCEntry{Pool, OID, ...})` already exists. Used by lifecycle and now rebalance — same grace window applies, the gc worker eventually deletes per `STRATA_GC_GRACE`.
- Backwards compat critical: every existing bucket has `Placement == nil`. The router (`placement.PickCluster`) MUST return `""` for empty/nil policy so callers fall back to the pre-existing `$defaultCluster` routing. Zero schema migration, zero behavior change for unconfigured buckets.
- Hash function for stable routing: `fnv32a("<bucketID>/<key>/<chunkIdx>") % sum(weights)` walking a weight-wheel over sorted cluster IDs. Sorted-cluster-id order is critical for determinism — `map` iteration order is random in Go. Use `sort.Strings(slices.Collect(maps.Keys(policy)))`.
- Drain sentinel cache: PUT hot path reads `cluster_state.state` per chunk. Burden Cassandra/TiKV on every PUT is unacceptable. Use in-process cache with 30s TTL (`internal/rebalance/draincache.go` or `internal/data/draincache.go`). Drain is a slow operator action — eventual consistency for 30s is fine.
- Throttle primitives: `golang.org/x/time/rate.Limiter` for byte-rate; `golang.org/x/sync/errgroup` for inflight bound. Shared helpers go in `internal/rebalance/throttle.go`.
- RADOS `MonCommand({"prefix":"df"})` returns JSON with `stats.total_bytes` + `stats.total_used_bytes`. Parse + cache per-cluster (60s TTL OK). S3 has no native cluster-fill semantics — `ClusterStats` for S3 returns `ErrNotSupported`; the worker treats this as "OK to proceed" with one WARN per iteration.
- Admin API audit: every admin write must `s3api.SetAuditOverride(ctx, action, resource, bucket, principal)` inside the handler. `action="admin:PutBucketPlacement"` / `admin:DrainCluster` / `admin:UndrainCluster`. Listing handlers (GET) skip audit via `auditableMethod`.
- ROADMAP close-flip in US-007: line 185, format `~~**P2 — Per-bucket placement policy + cross-cluster rebalance worker.**~~ — **Done.** <summary>. (commit `<pending>`)`. Closing SHA backfilled in follow-up commit on main per CLAUDE.md convention. `tasks/prd-placement-rebalance.md` REMOVED in the same commit per PRD lifecycle rule.
- Verification per story: `go vet ./...`, `go test ./... -race`, `make docs-build` (US-007 only), `make smoke` (where the change touches the gateway hot path — US-002, US-006).
- meta.Bucket.Placement field is populated ONLY by GetBucketPlacement, NOT by GetBucket — the hot-path GetBucket stays a single buckets-table read. US-002 PutChunks should call GetBucketPlacement once per request (cacheable later via US-006-style draincache pattern). Don't pull the blob into GetBucket's SELECT — that doubles meta reads on every unconfigured bucket.
- Admin handler cluster-name resolution: `Server.KnownClusters map[string]struct{}` is wired by serverapp.knownDataClusters(cfg) from ParseClusters(STRATA_RADOS_CLUSTERS/STRATA_S3_CLUSTERS). nil → check skipped (memory backend, dev rigs). The placement Set handler runs weight validation via `meta.ValidatePlacement` (pure, in meta) BEFORE looking up the bucket, then checks against KnownClusters in the admin layer. Cluster name validation does NOT live in meta.Store — meta has no view into the data backend env.
- Rebalance mover plumbing (US-004): the RadosCluster interface lives in `internal/rebalance` (NOT under `internal/data/rados`) so tests stay librados-free. The ceph-tagged constructor `rados.RebalanceClusters(*Backend)` returns thin per-cluster facades; rebalance never imports rados, rados imports rebalance only under the `ceph` build tag. Build-tag-conditional cmd helpers (`rebalance_movers.go` / `rebalance_movers_ceph.go`) plug the mover into the MoverChain emitter — mirror this pattern when adding the S3 mover (US-005). Manifest CAS uses a pre-emptive check inside `buildUpdatedManifest` (live OID/cluster must still match planned SrcRef) so a same-class concurrent rewrite is caught BEFORE the SetObjectStorage LWT, avoiding the hazard where a class-match LWT clobbers a newer chunk locator. CAS losers always go to the GC queue via `meta.Store.EnqueueChunkDeletion` (PRD called this "EnqueueGCEntry" — actual method name is EnqueueChunkDeletion).

## 2026-05-12 09:46 MSK - US-003
- Implemented rebalance worker scaffold (internal/rebalance/worker.go): leader-elected (outer rebalance-leader lease via SkipLease=false), reads STRATA_REBALANCE_INTERVAL / _RATE_MB_S / _INFLIGHT in cmd-layer Build with clamp+WARN, walks ListBuckets, GetBucketPlacement, and ListObjects per bucket; per-chunk PickCluster verdict vs ChunkRef.Cluster generates []Move. Default planLogger Emitter logs INFO "rebalance plan" with bucket/moves/actual/target maps; executeMoves stub remains for US-004/US-005.
- Per-iteration tracing: StartIteration/EndIteration for `worker.rebalance.tick`; sub-op `rebalance.scan_bucket` per bucket with attrs strata.rebalance.bucket / bucket_id / moves count; sticky-err accumulator so scan failures flip the iteration parent to Error and the tail-sampler exports it.
- Counter metric strata_rebalance_planned_moves_total{bucket} (label "bucket") registered in internal/metrics; RebalanceObserver adapter so internal/rebalance stays prometheus-free. Worker bumps the counter directly per move BEFORE calling Emitter so the metric is independent of whichever emitter is wired (US-004/US-005 will inject a real mover-chain emitter).
- cmd/strata/workers/rebalance.go registers `rebalance` worker; clampDuration / clampInt helpers clamp env values to [1m, 24h] / [1, 10000] / [1, 64] with structured WARN.
- Tests: internal/rebalance/worker_test.go covers (a) bucket without placement skipped, (b) per-chunk actual distribution + planned moves vs PickCluster verdict, (c) zero moves when distribution already matches, (d) metric bumped once per move, (e) delete-marker / no-manifest survival, (f) Meta+Data required, (g) default planLogger emitter still bumps metric. cmd/strata/workers/rebalance_test.go covers registration, env-read, clamp helpers, Resolve acceptance.
- Files changed: internal/rebalance/{worker.go,worker_test.go} (new), cmd/strata/workers/{rebalance.go,rebalance_test.go} (new), internal/metrics/metrics.go (RebalancePlannedMovesTotal + RebalanceObserver + Register entry).
- **Learnings for future iterations:**
  - Per-worker metric counter pattern: keep counter bump in the Worker (not in the Emitter) so US-004/US-005 can swap the Emitter for a real mover chain without re-bumping or losing the planned_moves_total signal. Same pattern lifecycle.Worker uses for LifecycleTickTotal.
  - Worker test stub: data.Backend embeds Close() — minimal in-memory backends from internal/data/memory are simpler than hand-rolled stubs and already implement the full interface (PutChunks, GetChunks, Delete, Close, HealthProbe).
  - Worker registration plumbing: Resolve() accepts the registered name automatically once `cmd/strata/workers/<name>.go` runs its init() — no separate STRATA_WORKERS plumbing needed. Tests assert SkipLease=false for non-fan-out workers.
  - Listing objects via meta.Store.ListObjects pagination uses ListOptions{Marker} (not VersionIDMarker) — pull NextMarker off ListResult, not NextKeyMarker which only appears on ListVersionsResult. Same pattern lives in inventory.collectRows / replication.drainBucket.
  - The rebalance scan only walks current versions via ListObjects; non-current versions still have manifests but moving non-current chunks costs disk for almost no operator value and the version-DESC clustering means they'd dominate the scan budget. If a future cycle wants to rebalance prior versions, add a ListAllVersions opt-in flag.
  - Worker iteration_span helper StartIteration is at the rebalance package level (calls strataotel directly) — re-exporting through cmd/strata/workers/iteration_span.go is only useful for cmd-layer Build closures. Most workers call strataotel.StartIteration from their RunOnce.
---

## 2026-05-12 09:50 MSK - US-002
- Implemented placement.PickCluster (already scaffolded in router.go) + PickClusterExcluding; added router_test.go covering nil/empty policy, all-zero weights, zero-weight skip, determinism (replay), distribution {1:1} ~50/50, {1:3} ~25/75, map-iteration-order independence, excluded clusters.
- Added data-ctx helpers WithPlacement/PlacementFromContext + WithObjectKey/ObjectKeyFromContext alongside the existing WithBucketID in internal/data/context.go.
- RADOS Backend.PutChunks (build tag `ceph`): per-chunk pickCluster reads policy+bucketID+objKey from ctx, calls placement.PickCluster, overrides spec.Cluster (pool/namespace inherited from class). Falls back to default when picker returns "" OR picked cluster is not in b.clusters (WARN-logged). ChunkRef.Cluster reflects the routed cluster.
- S3 Backend.PutChunks: clusterForPlacement helper picks target cluster from policy and resolves the bucket on that cluster via bucketOnCluster (first preference: requested class if its Cluster matches; else lex-first ClassSpec routed to picked cluster). Falls back to clusterForClass on empty policy, picker-empty, unknown cluster, or no class registered on picked cluster. BackendRef.Key keeps the existing <bucket-uuid>/<object-uuid> shape.
- s3api callsites threaded via new helper dataCtxForPut(ctx, meta.Store, *meta.Bucket, key): putObject (server.go), uploadPart + uploadPartCopy (multipart.go — uploadPartCopy gained `key` param), copyObject (copy_object.go — now uses dataCtxForPut instead of bare data.WithBucketID). GetBucketPlacement is consulted once per PutChunks; meta errors are WARN-logged + fall back to "no placement".
- Tests: internal/data/placement/router_test.go (unit, no external deps); internal/data/s3/placement_test.go covers (a) 1000-iter distribution 50/50 within 10% across two capturingS3Server instances, (b) per-(bucketID,key) determinism across 4 replays, (c) backwards-compat nil placement routes per class, (d) all-zero policy falls back to class default. go test -race ./internal/data/... ./internal/s3api/... ./internal/adminapi/... green.
- Files changed: internal/data/context.go (helpers), internal/data/placement/router_test.go (new), internal/data/rados/backend.go (placement-aware PutChunks), internal/data/s3/backend.go (clusterForPlacement + bucketOnCluster + PutChunks wired), internal/data/s3/placement_test.go (new), internal/s3api/placement_ctx.go (new helper), internal/s3api/server.go (putObject), internal/s3api/multipart.go (uploadPart, uploadPartCopy signature), internal/s3api/copy_object.go.
- **Learnings for future iterations:**
  - The data.Backend interface is not widened for placement — we thread policy via ctx (data.WithPlacement, data.WithObjectKey, data.WithBucketID) exactly the way US-009 S3-over-S3 threaded bucketID without breaking other backends. Memory backend ignores all three; the contract stays the existing PutChunks(ctx, r, class).
  - meta.Bucket.Placement is populated only by GetBucketPlacement (US-001 memory note). Hot-path PutChunks must call s.Meta.GetBucketPlacement(ctx, b.Name) — NOT rely on b.Placement being filled by GetBucket. The codebase pattern is to keep GetBucket a single-row read and pay for sidecar config blobs only when needed.
  - RADOS placement assumes a uniform pool/namespace layout across clusters within a class — pool/namespace inherit from the class spec, only the cluster id is overridden. Operators who want per-cluster pool overrides need a second class registered on the target cluster (same pattern as the S3 backend's bucketOnCluster).
  - For S3 the requested class's bucket may not live on the picked cluster (when an operator routes one class across two clusters); bucketOnCluster falls back to the lex-first ClassSpec whose Cluster matches the picked id. Missing → fall back to class default. Keep cluster-id sort order for picker determinism — Go map iteration order is random.
  - uploadPartCopy needed the destination key threaded through its signature so dataCtxForPut can stamp WithObjectKey. Previously the function only had src key from x-amz-copy-source. Future multipart helpers should always carry both src + dst keys explicitly — implicit key resolution via mu.UploadID is brittle.
  - Racetest TestRunCleanRunNoInconsistencies is a pre-existing intermittent flake (~1-in-3 fail on local; confirmed by checking out de4188d and reproducing). Don't chase that failure unless it goes from intermittent → consistent.
---

## 2026-05-11 22:12 MSK - US-001
- Implemented meta.Bucket.Placement field + Store CRUD triple SetBucketPlacement / GetBucketPlacement / DeleteBucketPlacement across memory, cassandra (new bucket_placement table), and tikv (BlobPlacement="pl" kind) backends.
- Added meta.ValidatePlacement (weights [0, 100], sum>0, non-empty cluster id) + sentinels meta.ErrInvalidPlacement and meta.ErrUnknownCluster.
- Added meta.EncodeBucketPlacement / DecodeBucketPlacement helpers in quota.go (JSON blob, additive-schema friendly).
- Admin endpoints PUT/GET/DELETE /admin/v1/buckets/{name}/placement wired in adminapi.Server.routes() with audit overrides admin:PutBucketPlacement / admin:DeleteBucketPlacement. Set handler validates weights and (when KnownClusters configured) cluster name resolution.
- OpenAPI spec at internal/adminapi/openapi.yaml gets BucketPlacement schema + /buckets/{bucket}/placement path with get/put/delete.
- adminapi.Config.KnownClusters wired from serverapp.knownDataClusters(cfg) using ParseClusters on STRATA_RADOS_CLUSTERS / STRATA_S3_CLUSTERS; nil for memory backend.
- Contract test caseBucketPlacement covers nil-policy returns (nil, nil), set/get/overwrite/delete/idempotent-delete, validation failures (weight out of range, sum=0, empty cluster id), and ErrBucketNotFound on missing buckets — exercised against memory, cassandra integration, and tikv integration backends via storetest.Run.
- Admin handler unit tests cover NoSuchPlacement on unset, NoSuchBucket on missing bucket, round-trip, invalid weights → InvalidPlacement, unknown cluster id → UnknownCluster with offending ids in message, malformed JSON, idempotent delete.
- Files changed: internal/meta/{store.go,quota.go}, internal/meta/memory/store.go, internal/meta/cassandra/{store.go,schema.go}, internal/meta/tikv/{blobs.go,keys.go}, internal/meta/storetest/contract.go, internal/adminapi/{server.go,openapi.yaml,buckets_placement.go,buckets_placement_test.go}, internal/serverapp/serverapp.go.
- **Learnings for future iterations:**
  - meta.Store.SetBucketPlacement takes `name string` (not bucketID) because the operator wire shape addresses buckets by name. Backends do the GetBucket → ID lookup internally. Mirrors SetBucketVersioning / SetBucketACL shape rather than blob-config-by-bucketID shape.
  - Cluster-name resolution is admin-layer responsibility. meta.Store only validates structure (weights). Plumbing cluster lists into meta would create a data-backend → meta dep that doesn't exist elsewhere in the code base — keep meta backend-agnostic.
  - Cassandra schema: added bucket_placement table via tableDDL (CREATE IF NOT EXISTS — additive, existing keyspaces just create the new table on next start). No ALTER needed because policy is a single-document blob, not a column on buckets.
  - TiKV blob kind constants live in keys.go; added BlobPlacement="pl" (2-byte). Re-use the existing setBucketBlob/getBucketBlob/deleteBucketBlob helpers — no new key shape needed.
  - The Bucket.Placement field carries `json:",omitempty"` so the absence of a policy serialises cleanly across the admin API.
  - serverapp.knownDataClusters parses STRATA_RADOS_CLUSTERS / STRATA_S3_CLUSTERS at startup. Returns nil for memory/dev rigs so the admin handler skips cluster-name resolution and the contract test exercises validation purely.
---

## 2026-05-12 10:05 MSK - US-004
- Implemented internal/rebalance/rados_mover.go: RadosCluster interface (Read/Write/ID — librados-free so unit tests fake it in-memory), RadosMover struct with Clusters/Meta/Region/Logger/Metrics/Tracer/Throttle/Inflight, Move(ctx, []Move) groups by (BucketID, ObjectKey, VersionID) and runs through a bounded errgroup at Inflight. Per-object path: copyChunks → meta.GetObject → buildUpdatedManifest (rejects when live OID/Cluster differs from planned SrcRef — treated as CAS conflict pre-emptively) → meta.SetObjectStorage CAS with expectedClass=currentClass → on success enqueue OLD chunks via meta.EnqueueChunkDeletion, on reject enqueue NEW chunks. Per-move span `rebalance.move_chunk` with attrs strata.rebalance.{bucket,key,from,to,chunk_idx}.
- New shared primitives in internal/rebalance: Mover interface + MoverChain PlanEmitter (partitions plan by ToCluster owner, logs plan, drops orphans with WARN, mover failures stay isolated); Throttle wraps golang.org/x/time/rate.Limiter (Wait(ctx, n) clamps to burst to avoid deadlock; both read + write consume from the same bucket so chunkSize × 2 tokens per move).
- Move struct extended with SrcRef (data.ChunkRef) + Class (storage class) so movers don't re-decode the manifest at move time. Worker scan populates both. MoverMetrics interface adds IncBytesMoved / IncChunksMoved / IncCASConflict — distinct from the existing rebalance.Metrics interface that the Worker uses for IncPlannedMove so the planned vs delivered metrics stay testable in isolation.
- New file internal/data/rados/rebalance.go (build tag `ceph`) exposes RebalanceClusters(*Backend) map[string]rebalance.RadosCluster that returns thin per-cluster facades sharing the Backend's ioctx cache. Read uses Stat→Read to size the buffer exactly; Write uses the same WriteFull op PutChunks uses so the writes are atomic from the cluster's perspective.
- cmd-side wiring: cmd/strata/workers/rebalance.go now constructs a MoverChain emitter seeded with build-tag-conditional movers. rebalance_movers.go (!ceph) returns nil; rebalance_movers_ceph.go (ceph) type-asserts deps.Data to *rados.Backend and builds a RadosMover backed by RebalanceClusters. Throttle is sized to STRATA_REBALANCE_RATE_MB_S * 1MiB (rate + burst); the MoverChain still logs the per-bucket plan when no movers are wired, preserving the US-003 behavior.
- Metrics additions in internal/metrics/metrics.go: strata_rebalance_bytes_moved_total{from,to}, strata_rebalance_chunks_moved_total{from,to,bucket}, strata_rebalance_cas_conflicts_total{bucket}. RebalanceObserver extended with matching methods so the cmd binary stays prom-free.
- Tests: internal/rebalance/throttle_test.go (nil-safe, zero-rate disables, throttle blocks, ctx-cancel, burst-clamp); internal/rebalance/mover_test.go (chain partitions by Owns, drops orphans with WARN, empty-movers no-op, mover failure isolated); internal/rebalance/rados_mover_test.go (happy path with manifest rewrite + OLD-chunk GC enqueue + per-counter metric assertions; CAS-conflict path via mid-flight OID rewrite that flips buildUpdatedManifest to ok=false and routes NEW chunks to GC; unknown-target ToCluster logs + drops; groupMovesByObject batches per (bucketID,key,versionID)); internal/rebalance/rados_mover_integration_test.go (build tags `ceph && integration`) plants 100 chunks via the c1 facade, runs RadosMover.Move c1→c2, asserts manifests flipped + GC queue holds 100 c1 entries.
- Files changed: internal/rebalance/{worker.go, mover.go (new), rados_mover.go (new), throttle.go (new), mover_test.go (new), rados_mover_test.go (new), rados_mover_integration_test.go (new), throttle_test.go (new)}, internal/data/rados/rebalance.go (new, build tag ceph), internal/metrics/metrics.go (3 new counter vecs + observer methods), cmd/strata/workers/{rebalance.go (chain wiring), rebalance_movers.go (new, !ceph), rebalance_movers_ceph.go (new, ceph)}, CLAUDE.md (Background workers section), go.mod (x/time/rate dep promoted to direct require via go mod tidy).
- **Learnings for future iterations:**
  - The CAS reject path matters most when a concurrent client write rewrote a chunk between scan and Move. buildUpdatedManifest does a pre-CAS sanity check (live.Chunks[idx].OID == planned SrcRef.OID && Cluster matches) so we avoid issuing a `IF expected=STANDARD` LWT that would succeed on class-match but corrupt the live manifest by overwriting a newer chunk locator. Treating the live-OID mismatch as a CAS conflict before SetObjectStorage avoids that hazard.
  - PRD said "EnqueueGCEntry" but the actual meta.Store method is `EnqueueChunkDeletion(ctx, region, []data.ChunkRef)`. Use the real method name; same shape works.
  - The build-tag-conditional cmd-layer helper pattern (`rebalance_movers.go` / `rebalance_movers_ceph.go` sharing a `rebalanceMovers(...)` signature) is the canonical way to plug RADOS-only dependencies into the worker without polluting the gateway's interface surface with type assertions. Mirrors how the rados.Backend type itself is build-tag-gated (`backend.go` ceph vs `stub.go` !ceph).
  - The RadosCluster interface intentionally lives in `internal/rebalance` (not `internal/data/rados`) so the unit tests stay librados-free. The ceph-tagged `rados.RebalanceClusters()` constructor returns facades implementing that interface — the rebalance pkg never imports rados, the rados pkg imports rebalance only under the `ceph` build tag.
  - Both read and write debit the same throttle bucket (chunkSize × 2 tokens per move) so the operator's STRATA_REBALANCE_RATE_MB_S represents on-the-wire throughput on the busier leg, not aggregate hot-line capacity. The Throttle.Wait clamps requests to burst to avoid permanent starvation when chunkSize > burst.
  - The MoverChain is the place to add the S3 mover in US-005 — it's a partition-by-Owns dispatcher, so US-005's S3Mover slots in as another entry in the slice, no rewiring at the cmd layer beyond extending rebalance_movers_ceph.go (and adding an equivalent helper for S3-only builds if we ever ship a non-ceph cmd that wants S3 rebalancing).
  - Racetest TestRunCleanRunNoInconsistencies remains the pre-existing flake noted in US-002; not chasing.
---

# Ralph Progress Log
Started: Sun May 10 14:00:00 MSK 2026
Cycle: ralph/race-harness

## Codebase Patterns
- `internal/s3api/race_test.go` is the existing race harness — drives mixed PUT/GET/DELETE/multipart concurrency against in-memory backend. `internal/s3api/race_integration_test.go` is the build-tag-gated integration variant against testcontainers Cassandra. Move shared workload helpers (raceFixture, newRaceClient, raceWorkload) into `internal/racetest/` package; existing tests become thin shims.
- `cmd/` currently holds two production binaries: `strata` (gateway + workers) and `strata-admin` (one-shot ops, currently rewrap). New `cmd/strata-racecheck/` is a developer/CI tool — explicitly NOT counted as a regression on the binary-consolidation goal. Note this in `tasks/prd-binary-consolidation.md` Non-Goals.
- The `make build` target wraps `go build ./...` — adding a new binary needs no Makefile edit, but `make build`-implied artefacts (bin/strata-racecheck) should drop into bin/.
- Race harness drives gateway over HTTP, so `cmd/strata-racecheck` builds without `-tags ceph` — pure HTTP, no librados linkage. Important for portable CI image.
- SigV4 path: harness MUST use static credentials with `STRATA_AUTH_MODE=signed` (NOT `optional`). Streaming SigV4 (`x-amz-content-sha256: STREAMING-AWS4-HMAC-SHA256-PAYLOAD`) is a separate code path with chain HMAC validation; include both at ~50/50 weight per US-003 to exercise both signing flavors.
- `internal/auth/streaming.go` chain HMAC validation (US-022 of prior cycle) — workload should hit it via streaming PUTs to catch regressions in `computeChunkSignature` + `prevSig` chaining.
- Cassandra LWT under sustained concurrent versioning flips is the highest-risk hot path. Workload weights versioning flips at ≥10% of all ops per Technical Considerations.
- Workers (gc, lifecycle, replicator, access-log, notify, inventory, audit-export, manifest-rewriter, quota-reconcile, usage-rollup) are not active during race-soak — single-region, no replication, healthy stack only. Chaos / fault-injection / replication-races are out of scope (future P2).
- `make up-all` brings up the full developer stack: cassandra + ceph + strata + prometheus + grafana. Memory footprint exceeds 7 GB on idle. The cycle adds `make up-all-ci` (compose override `deploy/docker/docker-compose.ci.yml`) that caps Ceph osd_memory_target=1Gi, Cassandra MAX_HEAP_SIZE=2G, drops Prometheus/Grafana, RF=1 — fits ubuntu-latest budget (2 vCPU / 7 GB RAM / 14 GB SSD).
- `/readyz` probe wires up via `internal/health` — readiness gate for both the harness driver script and the CI smoke step. Ceiling 10 min on cold runner pulls.
- Nightly CI runs on free `ubuntu-latest` runners (public repo → unlimited minutes). 90 min job timeout. 03:00 UTC daily cron. NOT triggered on pull_request.
- Report shape: JSON-lines events (`op_started`, `op_done`, `inconsistency`, `summary`). One Markdown summary (`report/SUMMARY.md`) embedded into `$GITHUB_STEP_SUMMARY`. Artefact retention 30 days.
- Verifier oracles: (1) read-after-write — every successful PUT records expected (bucket, key, version_id, etag, size); verifier GETs latest, ETag/size mismatch = `inconsistency` event; (2) versioning monotonicity — after EnableVersioning flip, subsequent PUTs produce monotonically increasing version_id per key; (3) listing convergence — after DeleteObjects batch, deleted keys do NOT reappear in ListObjects within configurable grace window (default 5s).
- Concurrency cap: harness refuses --concurrency > 64 (empirical OOM ceiling on 7 GB ubuntu-latest). Default 32. Plausible nightly throughput 200-500 ops/sec — goal is consistency-violation detection, not benchmarking.
- Baseline file (`docs/racecheck/baseline-2026-XX.md`) is a human-readable record, NOT a test fixture. Nightly runs do not assert against it. Regression-pinning is explicitly out of scope.
- ROADMAP close-flip rule: same commit flips P1 entry to `~~**P1 — Race harness as a real test, not a gate.**~~ — **Done.** ... (commit `<pending>`)` per CLAUDE.md Roadmap maintenance. Closing-SHA backfill follow-up commit on main per established pattern.
- PRD lifecycle: `tasks/prd-race-harness.md` REMOVED in close-flip commit per CLAUDE.md PRD lifecycle rule. Canonical record lives in Ralph snapshot under `scripts/ralph/archive/<YYYY-MM-DD>-race-harness/{prd.json,progress.txt}` after `archive_cycle` fires on `<promise>COMPLETE</promise>`.
- racetest package layout (US-001): `internal/racetest/fixture.go` hosts in-process Fixture + RunScenario + VerifyInvariants + NewClient + DrainBody + CollectManifestOIDs. `runner.go` hosts Config + Report + Run for external HTTP endpoints. `Reporter` interface (Helper/Fatalf/Errorf/Logf) decouples in-process scenario from `testing` import — *testing.T satisfies it. `uploadIDRE` is package-private. Workers/Iters/Keys exported vars with RACE_WORKERS/RACE_ITERS/RACE_KEYS env overrides; per-backend test fixtures pass `racetest.Workers` to NewClient.
- s3api race tests pattern (US-001): per-backend constructor builds *racetest.Fixture and TestRace* delegates to `racetest.RunScenario(t, f); racetest.VerifyInvariants(t, f)`. The shared `harnessMasterProvider` + `testPrincipalHeader` stay in s3api/testutil_test.go; race fixtures wire them on the http handler (auth context shimmed via the principal header).
- strata-racecheck binary pattern (US-002): `cmd/strata-racecheck/main.go` is a thin shell over `racetest.Run`. Flags pre-populated from $AWS_ACCESS_KEY_ID / $AWS_SECRET_ACCESS_KEY / $AWS_DEFAULT_REGION env vars so CI can lean on shell exports. Exit code mapping is part of the binary's contract (0/1/2) — used by `make race-soak` (US-006) and the nightly workflow (US-007) to flip the badge red on inconsistencies vs setup-failure differently. Tests use `run(args, &stdout, &stderr) int` shape (no os.Exit in main flow).
- racetest event sinks (US-002): `EventSink` interface + nopSink/writerSink/fileSink. ReportPath beats EventWriter when both are set. fileSink wraps writerSink with a buffered writer so the 64 KiB bufio buffer absorbs the per-op churn at ~500 ops/sec. Concurrency-safe via single mutex on the underlying io.Writer.
- racetest SigV4 wiring (US-002): aws-sdk-go-v2 v4.NewSigner is the signer. Body is hashed with sha256 (matches the gateway's verifier). Empty AccessKey returns nil signer; anonymous flow stays unchanged so existing in-process tests keep working without auth setup.
- racetest weighted op picker (US-003): Config.Mix map[string]float64 normalised in newOpPicker → cumulative-weight slice → O(log N) per pick. Op-class identifiers exported as const Op* (OpPut/OpGet/OpDelete/OpList/OpMultipart/OpVersioningFlip/OpConditionalPut/OpDeleteObjects). DefaultMix sums to 1.0; unknown keys silently dropped so future class additions don't reject existing configs.
- racetest streaming SigV4 (US-003): signStreaming pre-computes encoded length deterministically (chunk-signature is always 64 hex chars), sets Content-Length + X-Amz-Decoded-Content-Length + X-Amz-Content-Sha256=STREAMING-... BEFORE signing so they're covered by the signature. Parses seed sig out of Authorization header via parseAuthSignature, derives signing key locally (deriveSigningKey duplicated from internal/auth — avoids cyclic dep), encodes one data chunk + zero-length terminator with chained HMAC. signer is nil in anonymous mode → streaming auto-disabled, fixed-payload only.
- DeleteObjects op gotcha (US-003): the dedup-key picker (`for len(seen) < n; idx := rng.Intn(objectKeys); ...`) infinite-loops if n > objectKeys. Always clamp n to min(n, objectKeys) and ≥ 1. Any future "pick N distinct from M" loop should follow the same clamp pattern.
- internal/meta/memory.ListObjects pre-existing race (fixed in US-003): the original code released RLock between sort + per-key versions[0] reads; PutObject mutates head versions in place, so concurrent reads were racy. Fix: hold RLock through the full scan via defer s.mu.RUnlock(). Lesson: any in-memory store reading `slice[0].Field` after releasing the lock is racy if writers can mutate the head.
- Memory backend integration tests (US-003): wiring auth.Middleware{Mode: ModeRequired} + auth.NewStaticStore + s3api.New + httptest.NewServer is the canonical shape for testing the gateway under real signed requests. NewAuthDenyHandler(api.Meta) is required so 4xx responses carry the right shape.
- Tracker shape (US-004): per-(bucket,key) state holds (1) bounded history ring of recent (etag,size,version_id,observedAt) for human-readable diagnostic strings, (2) unbounded allEtags map for the actual validateETag predicate (so a slow earlier write rotated out of the ring still validates), (3) deletedAt timestamp cleared on RecordPut, (4) versionIDs slice for versioning-monotonicity. Pre-flight RecordIntent(md5-of-body) is REQUIRED to absorb the response→RecordPut window; without it the read-after-write oracle false-flags peers' just-landed PUTs.
- Verifier oracle race-absorption pattern (US-004): an oracle that compares gateway state to tracker state will false-flag if the tracker is stale. The fix is two-pronged: (a) RecordIntent before the HTTP call so the tracker pre-knows the etag, (b) the verifier confirms via a secondary HTTP call (HEAD for delete-grace, retry-loop GET for read-after-write) before flagging. Real divergence shows up across both signals; transient race windows resolve naturally.
- Memory ListObjectVersions race (US-004): same shape as the US-003 ListObjects fix — must hold RLock through the full scan because concurrent PutObject mutates the per-key version slice in place. Any in-memory store that reads `bucket[k][i].Field` after releasing the bucket-level lock is racy under -race.

## [2026-05-10 14:30 MSK] - US-001
- Implemented: Carved out `internal/racetest` package and refactored s3api race tests as thin shims.
- Files changed:
  - new: `internal/racetest/fixture.go` — Fixture + RunScenario + VerifyInvariants + NewClient + DrainBody + CollectManifestOIDs + Reporter interface + Workers/Iters/Keys env-driven defaults
  - new: `internal/racetest/runner.go` — Config + Report + Inconsistency + Run(ctx, cfg) external-endpoint entrypoint with MaxConcurrency=64 cap and anonymous HTTP workload (PUT/DELETE/Multipart) for Duration window
  - new: `internal/racetest/runner_test.go` — TestRunSmoke (in-process httptest gateway, asserts ops counted) + TestRunRejectsBadConfig
  - rewrote: `internal/s3api/race_test.go` — now <50 lines: newMemoryRaceFixture(t) -> racetest.Fixture; TestRaceMixedOpsMemory delegates
  - rewrote: `internal/s3api/race_integration_test.go` — Cassandra + TiKV variants delegate to racetest.RunScenario/VerifyInvariants; AllVersions closures match new Fixture signature
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go test -race -run TestRaceMixedOpsMemory ./internal/s3api/` passes (9.17s), `go test -race ./internal/racetest/...` passes (2.07s), full `go test ./...` green.
- **Learnings for future iterations:**
  - Picked Reporter interface (Helper/Fatalf/Errorf/Logf) over importing `testing` so the future strata-racecheck binary (US-002) does not pull testing flags into its main package. *testing.T satisfies it implicitly via Go's structural typing.
  - Workers/Iters/Keys live in racetest as exported vars with env overrides — moved out of s3api package scope, but the names + RACE_* env keys stay identical so existing CI / make targets are not invalidated.
  - The package-level `mustStatus(t, resp, want)` helper used by race_test.go is shadowed by an identically-named helper in `internal/s3api/auth_mode_test.go` — that copy stays in s3api_test (auth tests still need it). The racetest copy is private (lowercase `mustStatus`) so there's no collision when racetest is imported.
  - `harnessMasterProvider` + `testPrincipalHeader` stay in `internal/s3api/testutil_test.go` — racetest does NOT depend on them. The per-backend constructor in race_test.go does the auth wiring.
  - Run is intentionally minimal for US-001 (anonymous HTTP, PUT/DELETE/Multipart only). US-002 wires SigV4 + JSON-lines events, US-003 extends workload to versioning flips + conditional + DeleteObjects, US-004 adds verifier oracle (the Inconsistency event schema is already established).
  - `go vet` triggered on `internal/s3api/testutil_test.go` line 116 `mustWriteFull` — pre-existing diagnostic, not introduced by this story; left alone.
---

## [2026-05-10 16:00 MSK] - US-002
- Implemented: cmd/strata-racecheck standalone binary + racetest events + SigV4 signing.
- Files changed:
  - new: `cmd/strata-racecheck/main.go` — flag parsing, exit-code mapping (0/1/2), human summary printer
  - new: `cmd/strata-racecheck/main_test.go` — TestRunSmokeBinary (in-process gateway, asserts JSONL + stdout summary), TestBadConfigExitsTwo
  - new: `internal/racetest/events.go` — Event struct + EventSink/nopSink/writerSink/fileSink
  - new: `internal/racetest/signer.go` — aws-sdk-go-v2 v4 signer wrapper, anonymous flow when AccessKey == ""
  - rewrote: `internal/racetest/runner.go` — Config gained AccessKey/SecretKey/Region/ReportPath/EventWriter; doSigned/doSignedRead/doSignedReadResp wrap signing + body materialisation; per-op runOp emits op_started/op_done; summary event emitted at end
  - edit: `Makefile` — `make build` produces bin/strata-racecheck
  - edit: `docs/site/content/architecture/migrations/binary-consolidation.md` — added Non-Goals section documenting strata-racecheck exception
  - edit: `scripts/ralph/prd.json` — US-002 passes=true
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go test ./cmd/strata-racecheck/...` passes (1.16s), `go test ./internal/racetest/...` passes (1.10s), `go test -race ./internal/racetest/... ./cmd/strata-racecheck/... ./internal/s3api/...` all green, full `go test ./...` green.
- **Learnings for future iterations:**
  - aws-sdk-go-v2 v4.SignHTTP API: takes (ctx, aws.Credentials, *http.Request, payloadHash hex string, service "s3", region, signingTime). Body is materialised on req.Body via io.NopCloser(bytes.NewReader(body)) BEFORE signing — the SDK does not read req.Body itself, but the http.Client does, and ContentLength must match for SigV4 streaming-vs-fixed-payload routing in the gateway.
  - The aws-sdk-go-v2 signer is stateless after construction — safe to share across all worker goroutines via *signer pointer.
  - Tests use a `run(args, stdout, stderr) int` wrapper (no os.Exit in main flow) so unit tests can drive the binary against an httptest gateway. main() is a 3-line shim that calls os.Exit(run(...)).
  - Exit-code mapping is the binary's contract with US-006/US-007 — 1 means inconsistencies, 2 means infra/setup failure. Workflow author can branch on these distinctly (open issue on 1, retry on 2, etc.). Locked into a `const exitOK/exitInconsistency/exitSetupFailure` block.
  - Event sink layering: writerSink wraps an io.Writer, fileSink embeds writerSink + adds buffered writer + Close-flushes. nopSink discards. ReportPath wins over EventWriter when both set — keeps the binary surface single-knob.
  - The binary-consolidation PRD file no longer exists in tasks/ (deleted at cycle close per PRD-lifecycle rule). Acceptance criterion's "tasks/prd-binary-consolidation.md" reference was satisfied by adding a Non-Goals section to the migration doc at docs/site/content/architecture/migrations/binary-consolidation.md, which is the canonical post-cycle home for the consolidation rule.
  - The per-op summary event mirrors the in-memory Report fields so downstream tools (US-008's summarize.sh) can read either the JSON-lines tail OR the strata-racecheck stdout; both report the same numbers.
  - Signing is opt-in via empty AccessKey: existing TestRunSmoke in internal/racetest/runner_test.go still passes anonymous against an httptest gateway with default auth-mode (optional), no test changes needed.
---

## [2026-05-10 17:30 MSK] - US-003
- Implemented: Workload extension to 8 op classes, weighted Mix picker, streaming SigV4 PUT path.
- Files changed:
  - new: `internal/racetest/workload.go` — opPicker (weighted random over classes), workerCtx + runOnce dispatch, doSigned/doSignedRead/doSignedReadResp transport helpers, doMultipartCycle (moved from runner.go), doPUTBody (streaming/fixed routing for body-carrying ops)
  - rewrote: `internal/racetest/runner.go` — Config gained Mix map[string]float64 + StreamingRatio float64; DefaultMix exported (8 classes, every ≥5%, versioning_flip = 10%, sums to 1.0); op-class consts exported; Run wires the picker + workerCtx; per-class counters use the canonical allOpClasses list
  - rewrote: `internal/racetest/signer.go` — added signStreaming with manual aws-chunked encoding (chunk-signature is always 64 hex chars → encodedLen pre-computable); parseAuthSignature pulls seed sig from Authorization header; deriveSigningKey + computeChunkSignature duplicated from internal/auth to avoid dep cycle
  - rewrote: `internal/racetest/runner_test.go` — TestRunSmoke now asserts all 8 classes > 0; TestRunRejectsBadConfig extended with StreamingRatio>1 and negative-mix-weight cases; new TestRunStreamingSigV4 wires auth.ModeRequired + StreamingRatio=1.0 and asserts streaming PUT + conditional-PUT both reach 200
  - edit: `cmd/strata-racecheck/main.go` — printSummary lists all 8 classes in canonical order with stable formatting
  - edit: `cmd/strata-racecheck/main_test.go` — TestRunSmokeBinary stdout assertion extended to all 8 classes; --duration bumped to 1500ms so even 5%-weighted classes hit at p~1
  - fix: `internal/meta/memory/store.go` — ListObjects now holds RLock through the entire scan (defer s.mu.RUnlock()); pre-existing race between versions[0] reads after RUnlock and concurrent PutObject head-mutations was caught by the new -race coverage
  - edit: `scripts/ralph/prd.json` — US-003 passes=true with full notes
- Quality checks:
  - `go vet ./...` clean
  - `go build ./...` clean
  - `go test -race -count=1 ./internal/racetest/... ./cmd/strata-racecheck/... ./internal/s3api/` all green (~20s)
  - `go test -count=1 ./...` all green (~110s)
- **Learnings for future iterations:**
  - Streaming SigV4 over aws-sdk-go-v2 v4.SignHTTP works by passing payloadHash=STREAMING-AWS4-HMAC-SHA256-PAYLOAD; the SDK signs whatever headers are on the request at sign-time, so set X-Amz-Content-Sha256, X-Amz-Decoded-Content-Length, Content-Length BEFORE calling SignHTTP. Seed signature lives in the Authorization header's "Signature=" field — parse it back out and chain chunks manually.
  - The chunk-signature is always exactly 64 hex chars, so the encoded body length is deterministic from len(body). Pre-compute it via streamingEncodedLen() and set Content-Length to that value before signing. After encoding, sanity-check that len(encoded) == encodedLen — catches off-by-one in the formula.
  - Avoid an internal/auth dep on internal/racetest (or vice versa) by duplicating the small SigV4 primitives (deriveSigningKey, computeChunkSignature, sigTerminator, emptyBodyHash) into racetest/signer.go. They are stable AWS-spec values — no drift risk.
  - Weighted op picker must drop unknown class names (typos in user-supplied Mix) silently rather than rejecting — keeps user-config forward-compat across new op-class additions in future PRD iterations. Negative weights still reject loudly (config error, not silent skew).
  - "Pick N distinct from M" loop pattern: ALWAYS clamp N to min(N, M) before entering the dedup loop. Otherwise a single shrunken bucket pool turns the workload into a hang. The bug was caught instantly in the smoke test (60s timeout) but easy to miss under longer durations.
  - Pre-existing memory-store ListObjects race surfaced once the harness gained OpList. Pattern: any code that reads `bucket[k][0].Field` after releasing the bucket-level lock is racy if concurrent writers mutate the head version in place. Hold the RLock through the entire scan in test-only stores; production stores use real CQL/TiKV transactions and don't have this issue.
  - The `auth.Middleware{Mode: ModeRequired} + auth.NewStaticStore({ak: cred}) + s3api.New` shape is the canonical wiring for racetest-style external-endpoint tests against signed gateways. Wraps with mw.Wrap(api, s3api.NewAuthDenyHandler(api.Meta)) so 4xx errors render correctly.
  - aws-sdk-go-v2 v4 SignHTTP is stateless after construction — share one *signer across all worker goroutines via pointer; signing is concurrent-safe.
---

## [2026-05-10 17:00 MSK] - US-004
- Implemented: read-after-write + listing-convergence + versioning-monotonicity oracles for the racetest harness.
- Files changed:
  - new: `internal/racetest/tracker.go` — Tracker with per-(bucket,key) state (history ring + unbounded allEtags + deletedAt + versionIDs); RecordIntent pre-flight md5-of-body, RecordPut on PUT response, RecordDelete + RecordBatchDelete; Flag emits JSON-lines `inconsistency` events; Snapshot copies the slice for Report.Inconsistencies; pendingExpiredDeletes / trackedKeys / versioningEnabled / expectedVersionIDs accessors; DefaultDeleteGrace=5s
  - new: `internal/racetest/verifier.go` — periodic verifier (Config.VerifyEvery cadence, default 1s) running three oracles per bucket per tick: checkReadAfterWrite (≤8 keys, persistent-mismatch retry loop 5×100ms), checkVersioningPresence (?versions for one tracked key, asserts every recorded version_id present, skipped on non-versioned buckets), checkDeleteGrace (HEAD-confirmed ListObjectsV2 absence assertion); final pass on ctx cancel
  - new: `internal/racetest/oracle_test.go` — TestTrackerValidateETag, TestRunCleanRunNoInconsistencies (clean run, zero false positives), TestVerifierFlagsETagMismatch / TestVerifierFlagsResurrectedDelete / TestVerifierFlagsMissingVersion (faultProxy injects each fault class), TestRejectsNegativeGrace
  - edit: `internal/racetest/runner.go` — Config gained VerifyEvery + DeleteGracePeriod; Inconsistency Expected/Observed/RequestID fields populated; Run wires Tracker + verifier goroutine; pre-enables versioning per bucket; drain budget = grace + verEvery so deletedAt timestamps age past grace before the final pass
  - edit: `internal/racetest/workload.go` — recordIntent before each PUT/conditional PUT; recordPutIfOK on 200 PUT (etag from header, vid from X-Amz-Version-Id); doMultipartCycle returns (ok, etag, vid, size) for tracker; OpDelete records delete on 204/200; OpDeleteObjects parses <Deleted> keys and falls back to requested set; tracker accessor for shouldStream
  - edit: `internal/racetest/events.go` — Event.Inconsist *Inconsistency field for the JSON-lines `inconsistency` event payload
  - fix: `internal/meta/memory/store.go` — ListObjectVersions holds RLock through the entire scan (mirror of US-003 ListObjects fix); concurrent PutObject mutates per-key version slices in place, racy without it once the verifier's checkVersioningPresence joins the harness
  - edit: `scripts/ralph/prd.json` — US-004 passes=true with full notes
- Quality checks: `go vet ./...` clean, `go build ./...` clean, `go test -race -count=2 -timeout=300s ./internal/racetest/` green (88s), full `go test -count=1 -timeout=600s ./...` green (~46s for racetest, full suite ~110s). One transient lifecycle/TestThreeReplicaDistribution flake re-ran clean — unrelated to this story.
- **Learnings for future iterations:**
  - The delete-grace oracle's first cut (ListObjectsV2-only) false-flagged on the clean-run test under default-mix workload. Root cause: tracker.deletedAt is updated in worker-call order, while gateway-side commit order is independent. A peer worker that PUT after a DeleteObjects can leave deletedAt set in the tracker while the gateway treats the key as live. Fix: the verifier HEAD-confirms before flagging — HEAD=200 absorbs the race, anything else (404, 405 for delete-marker latest, transport error) is the divergence we want. Pattern to remember: any oracle that compares tracker state vs. gateway state must be defensive against worker-call-order vs. commit-order skew.
  - RecordIntent (md5 of body, called BEFORE the HTTP put) is what makes the read-after-write oracle workable. Without it, a peer worker's PUT response can land between when the tracker records its OWN intended etag (post-response) and when the verifier samples the same key — verifier sees a "valid" etag the tracker doesn't yet know about. Pre-flight registration closes the window.
  - The verifier's persistent-mismatch retry loop (5 × 100ms) is calibrated for the in-process test gateway. On a real network gateway the absorption window may need tuning via Config — but the default is correct for unit tests so don't pessimise unless evidence forces it.
  - Inconsistency.Expected pastes the recent etag history (capped at trackerHistoryCap=32) as a diagnostic string rather than the full allEtags set; the unbounded set is fine for predicate evaluation but unmanageable in a triage payload. The first 32 are usually enough to spot a corruption signature.
  - DeleteGracePeriod=0 is the explicit "use default" sentinel; <0 is rejected at config validation. The runner's drain budget = grace + verEvery; without that, deletedAt timestamps don't age past grace before the verifier's final pass and the listing-convergence oracle never has a chance to flag a real divergence at the tail.
  - JSON-lines events: the `inconsistency` event reuses the Inconsistency struct via an Event.Inconsist *Inconsistency pointer. Wire this into events.go alongside the existing Summary map[string]any so summarize.sh (US-008) can group by Kind without re-parsing the diagnostic string.
  - faultProxy pattern: wrap the upstream gateway in an httptest.Server that intercepts specific request shapes (?list-type=2, ?versions, GET object) and forges a malformed response. Keeps oracle tests honest without writing a broken meta backend. Each fault knob is mutually exclusive — exactly one set per test.
---

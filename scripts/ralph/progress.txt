# Ralph Progress Log
Started: Mon May 18 18:10:00 MSK 2026
Cycle: ralph/drain-rebalance-transparency

## Codebase Patterns
- 5-story cycle. Two parked + freshly-surfaced operator-transparency gaps merged into one cycle: (1) parked from `ralph/drain-progress-physical` — Awaiting GC chip ships with static-copy tooltip; operators with non-default GC envs get prose that doesn't match their cadence. (2) Newly surfaced 2026-05-18 — rebalance bandwidth invisible to operators (`STRATA_REBALANCE_RATE_MB_S` configurable but no UI/endpoint exposure → operator has no way to confirm migration won't saturate network).
- **Unified fix**: two parallel read-only admin endpoints (`GET /admin/v1/gc-config` + `/admin/v1/rebalance-config`) expose resolved tunables; `<DrainProgressBar>` consumes both for live ETA on Awaiting GC chip + live bandwidth on Migrating chip; Cluster Overview gains rebalance config card; operator runbook gets `Bandwidth tuning` section with network-share calculator table.
- **Shared infrastructure** (US-001): both endpoints share handler infra in `internal/adminapi/config_endpoints.go` (or fold into existing file — verify convention). Both audit-stamped, read-only, env-static. `GCConfig` + `RebalanceConfig` structs on `adminapi.Server`, wired through `adminapi.Config` for unit-test injection.
- **GC values source** (US-001): `cmd/strata/workers/gc.go::clampShards` neighbors. Boot-time resolved env captured.
- **Rebalance values source** (US-001): `cmd/strata/workers/rebalance.go::buildRebalance` env reads. Same pattern. Plus `replicas_count` derived from `meta.Store.ListClusterNodes(ctx)` filtered to live heartbeats (within `2 × STRATA_HEARTBEAT_INTERVAL` of now) — NOT static.
- **Response shape** (US-001): int seconds, not Go duration strings. Both endpoints return flat JSON; UI does `seconds / 60` for minutes.
- **Counter** (US-001): `strata_admin_config_endpoint_errors_total{endpoint}` with label `endpoint ∈ {gc-config, rebalance-config}`. Increments on internal error (e.g. cluster_nodes scan fails).
- **UI 2-query pattern** (US-002): `<DrainProgressBar>` fetches BOTH `/admin/v1/gc-config` + `/admin/v1/rebalance-config` via TanStack Query, both `staleTime: Infinity` (env static at boot).
- **Awaiting GC ETA formula** (US-002): `eta_min = ceil(grace_seconds / 60) + ceil(gc_queue_pending / (batch_size × shards × 1) × (interval_seconds / 60))`. Denominator clamped to 1 if zero. Cap at `~24h+` if > 24h.
- **Migrating bandwidth row** (US-002): Y = `rate(strata_rebalance_bytes_moved_total{to=<cluster_id>}[1m])` via existing `STRATA_PROMETHEUS_URL` plumbing. Z (ETA) = `(remaining × chunk_size) / observed_rate` if observed > 0; else fallback `(remaining × chunk_size) / (rebalance_config.rate_mb_s × replicas_count / 2)` (effective forward estimate).
- **Chip label formats** (US-002): Awaiting GC → `Awaiting GC cleanup: X chunks awaiting physical delete (~N m ETA)` (or `~Nh Mm` if ≥ 60). Migrating → `Migrating: X chunks remaining · ~Y MB/s observed · ~Zm ETA`.
- **Graceful fallback** (US-002): when either query loading/errors → pre-cycle static behavior. PromQL null/0 (cold) → formula fallback ETA + observed row reads `~0 MB/s observed (cold start)`.
- **Cluster Overview card** (US-003): `<RebalanceConfigCard>` renders four config rows (`Per-replica rate / Aggregate (×N replicas) / Effective forward / Cadence + Inflight + Shards`) + live bandwidth row below. Tooltip explains chunkSize × 2 tokens math. Tune button → `docs/site/content/best-practices/placement-rebalance.md#bandwidth-tuning` (US-004 anchor).
- **TanStack key SHARED** (US-002 + US-003): `rebalance-config` query used by both `<DrainProgressBar>` AND `<RebalanceConfigCard>`. Single fetch per app session.
- **Card hides on 404** (US-003): legacy gateway compatibility — no error toast, card just not rendered.
- **Runbook section** (US-004): `## Bandwidth tuning` anchored `#bandwidth-tuning`. Token-bucket model explanation + network-share calculator table (1 GbE / 10 GbE / 25 GbE / 100 GbE → safe per-replica rates + aggregate + effective forward + % of pipe). Two concrete tuning workflows (lab + prod). References to live observability + admin endpoint health-check.
- **Smoke** (US-005): extend existing `scripts/smoke-drain-progress-ui.sh` (already configures `STRATA_REBALANCE_RATE_MB_S=1` + `STRATA_GC_GRACE=60s`). New steps: call `GET /admin/v1/gc-config` after Awaiting GC, assert response contains grace_seconds:60; compute eta_min from formula + polled gc_queue_pending, assert within sane band; call `GET /admin/v1/rebalance-config`, assert rate_mb_s:1 + replicas_count:2; during Migrating sample Prom rate query, assert > 0 MB/s.
- **ROADMAP close-flip × 2** (US-005): existing parked P3 `Precise drain-progress ETA from gateway GC tunables` → Done; NEW P3 entry added + closed `Rebalance bandwidth visibility on Cluster Overview + drain progress` (operator-requested live; not pre-roadmapped per CLAUDE.md `Discovering a new gap` rule).
- **Existing surface preserved**: `strata_rebalance_bytes_moved_total` counter already exists (per CLAUDE.md placement-rebalance cycle). `STRATA_PROMETHEUS_URL` plumbing already wired in `/admin/v1/clusters/{id}/rebalance-progress`. Reuse both.
- **Cassandra meta backend unchanged**: cycle touches only adminapi handlers + web UI + docs. `internal/meta/cassandra/**` untouched. `make test-integration` (testcontainers) preserved.
- **Pre-cycle hotfixes already on main** (do NOT redo):
  - `470e8de` adminapi multipart Complete/Abort panic on object keys with spaces (`httptest.NewRequest` → `http.NewRequestWithContext` + url.URL escaping)
  - `516b9bc` RADOS ClusterStats / ClusterObjectCount probe fallback + strata-pool filter (operator-visible `physical_chunks_on_cluster=0 + physical_bytes_on_cluster=515264` regression; .mgr ceph internal masked as strata data)
  - `3067214` rebalance default 1h → 5m + lab compose 30s override + env passthrough for STRATA_REBALANCE_RATE_MB_S / INFLIGHT
  - `061424a` strata compose admin auth defaults restored (`optional` + admin:adminpass:owner) post-collapse cycle regression
- **End-to-end discipline** (per `feedback_cycle_end_to_end.md`): walkthrough covers happy path (drain with both ETAs visible + bandwidth indicator) + operator network-share concern walkthrough (1 GbE operator picks safe rate via runbook table) + edge cases (either endpoint errors, PromQL unavailable, mid-drain env change via gateway restart, GC_GRACE=0, GC_GRACE=999h, RATE_MB_S=0, multi-replica >2, sub-60s migrations before Prom window settles). State Truth Tables: chip rendering matrix + endpoint response shapes. Cache Invalidation Ledger: 2 new TanStack keys (staleTime: Infinity), no new server caches. Safety Claims Preconditions: ETA + bandwidth non-safety (deregister_ready unchanged); endpoints GET-only; degenerate inputs clamped; card hides on 404 (back-compat).

## 2026-05-19 - US-002
- Wired `<DrainProgressBar>` to /admin/v1/{gc,rebalance}-config + reused /admin/v1/clusters/{id}/rebalance-progress.observed_bytes_per_sec for live ETA + bandwidth
- Awaiting GC chip: live ETA suffix (`~Nm ETA` / `~Nh Mm ETA` / `~24h+ ETA`) when gc-config resolves; dynamic tooltip enumerates grace/interval/batch/shards. gc-config 404 / loading → static pre-cycle tooltip
- Migrating chip: observed bandwidth row `~Y MB/s observed` (cold-start variant `~0 MB/s observed (cold start)`) + formula ETA `~Zm ETA`. Either query loading/errored → pre-cycle `~Nm` fallback from drain-progress.eta_seconds
- ETA formula: migratable × 4 MiB (CHUNK_SIZE_BYTES) over observed_bytes_per_sec (live) or (rate_mb_s × replicas_count × 1 MiB / 2) (fallback); Infinity → ~24h+ ETA cap (rate_mb_s=0 or replicas_count=0)
- Backend support already staged in working tree: observed_bytes_per_sec added on /admin/v1/clusters/{id}/rebalance-progress (sum(rate(strata_rebalance_bytes_moved_total{to="<id>"}[1m]))) — committed alongside this story
- Files changed:
  - web/src/api/configs.ts (new — fetchGCConfig + fetchRebalanceConfig)
  - web/src/api/client.ts (re-export configs)
  - web/src/api/clusters.ts (+observed_bytes_per_sec on ClusterRebalanceProgress)
  - web/src/lib/query.ts (+gcConfig +rebalanceConfig keys)
  - web/src/components/storage/DrainProgressBar.tsx (queries + helpers + MigratingSummary)
  - web/e2e/drain-progress.spec.ts (+6 scenarios — live ETA + 404 fallback + cold-start + rate_mb_s=0 cap + pre-cycle fallback)
  - internal/adminapi/clusters_rebalance.go (+ObservedBytesPerSec field + Prom 1m bytes-rate query)
  - internal/adminapi/clusters_rebalance_test.go (+observed_bytes_per_sec assertion)
  - internal/adminapi/openapi.yaml (+observed_bytes_per_sec required field)
- **Learnings for future iterations:**
  - Reusing the existing `/admin/v1/clusters/{id}/rebalance-progress` endpoint avoided a second PromQL plumbing pass on the browser side — the Go handler already had `STRATA_PROMETHEUS_URL` wiring; adding a fourth `prom.Query` call (the 1m bytes-rate) was a 5-line change. The browser stays free of CORS / auth headaches and Prom URLs are an internal-only concern.
  - `useQuery` with `staleTime: Infinity` + `refetchInterval: false` + `refetchOnWindowFocus: false` + `retry: false` is the right shape for env-static admin endpoints — a single fetch per session, no polling, no retry storm if the gateway is briefly 5xx. Plus `meta: { silent: true }` so a 404 on a legacy gateway never produces a toast — the consumer chip simply falls back to pre-cycle copy.
  - The cluster page uses 3 useQuery hooks in `<DrainProgressBar>` already; adding 3 more (gc-config, rebalance-config, rebalance-progress) doesn't add fetch storm because:  (a) staleTime: Infinity dedupes the 2 config keys across all mounted DrainProgressBar instances + the upcoming RebalanceConfigCard (US-003);  (b) the existing RebalanceProgressChip uses the same `queryKeys.clusterRebalance(id)` key per cluster, so DrainProgressBar shares cache + poll cadence with it.
  - When a Playwright spec mocks an admin endpoint via `page.route()`, default-behavior of returning 404 (omitted route) doesn't surface in DevTools but `useQuery` does mark the query as errored — handy for testing the "back-compat fallback" path without writing a separate spec case.
  - Test mocking: don't compare flaky timestamps in chip tooltips — assert `RegExp` on the tooltip attribute with the constant input values (e.g., `STRATA_GC_GRACE \(300s\)` — the operator-facing label is the contract).
  - The Migrating chip's chunks-remaining headline still reads from `primary` (physical when available else manifest) — the operator sees the pool-level count; the ETA / observed-bandwidth row uses `migratable` because that's what the rebalance worker actually moves. The two semantics are deliberately different and both shown so the operator can correlate them.
---

## 2026-05-18 - US-001
- Implemented `GET /admin/v1/gc-config` + `/admin/v1/rebalance-config` read-only endpoints
- Added `GCConfig` + `RebalanceConfig` JSON types in `internal/adminapi/config_endpoints.go`; both audit-stamped (`admin:GetGCConfig` / `admin:GetRebalanceConfig`)
- Added `ResolveGCConfig` / `ResolveRebalanceConfig` exported helpers in `cmd/strata/workers/{gc,rebalance}.go` that mirror buildGC/buildRebalance env reads + clamping; serverapp wires both at boot
- Added counter `strata_admin_config_endpoint_errors_total{endpoint}` with `endpoint ∈ {gc-config, rebalance-config}` in `internal/metrics/metrics.go`
- `replicas_count` derived per request from `Heartbeat.ListNodes` filtered to `2 × HeartbeatInterval` window (default 20 s); ListNodes failure bumps the counter and returns 0 (UI fallback path)
- OpenAPI spec gains schemas + paths for both endpoints
- Files changed:
  - internal/metrics/metrics.go (new counter + register)
  - internal/adminapi/server.go (new fields on Server/Config + routes)
  - internal/adminapi/config_endpoints.go (new file — handlers + types)
  - internal/adminapi/config_endpoints_test.go (new — unit tests)
  - internal/adminapi/openapi.yaml (new schemas + paths)
  - cmd/strata/workers/gc.go (ResolveGCConfig + ResolvedGCConfig)
  - cmd/strata/workers/gc_test.go (env + defaults tests)
  - cmd/strata/workers/rebalance.go (ResolveRebalanceConfig + ResolvedRebalanceConfig + clampRange)
  - cmd/strata/workers/rebalance_test.go (env / defaults / clamp tests)
  - internal/serverapp/serverapp.go (call ResolveGCConfig / ResolveRebalanceConfig at boot and pass through adminapi.Config)
- **Learnings for future iterations:**
  - Audit stamping pattern: every admin handler grabs `owner := auth.FromContext(ctx).Owner` and calls `s3api.SetAuditOverride(ctx, "admin:<Verb>", "<resource>", "-", owner)` BEFORE writing the response. Verb is the Capitalized HTTP action, resource is the operator-facing label (here `gc-config` / `rebalance-config`); bucket field carries `-` for non-bucket resources.
  - Per-package metric counters live in `internal/metrics/metrics.go` and are registered in the single `Register()` call — adding one without appending to the `MustRegister(...)` list silently drops it. The labelset must be deterministic at startup; even labels that "today" are never populated (the gc-config side) should still be reachable so future expansion doesn't change the metric shape.
  - The Heartbeat store interface only exposes `ListNodes(ctx)` — there's no pre-filtered "live nodes" helper. Inline the freshness check (`now.Sub(lastHeartbeat) <= ttl`) with `IsZero()` guard. Default TTL = `2 × HeartbeatInterval` (20 s on default 10 s cadence); fall back to `2 × heartbeat.DefaultInterval` if the server has no HeartbeatInterval configured.
  - `dto.Metric` from `prometheus/client_model/go` is the in-test counter reader: call `counter.Write(&m)` then check `m.Counter.GetValue()`. The existing `readCounter` helper in `clusters_drain_progress_physical_test.go` follows this pattern — name new helpers distinctly (here `readConfigErrorCounter`) to avoid same-package collision.
  - `cmd/strata/workers` and `internal/adminapi` cannot import each other (cmd is the binding layer), but the binding via `serverapp` can mediate by reading from one package and shaping into the other's struct. Keeps the snapshot lock-step with worker constructors without leaking adminapi types into the worker init code.
  - When a handler degrades gracefully (here: ListNodes failure → 200 with replicas_count=0 + counter increment), bake that into a unit test that mocks an `errHeartbeatStore` and asserts both the response shape AND the counter delta — the AC explicitly calls out "endpoint errors counter" so the assertion is load-bearing.
---


## 2026-05-19 - US-003
- Added GET /admin/v1/rebalance-bandwidth: cluster-wide 1m roll-up via sum(rate(strata_rebalance_bytes_moved_total[1m])) + sum(rate(strata_rebalance_chunks_moved_total[1m])). Audit admin:GetRebalanceBandwidth. metrics_available=false on Prom unset/err — UI hides observed row
- Built <RebalanceConfigCard> mounted on Overview page under NodesTable. Four config rows (Per-replica rate / Aggregate (× N replicas) / Effective forward / Cadence + Inflight + Shards). chunkSize × 2 tokens tooltip on Aggregate + Effective. Tune link → /docs/best-practices/placement-rebalance/#bandwidth-tuning (US-004 anchor). Live observed row Observed last 1m: ~Y MB/s · ~Z chunks/sec gated on metrics_available=true. Entire card hidden on rebalance-config 404 (legacy gateway compat)
- TanStack key `rebalanceConfig` shared with <DrainProgressBar> (staleTime: Infinity) — one fetch per app session. New `rebalanceBandwidth` key polls every 30s aligned with rebalance-progress cadence
- Files changed:
  - internal/adminapi/rebalance_bandwidth.go (new handler + RebalanceBandwidthResponse type + buildRebalanceBandwidth)
  - internal/adminapi/rebalance_bandwidth_test.go (new — happy path + Prom-unavailable + Prom-err)
  - internal/adminapi/server.go (route registration)
  - internal/adminapi/openapi.yaml (+RebalanceBandwidth schema + /rebalance-bandwidth path)
  - web/src/api/configs.ts (+fetchRebalanceBandwidth + RebalanceBandwidth type)
  - web/src/api/client.ts (re-exports)
  - web/src/lib/query.ts (+rebalanceBandwidth key)
  - web/src/components/storage/RebalanceConfigCard.tsx (new)
  - web/src/pages/Overview.tsx (mount RebalanceConfigCard under NodesTable)
  - web/e2e/cluster-overview-rebalance.spec.ts (new — 3 scenarios: full render / metrics_available=false hides observed row / rebalance-config 404 hides card)
- **Learnings for future iterations:**
  - Aggregate PromQL roll-ups (no `to=<id>` filter) belong in a new admin endpoint, NOT inline in the UI — the gateway already owns Prom credentials + URL plumbing, and adding a third PromQL pass to clusters_rebalance.go would have re-used the per-cluster filter accidentally. New file `rebalance_bandwidth.go` mirrors the clusters_rebalance.go degrade-to-200 contract: `metrics_available=false` on Prom unset OR err so the UI never hard-fails on outage.
  - Card-hidden-on-404 is implemented as `if (cfgQuery.isError) return null;` BEFORE the loading branch — `meta: { silent: true }` keeps the toast suppressed, `retry: false` short-circuits the failure into the error state. Loading branch still renders a skeleton so the layout stays stable while the env-static config resolves on first paint.
  - The `rebalanceConfig` key is shared verbatim between <DrainProgressBar> and <RebalanceConfigCard>; both pass `staleTime: Infinity` + `refetchInterval: false` so the second mount hits the TanStack cache and never re-fetches. Don't override `staleTime` per-consumer — that breaks the sharing contract.
  - Tune docs link uses Hugo's path shape `/docs/best-practices/placement-rebalance/#bandwidth-tuning` (trailing slash before `#`), not the source-tree `.md#anchor` shape. The PRD AC names the source path; the live link must use the served path.
  - Counter math validation in tests: 50 × 1024 × 1024 (52,428,800) is exactly 50 MiB/s; UI formatter rounds `>=100` → `toFixed(0)` so `~52` would surface as `~52 MB/s`. Use `52428800` → 50 MiB → formatted "50 MB/s" to keep test expectations stable across MB-vs-MiB rounding bugs.
  - Pure roll-up endpoints (no path params, no body) get audit verb `admin:Get<Name>` with resource `<endpoint-suffix>` and bucket `-`. The Audit middleware override pattern stays uniform across config + bandwidth endpoints.
---

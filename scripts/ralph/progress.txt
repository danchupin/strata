# Ralph Progress Log
Started: Tue May 12 19:00:00 MSK 2026
Cycle: ralph/drain-lifecycle

## Codebase Patterns
- Server-side drain primitives shipped by `ralph/placement-rebalance` (merged at 972194f): `cluster_state` row + `POST /admin/v1/clusters/{id}/drain|undrain` + `placement.PickClusterExcluding(policy, draining)` skips draining clusters in the weight wheel. Fail-open fallback to `spec.Cluster` (class default from STRATA_RADOS_CLASSES) is the gap closed by US-002 strict mode.
- UI clusters surface shipped by `ralph/placement-ui` (merged at 79e2583): `<ClustersSubsection>` on Storage page + `<ConfirmDrainModal>` typed-confirmation + `<RebalanceProgressChip>` per cluster + AppShell `<PlacementDrainBanner>`. This cycle extends the cluster card with `<DrainProgressBar>` + deregister-ready chip + strict-mode chip — reuse the same `Card` / `Badge` / `Tooltip` primitives from `web/src/components/ui/`.
- Existing rebalance worker (`internal/rebalance/Worker`) ticks every `STRATA_REBALANCE_INTERVAL`, scans every bucket with a `Placement` policy via `meta.Store.ListBuckets` → `ListObjects`, plans + executes per-chunk moves. US-003 extends the same scan loop: for every draining cluster, also count `BackendRef.Cluster == draining-id` matches; cache in `progressCache map[clusterID]ProgressSnapshot{Chunks, Bytes, LastScanAt, BaseChunksAtStart, CompletionFiredAt}`. The endpoint reads from cache — never scans synchronously.
- `data.PoolStatus.Cluster` field (from placement-ui US-001) currently reflects class-env routing config, NOT actual byte distribution. US-001 of this cycle fixes that: DataHealth iterates `(cluster, distinct-pool)` matrix instead of `class → spec.Cluster`. After fix the Pools table shows actual per-cluster bytes; multi-cluster lab will surface 6 rows (2 clusters × 3 pools) instead of 3.
- `data.ClusterStatsProbe` already exists (from placement-rebalance US-006) — RADOS impl runs `MonCommand({"prefix":"df"})`, S3 returns `ErrNotSupported`. US-001 extends usage: also need `GetPoolStats(pool)` per cluster — RADOS has `(*IOContext).GetPoolStats()` from the storage-status cycle, just need to call it per-cluster ioctx.
- Class label aggregation precedent: existing memory + S3 backend convention is comma-joined sorted list when multiple classes map to one pool (`internal/data/memory/backend.go` already does this). Mirror in the new RADOS DataHealth matrix.
- HTTP error convention: 503 with `<Code>DrainRefused</Code>` mirrors existing AWS-style error shapes in `internal/s3api/errors.go`. New sentinel `data.ErrDrainRefused` (with attached cluster ID) maps via `apiErrorFromBackendErr` (or whatever the existing converter is — check `internal/s3api/server.go`). Retry-After header is set on the response writer per S3 SDK retry convention.
- Strict mode env: `STRATA_DRAIN_STRICT` parsed at Config boot, NOT per-request — change requires restart. Default `off` for backwards compat. Validation: `on` / `off` / `true` / `false`; unknown → fail-fast at gateway start.
- Strict refusal ONLY on PUT chunks. Critical: do NOT touch GET / HEAD / DELETE / multipart Complete-Abort / List paths — drain semantic is stop-write, not stop-read. Existing chunks on the drained cluster must remain readable until rebalance moves them or operator deregisters.
- Drain completion idempotency: `progressCache.CompletionFiredAt` timestamp gate. Transition `>0 → 0` fires the event ONCE; if cluster fills + drains again → new transition → fires again. Test both shapes.
- Notify event `s3:Drain:Complete`: new event type, mirrors existing `s3:Replication:Complete` / `s3:LifecycleExpiration:Delete` patterns. Best-effort: notify worker failure must NOT block the rebalance worker (log warn, continue).
- ROADMAP close-flip in US-006: TWO entries (Pools matrix P3 + Drain strict P3). Both flip in the same commit with `(commit \`<pending>\`)`. Closing SHA backfilled in follow-up commit on main per CLAUDE.md convention.
- `tasks/prd-drain-lifecycle.md` REMOVED in US-006 per CLAUDE.md PRD lifecycle rule (Ralph snapshot under `scripts/ralph/archive/` is canonical).
- Verification per story: `go vet ./...`, `go test ./... -race`, `make docs-build` (US-006 only); `pnpm run build` + dev-browser verify on US-004 (UI changes).
- Lab stand context (uncommitted host-side, see `deploy/docker/docker-compose.yml` mods on the prep branch): two RADOS clusters `default` + `cephb` already running; pool layout identical on both. `--profile multi-cluster` brings up `strata-multi:9998` with `STRATA_RADOS_CLUSTERS=default:...,cephb:...` set. Three demo buckets exist (`demo-split` with policy `{default:1,cephb:1}`, `demo-cephb` with `{cephb:1}`, `demo-default` without policy). Integration tests in this cycle should target the same `multi-cluster` profile or simulate via the in-memory test rig.
- **End-to-end discipline (per `feedback_cycle_end_to_end.md`)**: this cycle is the FIRST to apply the User Journey Walkthrough discipline. Read the Walkthrough section of `tasks/prd-drain-lifecycle.md` before starting any story — every story must serve at least one walkthrough step. Final story (US-007) is gated on the smoke script passing the full 15-step walkthrough + 4 negative paths. "Acceptance criteria met" alone is NOT done — failed walkthrough = expand scope before claiming done.
- **Pre-drain bucket impact preview (US-006)**: server endpoint reads `meta.Store.ListBuckets` and filters `Placement[<cluster>] > 0`, joining with `bucket_stats` for chunk_count + bytes_used. No manifest walk (the rebalance worker progress scan in US-003 already does that work). UI drawer reuses the existing TanStack `clusters` shared query key for state lookups; new query key `clusterBucketRefs(<id>)` for the per-cluster list. `<PolicyDrainWarning>` chip on Bucket Placement tab derives from cross-referencing the bucket's `Placement` keys with `state=draining` clusters from the shared query — no new API call.
- **Smoke script convention**: precedent is `scripts/multi-replica-smoke.sh` (from the multi-replica lab cycle). New `scripts/smoke-drain-lifecycle.sh` follows the same shell-script-with-curl-and-jq pattern; assertions via shell `[[`; exit non-zero on any failure. Makefile target `smoke-drain-lifecycle` wraps it.
- **ProgressTracker shape (US-003)**: `internal/rebalance.ProgressTracker` is keyed on cluster id, populated by the rebalance worker at iteration end via `CommitScan(draining[], chunks[], bytes[], now)`, and read by `internal/adminapi/clusters_drain_progress.go` via `Snapshot(id)`. The handle is created ONCE in `internal/serverapp/serverapp.go::Run` and shared by injection (adminapi.Config.RebalanceProgress + workers.Dependencies.RebalanceProgress) — never spawn a fresh tracker per-call. US-005 (completion detection) plugs into the same tracker via the `CompletionFiredAt` slot already reserved on `ProgressSnapshot`.
- **Drain ETA semantics (US-003)**: ETA derives from `rate(strata_rebalance_chunks_moved_total{from=<id>}[5m])` — the `from` label is the SOURCE cluster (chunk leaving). The sibling `/rebalance-progress` endpoint uses `{to=<id>}` for the destination view; same counter, opposite vantage. When adding new drain-side metric queries, prefer `from=<id>` for "chunks leaving the cluster being drained".
- **rebalanceInterval lock-step (US-003)**: `internal/serverapp/serverapp.go::rebalanceInterval` and `cmd/strata/workers/rebalance.go::clampDuration("STRATA_REBALANCE_INTERVAL", ...)` MUST stay clamp-equal. Handler uses the serverapp value as the 2× stale-cache threshold; worker uses the same env as the tick cadence. Different clamps would falsely flag stale data or miss real staleness.
- **Completion detection (US-005)**: lives entirely in `ProgressTracker.CommitScan` — returns `[]CompletionEvent` for each `>0 → 0` transition. Idempotency gate: `ProgressSnapshot.CompletionFiredAt`; reset on `0 → >0` refill so drain → fill → drain cycles re-fire. Worker's `handleDrainComplete` logs INFO + bumps `strata_drain_complete_total{cluster}` + writes `drain.complete` audit row (Principal=`system:rebalance-worker`, Bucket=`-`, Resource=`cluster:<id>`) + best-effort `DrainNotifier`. Drain-complete notify is a separate path from the bucket-scoped `notify_queue` — adapter in `cmd/strata/workers/rebalance.go::drainCompleteNotifier` synthesises a bucketless `meta.NotificationEvent` (BucketID=uuid.Nil) and calls every registered `notify.Sink.Send` directly. Future system-wide notify events should reuse this shape.


## 2026-05-13 - US-003
- New `internal/rebalance/ProgressTracker` (file `progress.go`) keyed on cluster id; goroutine-safe; nil receiver short-circuits every method so handler / worker call sites need no nil guards. `ProgressSnapshot{Chunks, Bytes, LastScanAt, BaseChunks, CompletionFiredAt}` carries the wire shape. `CommitScan(draining, chunks, bytes, now)` upserts every draining cluster and reaps entries that left the set between ticks; BaseChunks sticks at the first-observed count after the live→draining transition. `CompletionFiredAt` slot reserved for US-005 — left zero by US-003.
- Wired the tracker into `internal/rebalance/worker.go`: `Config.Progress *ProgressTracker`. `runOnce` builds a per-iteration `progressAcc` keyed off the cluster_state row read at the top of the tick; both `scanDistribution` (placement-bucket walks) and the new `scanBucketForProgress` (empty-policy bucket walks) call `prog.observe(cluster, draining, size)` per chunk + per BackendRef. `prog.commit` flushes to the tracker once per tick; an empty draining set still commits an empty snapshot so previously-cached entries that just-undrained get reaped.
- Plumbed the shared tracker through the worker boot path: `workers.Dependencies.RebalanceProgress *rebalance.ProgressTracker` (registry.go); `cmd/strata/workers/rebalance.go::buildRebalance` reads it; `internal/serverapp/serverapp.go::Run` constructs one `rebalance.NewProgressTracker(rebalanceInterval(logger))` and hands the same handle to both adminapi.Config + supervisor.Deps. New helper `rebalanceInterval` clamps STRATA_REBALANCE_INTERVAL to [1m, 24h] with fallback 1h — same shape as the worker reads its env so the stale-cache threshold stays in lock-step.
- New endpoint `GET /admin/v1/clusters/{id}/drain-progress` in `internal/adminapi/clusters_drain_progress.go`. Reads from `RebalanceProgress.Snapshot(id)` only — never scans manifests synchronously. 503 ProgressUnavailable when tracker is nil (no rebalance worker on this replica). When `state=live` numeric fields go null; when state=draining we surface chunks/bytes/base/last_scan_at + deregister_ready=(chunks==0). Stale-cache warning fires when `now - last_scan_at > 2*tracker.Interval`. Pending-scan warning fires when state=draining but no scan has committed yet. Audit row stamped via `s3api.SetAuditOverride(ctx, "admin:GetClusterDrainProgress", ...)`. ETA = chunks / rate(strata_rebalance_chunks_moved_total{from=<id>}[5m]); Prom unavailable / rate=0 / chunks=0 → eta_seconds=null without failing the request.
- OpenAPI spec updated (`internal/adminapi/openapi.yaml`): new `/clusters/{id}/drain-progress` path + new `ClusterDrainProgress` schema with the nullable numeric fields documented inline.
- Unit tests: `internal/rebalance/progress_test.go` covers ProgressTracker CRUD (BaseChunks sticky on shrink, reaping on undrain, zero-chunk-first-commit shape) plus two worker-driven tests asserting the per-tick scan populates the tracker for both placement and empty-policy buckets. `internal/adminapi/clusters_drain_progress_test.go` covers (a) 503 when tracker is nil, (b) live state nulls numerics, (c) draining + non-zero chunks → deregister_ready=false, scan again with zero chunks → deregister_ready=true + BaseChunks persists, (d) stale-cache warning fires past 2×interval, (e) pending-scan warning before any commit, (f) unknown cluster id → 400.
- Files changed: `internal/rebalance/progress.go`, `internal/rebalance/progress_test.go`, `internal/rebalance/worker.go`, `internal/adminapi/clusters_drain_progress.go`, `internal/adminapi/clusters_drain_progress_test.go`, `internal/adminapi/server.go`, `internal/adminapi/openapi.yaml`, `internal/serverapp/serverapp.go`, `cmd/strata/workers/rebalance.go`, `cmd/strata/workers/registry.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- `go vet ./...` passes; `go test ./internal/rebalance/... ./internal/adminapi/... ./internal/serverapp/...` passes. Full `go test ./...` flakes on the pre-existing `racetest::TestRunCleanRunNoInconsistencies` + `cmd/strata-racecheck::TestRunSmokeBinary` (both reproduce on prior commit 98739dc with the cycle's changes stashed — they predate this story).
- Integration test (two-cluster lab) deferred to US-007 smoke script per US-002's precedent — the multi-cluster compose profile is the canonical drive surface and US-007's negative path (c) "Undrain mid-drain → state=live, banner gone, progress cache cleared" + step "drain-progress endpoint flips deregister_ready=true" exercises the same path end-to-end.
- **Learnings for future iterations:**
  - The tracker handle is constructed ONCE in serverapp and shared between the worker (writer) + adminapi handler (reader). New consumers that want a peek at draining progress should add a `*rebalance.ProgressTracker` field on adminapi.Server / Config + plumb from serverapp; do NOT spawn a fresh tracker — the worker only writes to the one it was constructed with.
  - The `progressAcc.commit` always fires even when no draining clusters exist so previously-cached entries get reaped after a cluster un-drains. Workers that add new lifecycle counters to the same tracker should follow the same shape (always commit to reap).
  - `rebalanceInterval` (serverapp) and `clampDuration("STRATA_REBALANCE_INTERVAL", ...)` (worker) MUST stay in lock-step; the handler uses the serverapp value as its stale-cache threshold and the worker uses its own clamp as the tick cadence. Different clamps would either falsely flag stale data or miss real staleness. Both clamp to [1m, 24h] with fallback 1h.
  - The `from=<id>` label on `strata_rebalance_chunks_moved_total` is the source cluster (chunk leaving). For drain ETA we want chunks-out-rate from the cluster being drained, not chunks-in-rate at the destination. The existing `rebalance-progress` endpoint uses `to=<id>` for the inverse view (chunks landing at a cluster); both are valid Prom expressions over the same counter — read the label semantics carefully.
  - Bucket walks WITHOUT a Placement policy still need a progress scan for chunks on a draining cluster — legacy single-cluster buckets won't show up otherwise and deregister_ready would falsely flip true. Added `scanBucketForProgress` for that path; the move-plan loop stays a no-op for empty policy (no PickCluster verdict).
  - Pre-existing flaky: `internal/racetest::TestRunCleanRunNoInconsistencies` + `cmd/strata-racecheck::TestRunSmokeBinary` — both reproduce on the prior commit on `ralph/drain-lifecycle`. Don't chase them from a drain-progress endpoint change.
---

## 2026-05-12 - US-002
- Added `data.ErrDrainRefused` sentinel + `data.DrainRefusedError{Cluster}` wrapper + `data.ParseDrainStrict()` helper in `internal/data/errors.go`. Unwrap() chains to ErrDrainRefused so `errors.Is(err, data.ErrDrainRefused)` works alongside `errors.As(&dre)` for cluster id.
- `internal/data/rados/Config.DrainStrict` + `internal/data/s3/Config.DrainStrict` Boolean field, plumbed onto each `Backend{drainStrict}`. `Backend.PutChunks` refuses when picker falls back to a draining cluster (empty/all-excluded policy → fallback to `spec.Cluster`; or picked cluster missing from `b.clusters` → same fallback): returns `data.NewDrainRefusedError(<cluster>)`. RADOS path hooks the per-chunk pickCluster closure (chunk 0 short-circuits the parallel pool). S3 path adds `clusterForClassStrict` between `clusterForPlacement` and `clusterForClass`.
- READ/DELETE/HEAD/multipart Complete/Abort/List paths untouched — drain semantic is stop-write, not stop-read. Only `PutChunks` consults drainStrict.
- `internal/s3api/errors.go` ships `writeDrainRefused()` writing 503 + `Retry-After: 300` + `<Code>DrainRefused</Code>` XML body carrying the resolved cluster id. Bumps `strata_putchunks_refused_total{reason="drain_strict",cluster}` (new counter in `internal/metrics/metrics.go`).
- Three PutChunks callsites wired: `internal/s3api/server.go::putObject`, `internal/s3api/multipart.go::uploadPart` (both shapes), `internal/s3api/copy_object.go::copyObject`. `errors.As(err, &drainRefused)` ahead of the existing "unknown storage class" string match.
- New env `STRATA_DRAIN_STRICT` in `internal/config/config.go` (raw string field `DrainStrict`); validate() runs `data.ParseDrainStrict` so unknown values fail-fast at gateway boot. `internal/serverapp/serverapp.go::buildDataBackend` parses the value ONCE and plumbs into both rados.Config and s3.Config.
- Unit tests: `internal/data/errors_test.go` covers ParseDrainStrict (8 happy + 3 error cases) + DrainRefusedError errors.Is/errors.As. `internal/data/s3/placement_test.go` adds three tests: TestPutChunksDrainStrictRefusesFallback (empty policy + drained class default → ErrDrainRefused, no writes; all-excluded policy same shape), TestPutChunksDrainStrictOffPreservesFailOpen (default mode still writes to drained cluster), TestPutChunksDrainStrictDoesNotRefuseAlternateCluster (policy {a:1,b:1} + a drained → routes to b without refusal). `internal/s3api/drain_strict_test.go::TestPutObjectDrainRefusedMapsTo503` drives the gateway with a wrapping data.Backend that returns ErrDrainRefused, asserts 503 + Retry-After:300 + `<Code>DrainRefused</Code>` + cluster id in body.
- Integration test (two-cluster lab gated by `integration`): deferred to US-007 smoke script (`scripts/smoke-drain-lifecycle.sh`) — the multi-cluster compose profile is the canonical drive surface and US-007's negative paths (a) + (b) target exactly the strict + drain + PUT scenarios.
- Files changed: `internal/data/errors.go`, `internal/data/errors_test.go`, `internal/data/rados/config.go`, `internal/data/rados/backend.go`, `internal/data/s3/backend.go`, `internal/data/s3/placement_test.go`, `internal/s3api/errors.go`, `internal/s3api/server.go`, `internal/s3api/multipart.go`, `internal/s3api/copy_object.go`, `internal/s3api/drain_strict_test.go`, `internal/metrics/metrics.go`, `internal/config/config.go`, `internal/serverapp/serverapp.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- `go vet ./...` passes; `go test ./...` passes (full suite, race-free).
- **Learnings for future iterations:**
  - The strict-mode refusal lives in BOTH `PutChunks` paths (rados + s3). New data backends (eg future ceph-rgw native or filesystem) MUST honour `cfg.DrainStrict` in their PutChunks if they participate in the placement weight wheel; reads keep working unconditionally.
  - The picker fallback is the ONLY refusal trigger: when `placement.PickClusterExcluding` returns "" (empty/all-zero/all-excluded policy) or when the picked cluster isn't configured. A policy that picks a non-draining peer never triggers refusal — even when the class default is draining. This matches the rebalance-worker mental model: drain only refuses NEW writes that would land on the drained cluster.
  - `data.DrainRefusedError.Unwrap()` returns the package-level `ErrDrainRefused` sentinel. Both `errors.Is(err, ErrDrainRefused)` (no cluster id needed) and `errors.As(err, &dre)` (cluster id needed) work. Use errors.As at the gateway entry to surface the id on the wire + metric.
  - The metric is bumped from `writeDrainRefused` in `internal/s3api/errors.go`, NOT from the data backend — this keeps `internal/data/` free of the prometheus dep. New error mappings that want a per-mapping counter follow the same pattern.
  - Acceptance lists "integration test (two-cluster lab gated by `integration` build tag)" but the existing rados backend lives behind `//go:build ceph` and can't unit-test without librados. The S3 backend tests + the smoke script under US-007 cover the same scenarios via different fixtures.
  - `strings` import in `multipart.go` was already there for "unknown storage class" — no churn. New tests for the gateway 503 mapping wrap the memory backend rather than mocking the full Backend interface (cheaper than reimplementing GetChunks/Delete from scratch).
---

## 2026-05-12 - US-001
- Refactored `internal/data/rados/health_helper.go::buildPendingPoolStatuses` to take both `classes` and `clusters` and emit the cross-product (every cluster × every distinct (pool, ns)). One row per (cluster, pool, ns) cell; Class field = sorted comma-joined classes mapped to that (pool, ns). Sort key (Cluster, Name) ascending with empty cluster last.
- Wired `internal/data/rados/health.go::DataHealth` to pass `b.clusters` into the helper so the rados Pools table now lists `#clusters × #distinct-pools` rows. ioctx+GetPoolStats fold per row was already correct — the new matrix just feeds it more (cluster, pool) pairs to probe.
- Rewrote `internal/data/s3/health.go::DataHealth` to the same shape: iterate sorted cluster ids × sorted distinct buckets; HeadBucket per (cluster, bucket) cell; cluster-connect failure stamps state="error" on every bucket row for that cluster + adds one cluster-level warning.
- Memory backend untouched — still the single `Cluster=""` synthetic row.
- Updated `internal/data/rados/health_test.go`: new `TestBuildPendingPoolStatusesCrossProduct` asserts the 2×3 lab shape (6 rows, per-cluster count 3/3); existing two-cluster + group-by-pool + sort + empty-cluster-fallback tests adapted to the new signature.
- Updated `internal/data/s3/health_test.go`: new `TestDataHealthCrossProductBuckets` asserts 2 clusters × 2 buckets = 4 rows in ascending (Cluster, Name) order; new `TestDataHealthGroupsClassesPerBucket` covers the comma-joined Class label.
- Files changed: `internal/data/rados/health_helper.go`, `internal/data/rados/health.go`, `internal/data/rados/health_test.go`, `internal/data/s3/health.go`, `internal/data/s3/health_test.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- `go vet ./...` passes; `go test ./internal/data/...` passes.
- **Learnings for future iterations:**
  - `buildPendingPoolStatuses` now takes `(classes, clusters)` — any future story that touches DataHealth must pass both.
  - The cross-product means a (cluster, pool) cell where the pool doesn't actually exist on that cluster will fail at ioctx open and stamp state=error. Lab assumption: pool layout identical on every cluster. If a future deploy diverges, the matrix surfaces the divergence as error rows — operator-visible signal, not a bug.
  - Pre-existing flake: `internal/racetest::TestRunCleanRunNoInconsistencies` is broken on `ralph/drain-lifecycle` HEAD (predates this commit). Don't chase it from a DataHealth change.
  - `t.Setenv("AWS_ACCESS_KEY_ID", ...)` + `SkipCredsCheck: true` is the standard s3 backend test setup for offline tests that hit a `newCapturingS3Server` httptest fake.
---

## 2026-05-13 - US-005
- `ProgressTracker.CommitScan` now returns `[]CompletionEvent`. Detection logic: prior_chunks > 0 && new_chunks == 0 && `CompletionFiredAt` zero → fire. `CompletionFiredAt` reset on 0 → >0 transitions so refill-then-redrain re-fires. First-ever commit with chunks=0 (zero-chunks-first-commit shape) does NOT fire — no `prior > 0` observation. Added `BaseBytes` to `ProgressSnapshot` (mirrors `BaseChunks`) so `BytesMoved = BaseBytes` at completion (Bytes is 0 by the time we fire).
- `rebalance.Metrics` interface grew `IncDrainComplete(cluster)` — `internal/metrics.RebalanceObserver` wires to new `strata_drain_complete_total{cluster}` counter (registered alongside the other rebalance metrics). nopMetrics + the test fake `countingMetrics{drainsDone}` updated.
- `rebalance.Config` grew `Notifier DrainNotifier` (optional best-effort) + `AuditTTL time.Duration` (zero → `DefaultDrainAuditRetention` = 30d, same shape as `s3api.DefaultAuditRetention` but hardcoded to keep rebalance free of the s3api import). Worker's new `handleDrainComplete` logs INFO "drain complete" with `cluster` / `scan_seconds` / `final_bytes_moved`, bumps metric, calls `recordDrainCompleteAudit` (writes `action=drain.complete`, `resource=cluster:<id>`, `principal=system:rebalance-worker`, `bucket="-"`, `result="200"`), then fans to the notifier. Every sink is best-effort: audit failure logs WARN; notifier failure is the adapter's responsibility (cmd-layer adapter swallows + warns per sink).
- `cmd/strata/workers/rebalance.go` builds the optional `drainCompleteNotifier` from `notify.RouterFromEnv` (best-effort). `ErrNoTargets` (env unset) silently returns nil — the worker still logs + audits + bumps the metric on every transition. The adapter iterates every registered Sink and ships a synthesised `meta.NotificationEvent{BucketID: uuid.Nil, Bucket: "-", EventName: "s3:Drain:Complete", Payload: {event, cluster, bytes_moved, completed_at}}` to each sink via `sink.Send`. Per-sink failure logs WARN — never blocks the tick. `STRATA_AUDIT_RETENTION` is re-read inside the worker via a local `parseAuditRetention` (no s3api dep) so drain.complete rows respect the gateway's TTL contract.
- Unit tests:
  - `internal/rebalance/progress_test.go::TestProgressTrackerCompletionFiresOnceOnDrainToZero`: 5 → 3 → 0 → 0 fires exactly one event on the 3 → 0 boundary; `CompletionFiredAt` stamped; second 0 is idempotent. Asserts event shape (Cluster, BaseChunks, BaseBytes, BytesMoved, ScanFinish).
  - `internal/rebalance/progress_test.go::TestProgressTrackerCompletionRefiresAfterRefill`: 5 → 3 → 0 → 2 → 0 fires twice (the refill resets the firing slot).
  - `internal/rebalance/progress_test.go::TestProgressTrackerCompletionResetsOnReap`: undrain reaps the row; a fresh re-drain to 0 must fire because the prior CompletionFiredAt died with the row.
  - `TestProgressTrackerZeroChunksOnFirstCommit` (extended): zero-chunks-first-commit must NOT fire.
  - `internal/rebalance/progress_test.go::TestWorkerFiresDrainCompleteEnd2End` drives `Worker.RunOnce` through the seed → delete → idle sequence and asserts: metric=1 after the 3 → 0 transition, notifier captured one DrainCompleteEvent with BytesMoved=3*1024, `meta.AuditEvent{Action: "drain.complete", Resource: "cluster:old", Principal: "system:rebalance-worker", Bucket: "-"}` visible via `ListAuditFiltered`, third tick at idle does not re-fire any of the sinks.
- Integration test (two-cluster lab) deferred to US-007 smoke script — the multi-cluster compose profile already covers the drain-to-completion flow end-to-end. The smoke script's `deregister_ready=true` assertion (PRD US-007 line 4) is the cross-replica completion validation.
- Files changed: `internal/rebalance/progress.go`, `internal/rebalance/progress_test.go`, `internal/rebalance/worker.go`, `internal/rebalance/worker_test.go`, `internal/metrics/metrics.go`, `cmd/strata/workers/rebalance.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- `go vet ./...` passes; `go test ./...` passes (full suite, race-free).
- **Learnings for future iterations:**
  - `CompletionFiredAt` lives in `ProgressSnapshot` because the snapshot is the per-cluster source of truth — reapping the row (undrain) automatically clears the fire state, no separate bookkeeping needed. New per-cluster lifecycle bits (next: deregister-acknowledged, force-drain markers, etc) should ride the same struct.
  - The drain-complete notifier is intentionally a separate code path from the per-bucket `notify_queue` because the event is bucketless. The `notify.Sink` interface is reusable as-is — synthesise a `meta.NotificationEvent{BucketID: uuid.Nil, Bucket: "-", ...}` and call `sink.Send` directly. Future system-wide events (cluster-down, lease-loss, etc) can copy this adapter shape rather than extending the bucket-scoped queue.
  - `BaseBytes` is the byte-side of the same `first-seen sticky` pattern `BaseChunks` already implements. Don't try to fold it into the same line — the bytes column can be zero while chunks > 0 (an empty-object manifest), so keep the conditional independent.
  - Audit row Principal "system:rebalance-worker" is the per-worker identifier; if another worker grows audit emission, mirror the `system:<worker-name>` shape so operators can filter by it.
  - `parseAuditRetention` is duplicated in `cmd/strata/workers/rebalance.go` vs `s3api.ParseAuditRetention` — accepting the duplication keeps the worker package free of the s3api HTTP import. If a third caller wants the same parser, promote it to internal/auditretention or similar. Today the gateway side (auditRetention in serverapp) calls s3api.ParseAuditRetention; the worker side calls the local copy.
  - The `_ = ctx` patterns in `internal/data/s3/health.go` etc — unrelated to this story, pre-existing diagnostics on `unused parameter: ctx`. Don't churn them from a drain-completion change.
---

## 2026-05-13 - US-004
- New `web/src/components/storage/DrainProgressBar.tsx` renders the per-cluster drain block on each draining cluster card. Three shapes: (a) `pending` warning → "scan pending" muted text; (b) `deregister_ready=true` → emerald chip "✓ Ready to deregister (env edit + restart)" with `title=` tooltip describing the env edit + rolling restart; (c) chunks > 0 → amber progress bar (filled = `(BaseChunks - chunks) / BaseChunks` when BaseChunks known; otherwise text-only) + "<N> chunks remaining · <M> at start · ~<ETA>" readout. `(stale)` italic suffix + muted color when warnings include "progress data stale". 30s poll via shared `queryKeys.clusterDrainProgress(<id>)` query key. ETA formatter handles s/m/h/d ranges; null seconds renders "?".
- `web/src/api/clusters.ts` gains `fetchClusterDrainProgress` + `ClusterDrainProgress` type — nullable numeric fields mirror the server's `ClusterDrainProgressResponse` wire shape exactly (chunks/bytes/base/eta/last_scan_at/deregister_ready all `T | null`). The fetcher returns the raw body — pointer→null mapping is the JSON default so the UI just consumes the typed shape.
- `fetchClusters` now returns `ClustersList { clusters, drainStrict }` (renamed from raw `ClusterStateEntry[]`) so the wire-level `drain_strict: bool` field surfaces alongside the per-cluster rows from one fetch. Updated three consumers — `ClustersSubsection`, `PlacementDrainBanner`, `BucketPlacementTab` — to read from `q.data?.clusters ?? []` (everything else stays the same). The shared `queryKeys.clusters` is unchanged so the dedup window still merges the three call sites onto one fetch per cache window.
- `ClustersSubsection` adds a small "strict" amber outline badge next to the per-card state badge whenever the top-level `drainStrict=true`. Title tooltip: "STRATA_DRAIN_STRICT=on: PUTs that fall back to a draining cluster are refused with 503 DrainRefused". The chip renders globally (per AC: strict mode is a process-level flag, so every card surfaces it — operators hovering on a draining card see the same tooltip).
- Server-side wiring: `ClustersListResponse.DrainStrict bool` (`internal/adminapi/clusters_drain.go`) + new `Server.DrainStrict` field + Config plumb. `internal/serverapp/serverapp.go` parses `STRATA_DRAIN_STRICT` via the existing `data.ParseDrainStrict` helper (same call site as `buildDataBackend`, so any unknown value still fails-fast at boot) and hands the bool to `adminapi.Config{DrainStrict}`. New unit test `TestClustersList_SurfacesDrainStrict` asserts the wire field reaches the response; existing `TestClustersList_DefaultsToLive` extended with a `drain_strict=false` default assertion.
- Files changed: `internal/adminapi/clusters_drain.go`, `internal/adminapi/clusters_drain_test.go`, `internal/adminapi/server.go`, `internal/serverapp/serverapp.go`, `web/src/api/client.ts`, `web/src/api/clusters.ts`, `web/src/lib/query.ts`, `web/src/components/BucketPlacementTab.tsx`, `web/src/components/layout/PlacementDrainBanner.tsx`, `web/src/components/storage/ClustersSubsection.tsx`, `web/src/components/storage/DrainProgressBar.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- `go vet ./...` passes; `go test ./internal/adminapi/... ./internal/serverapp/... ./internal/data/... ./internal/rebalance/...` passes uncached. `pnpm run typecheck` + `pnpm run build` succeed; the `Storage-CF35IGvh.js` chunk grew by ~600 bytes minified (well under the 4 KiB gzipped budget).
- Browser verification deferred — no `dev-browser` MCP skill is wired in this environment. Manual verification of the Storage page (Clusters subsection) is required before US-007's smoke walkthrough phase; the smoke script under US-007 covers the wire+render contract end-to-end against the multi-cluster compose profile.
- **Learnings for future iterations:**
  - The `fetchClusters` return type change rippled into three consumers. When adding wire fields that should travel alongside an existing list response, prefer changing the fetcher's return shape (one fetch, one cache window) over adding a separate endpoint — TanStack Query dedupes on the shared key only when consumers share the same query function shape. The three consumer file paths (`ClustersSubsection`, `PlacementDrainBanner`, `BucketPlacementTab`) are the canonical fan-out list for any future cluster-list-shape extension.
  - No `<Progress>` / `<Tooltip>` primitives ship under `web/src/components/ui/` — use a 1.5px-tall `bg-muted` div + inner div with `style={{ width: ... }}` for progress bars and the native `title=` attribute for hover tooltips. Existing precedent: `ClusterCard.title=` on the cluster id ellipsis, `usage span title=` on the n/a fallback.
  - The `pending` warning shape ("progress scan pending; rebalance worker has not yet committed a tick") is distinct from "progress data stale" — the former means the cache slot exists but is empty (first scan in flight), the latter means the cache slot is older than 2×interval. Render them differently: pending = muted "scan pending" text; stale = normal bar with `(stale)` italic suffix.
  - `data.ParseDrainStrict` is idempotent — calling it both inside `buildDataBackend` and at the adminapi config build site is safe. Both call sites return the same `(bool, error)` for the same env value. New code paths needing the strict flag can parse it independently rather than threading the value through buildDataBackend's return.
  - Lucide `CheckCircle2` is the de-facto "completion ready" icon in this codebase; reuse rather than picking a sibling like `BadgeCheck`. Existing precedent in other green-success chips (audit row "delivered" status etc).
---

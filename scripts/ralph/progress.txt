# Ralph Progress Log
Started: Sun May 10 16:00:00 MSK 2026
Cycle: ralph/s3-compat-finish

## Codebase Patterns
- `UploadPartCopy` handler lives in `internal/s3api/multipart.go::uploadPartCopy` (around line 270). NOT in `internal/s3api/multipart_copy.go` — that file does not exist. Test file IS `internal/s3api/multipart_copy_test.go`.
- `initiateMultipartUpload` (`multipart.go:30..125`) builds `mu.ChecksumAlgorithm` from `x-amz-checksum-algorithm` request header (uppercased) and `mu.ChecksumType` via `normalizeChecksumType(r.Header.Get(\"x-amz-checksum-type\"))`. `normalizeChecksumType` returns `\"FULL_OBJECT\"` / `\"COMPOSITE\"` for valid input, EMPTY otherwise. Empty type passes through to Complete.
- `composeMultipartChecksums` (`checksum.go:201`) — `fullObject := strings.EqualFold(checksumType, \"FULL_OBJECT\")`. When type is empty, fullObject = false → COMPOSITE shape (`BASE64(HASH(concat(raws)))-N`) is applied to ALL algos including CRC family. This is the bug for CRC64NVME — AWS spec says CRC64NVME only supports FULL_OBJECT (no COMPOSITE+CRC64NVME on AWS). boto3 1.36+ default-on FlexibleChecksum sends `ChecksumAlgorithm=CRC64NVME` with FULL_OBJECT validation expectation; on GET it streams body + recomputes raw CRC, divergent from server's COMPOSITE-with-`-N` shape → `FlexibleChecksumError`.
- US-001 fix: in `initiateMultipartUpload`, after `mu` is built, if `mu.ChecksumAlgorithm == \"CRC64NVME\"` and `mu.ChecksumType == \"\"` → force `mu.ChecksumType = \"FULL_OBJECT\"`. Reject `CRC64NVME + COMPOSITE` explicitly as 400 InvalidArgument. For CRC32 / CRC32C with empty type → default FULL_OBJECT (modern boto3 default). SHA1 / SHA256 stay empty / COMPOSITE.
- The bug is NOT specific to UploadPartCopy. boto3's high-level transfer manager triggers auto-checksum default-on, while native multipart tests in US-003 of prior cycle set algorithm/type explicitly and avoided the gap. Fix at Initiate level applies to both copy and non-copy paths.
- TestUploadPartCopyChecksumPersistsToCompositeOnComplete (`multipart_copy_test.go:143`) uses SHA256 explicitly — passes today because SHA256 + COMPOSITE is the supported shape. Doesn't catch CRC64NVME bug.
- `completeMultipartUpload` strict-ascending check at `internal/s3api/multipart.go:430` is `if p.PartNumber <= prev { writeError ErrInvalidPartOrder }`. Confirmed by code read. US-002 fix: `< prev` + dedupe-to-LAST per PartNumber matches AWS / RGW take-latest semantics.
- Per-part ETag validation runs against `meta.Store.SavePart` per-PartNumber rows. After dedupe-to-last in the request, validate the LAST submitted ETag for each PartNumber against the LATEST stored part (last-write-wins in all 3 backends).
- Strict OUT-OF-ORDER (e.g. `[1, 3, 2]`) MUST still fail: `InvalidPartOrder`. Only EQUAL adjacent PartNumbers (resend) are now acceptable.
- s3-tests baseline lives in `scripts/s3-tests/README.md`. Prior cycle landed at 162/177 (91.5%). This cycle targets +4 passes (~94%). Run via `scripts/s3-tests/run.sh` against full local stack (`make up-all`). Update README with new headline + per-test diff in close-flip commit.
- Two ROADMAP P2 entries close together in US-003: 'Multipart copy GET-side checksum echo divergence' (3 tests) + 'Multipart Complete rejects duplicate PartNumber in Parts list' (1 test). Same close-flip commit; both flip to `~~**P2 — ...**~~ — **Done.** ... (commit `<pending>`)`.
- If any of the 4 target s3-tests still fail after US-001+US-002, file as new ROADMAP entry per CLAUDE.md 'Discovering a new gap' rule BEFORE flipping the headline.
- `tasks/prd-*.md` is intentionally NOT created for this cycle — narrow 3-story scope, prd.json description suffices. PRD lifecycle rule: markdown PRD is a disposable design draft used to brief operators; for small cycles JSON is the canonical brief.
- s3-tests requires `STRATA_AUTH_MODE=required` + `STRATA_STATIC_CREDENTIALS` shape from `scripts/s3-tests/README.md` (MAIN_AK / ALT_AK / TENANT_AK creds). NOT `optional` mode.
- Reproduction needs `make up-all` (cassandra + ceph + strata via docker compose). On macOS + Lima docker context, set `DOCKER_HOST=unix:///Users/.../.lima/.../sock/docker.sock` per project CLAUDE.md.
- `internal/s3api/checksum.go::combineCRCParts` does AWS `FULL_OBJECT` composite math (zlib gf2 matrix square+times) for CRC32 / CRC32C / CRC64NVME — already correct, no fix needed there.
- `data.Manifest.PartChecksums` carries per-part stored checksum maps. Updated atomically with manifest writes. Schema-additive — old rows decode with zero-values, no `ALTER`.
- `meta.Store.SavePart` overwrites on PartNumber resend (last-write-wins) on all 3 backends. Re-uploads of same PartNumber produce a single stored row at the LATEST ETag.
- DO NOT use `STRATA_AUTH_MODE=optional` in e2e — sign with static creds against SigV4 path, matches s3-tests harness.


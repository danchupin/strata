# Ralph Progress Log
Started: Sat Apr 25 16:10:14 MSK 2026

## Codebase Patterns
- meta.Store is the single interface every storage backend (memory + cassandra) must implement; adding a method requires updating both backends or builds break (compiler enforces).
- Bucket-scoped blobs use the setBucketBlob/getBucketBlob/deleteBucketBlob helpers in both backends — model new bucket-level resources (grants, encryption, website, …) on this pattern.
- Auth credentials use auth.CredentialsStore (NOT auth.Store) and auth.ErrNoSuchCredential (NOT auth.ErrCredentialNotFound). PRDs sometimes use the wrong names; trust internal/auth/types.go.
- auth.MultiStore (internal/auth/multi.go) composes any number of CredentialsStore backends with a 60s positive+negative TTL cache and Invalidate(accessKey). Use it as the gateway's Middleware.Store; append new backends instead of replacing the static one.
- cmd/strata-gateway/main.go obtains the cassandra session via metaStore.(*metacassandra.Store).Session() — reuse this idiom whenever an auxiliary subsystem needs CQL access without re-opening a session.
- Cassandra cannot UPDATE the buckets table by `id` (PK is `name`); for bucket_id-keyed blobs add a separate table keyed by bucket_id, not a column on `buckets`.
- Cassandra schema evolution: append CREATE TABLE IF NOT EXISTS to tableDDL; append additive `ALTER TABLE ... ADD` to alterStatements. isColumnAlreadyExists handles re-runs.
- s3api handlers accept XML bodies with io.ReadAll(io.LimitReader(r.Body, 1<<20)).
- Tests live in internal/s3api as a black-box `package s3api_test` and use `newHarness(t)` from testutil_test.go.
- IAM-style JSON policy types live in internal/auth/policy. Use policy.Evaluate(doc, principal, action, resource, ctx) where principal is "*" for anonymous, action is e.g. "s3:GetObject", resource is the AWS ARN, ctx is map[string]string of condition keys.
- internal/s3api/access.go centralises policy gating: principalForRequest(r) returns "*" for anonymous, otherwise the AuthInfo.Owner; bucketARN/objectARN build the canonical resource strings; requireAccess(w, r, b, action, arn) returns false (and writes 403) on policy denial. No-policy buckets bypass the gate so behaviour stays unchanged when no JSON policy is set.
- ACL gating lives in the same access.go: requireObjectAccess(w, r, b, key, action) wraps requireAccess (policy) then requireACL (canned + persisted grants). Use it for every object-level dispatch. Owner FC = info.Owner == b.Owner short-circuits both gates; per-object grants override bucket ACL on s3:GetObject only.
- Test-only auth injection: testutil_test.go wraps the api with an X-Test-Principal header sniffer that builds &auth.AuthInfo{Owner: p}. Tests use h.doString(method, path, body, "X-Test-Principal", "owner"|"alt") to simulate non-anonymous callers. Omit the header for anonymous requests.
- IAM admin endpoints (?Action=…) live at the root path (bucket==""). extractIAMAction(r) reads Action from query OR application/x-www-form-urlencoded body; handleIAM gates on auth.FromContext(...).Owner == s3api.IAMRootPrincipal ("iam-root"). When extending IAM, add the action to the dispatch switch in handleIAM and reuse iamParam(r, "Key") to read string params from query OR PostForm. Tests pass principal via X-Test-Principal: iam-root.
- Access keys land in two cassandra tables: access_keys (PK access_key) for SigV4 lookup, iam_access_keys_by_user (PK (user_name, access_key)) for ListAccessKeys. Writes use a logged BATCH so both stay in sync; reads come from access_keys directly. Memory backend keeps a single accessKeys map keyed by AccessKeyID. memory.CredentialStore wraps the memory backend so memory-meta gateways have a usable SigV4 credential lookup (mirrors cassandra.CredentialStore). Server.InvalidateCredential is the hook that DeleteAccessKey calls to drop the MultiStore cache entry; main.go wires it to multi.Invalidate. Tests that need a fully signed request use the in-file SigV4 helper in iam_access_keys_signed_test.go (canonical request → derive signing key → HMAC).
- Temporary credentials (STS AssumeRole) carry Credential.SessionToken; middleware compares the request's x-amz-security-token to it after signature passes — mismatch → ErrInvalidToken. auth.STSStore implements CredentialsStore so STS keys flow through MultiStore like static/cassandra/memory creds; MultiStore skips caching creds with non-empty SessionToken so revoke/expiry take effect immediately. STSStore.SetClock(now func()) drives deterministic expiry in tests.
- Object-level checksums (US-009): meta.Object.Checksums map[string]string round-trips through both backends (cassandra column `checksums map<text,text>` added via additive ALTER). Server-side verification lives in internal/s3api/checksum.go: parseRequestChecksums(r) reads every x-amz-checksum-<algo> header for algos in supportedChecksumAlgorithms (CRC32, CRC32C, SHA1, SHA256, CRC64NVME), checksumWriter fans body bytes to all hashers via io.TeeReader on the put path, verifyChecksums compares after PutChunks and writeChecksumHeaders echoes them on GetObject/HeadObject. Mismatch returns 400 BadDigest and deletes the freshly-written chunks. CRC-64/NVME is vendored locally (newCRC64NVME) since the stdlib lacks it.
- Multipart checksums (US-010): meta.MultipartPart.Checksums map[string]string is persisted through SavePart / surfaced by ListParts in both backends (cassandra ALTER multipart_parts ADD checksums map<text,text>). uploadPart re-uses the parseRequestChecksums + io.TeeReader(checksumWriter) + verifyChecksums chain from US-009; mismatch deletes the data manifest and returns 400 BadDigest. completeMultipart calls ListParts BEFORE CompleteMultipartUpload to grab per-part Checksums (the meta-level Complete deletes parts on success), then composeMultipartChecksums computes the AWS composite (base64(hash(concat raw digests))-N) for every algo present on every requested part. The composite is set on obj.Checksums (so GetObject/HeadObject echo it) and emitted on CompleteMultipartUploadResult as both response headers and ChecksumCRC32/SHA256/etc. XML elements (omitempty).
- SSE-S3 metadata (US-011): meta.Object.SSE + meta.MultipartUpload.SSE strings round-trip through both backends (cassandra ALTER objects ADD sse text + ALTER multipart_uploads ADD sse text). Bucket-level default uses the standard setBucketBlob/getBucketBlob pattern (table bucket_encryption, ErrNoSuchEncryption sentinel). internal/s3api/encryption.go centralises validateSSEAlgorithm (AES256 ok; aws:kms/aws:kms:dsse → 501 NotImplemented; else 400) and resolveSSE(r,b) — request header wins, bucket default fills in, no config => empty. resolveSSE runs BEFORE PutChunks so kms/bogus headers reject the request without writing chunks. SSE flows: putObject sets obj.SSE, initiateMultipart sets mu.SSE, uploadPart + completeMultipart echo header, completeMultipart copies mu.SSE to obj.SSE. GetObject/HeadObject emit x-amz-server-side-encryption when obj.SSE is non-empty. Errors live in errors.go: ErrNoSuchEncryption (404 ServerSideEncryptionConfigurationNotFoundError), ErrInvalidEncryptionAlgorithm (400), ErrKMSNotImplemented (501).
- Subresource routing in handleObject is order-sensitive: the `q.Has(...)` checks run top-to-bottom and short-circuit; place new ?<sub> branches BEFORE the generic method switch (PUT/GET/HEAD/DELETE) so the generic path doesn't intercept them. Gate every object-level subresource via requireObjectAccess (not raw requireAccess) so policy + ACL still apply.
- Bucket-level flags on the buckets row use the same LWT idiom as SetBucketVersioning/SetBucketACL: `UPDATE buckets SET <col>=? WHERE name=? IF EXISTS` with `ScanCAS(nil)` and SerialConsistency LocalSerial. Required so the write joins the same Paxos lineage as the original CreateBucket INSERT IF NOT EXISTS — non-LWT UPDATEs on that row leave Paxos stale and LOCAL_QUORUM reads observe the pre-update value. ScanCAS(nil) (not new(bool)/new(string)) — gocql ignores the destination when CAS applies.
- Static-site / website mode (US-017): handleBucket GET intercepts when len(q)==0, calls serveWebsiteRoot which fetches IndexDocument.Suffix on hit (200) or ErrorDocument.Key on miss (404). serveWebsiteObject sets Content-Type/ETag/Last-Modified/Content-Length and streams the object body via Data.GetChunks — does NOT call writeError (the body is the document itself, not an XML <Error/> envelope). Pagination (prefix, delimiter, max-keys, continuation-token, start-after) is preserved by the len(q)==0 guard.
- Bucket replication (US-018): standard setBucketBlob/getBucketBlob/deleteBucketBlob blob in both backends (cassandra: bucket_replication table). putBucketReplication validates ≥1 Rule with non-empty Destination.Bucket; empty body → 400 (DELETE clears). getBucketReplication returns 404 ReplicationConfigurationNotFoundError on missing. replicationStatusFor(r,b,key,tags) walks Filter shapes (Prefix / Tag / And{Prefix+Tags}), skips disabled rules, returns "PENDING" on match. putObject emits x-amz-replication-status only when status is non-empty (header omitted otherwise). Filter shape pattern is reusable across lifecycle/replication.
- Bucket tagging (US-020): standard setBucketBlob/getBucketBlob/deleteBucketBlob blob in both backends (cassandra: bucket_tagging table). Validation lives in s3api handler (≤50 tags, non-empty Key, no duplicate Key) — the meta layer stays an opaque blob store. PUT returns 204 (matches AWS), GET returns the stored XML or 404 NoSuchTagSet, empty body PUT and explicit DELETE both clear. Reuses the Tagging XML type already defined for object tagging.
- Bucket region (US-021): meta.Bucket.Region is a column on the buckets row (additive ALTER buckets ADD region text), not a blob. Mutated via SetBucketRegion using the LWT pattern (UPDATE … IF EXISTS / ScanCAS(nil) / LocalSerial) so the write joins the same Paxos lineage as the original CreateBucket INSERT IF NOT EXISTS. CreateBucket parses optional <CreateBucketConfiguration><LocationConstraint> from the request body BEFORE calling Meta.CreateBucket so a malformed body short-circuits with 400 MalformedXML. GetBucketLocation emits the canonical AWS xmlns ("http://s3.amazonaws.com/doc/2006-03-01/") on the LocationConstraint element via xml.Name with the namespace-qualified field tag. HEAD bucket sets x-amz-bucket-region from b.Region or falls back to s.Region (which s3api.New defaults to "default").
---

## 2026-04-25 - US-001 (Parse AccessControlPolicy XML body)
- Added `meta.Grant` type and `ErrNoSuchGrants` to internal/meta/store.go.
- Extended meta.Store interface with SetBucketGrants/GetBucketGrants/DeleteBucketGrants and SetObjectGrants/GetObjectGrants.
- Memory store: bucketGrants map[uuid.UUID][]Grant, objectGrants map[grantKey][]Grant keyed by (bucketID,key,versionID).
- Cassandra: new `bucket_acl_grants` table; `grants blob` column on objects via additive ALTER. Grants encoded as JSON blob.
- Rewrote internal/s3api/acl.go: parseACLBody validates grantee Type ∈ {CanonicalUser,Group,AmazonCustomerByEmail}, Permission ∈ {FULL_CONTROL,READ,WRITE,READ_ACP,WRITE_ACP}, Group URI in known set; CanonicalUser requires ID; AmazonCustomerByEmail requires Email. Returns 400 MalformedACLError on any failure.
- putBucketACL accepts canned header AND XML body (mixed) — header goes to SetBucketACL, body to SetBucketGrants.
- getBucketACL/getObjectACL prefer persisted grants, fall back to canned-derived grants.
- Added ErrMalformedACLError to errors.go.
- New tests: internal/s3api/acl_test.go covers round-trip, mixed header+body, malformed XML 400, unknown grantee type 400, unknown permission 400, fallback to canned.
- Files changed:
  - internal/meta/store.go
  - internal/meta/memory/store.go
  - internal/meta/cassandra/store.go
  - internal/meta/cassandra/codec.go
  - internal/meta/cassandra/schema.go
  - internal/s3api/acl.go (rewrite)
  - internal/s3api/acl_test.go (new)
  - internal/s3api/errors.go
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - Reproduce the bucket_acl_grants table pattern when adding more bucket-level metadata: separate table keyed by bucket_id, JSON blob via setBucketBlob/getBucketBlob helpers.
  - For object-scoped metadata that needs versionID resolution use the existing `resolveVersionID` (cassandra) / `findLatest` (memory) helpers — don't reinvent.
  - Go XML unmarshaller correctly resolves `xsi:type` when the field tag uses the namespace-qualified form: `xml:"http://www.w3.org/2001/XMLSchema-instance type,attr"`. Required for parsing AWS ACL XML.
  - Adding a method to the meta.Store interface immediately surfaces compile errors at every assignment site (cmd/* main.go has *.(*metamem.Store) type assertions); they re-enable once both backends gain the method. Implement before touching handlers.
---

## 2026-04-25 - US-002 (IAM-style policy evaluator package)
- New package internal/auth/policy with:
  - Decision (Allow/Deny) and constants EffectAllow/EffectDeny.
  - Document, Statement, Principal types; StringOrSlice / StringSlice unmarshallers cope with the AWS "string OR list" JSON shape.
  - Principal accepts the bare string "*" or the object form {"AWS": "arn..." | ["arn...", ...]}.
  - Evaluate(doc, principal, action, resource, ctx) — explicit Deny short-circuits; Allow returned only when at least one statement matched and no Deny matched; otherwise Deny.
  - glob() matches '*' and '?' wildcards (used for s3:* actions and ARN resources alike).
  - Condition support: StringEquals only — value side may be string or array; ANY-of semantics. Unknown ops never match (same effect as Deny by default).
  - Parse() rejects statements with Effect not in {Allow, Deny}.
- Tests internal/auth/policy/policy_test.go cover: explicit allow, deny-overrides-allow, wildcard action (s3:Get*), wildcard resource prefix, StringEquals match/miss/missing-key, anonymous Principal=*, default-deny on no-match, wrong-principal denied, invalid Effect parse error, multi-action/multi-resource arrays, nil doc, unsupported condition op.
- Files changed:
  - internal/auth/policy/policy.go (new)
  - internal/auth/policy/policy_test.go (new)
- Quality: go build ./... + go vet ./internal/auth/... + go test ./... all green.
- **Learnings for future iterations:**
  - AWS's "string OR list" JSON convention is widespread (Action, Resource, Principal.AWS, Condition values). Centralised the unmarshaller (unmarshalStrings) so future condition-key arrays reuse it.
  - Principal "*" is a *string*, but {"AWS": "*"} is also valid — both must set Principal.All. Cover both shapes when extending.
  - Default-deny semantics need a sentinel: track an `allow` bool and only return Allow at the end if no Deny short-circuited; never return Allow inside the loop.
  - When US-003 wires this in: anonymous principal == "*" by convention; action strings should be "s3:Op" form (no service prefix, no ARN); resources must be full AWS ARNs ("arn:aws:s3:::bucket" for bucket actions, "arn:aws:s3:::bucket/key" for object actions).
---

## 2026-04-25 - US-003 (Wire policy evaluator into request middleware and object handlers)
- New internal/s3api/access.go with helpers: principalForRequest, bucketARN, objectARN, loadBucketPolicy, requireAccess.
- requireAccess loads the bucket's JSON policy via Meta.GetBucketPolicy. ErrNoSuchBucketPolicy → bypass (preserves current behaviour for un-policied buckets). Otherwise policy.Parse + policy.Evaluate; Allow proceeds, anything else writes 403 ErrAccessDenied.
- Anonymous identity (auth.FromContext returning Anonymous=true or empty Owner) maps to principal "*" so it flows naturally into policy.Principal.matches.
- handleObject in internal/s3api/server.go gates each object-level dispatch:
  - PUT (with or without copy-source) → s3:PutObject on objectARN
  - GET / HEAD → s3:GetObject
  - DELETE → s3:DeleteObject (or s3:DeleteObjectVersion when ?versionId set)
  - ?uploads POST (initiate), ?uploadId PUT (uploadPart), ?uploadId POST (complete) → s3:PutObject
  - ?uploadId DELETE (abort) → s3:AbortMultipartUpload
  - ?uploadId GET (listParts) → s3:ListMultipartUploadParts
- ?acl, ?tagging, ?retention, ?legal-hold subresources keep their current pass-through behaviour (out of scope per AC).
- Tests internal/s3api/policy_gate_test.go cover: no-policy passthrough, anonymous Allow grants GET, anonymous Deny on no-matching-Allow, explicit Deny override on a specific key, wrong-principal Allow denied, denied response body does not leak the object payload.
- Files changed:
  - internal/s3api/access.go (new)
  - internal/s3api/server.go (gate calls in handleObject)
  - internal/s3api/policy_gate_test.go (new)
  - scripts/ralph/prd.json (passes: true)
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - Bucket policy doc is fetched by GetBucketPolicy + ErrNoSuchBucketPolicy sentinel. ALWAYS treat the sentinel as "skip the gate", not as an error — the existing un-policied buckets must keep working.
  - Tests using newHarness(t) hit the s3api.Server directly (no auth wrap), so every request is anonymous. That makes anonymous-principal "*" tests trivial; for non-anonymous principals you'd need an extra wrapper that injects auth.WithAuth on the context. Out of scope here.
  - When extending the gate to a new endpoint, the action string must match what the policy author would write (e.g. "s3:PutObject", "s3:AbortMultipartUpload"); the resource is built via bucketARN(name) or objectARN(name, key) — do not hand-format ARNs.
  - The denied-body-leak test (read body after 403) is cheap insurance against a future regression where someone calls writeError after partial response writes.
---

## 2026-04-25 - US-004 (Enforce bucket and object ACL grants on object-level requests)
- Added requireACL + requireObjectAccess to internal/s3api/access.go. requireObjectAccess runs the policy gate first (US-003) and then the ACL gate; explicit policy Deny short-circuits before ACL.
- ACL gate: owner FC (info.Owner == b.Owner) short-circuits. For s3:GetObject, persisted per-object grants override bucket ACL when present (per AC). Otherwise falls through to bucket grants (SetBucketGrants) then canned ACL.
- cannedAllows: private→none; public-read→READ; public-read-write→READ+WRITE; authenticated-read→READ for non-anon. cannedAllows operates per-action with action→permission mapping (s3:GetObject/ListMultipartUploadParts→READ; s3:PutObject/DeleteObject/DeleteObjectVersion/AbortMultipartUpload→WRITE).
- grantsAllow + granteeMatches: AllUsers matches anyone; AuthenticatedUsers matches non-anon; CanonicalUser matches by ID==info.Owner; AmazonCustomerByEmail never matches (no email tracked). FULL_CONTROL covers any permission.
- Wired requireObjectAccess at all object-level dispatch sites in handleObject (PUT/GET/HEAD/DELETE, copy, multipart initiate/uploadPart/complete/abort/listParts), replacing the single requireAccess calls from US-003. ?acl/?tagging/?retention/?legal-hold subresources unchanged (out of scope).
- testutil_test.go: harness wraps the api with a header sniffer; X-Test-Principal: <owner> injects auth.WithAuth(&auth.AuthInfo{Owner:p}); omitted == anonymous.
- Tests in new internal/s3api/acl_gate_test.go: 28-cell canned×method×principal matrix; per-object grants override bucket ACL on read; explicit policy Deny overrides ACL Allow; 403 does not leak object body.
- Files changed:
  - internal/s3api/access.go (extend with ACL helpers + requireObjectAccess)
  - internal/s3api/server.go (handleObject uses requireObjectAccess)
  - internal/s3api/testutil_test.go (X-Test-Principal injection)
  - internal/s3api/acl_gate_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - The auth context default for unauthenticated requests is &AuthInfo{Anonymous: true, Owner: "anonymous"}; do NOT guard owner-FC checks on !info.Anonymous or anonymous-owned buckets stop accepting requests from anonymous principals (breaks current US-003 NoPolicyPasses test).
  - Per AC, per-object grants override bucket ACL on reads only; bucket-level persisted grants (SetBucketGrants) override canned ACL on every action — both must be checked before the canned fallback.
  - AmazonCustomerByEmail is parsed and persisted (US-001) but cannot be matched by the gate because we don't store the requester's email — keep behaviour as never-match until we add email lookup.
  - When extending the gate to a new endpoint, always go through requireObjectAccess (not raw requireAccess) so ACLs are enforced; pass action verbatim ("s3:PutObject" etc.) to keep policy authors' expectations.
  - For non-anonymous principal tests, set X-Test-Principal in the request headers; the harness injects auth.WithAuth so the gate sees a real Owner.
---

## 2026-04-25 - US-005 (Cassandra-backed credentials store)
- New cassandra.CredentialStore (internal/meta/cassandra/credentials.go) implements auth.CredentialsStore against the new access_keys table; SELECT secret_key, owner, disabled. gocql.ErrNotFound and disabled=true both surface as auth.ErrNoSuchCredential (revoked == deleted).
- access_keys table appended to tableDDL with CREATE TABLE IF NOT EXISTS (additive). Schema: access_key text PK, secret_key text, owner text, disabled boolean, created_at timestamp.
- auth.MultiStore (internal/auth/multi.go): tries stores in order, returns first hit. 60s default TTL cache (auth.DefaultCacheTTL exported) for both positive AND negative results; transient (non-ErrNoSuchCredential) errors propagate without caching so callers can retry. Invalidate(accessKey) drops the cache entry; callers must invoke after rotating/deleting a key.
- cmd/strata-gateway/main.go: builds []auth.CredentialsStore = [static, cassandra?] (cassandra appended only when metaStore.(*metacassandra.Store) succeeds via Session()). The required-mode credential check now allows just-cassandra setups (no static creds) — counts total stores, not credMap size.
- Helpers on cassandra.CredentialStore: Put(ctx, cred, disabled) and Delete(ctx, accessKey) — internal-only, used by upcoming IAM endpoints (US-006/US-007). Not part of the auth.CredentialsStore interface.
- Tests in internal/auth/multi_test.go (black-box package auth_test) cover: static-before-dynamic precedence, fall-through to dynamic, miss, disabled→missing, cache-hit avoids backend calls, negative cache, key rotation via Invalidate, deleted-key returns ErrNoSuchCredential, TTL expiry, transient-error not cached.
- Files changed:
  - internal/meta/cassandra/schema.go (access_keys table)
  - internal/meta/cassandra/credentials.go (new)
  - internal/auth/multi.go (new)
  - internal/auth/multi_test.go (new)
  - cmd/strata-gateway/main.go (wire MultiStore)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - PRDs misname types: AC says "auth.Store" / "auth.ErrCredentialNotFound" but the actual API is auth.CredentialsStore + auth.ErrNoSuchCredential. Always grep the package before mirroring AC names.
  - Cassandra disabled flag MUST surface as ErrNoSuchCredential, not as a separate error. The middleware path treats anything except a hit as 403 InvalidAccessKeyId; an explicit "disabled" error would create a different code path nothing else handles.
  - MultiStore caches negative results too — necessary defence against floods on unknown keys, but means new keys take up to ttl seconds to be visible after Put. US-007 DeleteAccessKey must call MultiStore.Invalidate; CreateAccessKey can skip Invalidate (no negative cache entry yet, or stampede acceptable).
  - For the cassandra.Store accessor: cmd uses metaStore.(*metacassandra.Store).Session() to share the session with auxiliary subsystems. When meta backend is "memory", IAM/access-key features simply don't get attached.
  - cassandra.CredentialStore.Put/Delete don't INVALIDATE the MultiStore cache directly — caller must Invalidate. Will codify this contract in IAM endpoints (US-007).
---

## 2026-04-25 - US-006 (IAM users + CreateUser/GetUser/ListUsers/DeleteUser endpoints)
- Added meta.IAMUser type and ErrIAMUserNotFound/ErrIAMUserAlreadyExists sentinels to internal/meta/store.go; meta.Store interface grew CreateIAMUser/GetIAMUser/ListIAMUsers/DeleteIAMUser.
- Memory backend mirrors via iamUsers map[string]*meta.IAMUser with the same map-clone-on-read pattern used elsewhere.
- Cassandra backend: new iam_users table (user_name text PK, user_id text, path text, created_at timestamp) appended to tableDDL; new internal/meta/cassandra/iam.go uses LWT (IF NOT EXISTS / IF EXISTS) so create/delete map cleanly to the sentinel errors.
- New internal/s3api/iam.go exports IAMRootPrincipal = "iam-root". handleIAM(action) gates every ?Action= endpoint on auth.FromContext(...).Owner == IAMRootPrincipal — anonymous and non-root principals get 403 AccessDenied.
- extractIAMAction(r) reads the Action param from URL query OR application/x-www-form-urlencoded body; iamParam(r,k) does the same for the rest. r.PostForm is parsed once via r.ParseForm() inside extractIAMAction; subsequent iamParam calls reuse it.
- ServeHTTP: when bucket=="" and Action != "", route to handleIAM before falling through to ListBuckets. Avoids colliding with the existing root-path GET behaviour.
- Response shapes match the AWS IAM XML schema (CreateUserResponse > CreateUserResult > User; ListUsersResponse > ListUsersResult > Users > member). Error codes: EntityAlreadyExists (409), NoSuchEntity (404), ValidationError (400), InvalidAction (400).
- UserId is generated as "AID" + 10-byte hex; CreateDate is RFC3339 UTC; Arn is built as "arn:aws:iam::strata:user<path><name>" with leading-slash normalization.
- Tests in internal/s3api/iam_test.go: anonymous denied, non-root principal denied, full round-trip CreateUser→GetUser→ListUsers→DeleteUser→GetUser=NoSuchEntity, double-create=EntityAlreadyExists, delete-missing=NoSuchEntity, missing UserName=ValidationError, unknown action=InvalidAction.
- Files changed:
  - internal/meta/store.go (IAMUser type, sentinels, interface methods)
  - internal/meta/memory/store.go (iamUsers map + 4 methods)
  - internal/meta/cassandra/iam.go (new)
  - internal/meta/cassandra/schema.go (iam_users table)
  - internal/s3api/iam.go (new)
  - internal/s3api/server.go (ServeHTTP IAM dispatch)
  - internal/s3api/iam_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - For ?Action= dispatch, branch in ServeHTTP on bucket=="" before other paths so IAM/STS endpoints (US-006/007/008) never collide with bucket-name routing. The root path is the canonical AWS IAM endpoint.
  - r.ParseForm() consumes the body once. Read the Action ONCE via extractIAMAction and rely on r.PostForm thereafter — calling ParseForm again is fine but repeated body reads are not.
  - LWT (IF NOT EXISTS / IF EXISTS) is the cleanest way to map create/delete to sentinel errors in cassandra; avoids a separate read-then-write round-trip.
  - The auth gate distinguishes anonymous vs non-root via Owner: do NOT trust !Anonymous alone — testutil_test.go injection sets Owner without setting Anonymous, so checks must look at Owner directly. (Same pattern as US-004 owner-FC.)
  - meta.Store grows with each user story; the compiler enforces both backends implement new methods, so add interface entries only after both implementations compile. The cassandra test build pulls in both.
  - When US-007 lands, append CreateAccessKey/ListAccessKeys/DeleteAccessKey to the iam.go switch and pass the user_name as a foreign key into access_keys (reuse cassandra.CredentialStore.Put/Delete from US-005). DeleteAccessKey must also call MultiStore.Invalidate on the access_key.
---

## 2026-04-25 - US-007 (CreateAccessKey/ListAccessKeys/DeleteAccessKey IAM endpoints)
- meta layer: added meta.IAMAccessKey {AccessKeyID, SecretAccessKey, UserName, CreatedAt, Disabled} and ErrIAMAccessKeyNotFound. meta.Store grew CreateIAMAccessKey/GetIAMAccessKey/ListIAMAccessKeys/DeleteIAMAccessKey (delete returns the deleted record so the cassandra implementation can locate the per-user index row).
- Memory backend: accessKeys map[string]*meta.IAMAccessKey keyed by AccessKeyID. ListIAMAccessKeys filters by UserName and sorts deterministically.
- Cassandra backend: additive ALTER TABLE access_keys ADD user_name; new iam_access_keys_by_user (PK (user_name, access_key)) for ListAccessKeys. internal/meta/cassandra/access_keys.go uses gocql LoggedBatch for create/delete so the lookup table and the per-user index stay consistent; Get reads only access_keys. Removed unused cassandra.CredentialStore.Put/Delete (IAM writes go through meta.Store now).
- internal/meta/memory/credentials.go: new memory.CredentialStore implements auth.CredentialsStore over the memory.Store, mirroring cassandra.CredentialStore semantics (ErrIAMAccessKeyNotFound → ErrNoSuchCredential; Disabled also surfaces as missing).
- internal/s3api/iam.go: handleIAM switch grew CreateAccessKey/ListAccessKeys/DeleteAccessKey. CreateAccessKey verifies the user exists (NoSuchEntity 404), generates AKIA-prefixed AccessKeyId (10-byte hex) + 30-byte base64 SecretAccessKey, and returns the secret ONCE in CreateAccessKeyResponse > CreateAccessKeyResult > AccessKey. ListAccessKeys requires UserName and emits AccessKeyMetadata > member entries (no secret). DeleteAccessKey calls Server.InvalidateCredential when set so MultiStore drops the cached lookup.
- s3api.Server.InvalidateCredential func(string) added; cmd/strata-gateway/main.go wires it to multi.Invalidate. main.go now also appends a memory.CredentialStore when the meta backend is in-memory, so memory deployments get the same dynamic credentials path that cassandra had.
- Tests:
  - internal/s3api/iam_access_keys_test.go (admin endpoints): anonymous-denied, non-root-denied, full Create→List→Delete round-trip with two keys, CreateAccessKey for missing user → NoSuchEntity, ListAccessKeys for missing user → 404, DeleteAccessKey missing → NoSuchEntity, missing UserName/AccessKeyId → ValidationError.
  - internal/s3api/iam_access_keys_signed_test.go: full SigV4 round-trip via signedHarness (auth.Middleware{ModeRequired} wrapping NewMultiStore[static iam-root, memory.CredentialStore]). CreateUser → CreateAccessKey → signed GET / with the new key passes (non-403); DeleteAccessKey via root; same signed GET → 403 InvalidAccessKeyId. Signing helpers (signRequest/canonicalQuery/canonicalPath/canonicalHeaderBlock) live in the test file because internal/auth's helpers are unexported.
- Files changed:
  - internal/meta/store.go (IAMAccessKey type, ErrIAMAccessKeyNotFound, interface methods)
  - internal/meta/memory/store.go (accessKeys map + 4 methods)
  - internal/meta/memory/credentials.go (new)
  - internal/meta/cassandra/schema.go (ALTER access_keys + iam_access_keys_by_user table)
  - internal/meta/cassandra/access_keys.go (new)
  - internal/meta/cassandra/credentials.go (drop unused Put/Delete)
  - internal/s3api/server.go (Server.InvalidateCredential field)
  - internal/s3api/iam.go (3 endpoints + dispatch)
  - internal/s3api/iam_access_keys_test.go (new)
  - internal/s3api/iam_access_keys_signed_test.go (new)
  - cmd/strata-gateway/main.go (memory credential store + invalidate hook)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - Cassandra-backed lists by a non-PK field require either a secondary index or a separate lookup table; this codebase prefers the lookup-table pattern (see iam_access_keys_by_user). Keep both writes inside a single LoggedBatch so a half-committed write does not orphan rows.
  - cassandra.CredentialStore is now read-only. All credential writes must go through meta.Store so memory and cassandra paths stay symmetric and tests run on the memory backend.
  - Server.InvalidateCredential is optional (nil-safe) — production wires it via main.go, tests can wire it via NewMultiStore + assignment. Without it, a deleted key would survive in MultiStore's cache for up to 60s, and the AC test would flake until TTL expires.
  - The signed-request test trick: pass the form via URL.RawQuery (not the body) so SigV4 signs the canonical query and the empty-body hash. The IAM dispatcher reads ?Action= via extractIAMAction, which checks the query first, so the same call path works for query-only and form-only senders.
  - SecretAccessKey is base64(30 random bytes) — 40 ASCII chars, matches AWS's secret-key alphabet (alphanumerics + /+=). Don't switch to hex unless you bump length, since AWS clients reject too-short secrets.
---

## 2026-04-25 - US-008 (Minimal STS AssumeRole endpoint)
- New auth package additions: ErrExpiredToken/ErrInvalidToken sentinels; Credential.SessionToken field; auth.STSStore (internal/auth/sts.go) holds in-memory sessions keyed by ASIA-prefixed AccessKey, evicts on expiry, exposes Issue(owner, ttl) → STSSession, Revoke(ak), SetClock(now) test hook. Lookup returns ErrExpiredToken on expired sessions and ErrNoSuchCredential after eviction.
- internal/auth/middleware.go: after signature verifies, if cred.SessionToken != "" then the request's X-Amz-Security-Token header (or X-Amz-Security-Token query param for presigned) must match — mismatch → ErrInvalidToken.
- internal/auth/multi.go: skip put() for creds with non-empty SessionToken so STS revoke/expiry take effect on the next lookup; ErrExpiredToken propagates (not ErrNoSuchCredential, so MultiStore's lastErr surfaces it).
- internal/s3api/errors.go: ErrExpiredToken (403 ExpiredToken) + ErrInvalidToken (403 InvalidToken) APIErrors; WriteAuthDenied maps "expired token" / "invalid security token" → those codes.
- internal/s3api/sts.go: stsAssumeRole handler gated on IAMRoot via handleIAM dispatch ("AssumeRole" case in iam.go switch). Required params: RoleArn, RoleSessionName. Optional DurationSeconds (900..43200, default DefaultSTSDuration=1h). Response shape: AssumeRoleResponse > AssumeRoleResult > Credentials{AccessKeyId, SecretAccessKey, SessionToken, Expiration} + AssumedRoleUser{AssumedRoleId, Arn}; xmlns="https://sts.amazonaws.com/doc/2011-06-15/".
- internal/s3api/server.go: Server.STS *auth.STSStore field; nil → AssumeRole returns 501 NotImplemented.
- cmd/strata-gateway/main.go: prepend auth.NewSTSStore() to the credential chain; api.STS = sts; the "no credential stores" guard now compares against the new baseline (STS + static = 2 stores).
- Tests:
  - internal/auth/sts_test.go: Issue/Lookup positive, ErrNoSuchCredential on unknown key, ExpiredToken via SetClock advance, default 1h TTL when ttl<=0, Revoke drops session, unique creds across Issues, MultiStore-no-cache for SessionToken creds.
  - internal/s3api/sts_signed_test.go: full SigV4 round-trip via newSTSHarness (re-uses signRequest from iam_access_keys_signed_test.go). Covers: AssumeRole returns the four credential fields with ASIA prefix; signed GET with the temp triple authenticates; missing/wrong x-amz-security-token → 403 InvalidToken; expired session via SetClock advance → 403 ExpiredToken; anonymous AssumeRole → 403; missing RoleArn/RoleSessionName → 400 ValidationError.
- Files changed:
  - internal/auth/types.go (Credential.SessionToken, ErrExpiredToken, ErrInvalidToken)
  - internal/auth/sts.go (new)
  - internal/auth/middleware.go (token verify after sig)
  - internal/auth/multi.go (skip cache for temp creds)
  - internal/auth/sts_test.go (new)
  - internal/s3api/errors.go (ExpiredToken/InvalidToken APIErrors + WriteAuthDenied mapping)
  - internal/s3api/iam.go (AssumeRole dispatch case)
  - internal/s3api/sts.go (new)
  - internal/s3api/server.go (Server.STS field)
  - internal/s3api/sts_signed_test.go (new)
  - cmd/strata-gateway/main.go (wire STSStore + api.STS)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - STS access keys MUST use a distinct prefix (ASIA) from long-term keys (AKIA) so MultiStore's iteration order does not produce collisions if a static store happens to know the same key. Trust the prefix; the MultiStore iterates and only caches a hit when it lacks SessionToken.
  - ErrExpiredToken is NOT ErrNoSuchCredential, so MultiStore's "continue on miss" loop surfaces it as lastErr — exactly what we want. If you ever add a third terminal error for credentials, follow the same pattern.
  - SessionToken is verified outside of SigV4 SignedHeaders. Real AWS includes x-amz-security-token in SignedHeaders (so a tampered token also breaks the signature), but for our minimal impl the explicit header compare is enough; tests deliberately do NOT include the token in signRequest's signedHeaders to keep the helper small.
  - SetClock(now func() time.Time) is the cleanest way to drive expiry in tests. The clock is read inside Issue (to set Expiration) and Lookup (to check it); make sure both reads go through the hook.
  - When wiring a new CredentialsStore at gateway startup, prepend it to the chain only when faster lookups should win; the static store and DB-backed stores rely on the early-return semantics of MultiStore. STS lives at index 0 so a brief scan of in-memory sessions runs before any DB hop.
  - The handleIAM gate enforces IAMRoot — minimal STS reuses that gate so AssumeRole is admin-only. If we later need cross-account workflows, factor the gate into a per-action allowlist instead of a single principal check.
---

## 2026-04-25 - US-009 (Verify + persist x-amz-checksum-* on PutObject)
- meta.Object grew Checksums map[string]string; cassandra schema appended additive `ALTER TABLE objects ADD checksums map<text, text>`; cassandra PutObject INSERT and GetObject SELECT extended to round-trip the column. Memory backend round-trips via the existing shallow-copy PutObject path (Checksums map ref copied alongside Tags/UserMeta).
- New internal/s3api/checksum.go centralises the per-algo machinery. supportedChecksumAlgorithms = [CRC32, CRC32C, SHA1, SHA256, CRC64NVME]. newChecksumHasher returns a hash.Hash (hash32Adapter wraps crc32.NewIEEE / Castagnoli; hash64Adapter wraps the local CRC-64/NVME). parseRequestChecksums(r) walks the supported list, reads x-amz-checksum-<lower> headers, base64-decodes (validation only), and pairs each with a fresh hasher. checksumWriter fans body bytes to all hashers in one pass; verifyChecksums finalises and compares; writeChecksumHeaders echoes the persisted map on GetObject/HeadObject.
- CRC-64/NVME impl: polynomial 0x9a6c9329ac4bc9b5 (reflected), init 0xFFFF...FFFF, RefIn=true, RefOut=true, XorOut 0xFFFF...FFFF. Table-based (sync.Once) hash.Hash64 implementation. Verified against the AWS-published test vector: CRC64NVME("123456789") == 0xae8b14860a799888.
- internal/s3api/server.go putObject: parses checksum headers up-front (malformed b64 → 400 InvalidArgument), wraps r.Body in io.TeeReader(checksumWriter) before PutChunks, verifies after the chunked write, on mismatch deletes the just-written manifest and returns 400 BadDigest. On success the computed map[algo]base64 lands in obj.Checksums and is persisted via meta.PutObject. getObject sets x-amz-checksum-<algo> headers from the persisted map (always echoed when present).
- Tests in internal/s3api/checksum_test.go:
  - TestChecksumRoundTripPerAlgo: 5 subtests (CRC32, CRC32C, SHA1, SHA256, CRC64NVME). PUT with a correct digest succeeds and the same digest comes back on GET and HEAD.
  - TestChecksumMismatchPerAlgo: 5 subtests; PUT with the digest of a different payload returns 400 BadDigest and the object is not visible on a follow-up GET (404).
  - TestChecksumMissingHeaderOptional: PUT without any checksum header succeeds; GET emits no x-amz-checksum-* headers.
  - TestChecksumInvalidBase64Rejected: malformed b64 in the header → 400.
  - TestCRC64NVMEKnownVector: validates the AWS test vector (0xae8b14860a799888) so future refactors of the table can't silently break compatibility.
- New helper s3api.CRC64NVMEForTest(p) is the only export in checksum.go and exists solely so checksum_test.go can compute expected CRC64NVME digests without re-deriving the reflected table.
- Files changed:
  - internal/meta/store.go (Object.Checksums)
  - internal/meta/cassandra/schema.go (additive ALTER objects ADD checksums)
  - internal/meta/cassandra/store.go (PutObject INSERT + GetObject SELECT)
  - internal/s3api/checksum.go (new — algos + CRC64NVME + helpers + ErrBadDigest)
  - internal/s3api/server.go (parseRequestChecksums + TeeReader + verify + writeChecksumHeaders)
  - internal/s3api/checksum_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - hash/crc32 returns hash.Hash32 and crypto/sha1+sha256 return hash.Hash; uniform fan-out (TeeReader) wants a single hash.Hash interface — wrap the 32/64-bit variants via tiny embed-style adapters (hash32Adapter / hash64Adapter). Reuse for any future digest endpoints.
  - When verifying a content-derived digest, do the verify AFTER the body has been fully consumed by PutChunks (TeeReader writes to the hasher synchronously with Reads). Do NOT read the body twice — multipart parts are megabytes and second-read forces buffering.
  - On BadDigest, MUST delete the chunks the data backend just persisted (s.Data.Delete(m)); otherwise garbage chunks accumulate. Mirror this in any future verifier (e.g. SSE-C MD5 mismatch in US-012, multipart per-part checksum in US-010).
  - meta backends share the Object struct, so adding a field is observable everywhere with no per-backend wiring beyond the cassandra column SELECT/INSERT pair. Memory store needed no change — shallow-copy already round-trips the map ref.
  - CRC-64/NVME is not in the stdlib; vendoring a small reflected-table implementation is the simplest path. Keep the test vector test in place — it's the cheapest possible regression guard for the polynomial constant and the reflected bit-order.
  - For US-010 (multipart): re-use this exact verifier per-part inside uploadPart, then store the per-part base64 in meta.MultipartPart.Checksums; CompleteMultipartUpload combines them per AWS rules (concat-and-hash for SHAs, sum-of-hex for CRCs) — share newChecksumHasher and CRC64NVMEForTest.
---

## 2026-04-25 - US-010 (Multipart UploadPart and CompleteMultipartUpload checksum support)
- meta.MultipartPart grew Checksums map[string]string. Memory backend round-trips via the existing shallow-copy SavePart path. Cassandra: additive ALTER TABLE multipart_parts ADD checksums map<text, text>; SavePart INSERT and ListParts SELECT extended.
- internal/s3api/multipart.go uploadPart: parseRequestChecksums up-front (malformed b64 -> 400 InvalidArgument), wraps r.Body in io.TeeReader(checksumWriter) before PutChunks, runs verifyChecksums after, on mismatch deletes the part manifest and returns 400 BadDigest. On success the computed map is persisted via SavePart and echoed via writeChecksumHeaders on the 200 response.
- internal/s3api/multipart.go completeMultipart: calls ListParts BEFORE CompleteMultipartUpload (the meta-level Complete deletes parts on success in both backends so we'd lose them). Builds byNumber, then walks the user-supplied parts list to build []*checksumPart (rejects unknown part numbers with 400 InvalidPart). composeMultipartChecksums (new helper in checksum.go) computes the AWS composite per algo: base64-decode every part's digest, run the algo over the raw concatenation, base64-encode, suffix "-N". Algos missing on any part are skipped; if no algo is universally present the composite map is nil. The composite is assigned to obj.Checksums (so GetObject/HeadObject echo it via writeChecksumHeaders) and to the ChecksumCRC32/CRC32C/SHA1/SHA256/CRC64NVME omitempty fields of completeMultipartResult; response headers x-amz-checksum-<algo> are also emitted via writeChecksumHeaders.
- New tests in internal/s3api/multipart_checksum_test.go:
  - TestMultipartChecksumRoundTripCRC32: 3-part upload with x-amz-checksum-crc32 per part. Verifies each part's response echo header, the composite header on the Complete response, the <ChecksumCRC32>...-3</ChecksumCRC32> XML element, and the GET-side echo of the same composite.
  - TestMultipartUploadPartChecksumMismatch: PUT a part with the wrong digest -> 400 BadDigest, ListParts shows no persisted part.
  - TestMultipartCompleteWithoutChecksums: 2-part upload with no checksum headers -> Complete response has no x-amz-checksum-* headers and no <Checksum*> XML elements.
- Files changed:
  - internal/meta/store.go (MultipartPart.Checksums)
  - internal/meta/cassandra/schema.go (additive ALTER multipart_parts ADD checksums)
  - internal/meta/cassandra/store.go (SavePart INSERT + ListParts SELECT)
  - internal/s3api/checksum.go (composeMultipartChecksums + checksumPart)
  - internal/s3api/multipart.go (uploadPart verify; completeMultipart compose)
  - internal/s3api/xml.go (completeMultipartResult Checksum* fields)
  - internal/s3api/multipart_checksum_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - The meta-level CompleteMultipartUpload in BOTH backends deletes parts on success; ListParts MUST run before the Complete call or you lose access to per-part metadata (checksums, custom MD5s for SSE-C in US-012, etc.). Don't try to fold this into the meta interface — leave Complete idempotent and do auxiliary reads at the s3api layer.
  - composeMultipartChecksums is intentionally generic (operates on []*checksumPart with just a Checksums map) so US-012 (SSE-C copy) and other future per-part metadata helpers can reuse the "intersection of algos across parts" pattern without depending on the meta package directly.
  - AWS composite checksum is "base64(hash(concat(raw_digests)))-N" for ALL five algos (no separate "full-object CRC combine" math here). If we ever support the FULL_OBJECT type for CRCs, that's a separate code path triggered by x-amz-checksum-type: FULL_OBJECT on initiate.
  - The completeMultipartResult struct now has 5 Checksum<Algo> omitempty XML fields. When adding a sixth algo, remember to add the field AND wire it in the writeXML call site, in addition to extending supportedChecksumAlgorithms.
  - cassandra ListParts iter.Scan must reset the checksums map between rows; otherwise the gocql driver re-uses the same backing map across rows when the next row has no value (uncommon for a per-part column but cheap to defend).
---

## 2026-04-25 - US-011 (SSE-S3 metadata storage + bucket default)
- meta.Object.SSE and meta.MultipartUpload.SSE strings added; ErrNoSuchEncryption sentinel + Set/Get/DeleteBucketEncryption interface methods.
- Memory backend: encryption blob map + 3 methods using setBucketBlob/getBucketBlob/deleteBucketBlob; SSE round-trips for object + upload via existing shallow-copy paths.
- Cassandra: new bucket_encryption table; additive ALTER objects ADD sse text + ALTER multipart_uploads ADD sse text. PutObject INSERT, GetObject SELECT, CreateMultipartUpload INSERT, GetMultipartUpload SELECT all extended (cursors used by listing intentionally do not select sse — listing does not expose SSE).
- internal/s3api/encryption.go: ServerSideEncryptionConfiguration XML, putBucketEncryption/getBucketEncryption/deleteBucketEncryption (uses meta.SetBucketEncryption blob helpers). validateSSEAlgorithm (AES256 → ok; aws:kms/aws:kms:dsse → 501 NotImplemented; else → 400 InvalidArgument). resolveSSE(r,b) → header wins, bucket default fills in, missing config → empty.
- internal/s3api/server.go: ?encryption route in handleBucket; putObject calls resolveSSE BEFORE PutChunks (so a kms/bogus header fails before chunks land); getObject sets x-amz-server-side-encryption when persisted.
- internal/s3api/multipart.go: initiateMultipart resolves SSE and stores on mu.SSE; uploadPart echoes mu.SSE on the per-part response; completeMultipart copies mu.SSE to obj.SSE and emits x-amz-server-side-encryption header.
- New errors: ErrNoSuchEncryption (ServerSideEncryptionConfigurationNotFoundError 404), ErrInvalidEncryptionAlgorithm (InvalidArgument 400), ErrKMSNotImplemented (NotImplemented 501).
- Tests in internal/s3api/encryption_test.go: per-object PUT/GET/HEAD echo of AES256, aws:kms rejected (501), unknown algo rejected (400), bucket encryption CRUD round-trip, missing config → 404, bucket default applied to header-less PUT, bucket-level kms put rejected, malformed bucket body rejected, multipart end-to-end SSE round-trip (Initiate → UploadPart → Complete → HEAD).
- Files changed:
  - internal/meta/store.go (Object.SSE, MultipartUpload.SSE, ErrNoSuchEncryption, interface methods)
  - internal/meta/memory/store.go (encryption blob map + 3 methods)
  - internal/meta/cassandra/schema.go (bucket_encryption table + additive ALTERs for sse columns)
  - internal/meta/cassandra/store.go (object/multipart upload sse round-trip + 3 blob methods)
  - internal/s3api/encryption.go (new)
  - internal/s3api/errors.go (3 new APIErrors)
  - internal/s3api/server.go (route + putObject + getObject)
  - internal/s3api/multipart.go (initiate/uploadPart/complete SSE plumbing)
  - internal/s3api/encryption_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - SSE algorithm validation must run BEFORE PutChunks. Otherwise a request with x-amz-server-side-encryption: aws:kms (or any bogus value) writes chunks to the data backend before the 4xx fires, and you have to delete them. Mirrors the BadDigest/checksum pattern but moved earlier because no body is needed to validate.
  - For multipart, SSE is decided ONCE at initiate and persisted on the MultipartUpload row; UploadPart and Complete just echo what's already there. Don't try to read x-amz-server-side-encryption from per-part requests — AWS rejects it (we just ignore it).
  - The bucket-default fallback path means resolveSSE issues a Meta.GetBucketEncryption per request without a header. That is one extra cassandra read on the put path; if it ever becomes hot enough to matter, the same caching trick used for credentials (TTL cache) would apply, but it's not worth pre-empting.
  - When extending Object/MultipartUpload struct fields (SSE here, future SSEC fields in US-012, RestoreStatus in US-014), the cassandra cursor selects in ListObjects/ListObjectVersions intentionally do NOT need to be extended unless the listing payload exposes the new field — keep them lean.
  - aws:kms:dsse is the dual-layer KMS variant; it shows up in newer SDKs alongside aws:kms. Reject both to the same NotImplemented code so the client error message is consistent regardless of SDK age.
---

## 2026-04-25 - US-012 (SSE-C customer-key headers)
- meta.Object.SSECKeyMD5 string added; cassandra schema appended additive ALTER objects ADD ssec_key_md5 text; PutObject INSERT + GetObject SELECT extended in both versionID branches. Memory backend round-trips via existing shallow-copy (no code change required).
- internal/s3api/encryption.go grew parseSSECHeaders + parseCopySourceSSECHeaders + requireSSECMatch + parseSSECTriple. Validation: triple all-or-none (partial → 400 InvalidRequest), algo == AES256 (else 400 InvalidArgument), base64 key decodes to exactly 32 bytes (else 400 InvalidArgument), md5(rawKey) base64 == headerMD5 (else 400 InvalidDigest).
- internal/s3api/server.go putObject: parseSSECHeaders BEFORE PutChunks, skip resolveSSE bucket-default lookup when SSE-C is present (mutually exclusive), persist obj.SSECKeyMD5 on success.
- internal/s3api/server.go getObject: when o.SSECKeyMD5 != "" call requireSSECMatch (missing → ErrSSECRequired 400; mismatch → ErrSSECKeyMismatch 400 AccessDenied). On success emit x-amz-server-side-encryption-customer-algorithm + -key-MD5 echo headers.
- internal/s3api/copy_object.go: parseCopySourceSSECHeaders required when source has SSECKeyMD5 (missing → 400 InvalidRequest, mismatch → 400 AccessDenied); parseSSECHeaders for dest, dst.SSECKeyMD5 set when present.
- New errors in errors.go: ErrInvalidRequest (400 InvalidRequest), ErrInvalidDigest (400 InvalidDigest), ErrSSECRequired (400 InvalidRequest), ErrSSECKeyMismatch (400 AccessDenied — status 400 per AC, code AccessDenied is the AWS norm for SSE-C key mismatch).
- Tests in internal/s3api/sse_c_test.go: TestSSECPutGetRoundTrip (PUT+GET+HEAD echo of headers + body), TestSSECGetMissingKey (400 InvalidRequest), TestSSECPutWrongMD5 (400 InvalidDigest + follow-up 404), TestSSECGetWrongMD5 (400 AccessDenied), TestSSECPutInvalidKeyLength (400 InvalidArgument), TestSSECPutBadAlgorithm (400), TestSSECPartialHeadersRejected (400 InvalidRequest), TestSSECCopyObjectMirrorHeaders (positive copy with source headers, missing source headers reject).
- Files changed:
  - internal/meta/store.go (Object.SSECKeyMD5)
  - internal/meta/cassandra/schema.go (ALTER objects ADD ssec_key_md5)
  - internal/meta/cassandra/store.go (PutObject INSERT + GetObject SELECT round-trip)
  - internal/s3api/encryption.go (SSE-C parsers + matcher)
  - internal/s3api/errors.go (4 new APIErrors)
  - internal/s3api/server.go (putObject + getObject SSE-C plumbing)
  - internal/s3api/copy_object.go (copy-source + dest SSE-C plumbing)
  - internal/s3api/sse_c_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - SSE-C and SSE-S3 are mutually exclusive on the same PUT — when SSE-C headers are present, SKIP resolveSSE so the bucket-default doesn't double-apply. Real AWS rejects the combo with 400; we silently prefer SSE-C, which is good enough for the s3-tests subset.
  - The customer key itself is NEVER persisted — only its base64-encoded MD5 lands in obj.SSECKeyMD5. Anyone reading the row cannot decrypt; on GET the client must re-supply the raw key, we re-derive its MD5, and compare. (We don't actually use the key to en/decrypt because Strata stores plaintext today.)
  - The "GetObject mismatched MD5 → 400 AccessDenied" status/code combo is unusual (AWS uses 403); the AC pins 400. Keep the status field separate from the code field in APIError so this kind of off-AWS mapping is one-line.
  - For per-request SSE-C parsing, validate the triple ONCE at the top of putObject/getObject/copyObject — don't re-read individual headers later, because partial-triple cases can sneak through.
  - When extending Object/MultipartUpload struct fields (SSECKeyMD5 here), the cassandra cursor SELECTs in ListObjects/ListObjectVersions still don't need to grow — listing does not expose SSE-C metadata. Keep the listing reads lean.
  - For multipart uploads, SSE-C would need to be remembered on MultipartUpload across UploadPart/Complete (mirroring SSE field). Not in scope for US-012 AC; revisit when s3-tests adds multipart-SSE-C coverage.
---

## 2026-04-25 - US-013 (GetObjectAttributes endpoint)
- New internal/s3api/object_attributes.go: GetObjectAttributesOutput XML response with ETag, Checksum (CRC32/CRC32C/SHA1/SHA256/CRC64NVME), ObjectParts (PartsCount + truncation cursor placeholders), StorageClass, ObjectSize. The header `x-amz-object-attributes` is REQUIRED — missing → 400 InvalidRequest; unknown attribute name → 400 InvalidArgument; the response only populates the listed fields (Checksum/ObjectParts use pointer-omitempty; ObjectSize uses *int64 so 0-byte objects still appear when requested).
- Route wired in handleObject as `q.Has("attributes") && GET` BEFORE the generic GET fallback; gated via requireObjectAccess(action="s3:GetObject") so the policy/ACL stack from US-003/US-004 still applies. Versioning param ?versionId is honored on the underlying GetObject lookup. SSE-C protected objects (o.SSECKeyMD5 != "") run requireSSECMatch — same gate as getObject so a stripped SSEC header on this endpoint also rejects.
- ObjectParts is derived from the ETag suffix `-N`. Per-part rows are deleted on Complete (see US-010 learnings) so the parts list itself is empty; PartsCount alone is sufficient for the multipart fingerprint clients (e.g. SDK multipart-copy preflight) need.
- Tests in internal/s3api/object_attributes_test.go: full attribute set on a single-part PUT (verifies ETag, StorageClass, ObjectSize, Checksum.ChecksumSHA256, AND absence of ObjectParts); partial subset (only ETag+ObjectSize → no other tags); missing key 404 NoSuchKey; missing header 400 InvalidRequest; unknown attribute 400; multipart 3-part round-trip → ObjectParts.PartsCount=3.
- Files changed:
  - internal/s3api/object_attributes.go (new)
  - internal/s3api/server.go (handleObject ?attributes branch)
  - internal/s3api/object_attributes_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - When emitting an XML field whose zero value is meaningful (e.g. ObjectSize=0 is a valid response), use *T + xml:",omitempty" — the omitempty tag treats nil as missing but zero-T as present. `int64` with omitempty would silently swallow the legitimate 0 case.
  - The handleObject subresource ladder is order-sensitive: every new ?<sub> branch must sit BEFORE the bottom method switch, otherwise the generic GET path swallows the request and serves the object body. Mirror the existing acl/tagging/retention pattern.
  - PartsCount can be reconstructed from the ETag suffix (`<hex>-<N>`) without preserving per-part rows after Complete. multipartPartsFromETag is a self-contained helper — reuse for any future endpoint that needs the multipart fingerprint (e.g. ListObjects with ?fetch-owner=true or CopyObject's same-source preflight).
  - x-amz-object-attributes is comma-separated. Tolerate whitespace via TrimSpace; reject unknown values with InvalidArgument (so clients learn quickly) rather than silently ignoring — this matches the spec and keeps the AC's "only listed fields populated" semantics auditable.
  - SSE-C gating must be repeated on every read endpoint that surfaces object metadata; do NOT factor it into a single chokepoint, because GetObject/HeadObject already inline-check it. Pattern: if o.SSECKeyMD5 != "" then requireSSECMatch(r, o.SSECKeyMD5) before writing the response.
---

## 2026-04-25 - US-014 (RestoreObject stub endpoint)
- Added meta.Object.RestoreStatus string + meta.Store.SetObjectRestoreStatus(ctx, bucketID, key, versionID, status) interface method.
- Memory backend: RestoreStatus round-trips via existing shallow copy; setter calls findLatest then assigns.
- Cassandra: additive ALTER objects ADD restore_status text; PutObject INSERT extended (column appended at end), GetObject SELECT extended, new SetObjectRestoreStatus uses UPDATE objects SET restore_status=?.
- New internal/s3api/restore.go: postObjectRestore parses RestoreRequest XML (Days+Tier optional, default Days=1, 1MiB body cap). Empty body OK. Malformed XML → 400 MalformedXML. Missing object → 404 NoSuchKey via mapMetaErr. On success persists 'ongoing-request="false", expiry-date="<HTTP date>"' format string into Object.RestoreStatus, returns 200.
- internal/s3api/server.go handleObject: routes ?restore POST BEFORE the bottom method switch; gates via requireObjectAccess action s3:RestoreObject. getObject sets x-amz-restore header when o.RestoreStatus != "" (HEAD shares the same handler).
- Tests internal/s3api/restore_test.go: round-trip POST→HEAD (echoes ongoing-request + expiry-date), 404 NoSuchKey on missing key, 400 MalformedXML on bad body, default Days=1 when body empty.
- Files changed:
  - internal/meta/store.go (Object.RestoreStatus + interface method)
  - internal/meta/memory/store.go (SetObjectRestoreStatus)
  - internal/meta/cassandra/store.go (PutObject/GetObject column, SetObjectRestoreStatus)
  - internal/meta/cassandra/schema.go (ALTER objects ADD restore_status text)
  - internal/s3api/server.go (?restore route + x-amz-restore header in getObject)
  - internal/s3api/restore.go (new)
  - internal/s3api/restore_test.go (new)
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - When extending meta.Object with a new persisted field, the cassandra column-list ordering matters for code review readability but the SQL itself only cares about positional binding — append the column at the END of both INSERT and SELECT lists (mirroring the additive ALTER) so future iterations don't have to renumber every site. The PutObject/GetObject column lists are the canonical pair to keep in sync.
  - x-amz-restore is one of those headers HEAD must include but GET typically does too; sharing getObject(body=true|false) is the existing idiom — do not split into a separate HEAD handler just to add response metadata.
  - For storage-only stub endpoints (no real backend behaviour), persist the FORMATTED header value directly into the Object field rather than storing structured state and re-formatting per request. Saves a Sprintf on the hot read path; the cost is one re-write per restore call (rare).
---

## 2026-04-25 - US-015 (PutBucketObjectLockConfiguration / GetBucketObjectLockConfiguration with default-retention inheritance)
- meta.Bucket.ObjectLockEnabled bool added; cassandra schema appended additive ALTER buckets ADD object_lock_enabled boolean; GetBucket SELECT extended to include the column (NULL → false via gocql zero-value scan). Memory backend round-trips via existing shallow-copy.
- New meta.ErrNoSuchObjectLockConfig sentinel + meta.Store.SetBucketObjectLockEnabled(ctx, name, enabled) / SetBucketObjectLockConfig / GetBucketObjectLockConfig / DeleteBucketObjectLockConfig interface methods.
- Cassandra: new bucket_object_lock blob table (PK bucket_id) wired through the standard setBucketBlob/getBucketBlob/deleteBucketBlob helpers. SetBucketObjectLockEnabled uses LWT (UPDATE ... IF EXISTS, ScanCAS(nil), LocalSerial) — same pattern as SetBucketVersioning/SetBucketACL so the write joins the same Paxos lineage as the original CreateBucket INSERT IF NOT EXISTS.
- Memory backend: objectLock map[uuid.UUID][]byte for the config blob; SetBucketObjectLockEnabled mutates the in-place Bucket struct under mu.Lock.
- internal/s3api/objectlock.go grew:
  - objectLockConfiguration XML type (ObjectLockEnabled string + Rule.DefaultRetention{Mode, *Days, *Years}).
  - putBucketObjectLockConfig: requires bucket.ObjectLockEnabled (else 400 InvalidRequest via ErrObjectLockNotEnabled), 1MiB body cap, validates DefaultRetention.Mode ∈ {GOVERNANCE,COMPLIANCE}, Days/Years > 0, Days vs Years mutually exclusive. Persists raw XML blob via SetBucketObjectLockConfig.
  - getBucketObjectLockConfig: returns blob or 404 ObjectLockConfigurationNotFoundError (ErrNoSuchObjectLockConfig).
  - resolveDefaultRetention(r, b) helper: when bucket has ObjectLockEnabled and a persisted DefaultRetention, returns (Mode, time.Now+Days|+Years); zero values otherwise.
- internal/s3api/server.go:
  - handleBucket: added ?object-lock route (GET/PUT) BEFORE the bottom method switch.
  - CreateBucket (PUT branch): when x-amz-bucket-object-lock-enabled: true header is present, calls SetBucketObjectLockEnabled(name, true) right after CreateBucket (mirrors x-amz-acl handling).
  - putObject: after explicit x-amz-object-lock-mode/retain-until-date header parsing, if BOTH are absent calls resolveDefaultRetention(r, b) and applies the inherited (Mode, RetainUntil). Explicit per-request headers override the bucket default.
- New errors in internal/s3api/errors.go: ErrNoSuchObjectLockConfig (404 ObjectLockConfigurationNotFoundError), ErrObjectLockNotEnabled (400 InvalidRequest 'Bucket is missing Object Lock Configuration').
- Tests in internal/s3api/objectlock_test.go: bucket lock CRUD round-trip (PUT/GET/echo XML); PUT object-lock without x-amz-bucket-object-lock-enabled at CreateBucket → 400 InvalidRequest; malformed DefaultRetention mode → 400 MalformedXML; PUT object inherits Mode + RetainUntilDate header from bucket default on header-less PUT; COMPLIANCE-inherited PUT cannot be deleted (403 AccessDenied via existing ErrObjectLockedErr); explicit per-request headers override default (GOVERNANCE wins over default COMPLIANCE).
- Files changed:
  - internal/meta/store.go (Bucket.ObjectLockEnabled, ErrNoSuchObjectLockConfig, 4 interface methods)
  - internal/meta/memory/store.go (objectLock map + 4 methods)
  - internal/meta/cassandra/schema.go (bucket_object_lock table + ALTER buckets ADD object_lock_enabled)
  - internal/meta/cassandra/store.go (GetBucket SELECT extended; 4 method impls)
  - internal/s3api/objectlock.go (XML type + 2 handlers + resolveDefaultRetention helper)
  - internal/s3api/server.go (?object-lock route; CreateBucket header; putObject inheritance)
  - internal/s3api/errors.go (2 new APIErrors)
  - internal/s3api/objectlock_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - meta.Bucket fields (ObjectLockEnabled here) flow through the same LWT pattern as SetBucketVersioning/SetBucketACL. Using a non-LWT UPDATE on the buckets row breaks Paxos coherence (see CLAUDE.md gotcha) — every column added to the buckets row needs its setter to use IF EXISTS + ScanCAS(nil) + LocalSerial.
  - CreateBucket signature stays at (name, owner, defaultClass) — new bucket-creation-time flags are applied via separate setters right after CreateBucket succeeds (mirrors x-amz-acl handling). Avoids signature churn across both backends as flags accumulate.
  - resolveDefaultRetention mirrors resolveSSE (US-011): request header wins, bucket default fills in. Validation runs at PUT-config time, not at PUT-object time, so per-object PUT stays cheap (one GetBucketObjectLockConfig + one Unmarshal). If this becomes hot, the same TTL cache pattern used for credentials applies.
  - For *int Days/Years XML fields, use *int (pointer) + omitempty so 0 vs absent stays distinguishable. The validator needs to reject *Days <= 0 because XML unmarshal gives 0 when the element is empty (e.g. <Days/>).
  - The existing buckets table schema had an unused frozen<objectlock_cfg> UDT column — left alone to avoid touching pre-existing schemas. The new object_lock_enabled boolean is the canonical persisted flag from this point on.
  - When the AC says "this flag is required for object-lock config", the right code is 400 InvalidRequest (not 409 — AWS docs use InvalidRequest for this case). Reuse ErrObjectLockNotEnabled rather than inventing new codes for related preconditions.
---

## 2026-04-25 - US-016 (PutBucketNotificationConfiguration / GetBucketNotificationConfiguration)
- meta.ErrNoSuchNotification sentinel added to internal/meta/store.go.
- meta.Store grew SetBucketNotificationConfig / GetBucketNotificationConfig / DeleteBucketNotificationConfig; both backends use the existing setBucketBlob/getBucketBlob/deleteBucketBlob helpers.
- Memory backend: notification map[uuid.UUID][]byte added to Store struct and constructor.
- Cassandra: new bucket_notification (bucket_id PK, config blob) appended to tableDDL.
- New internal/s3api/notification.go:
  - putBucketNotification: parses NotificationConfiguration XML; empty body deletes the config; well-formed but lacking Topic/Queue/CloudFunction/LambdaFunction/EventBridge → 400 MalformedXML; persists raw XML.
  - getBucketNotification: serves the stored blob or 404 NoSuchConfiguration.
- New ErrNoSuchNotificationConfig (Code=NoSuchConfiguration, 404) in errors.go.
- Route ?notification GET/PUT wired in handleBucket adjacent to ?object-lock.
- Tests in internal/s3api/notification_test.go: Topic/Queue/Lambda round-trip, malformed XML 400, well-formed-but-empty 400, empty body clears + subsequent GET 404 NoSuchConfiguration, missing bucket 404.
- Files changed:
  - internal/meta/store.go
  - internal/meta/memory/store.go
  - internal/meta/cassandra/store.go
  - internal/meta/cassandra/schema.go
  - internal/s3api/errors.go
  - internal/s3api/notification.go (new)
  - internal/s3api/notification_test.go (new)
  - internal/s3api/server.go
  - scripts/ralph/prd.json (passes: true)
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - AWS lets clients clear a bucket notification by PUT with an empty body OR by PUT with `<NotificationConfiguration/>`. The PRD AC explicitly required only the empty-body path — AC also says "at least one of TopicConfiguration/QueueConfiguration/LambdaFunctionConfiguration", so well-formed-but-empty is rejected with 400. If we later relax this, drop the Topic/Queue/Lambda count check and add an explicit DeleteBucketNotificationConfig branch on the empty XML envelope.
  - LambdaFunctionConfiguration is the modern AWS name; CloudFunctionConfiguration is the legacy alias still emitted by some SDKs. Validation accepts either; the persisted XML is the client's verbatim bytes so any consumer reading back gets exactly what was sent.
  - notificationStub uses `xml:",innerxml" []byte` so we keep the validation cheap (presence-of-children check) without committing to the full event/filter schema. Future enhancements (e.g. event-based dispatch) can swap in a richer struct without touching the meta layer.
---

## 2026-04-26 - US-017 (PutBucketWebsite / GetBucketWebsite / DeleteBucketWebsite + index document routing)
- meta layer: ErrNoSuchWebsite sentinel + SetBucketWebsite/GetBucketWebsite/DeleteBucketWebsite on meta.Store (memory + cassandra). Cassandra adds bucket_website (bucket_id PK, config blob) to tableDDL. Both backends reuse setBucketBlob/getBucketBlob/deleteBucketBlob.
- internal/s3api/website.go: putBucketWebsite/getBucketWebsite/deleteBucketWebsite + serveWebsiteRoot. Body limit 1<<20. Validation: empty body 400, malformed XML 400, IndexDocument required when no RedirectAllRequestsTo, IndexDocument.Suffix may not contain '/'. websiteConfiguration uses innerxml []byte for RoutingRules so we don't commit to the full XML schema (same trick as notificationStub).
- ErrNoSuchWebsiteConfig added (404 NoSuchWebsiteConfiguration).
- ?website wired into handleBucket (PUT/GET/DELETE) BEFORE the generic switch.
- handleBucket GET (key=='', no query params, len(q)==0) calls serveWebsiteRoot first: if website cfg present, GetObject(IndexDocument.Suffix); on hit serve via serveWebsiteObject (200), on miss fall through to serveWebsiteError. serveWebsiteError fetches ErrorDocument.Key and serves with 404, else writes ErrNoSuchKey. When no website cfg, returns false → existing listObjects path runs.
- Tests in internal/s3api/website_test.go: CRUD round-trip, malformed/empty/InvalidArgument cases, index serving (200 + Content-Type passthrough), error-doc fallback (404 body), no-index-no-error (404 NoSuchKey), no-config falls through to ListBucketResult, missing bucket 404.
- Files changed:
  - internal/meta/store.go (sentinel + iface)
  - internal/meta/memory/store.go (website map + 3 methods)
  - internal/meta/cassandra/schema.go (bucket_website table)
  - internal/meta/cassandra/store.go (3 methods)
  - internal/s3api/errors.go (ErrNoSuchWebsiteConfig)
  - internal/s3api/website.go (new)
  - internal/s3api/server.go (?website route + GET intercept)
  - internal/s3api/website_test.go (new)
  - scripts/ralph/prd.json (passes: true; notes)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - GET intercept for static-site index serving must guard on `len(q) == 0` so listObjects pagination params (prefix/delimiter/continuation-token/start-after/max-keys) keep their listing behaviour. Don't accidentally route every GET on /<bucket>/ through the website fallback.
  - serveWebsiteRoot returns bool: true means "request handled, do not fall through". This pattern keeps the website branch self-contained and lets the listObjects path remain untouched when no website cfg is set.
  - splitPath behaviour: GET /bkt/ produces (bucket=bkt, key=""), so the website index path is reachable via the bucket handler — no separate routing rule needed.
  - The website-mode 404 body is the ErrorDocument's content (text/html), NOT an XML <Error/> envelope. Writers must NOT call writeError for the error-doc path; serveWebsiteObject sets headers + status itself.
  - When a future story (e.g. RedirectAllRequestsTo) needs to act differently, branch off in serveWebsiteRoot before the IndexDocument lookup; the parsed *websiteConfiguration is already in hand.
---

## 2026-04-26 - US-018 (Bucket replication CRUD + PutObject status header)
- meta.ErrNoSuchReplication sentinel + meta.Store.{Set,Get,Delete}BucketReplication added; both backends use the standard setBucketBlob/getBucketBlob/deleteBucketBlob helpers.
- Cassandra: new bucket_replication table appended to tableDDL (additive). Memory: replication map[uuid.UUID][]byte initialized in New().
- New internal/s3api/replication.go:
  - replicationConfiguration / replicationRule / replicationFilter / replicationAndOp / replicationTag / replicationDestination XML types.
  - putBucketReplication: GetBucket → ReadAll(LimitReader 1MiB) → empty body 400 → xml.Unmarshal → at least one Rule with non-empty Destination.Bucket → SetBucketReplication → 200.
  - getBucketReplication: 404 ReplicationConfigurationNotFoundError when meta.ErrNoSuchReplication, else writes the persisted blob.
  - deleteBucketReplication: 204.
  - replicationStatusFor(r,b,key,tags): loads blob (silent-empty on error), iterates rules, skips Status != Enabled, runs replicationRuleMatches (Filter.And.Prefix+Tags / Filter.Tag+Prefix / Filter.Prefix / rule.Prefix). Returns "PENDING" on first match, else "".
- New ErrNoSuchReplicationConfig (404 ReplicationConfigurationNotFoundError) in errors.go.
- handleBucket gained ?replication GET/PUT/DELETE branch.
- putObject sets x-amz-replication-status header AFTER PutObject persists, using obj.Tags (parsed from x-amz-tagging earlier in the same handler).
- Tests (internal/s3api/replication_test.go): CRUD (GET 404 → PUT → GET 200 echoes rule → DELETE → GET 404), malformed body 400, no rules 400, rule without destination 400, prefix match emits PENDING, no-match no header, no-config no header, disabled rule no header, And { Prefix + Tag } filter (missing tag → empty, matching tag → PENDING, wrong tag value → empty, wrong prefix even with tag → empty).
- Files changed:
  - internal/meta/store.go
  - internal/meta/memory/store.go
  - internal/meta/cassandra/store.go
  - internal/meta/cassandra/schema.go
  - internal/s3api/errors.go
  - internal/s3api/server.go
  - internal/s3api/replication.go (new)
  - internal/s3api/replication_test.go (new)
  - scripts/ralph/prd.json (passes: true)
  - scripts/ralph/progress.txt
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - x-amz-replication-status is an emit-only response header on PutObject (not stored). Compute it AFTER persistence using obj.Tags so future per-object replication views can be derived from the same rule evaluator.
  - Replication Filter is the third XML shape variant for bucket-rule schemas (also seen in lifecycle): Prefix-only (legacy) | Tag-only | And { Prefix + Tags ≥1 }. replicationRuleMatches handles all three; lifecycle would have a sibling.
  - Empty-body PUT on ?replication is malformed (rejected 400 MalformedXML), not "clear" — DELETE clears. This differs from ?notification where empty body clears. Always read the AC for clearing semantics; they vary per subresource.
  - When a future story needs to short-circuit on load errors (e.g. policy-equivalent failure mode for replication), the silent-empty-on-error in replicationStatusFor mirrors what the website branch does on a load error: degrade to "no config" rather than 5xx.
---

## 2026-04-26 - US-019 (PutBucketLogging / GetBucketLogging)
- Added meta.ErrNoSuchLogging and Set/Get/DeleteBucketLogging to meta.Store; both backends mirror the standard blob pattern (memory: logging map; cassandra: bucket_logging table appended to tableDDL).
- internal/s3api/logging.go: putBucketLogging accepts XML body — empty body OR a BucketLoggingStatus without LoggingEnabled both clear via DeleteBucketLogging; LoggingEnabled requires non-empty TargetBucket (else 400 MalformedXML); valid body persisted via SetBucketLogging.
- getBucketLogging returns 200 + persisted blob on hit, OR 200 + canonical empty `<BucketLoggingStatus xmlns=".../">`</BucketLoggingStatus>` document on ErrNoSuchLogging (per AC). Missing bucket still surfaces 404 NoSuchBucket via mapMetaErr.
- Wired ?logging GET/PUT into handleBucket (same shape as ?website but no DELETE — S3 has no DeleteBucketLogging endpoint; clearing is empty-body PUT).
- Tests internal/s3api/logging_test.go: empty-on-fresh, round-trip, clear via empty body, clear via empty BucketLoggingStatus, malformed XML 400, missing TargetBucket 400, missing bucket 404.
- Files changed:
  - internal/meta/store.go (Set/Get/DeleteBucketLogging + ErrNoSuchLogging)
  - internal/meta/memory/store.go (logging map + 3 methods)
  - internal/meta/cassandra/schema.go (bucket_logging table)
  - internal/meta/cassandra/store.go (3 methods)
  - internal/s3api/logging.go (new)
  - internal/s3api/server.go (handleBucket ?logging dispatch)
  - internal/s3api/logging_test.go (new)
  - scripts/ralph/prd.json (passes: true)
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - GET-on-fresh semantics differ per subresource: notification/replication/website return 404, logging returns 200 + empty document. Always check AC wording — "empty document" means baked-in canonical XML, not XML envelope of stored zero-value.
  - S3 has no DeleteBucketLogging endpoint; clearing is via empty-body PUT (or a BucketLoggingStatus with no LoggingEnabled). The internal Delete method on meta.Store is still useful (and called from handleBucketLogging on empty body) — keep it on the interface for symmetry and future codepaths.
  - When a config has a single optional inner element (LoggingEnabled here, ServerSideEncryptionConfiguration on encryption), validate the inner element's required attrs ONLY when present; absence = clear.
---

## 2026-04-26 - US-020 (PutBucketTagging / GetBucketTagging / DeleteBucketTagging)
- New meta.ErrNoSuchTagSet sentinel + meta.Store.Set/Get/DeleteBucketTagging interface methods.
- Memory backend: tagging map[uuid.UUID][]byte via the standard setBucketBlob/getBucketBlob/deleteBucketBlob helpers.
- Cassandra backend: new bucket_tagging table appended to tableDDL (CREATE TABLE IF NOT EXISTS bucket_tagging (bucket_id uuid PRIMARY KEY, config blob)); SetBucketTagging/GetBucketTagging/DeleteBucketTagging via the same setBucketBlob helpers.
- New internal/s3api/tagging.go handlers: putBucketTagging validates ≤50 tags, non-empty Key, no duplicate Key (over-limit / empty / dup → 400 InvalidTag); malformed XML → 400 MalformedXML; empty/whitespace body clears via DeleteBucketTagging (returns 204). getBucketTagging serves persisted blob or 404 NoSuchTagSet via new ErrNoSuchTagSet APIError. deleteBucketTagging is idempotent.
- Route ?tagging GET/PUT/DELETE wired in handleBucket adjacent to ?logging.
- Tests in bucket_tagging_test.go cover round-trip, GET-on-missing 404 NoSuchTagSet, DELETE clears, PUT empty body clears, over-limit (51 tags) 400 InvalidTag, malformed body 400 MalformedXML, missing bucket 404 NoSuchBucket, duplicate keys 400 InvalidTag, empty key 400 InvalidTag.
- Files changed:
  - internal/meta/store.go
  - internal/meta/memory/store.go
  - internal/meta/cassandra/store.go
  - internal/meta/cassandra/schema.go
  - internal/s3api/tagging.go
  - internal/s3api/errors.go
  - internal/s3api/server.go (handleBucket route)
  - internal/s3api/bucket_tagging_test.go (new)
  - scripts/ralph/prd.json
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - Bucket tagging follows the exact same setBucketBlob/getBucketBlob/deleteBucketBlob blob pattern as logging/replication/website/notification — when a bucket-scoped XML config has no per-bucket-row column, prefer this over modifying the buckets table.
  - AWS uses 204 No Content (not 200) for successful Put/DeleteBucketTagging; PUT helpers in this repo are inconsistent (logging uses 200, replication uses 200, encryption uses 200). Tagging matches AWS Reference: 204.
  - Tag validation belongs in the handler (XML schema + AWS limits), not in meta.Store. The meta layer is the dumb blob store; semantic validation (≤50 tags, non-empty Key, no duplicate Key, no whitespace-only) is s3api-side so the same blob can stay opaque to backends.
---

## 2026-04-26 - US-021 (GetBucketLocation + x-amz-bucket-region + LocationConstraint body parsing)
- Added meta.Bucket.Region field; meta.Store.SetBucketRegion(ctx, name, region).
- Cassandra: ALTER TABLE buckets ADD region text (additive). CreateBucket INSERT, GetBucket SELECT, ListBuckets SELECT extended with region column. SetBucketRegion uses LWT (UPDATE ... IF EXISTS / ScanCAS(nil) / LocalSerial).
- Memory store: SetBucketRegion mutates Bucket.Region under mu.Lock; CreateBucket leaves Region empty; GetBucket already shallow-copies so Region round-trips automatically.
- internal/s3api/location.go: parseCreateBucketLocation reads optional <CreateBucketConfiguration><LocationConstraint> from PUT body (1<<16 limit, empty body returns "" with no error). getBucketLocation handler emits <LocationConstraint xmlns="http://s3.amazonaws.com/doc/2006-03-01/">{region}</LocationConstraint>. bucketRegionFor(b) returns b.Region or falls back to s.Region (defaults to "default").
- server.go: ?location GET routed in handleBucket adjacent to ?acl. PUT bucket parses LocationConstraint BEFORE CreateBucket so malformed body → 400 MalformedXML without creating a bucket. SetBucketRegion called only when region != "". HEAD bucket sets x-amz-bucket-region header via bucketRegionFor.
- Tests in internal/s3api/location_test.go: round-trip with us-west-2 (PUT body → GET ?location echoes), default region (no body → GET ?location returns empty LocationConstraint), HEAD region header (specific + default fallback), malformed PUT body → 400, GET ?location on missing bucket → 404 NoSuchBucket.
- Files changed:
  - internal/meta/store.go
  - internal/meta/cassandra/store.go
  - internal/meta/cassandra/schema.go
  - internal/meta/memory/store.go
  - internal/s3api/server.go
  - internal/s3api/location.go (new)
  - internal/s3api/location_test.go (new)
  - scripts/ralph/prd.json
- Quality: go build ./... + go vet ./... + go test ./... all green.
- **Learnings for future iterations:**
  - x-amz-bucket-region header on HEAD bucket is required by SDK pre-flight region detection. The fallback for un-located buckets is "default" (or s.Region), never empty — the header must always be present once the bucket exists.
  - GetBucketLocation's xmlns="http://s3.amazonaws.com/doc/2006-03-01/" is emitted by encoding/xml when the field tag uses the namespace-qualified form: `xml:"http://s3.amazonaws.com/doc/2006-03-01/ LocationConstraint"`. Same trick used for ACL xsi:type parsing.
  - When a PUT bucket reads its body for a sub-config (location, ACL XML), parse BEFORE calling Meta.CreateBucket so a malformed body short-circuits without creating a phantom bucket. CreateBucket itself takes no body kwarg — it's a separate optional input.
---

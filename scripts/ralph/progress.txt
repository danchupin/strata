# Ralph Progress Log
Started: Sat May 24 22:00:00 MSK 2026
Cycle: ralph/auth-dx-trailer-lima

## Codebase Patterns
- **Final non-globals bundle**: 4 P3 entries close in this cycle. Post-cycle ROADMAP holds ONLY 5 globals (P2 ScyllaDB benchmarks, P2 Content-addressed dedup, P3 Intelligent-Tiering, P3 Select Object Content, plus consolidated globals). User vision "leave only globals" fulfilled. Pre-launch product per [Pre-launch no deploys] memory.
- **6 stories** (shrunk from initial 9 after PRD review): US-001 per-bucket signing key schema + meta + KMS wiring + integration tests | US-002 admin API + STRATA_KEY_MAX_AGE + fail-closed | US-003 module split (cephimpl/ separate go.mod + go.work) | US-004 chunked-trailer non-sha256 algos (sha1/crc32/crc32c) | US-005 lima envelope CI workflow + auto-PR | US-006 smoke + ROADMAP close-flip × 4 + PRD removal.
- **CRITICAL discovery during PRD review**: `internal/crypto/kms` package ALREADY ships with `Provider` interface (`GenerateDataKey` + `UnwrapDEK`) + `AWSKMSProvider` (`aws-sdk-go-v2/service/kms`) + `VaultProvider` (`hashicorp/vault/api`) + `LocalHSMProvider`. Per `internal/crypto/kms/CLAUDE.md`. PRD initially planned US-001/002/003 building these from scratch — DROPPED. Cycle now WIRES existing provider, not builds new.
- **Story ordering**: US-001 (schema + wiring) BEFORE US-002 (admin API + max-age enforcement) — admin endpoints need the meta CRUD + auth path in place. US-003 (module split) independent (data backend, not auth). US-004 (trailer algos) independent. US-005 (CI workflow) independent. US-006 smokes everything.
- **Verified codebase facts from PRD review**:
  - `internal/crypto/kms/` package ships full Provider + 3 impls + CLAUDE.md. Tests use fakes (`fakeKMS`, `fakeVault`) — NO LocalStack/dev-Vault integration tests yet. US-001 adds them.
  - `go.mod` HAS `require github.com/ceph/go-ceph v0.39.0` direct (verified via grep). Module split IS real fix (premise from polish-dx US-004 drop was incorrect — current state IS hermetic at code level but NOT at go.mod level).
  - `internal/data/rados/` already has stub.go + ops_stub.go + pool_stub.go (build-tag-flipped). Stub-mirror pattern partial; cephimpl/ split completes it.
  - `internal/serverapp/serverapp.go:404` `buildDataBackend(cfg)` is config-driven (not direct env grep).
  - `internal/meta/cassandra/schema.go:164` shows `access_keys` table is IAM-scoped (user-bound). Per-bucket signing keys are NEW schema (additive columns on `buckets`), NOT replace.
  - ROADMAP line 254 entry wording explicitly says `Hooks onto the existing Vault provider` — confirms scope is wiring, not building.
- **Decisions zashited** (4 of 4 post-review picks):
  - 1A envelope encryption (per-bucket DEK wrapped by KMS CMK).
  - 2C self-hosted runner (zero ongoing cost; operator provides 16GB+ box).
  - 3A CI workflow + auto-PR with 10% regression threshold (auto-merge if < 10%, manual review > 10%).
  - 4A `go.work` checked into repo (simplest dev UX, IDE auto-discovers).
- **Per-bucket signing key shape (US-001)**: meta columns `signing_wrapped_dek blob`, `signing_key_id text`, `signing_key_created_at timestamp` added across all 3 backends. New `meta.Store` methods `GetBucketSigningKey` + `SetBucketSigningKey`. Auth middleware (sigv4.go) checks per-bucket key first; if absent (`ErrBucketSigningKeyNotSet`), falls through to existing IAM access-key path. Per-bucket key is OPT-IN per bucket.
- **DEK cache discipline (US-001)**: sync.Map TTL 5 min default (env `STRATA_DEK_CACHE_TTL`, range [30s, 1h]). Plaintext zeroed via `subtle.ConstantTimeCopy` before eviction. Cache invalidated on Rotate. Counter `strata_kms_decrypt_total{provider, outcome}` for ops visibility.
- **Fail-closed semantics (US-002)**: on `kms.ErrKMSUnavailable` (Provider.UnwrapDEK transient + cache miss) → HTTP 503 `KMSUnavailable` + `Retry-After: 30`. **NO fallback** to IAM access-key path — explicit per-bucket signing key means operator opted in; silent degradation breaks rotation semantics.
- **STRATA_KEY_MAX_AGE enforcement (US-002)**: default 90d (matches PCI-DSS / SOX rotation policy), range [1d, 365d]. Auth middleware rejects keys older than max-age with HTTP 401 `KeyExpired`. Operator must Rotate to recover.
- **Module split shape (US-003)**: `internal/data/rados/cephimpl/` separate Go module with own go.mod. Main module DROPS `github.com/ceph/go-ceph` require entirely. `go.work` checked into repo. Interface boundary: stub package exposes typed `Backend` interface; cephimpl provides concrete. Clean-clone hermetic check `go mod graph | grep -c go-ceph` = 0 (US-006 regression smoke).
- **Chunked-trailer algos (US-004)**: all stdlib (`hash/crc32` for crc32 + crc32c via Castagnoli table, `crypto/sha1`). Shared parsing factory `selectTrailerHash` slots each algo into existing chain-HMAC scheme from sha256 path (ralph/storage-correctness US-009). Test fixtures captured per algo from aws-cli 2.22+.
- **Lima envelope CI (US-005)**: self-hosted runner (`[self-hosted, strata-bench]`) provisioned by operator (16GB RAM, 200GB disk, Ubuntu 22.04, Docker 24+). Weekly cron `0 4 * * 0` (Sun 04:00 UTC) + manual `workflow_dispatch`. Auto-PR with markdown table from jsonl; auto-merge if max p99 regression < 10%, else manual review.
- **ROADMAP discipline (US-006)**: 4 entries close (lines 254 + 260 + 386 + 478) — no new entries. `(commit pending)` placeholder; SHA backfill on main as fast-follow commit (mirrors precedent).
- **cephimpl module pattern (US-003)**: Sub-module under main repo tree (`internal/data/rados/cephimpl/`) keeps `internal/` import semantics workable because the cephimpl module path (`github.com/danchupin/strata/cephimpl`) is a subpath of the main module's path. Local `replace github.com/danchupin/strata => ../../../..` in cephimpl's go.mod keeps `go mod download` working inside containers without publishing. Workspace MVS combines transitive closures across `use ()` modules — pin pre-split versions via `replace` in `go.work` if path collisions appear (e.g. `google.golang.org/genproto` monolithic-vs-split). Hermetic check is `GOWORK=off go mod graph | grep -c <dep>` (workspace off, main-module-only graph). Sub-modules should avoid importing heavy main packages (e.g. `internal/rebalance`) — re-declare structurally-compatible interfaces locally and let the main caller do the implicit conversion.

## 2026-05-24 — US-001
- Per-bucket signing key persisted on bucket meta: new `meta.ErrBucketSigningKeyNotSet`
  sentinel, 3-method surface `GetBucketSigningKey/SetBucketSigningKey/DeleteBucketSigningKey`
  implemented across memory + cassandra + tikv in lockstep.
- Cassandra: 3 ALTERs on `buckets` (`signing_wrapped_dek blob`, `signing_key_id text`,
  `signing_key_created_at timestamp`). Get reads the trio with `errors.Is(gocql.ErrNotFound)`
  → ErrBucketNotFound, empty trio → ErrBucketSigningKeyNotSet. Set/Delete LWT (`IF EXISTS`)
  for Paxos lineage parity with SetBucketVersioning.
- TiKV: 3 fields added on `meta.Bucket` (json:omitempty); existing `updateBucket` helper
  handles persistence — JSON marshal is the codec.
- Memory: in-struct fields; locker scoped Get/Set/Delete; idempotent re-delete.
- Contract test `caseBucketSigningKey` added: absent → set → roundtrip → re-set bumps
  createdAt → delete → absent → missing-bucket paths. Memory backend passes; cassandra
  + tikv exercised under -tags integration.
- DEK cache (`internal/auth/bucket_signing.go`): sync.Map keyed on bucket, TTL via
  `time.Now() + ttl`; plaintext zeroed via `subtle.ConstantTimeCopy(1, b, zeros)` on
  evict / invalidate / replace. `NewDEKCache(ttl)` clamped via serverapp's `dekCacheTTL`
  helper to [30s, 1h]. Test clock injectable via SetClockForTest.
- BucketSigningResolver: composes Store + KMS + Cache + counter + bucket extractor.
  ResolveSecret returns (nil, false, nil) on absent/IAM-only path; (dek, true, nil) on
  hit; (nil, false, err) on KMS error so caller can surface 503/401. Default bucket
  extractor: path-style first segment; skips `/admin/`, `/healthz`, `/readyz`,
  `/metrics`.
- Auth middleware (`internal/auth/middleware.go`): new `BucketSigning` field. When
  resolver returns a DEK, `secret = hex(DEK)` replaces `cred.Secret` for both the
  request-canonical signature and the chunked streaming signing key derivation.
  Plaintext DEK zeroed after `encodeDEKAsSecret`.
- HTTP mapping (`internal/s3api/errors.go`): new `ErrKMSUnavailable`(503 + Retry-After:30),
  `ErrKMSKeyDenied`(401), `ErrKMSKeyTampered`(401). `WriteAuthDenied` routes via
  errors.Is on `auth.ErrKMS{Unavailable,Denied,Tampered}`.
- Counter `strata_kms_decrypt_total{provider, outcome}` added to
  `internal/metrics/metrics.go` with outcomes ∈ {cache_hit, cache_miss_ok,
  unavailable, denied, tampered}; provider label derived in serverapp via type-assert
  on `*kms.AWSKMSProvider | *kms.VaultProvider | *kms.LocalHSMProvider`.
- ServerApp wiring (`internal/serverapp/serverapp.go` +
  `internal/serverapp/bucket_signing.go`): when `kmsProvider != nil`,
  `mw.BucketSigning = buildBucketSigningResolver(metaStore, kmsProvider, logger)`.
  KMS-less deployments stay zero-impact (resolver nil → middleware short-circuits).
- LocalStack KMS integration test (`internal/crypto/kms/aws_integration_test.go`,
  build-tag integration): pinned `localstack/localstack:3.8`, CreateKey symmetric
  + roundtrip + wrong-key-id error path.
- Vault Transit dev-mode integration test (`internal/crypto/kms/vault_integration_test.go`,
  build-tag integration): pinned `hashicorp/vault:1.15`, AppRole bootstrap inside the
  test (transit engine enable → CMK → approle enable → policy → role → role-id +
  secret-id mint), then `kms.NewVaultProvider(...).{GenerateDataKey, UnwrapDEK}`.
- Env-vars docs: `STRATA_DEK_CACHE_TTL` documented in
  `docs/site/content/reference/env-vars.md` (default 5m, range [30s, 1h]).
- Quality: `go vet ./...` + `go test -race ./internal/meta/... ./internal/auth/...
  ./internal/crypto/kms/... ./internal/serverapp/... ./internal/s3api/...` all green;
  full unit-test suite passes.
- Files: `internal/meta/store.go`, `internal/meta/memory/store.go`,
  `internal/meta/cassandra/store.go`, `internal/meta/cassandra/schema.go`,
  `internal/meta/tikv/buckets.go`, `internal/meta/storetest/contract.go`,
  `internal/auth/bucket_signing.go`, `internal/auth/bucket_signing_test.go`,
  `internal/auth/middleware.go`, `internal/auth/middleware_bucket_signing_test.go`,
  `internal/metrics/metrics.go`, `internal/s3api/errors.go`,
  `internal/serverapp/serverapp.go`, `internal/serverapp/bucket_signing.go`,
  `internal/crypto/kms/aws_integration_test.go`,
  `internal/crypto/kms/vault_integration_test.go`,
  `docs/site/content/reference/env-vars.md`.

**Learnings for future iterations:**
- meta.Bucket gains 3 fields → TiKV codec is JSON marshal, so persistence is free.
  Cassandra needs explicit ALTER + Get/Set column selection. Memory backend stores
  meta.Bucket directly.
- LWT (`UPDATE ... IF EXISTS`) on Cassandra is required for the signing-key trio
  to participate in the same Paxos lineage as CreateBucket — mirrors the
  SetBucketVersioning lesson (CLAUDE.md captures it).
- `subtle.ConstantTimeCopy(1, dst, src)` is the canonical zero-out for sensitive
  byte slices on eviction.
- HTTP middleware deny handler `s3api.WriteAuthDenied` switches on `err.Error()`
  string equality for legacy auth errors AND `errors.Is` for new typed sentinels —
  mixing both is the established pattern.
- Per-bucket signing key shape: SigV4 derives signing key from
  `hex(DEK)`-as-secret. Operators sign with the same hex on the client side; AWS
  CLI accepts arbitrary `aws_secret_access_key` so this is wire-compatible.
- Testcontainers v0.41.0 LocalStack image: pin `localstack/localstack:3.8` —
  `:latest` flips behavior periodically. Health endpoint
  `/_localstack/health` returns 200 once `SERVICES=kms` is up.
- Vault dev-mode in container: pass `Cmd: ["server", "-dev"]` + env
  `VAULT_DEV_ROOT_TOKEN_ID=root` + `VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200`.
  Wait via HTTP 200/429/472/473 — Vault sealed/standby surfaces these.
---

## 2026-05-24 — US-002
- Admin signing-key surface shipped: 3 new `/admin/v1/buckets/{name}/signing-key`
  endpoints — `POST /rotate`, `GET /status`, `DELETE` — all stamped with
  audit verbs (`admin:RotateBucketSigningKey`,
  `admin:GetBucketSigningKeyStatus`, `admin:DeleteBucketSigningKey`).
  Rotate optionally accepts `{key_id}` body to override the default CMK.
- `STRATA_KEY_MAX_AGE` env (default `2160h`/90d, range `[24h, 8760h]`)
  enforced at the SigV4 path. Auth resolver gains `MaxAge` field + a
  pre-unwrap check that returns the new `auth.ErrKMSKeyExpired`
  sentinel → s3api maps to HTTP 401 `KeyExpired`. Counter outcome
  `expired` added; KMS not consulted for expired keys (saves an unwrap
  round-trip on the rejected path).
- Fail-closed semantics: `kms.ErrKMSUnavailable` sentinel added.
  `kms.WrapTransient` helper detects `net.Error.Timeout`, `*net.OpError`,
  `context.DeadlineExceeded`, plus a short list of well-known transport
  phrases (connection refused, i/o timeout, throttlingexception, ...).
  AWS + Vault providers wrap UnwrapDEK / GenerateDataKey errors via
  `WrapTransient`; Vault additionally maps HTTP 5xx → `ErrKMSUnavailable`.
- `BucketSigningResolver.ClassifyUnwrap` hook: serverapp installs a
  translator that maps `kms.ErrKMSUnavailable` → `auth.ErrKMSUnavailable`
  (→ 503 + `Retry-After: 30`), `kms.ErrKeyIDMismatch` and every other
  KMS error → `auth.ErrKMSDenied` (→ 401 KeyDenied). NO fallback to IAM
  — opting a bucket in means rotation semantics are enforced.
- Admin layer Server gains `SigningKey SigningKeyConfig{Provider, Cache,
  DefaultKeyID, MaxAge}`. Rotate calls `Provider.GenerateDataKey`,
  persists wrapped form, drops cached DEK via `Cache.Invalidate`. Status
  reads meta + computes `age_days`/`expired`. Delete drops meta row +
  invalidates cache. Missing provider → 503 `SigningKeyDisabled` (kms-
  less deploys still boot).
- OpenAPI YAML extended with 3 endpoints + their schemas + 503
  `SigningKeyDisabled` / `KMSUnavailable` annotations. Both
  `docs/site/static/openapi.yaml` and `docs/site/public/openapi.yaml`
  updated in lockstep.
- Docs: new `docs/site/content/operate/key-rotation.md` (operator
  runbook: when to rotate, response shape, max-age semantics, fail-
  closed response matrix, status monitoring, tuning table, audit verbs,
  recovery flow). `STRATA_KEY_MAX_AGE` + `STRATA_KMS_DEFAULT_KEY_ID`
  added to `docs/site/content/reference/env-vars.md`.
- Tests: `internal/auth/bucket_signing_test.go` extended with 3 max-age
  cases (expired / fresh / disabled). `internal/serverapp/
  bucket_signing_test.go` covers `keyMaxAge` env clamping + default +
  parse-error fall-back + `classifyKMSUnwrapErr` translation matrix +
  `WrapTransient` for `net.Error` timeouts. `internal/adminapi/
  buckets_signing_key_test.go` covers happy-path rotate, override
  `key_id`, bucket missing, KMS unavailable → 503 + `Retry-After`,
  no provider → 503 `SigningKeyDisabled`, status not-set 404, status
  fresh, status expired, idempotent delete, meta-error surfacing.
  Integration: `internal/crypto/kms/aws_rotation_integration_test.go`
  exercises a full rotation cycle on LocalStack — 2 CMKs, GenerateDataKey
  under each, cross-CMK unwrap refuses via `ErrKeyIDMismatch`.
- Quality: `go vet ./...` + `go test -race ./internal/auth/...
  ./internal/adminapi/... ./internal/crypto/kms/...
  ./internal/serverapp/... ./internal/meta/... ./internal/s3api/...`
  all green; full `go test ./...` green (transient flake in
  `cmd/strata/workers` re-runs clean — pre-existing).
- Files: `internal/auth/bucket_signing.go` (+ MaxAge / ClassifyUnwrap /
  ErrKMSKeyExpired), `internal/auth/bucket_signing_test.go`,
  `internal/crypto/kms/kms.go` (ErrKMSUnavailable),
  `internal/crypto/kms/classify.go` (new — WrapTransient + isTransient),
  `internal/crypto/kms/aws.go` (WrapTransient at boundary),
  `internal/crypto/kms/vault.go` (WrapTransient + 5xx → ErrKMSUnavailable),
  `internal/crypto/kms/aws_rotation_integration_test.go` (new),
  `internal/s3api/errors.go` (ErrKMSKeyExpired + mapping),
  `internal/metrics/metrics.go` (help text adds `expired` outcome),
  `internal/serverapp/bucket_signing.go` (keyMaxAge env + classifier +
  buildSigningKeyAdminConfig), `internal/serverapp/bucket_signing_test.go`
  (new), `internal/serverapp/serverapp.go` (resolver var exposed +
  SigningKey wired into adminapi.Config),
  `internal/adminapi/server.go` (SigningKey field + 3 routes),
  `internal/adminapi/buckets_signing_key.go` (handlers, new),
  `internal/adminapi/buckets_signing_key_test.go` (new),
  `docs/site/content/operate/key-rotation.md` (new),
  `docs/site/content/reference/env-vars.md`,
  `docs/site/static/openapi.yaml`, `docs/site/public/openapi.yaml`.

**Learnings for future iterations:**
- `BucketSigningResolver.ClassifyUnwrap` keeps the `auth` package
  import-free of `kms` — kms-side sentinels translate to auth-side
  sentinels at wiring time. Future provider additions (e.g. GCP KMS)
  can return `kms.ErrKMSUnavailable` for transient and the existing
  serverapp classifier picks them up without code changes.
- `kms.WrapTransient` is the canonical "decide between 503 and 401"
  shim. New providers MUST call it at every external-call boundary so
  the auth path can fail-closed correctly. The string-marker list in
  `classify.go::transientMarkers` is intentionally short — extend with
  caution to avoid hiding permanent misconfigurations as 503.
- `adminapi.Server.SigningKey` is the shared admin-side surface for the
  3 rotate endpoints. Plumbing more rotation tooling (key-history,
  CMK migration) lands here without touching the gateway middleware.
- The DEK plaintext is returned ONCE in `POST /rotate` response —
  consumers (the operator console UI, scripted automation) must capture
  it client-side immediately. Subsequent `GET /status` does NOT expose
  the secret. Same one-shot pattern as the IAM access-key create flow.
- `meta.Bucket` schema is unchanged for US-002 — US-001 already shipped
  the trio (`signing_wrapped_dek`, `signing_key_id`,
  `signing_key_created_at`). When extending the surface (e.g. key
  history), prefer extending the existing trio over adding new columns;
  blob-style append works well for JSON history lists.
- `cmd/strata/workers` has a flaky unit test pre-existing; ignore on
  one-run failure and re-run.
---

## 2026-05-24 — US-003
- Module split for the librados-linked RADOS backend: new Go module at
  `internal/data/rados/cephimpl/` (`module github.com/danchupin/strata/cephimpl`)
  with its own `go.mod` requiring `github.com/ceph/go-ceph v0.39.0`. Main
  module's `go.mod` no longer requires go-ceph (verified via
  `GOWORK=off go mod graph | grep -c go-ceph` → 0).
- `go.work` checked in at repo root; lists main module + cephimpl. Workspace
  MVS pitfall resolved via in-`go.work` `replace google.golang.org/genproto
  => v0.0.0-20240903143218-8af14fe29dc1` to dodge the new-split-vs-old-
  monolithic genproto path collision tikv pulled in.
- Files moved from `internal/data/rados/` → `internal/data/rados/cephimpl/`
  (build tag `//go:build ceph` dropped — every cephimpl file links go-ceph
  by default): backend.go, ec.go, health.go, ops.go, pool.go, rebalance.go,
  backend_integration_test.go.
- Stub-side rename: main rados pkg's `stub.go` lost the `!ceph` tag —
  always-on stub `New(Config) → data.ErrRADOSNotCompiled`. `pool_stub.go`
  removed (no longer needed). `ops_stub.go` keeps the legacy
  `ErrRadosNotCompiled` sentinel but is tag-free.
- New `data.ErrRADOSNotCompiled` sentinel (in `internal/data/errors.go`)
  for cross-package use.
- Shared helpers exported from main rados pkg so cephimpl can consume them
  without duplication: `ChunkPutFn` (alias), `PutChunksParallel`,
  `PutConcurrencyFromEnv`, `ChunkGetFn` (alias), `NewPrefetchReader`,
  `GetPrefetchFromEnv`, `BatchOpsFromEnv`, `PoolSizeFromEnv`,
  `NextRoundRobin`, `BuildPendingPoolStatuses` (+ `PoolGroup`,
  `PendingPoolStatus` uppercase shapes). In-package lowercase names kept
  for the existing test surface.
- cephimpl exposes its own `RadosCluster` interface (structurally identical
  to `internal/rebalance.RadosCluster`) and `RebalanceClusters(*Backend) →
  map[string]RadosCluster`. Cephimpl deliberately does NOT import
  `internal/rebalance` — that would pull rebalance's tikv-transitive
  dependency chain into cephimpl's MVS view and break workspace
  resolution.
- Caller branching via build tag:
  - `internal/serverapp/data_rados_ceph.go` (//go:build ceph) →
    `cephimpl.New`; `data_rados_stub.go` (//go:build !ceph) →
    `data.ErrRADOSNotCompiled`. `serverapp.buildDataBackend` calls the
    new `newRADOSBackend` helper.
  - `cmd/strata/admin/bench_rados_{ceph,stub}.go` mirrors the same pattern
    for the bench harness.
  - `cmd/strata/workers/rebalance_movers_ceph.go` switched to
    `*cephimpl.Backend` + `cephimpl.RebalanceClusters`; converts to
    `map[string]rebalance.RadosCluster` at the call site.
  - `internal/rebalance/rados_mover_integration_test.go` (//go:build ceph
    && integration) updated to call `cephimpl.New` + type-assert
    `*cephimpl.Backend`.
- Makefile: `make vet` extended with a soft cephimpl vet attempt
  (GOWORK=off + -tags integration; falls through with a message when
  librados isn't installed). `make test-rados` now shells into the
  cephimpl dir inside the ceph docker image and runs
  `go test -tags integration ./...` — no `ceph` tag needed at the test
  command since the module is ceph-only by construction.
- `.github/workflows/ci.yml` `lint-build` job gained a `cd cephimpl &&
  GOWORK=off go mod tidy -diff` step to catch cephimpl go.mod drift on
  every PR. The librados-linked compile path runs via the existing
  `docker-build` + `e2e` jobs.
- `deploy/docker/Dockerfile` ships `go.work` + `go.work.sum` +
  cephimpl's `go.{mod,sum}` into the build stage so `go mod download` +
  `go build -tags ceph ./cmd/strata` works inside the ceph image.
- CLAUDE.md gained a "RADOS / cephimpl module split" section before
  "Single-binary invariant" documenting the layout, the workspace MVS
  pitfall + genproto replace, and the GOWORK=off hermetic check.
- Quality: `go vet ./...` + `go test -count=1 ./...` green (full unit
  test suite, including racecheck soak). Build matrix verified manually:
  default-tag `go build ./...` clean, `GOWORK=off go build ./...` clean
  with zero go-ceph in `go mod graph`, `go build -tags ceph ./...` fails
  on librados as expected on a librados-less host (the ceph docker image
  in CI/lab has it).
- Files: deleted internal/data/rados/{backend,backend_integration_test,
  ec,health,ops,pool,pool_stub,rebalance}.go; added
  internal/data/rados/cephimpl/{go.mod,go.sum,backend,backend_integration_test,
  ec,health,ops,pool,rebalance}.go; new internal/serverapp/
  data_rados_{ceph,stub}.go; new cmd/strata/admin/
  bench_rados_{ceph,stub}.go; new go.work + go.work.sum;
  edited internal/data/errors.go, internal/data/rados/{health_helper,
  ops_env,ops_stub,parallel,pool_env,pool_index,prefetch,stub}.go,
  internal/rebalance/rados_mover_integration_test.go,
  internal/serverapp/serverapp.go, cmd/strata/admin/bench_common.go,
  cmd/strata/workers/rebalance_movers_ceph.go, Makefile,
  deploy/docker/Dockerfile, .github/workflows/ci.yml, CLAUDE.md.

**Learnings for future iterations:**
- Workspace MVS combines the transitive closure of ALL `use ()` modules,
  so a sibling module's transitive require can resurface a pre-pruned
  dep in the main module's view. The genproto monolithic-vs-split case
  is the canonical breakage; pin to a post-split monolithic version via
  `replace` in `go.work` to keep import paths unambiguous.
- `internal/` import restriction is BY IMPORTER PATH, not by importer's
  module — so `github.com/danchupin/strata/cephimpl` (a subpath of
  `github.com/danchupin/strata/`) can legally import
  `github.com/danchupin/strata/internal/...`. That's why cephimpl
  doesn't need to live outside the main repo tree.
- A new sub-module under main module's tree (e.g. `internal/data/rados/
  cephimpl/`) needs a `replace github.com/danchupin/strata => ../../../..`
  in its `go.mod` so `go.work`-less builds (and `go mod download` inside
  containers) resolve the parent without needing it published as a real
  go module.
- Avoid sub-module imports of `internal/rebalance` or other heavy main
  module packages — workspace MVS pulls the entire transitive dep graph
  in. Re-declare structurally-compatible interfaces inside the sub-module
  and let the main-module caller do the implicit conversion.
- Hermetic check `GOWORK=off go mod graph | grep -c go-ceph` → 0 only
  works because `go.work` is NOT auto-respected with GOWORK=off. Without
  the override, workspace-on `go mod graph` includes cephimpl's
  transitive go-ceph (count ≥ 1) — that's not a regression of the
  hermetic invariant.
- `make test-rados` no longer passes `-tags ceph` because the cephimpl
  module's source is all ceph-linked by construction — every file in
  cephimpl imports go-ceph. Only `-tags integration` is needed to
  enable the integration_test.go gating.
- `go mod tidy` at the workspace root fails when a sibling module is
  imported only via go.work (no main `require`). Run tidy per-module
  with `GOWORK=off cd <module-dir> && go mod tidy`. CI's drift check
  (`go mod tidy -diff`) uses the same pattern.
---


## 2026-05-24 — US-004
- Chunked-trailer decoder accepts 4 algos: sha256 (US-009) + sha1 + crc32 +
  crc32c (US-004). Shared `selectTrailerHash(algoHeader)` factory in
  `internal/auth/streaming.go` returns a `*trailerHashSpec` (canonical
  trailer-header name + `hash.Hash` constructor). Unknown names →
  `ErrUnsupportedChecksumAlgorithm` (HTTP 400 InvalidRequest).
- `newStreamingTrailerReader` now accepts the spec; the body checksum is
  accumulated through the algo-specific `hash.Hash`. `readAndValidateTrailers`
  switches on `s.trailerHeader` (instance-bound) instead of the previous
  fixed `trailerHeaderChecksumSh` const.
- `internal/auth/middleware.go` drops the sha256-only gate. The trailer
  header is parsed via `selectTrailerHash` and threaded into the streaming
  reader. Anything outside the 4-algo set surfaces
  `ErrUnsupportedChecksumAlgorithm` before the body is drained — preserves
  the US-009 reject-early semantic.
- All stdlib: `crypto/sha1`, `hash/crc32`. Castagnoli table cached as a
  package-level var (`crc32CastagnoliTable`). No new deps.
- Test fixtures: `testdata/chunked-trailer-{sha1,crc32,crc32c}.bin`
  generated via `STRATA_REGEN_TRAILER_FIXTURES=1 go test -run
  TestRegenerateTrailerFixtures`. The regen test is skipped on default
  runs (env-gated) so CI never rewrites them. `.gitattributes` updated to
  mark all 4 fixtures binary.
- `TestStreamingTrailerReaderGoldenFixture` widened to all 4 algos —
  decodes each committed fixture and verifies plaintext recovery.
- New `TestStreamingTrailerReaderAllAlgos` exercises happy-path +
  body-checksum-mismatch round-trip per algo (8 subtests).
- New `TestSelectTrailerHashUnsupported` covers `""`, md5, sha512, `foo`.
- `TestMiddlewareUnsupportedTrailerAlgo` renamed to
  `TestMiddlewareSupportedTrailerAlgos`. Flipped semantics: 4 known algos
  pass validation; only `md5` (or other unknown) rejects.
- Integration test `streaming_trailer_integration_test.go` parameterised
  on algo (4 subtests). `buildTrailerPUT` accepts header + `newH` and
  signs the request under the chosen algo. All 4 round-trip via
  httptest.Server → middleware → inner handler.
- Quality: `go vet ./...` clean. `go test -race ./internal/auth/...` green.
  `go test ./...` full unit suite green (no flakes). Integration test
  `go test -tags integration ./internal/auth/` green for all 4 algos.
- Files: `internal/auth/streaming.go` (factory + spec + sha1/crc32
  imports + trailerHeader field), `internal/auth/middleware.go` (drop
  sha256-only gate, thread spec into reader), `internal/auth/types.go`
  (docstring update for ErrUnsupportedChecksumAlgorithm),
  `internal/auth/streaming_trailer_test.go` (helper accepts algo +
  newH, all-algo + unsupported tests),
  `internal/auth/streaming_trailer_fixtures_test.go` (new, env-gated
  regen test), `internal/auth/streaming_trailer_integration_test.go`
  (parameterised over 4 algos), `internal/auth/testdata/.gitattributes`,
  `internal/auth/testdata/chunked-trailer-sha1.bin` (new),
  `internal/auth/testdata/chunked-trailer-crc32.bin` (new),
  `internal/auth/testdata/chunked-trailer-crc32c.bin` (new).

**Learnings for future iterations:**
- `hash.Hash.Sum(nil)` for `hash/crc32` already produces a 4-byte
  big-endian slice. AWS S3 trailer checksum encodes that slice as
  base64. So the existing base64-string compare against the trailer
  header value works uniformly across sha256 / sha1 / crc32 / crc32c —
  no per-algo decode function needed at the decoder boundary.
- A test-only fixture-generation file gated by an env var
  (`STRATA_REGEN_TRAILER_FIXTURES=1`) keeps the regen path inside the
  package's test surface without leaking into normal `go test` runs.
  Pattern reusable for any other deterministic golden-fixture workflow.
- `selectTrailerHash` is the single seam to extend if a new algo
  (e.g. crc64-nvme — pending AWS adoption) needs to land. The factory
  + spec shape means the decoder/middleware never change again — only
  one switch arm.
- When refactoring a `func` signature shared across tests + production
  call sites, the GO compiler diagnostics surface every stale call
  site at once. `Edit` with `replace_all` on the exact call shape is
  the fastest way to rebase test files in lockstep.
---

## 2026-05-24 — US-005
- New GitHub Actions workflow `.github/workflows/bench-rgw.yml`: weekly
  cron `0 4 * * 0` (Sun 04:00 UTC) + manual `workflow_dispatch` (inputs:
  `regression_threshold`, `bench_args`). `runs-on: [self-hosted, strata-bench]`,
  `timeout-minutes: 240`, `concurrency: bench-rgw` (no in-flight cancel).
  Steps: checkout → collect runner specs (host/cpu/cores/mem/disk/kernel/
  docker) → `make up-all && wait-tikv && wait-ceph && wait-strata-lab` →
  `make up-bench-rgw && wait-rgw` + `--extract-rgw-creds` →
  `make $BENCH_MAKE_TARGET` → upload `rgw-comparison-*.jsonl` artifact
  (retention 90d) → `scripts/bench-update-doc.sh` (emits
  `max_regression_pct` / `changed` / `regression_detail` via GITHUB_OUTPUT)
  → tear down lab → branch `bench/rgw-comparison-<YYYY-MM-DD>` + commit
  + push + `gh pr create` against `main` → auto-merge gate: if Strata
  p99 regression < `BENCH_REGRESSION_THRESHOLD` (default 10) →
  `gh pr merge --auto --squash --delete-branch`, else
  `gh pr comment @danchupin` for manual review.
- New `scripts/bench-update-doc.sh`: reads latest
  `scripts/bench-results/rgw-comparison-*.jsonl` (lexical sort), groups
  rows by `(target, workload, object_size, concurrency)`, medians
  p50/p95/p99/MB-s/ops-s + sums errors, renders the auto-managed block
  between `<!-- BENCH-AUTO-START -->` / `<!-- BENCH-AUTO-END -->` markers
  in the doc, persists a baseline JSON
  (`docs/site/content/architecture/benchmarks/data/rgw-bench-baseline.json`),
  computes max p99 regression vs previous baseline (Strata target only —
  RGW jitter is noisy on the lima envelope per PRD), and prints
  `max_regression_pct=<N>` / `changed=<bool>` to stdout + appends to
  `$GITHUB_OUTPUT` when set. Refresh date is sourced from the JSONL
  filename so re-runs against the same JSONL produce byte-identical doc
  bytes. `--check` mode validates doc matches latest JSONL without
  writing (drift detector for CI). `--help`/`-h` prints the embedded
  usage block. Idempotence smoke-verified: `bash scripts/bench-update-doc.sh`
  + diff → 0 lines changed, `--check` → `OK: doc matches latest jsonl`.
- New baseline JSON `rgw-bench-baseline.json` (~6 Strata + RGW rows from
  the existing `rgw-comparison-2026-05-24.jsonl`). Sorted keys → byte-
  stable diffs. Persisted under `docs/site/content/architecture/benchmarks/data/`
  so Hugo doesn't surface it as a page but a future workflow run has
  numerical anchor for regression diff.
- Doc edit `docs/site/content/architecture/benchmarks/rgw-comparison.md`:
  auto-managed block injected before `## Workload-by-workload` carrying
  date / commit / RGW image / JSONL name / full per-row table. Operator-
  curated sections (`Methodology`, `Workload-by-workload`,
  `Reproducibility`, `See also`, etc.) untouched.
- New operator runbook `docs/site/content/developers/bench-runner-setup.md`:
  required hardware (8 cores / 16 GiB RAM / 200 GiB disk / Ubuntu 22.04
  / Docker 24.x), Docker install steps, GitHub Actions self-hosted runner
  registration with both labels (`self-hosted,strata-bench`), systemd
  service install, manual-dispatch verification, auto-PR behaviour,
  Lima box reduced smoke (`scripts/bench-rgw-comparison.sh put-small both
  --runs=1 --concurrency=8`), troubleshooting (queued runs, disk pre-
  flight, no-diff PR, GraphQL permission). Cross-linked from
  `docs/site/content/developers/_index.md` "See also".
- Quality: `go vet ./...` clean (no Go-side churn). `bash scripts/bench-
  update-doc.sh` smoke + idempotent re-run + `--check` mode all green.
  `python3 -c "import yaml; yaml.safe_load(open(...))"` validates the
  workflow YAML. `cd docs/site && hugo --quiet` exits 0 (doc renders).
- Files: `.github/workflows/bench-rgw.yml` (new),
  `scripts/bench-update-doc.sh` (new, executable),
  `docs/site/content/architecture/benchmarks/data/rgw-bench-baseline.json`
  (new), `docs/site/content/architecture/benchmarks/rgw-comparison.md`
  (auto-block injected), `docs/site/content/developers/bench-runner-setup.md`
  (new), `docs/site/content/developers/_index.md` (cross-link added).

**Learnings for future iterations:**
- The doc auto-managed block uses HTML-comment markers
  (`<!-- BENCH-AUTO-START -->` / `<!-- BENCH-AUTO-END -->`) — same
  pattern works for any future doc auto-block. The Python splicer in
  `bench-update-doc.sh` is reusable as-is; copy + change the marker
  names + the per-row formatter.
- Refresh date derived from the JSONL filename, NOT `datetime.now()`,
  is the canonical trick for byte-identical re-runs on the same JSONL.
  Without it, idempotence smoke (`scripts/bench-update-doc.sh && diff`)
  reports false drift.
- Sorted JSON keys (`sort_keys=True` in `json.dump`) keep baseline diffs
  reviewer-friendly. Pair with the explicit `(target, workload, conc)`
  group-sort in the markdown table for both files to be diff-stable.
- Workflow `concurrency: bench-rgw` + `cancel-in-progress: false`
  serialises overlapping cron + manual dispatches without dropping the
  in-flight run. Critical for 4-hour bench cycles where mid-run cancel
  wastes the entire wall clock.
- Self-hosted runner labels are AND-matched — `runs-on: [self-hosted,
  strata-bench]` requires BOTH labels on the runner; missing either
  queues the job indefinitely. Bench-runner-setup.md surfaces this in
  the troubleshooting section.
- `BENCH_REGRESSION_THRESHOLD` watches only the Strata side because RGW
  jitter on the single-OSD memstore is large (per rgw-comparison.md
  Limitations). If you ever extend the gate to RGW p99, raise the
  threshold (~30%) to absorb the noise floor.
- `gh pr merge --auto --squash` requires branch-protection-required-checks
  to be configured; without them the merge happens immediately. The
  workflow assumes the operator wires required checks before relying on
  the auto-merge gate.
---

## 2026-05-24 — US-006
- Final close-flip + smoke story for the `ralph/auth-dx-trailer-lima` cycle.
- ROADMAP.md close-flip × 4 in one commit:
  - **P3 line 254** "Per-bucket request signing keys (KMS-backed)" — Done.
    Summary references US-001..US-002 + per-bucket DEK schema (3 columns:
    `signing_wrapped_dek` / `signing_key_id` / `signing_key_created_at`) +
    `meta.Store.{Get,Set,Delete}BucketSigningKey` + `caseBucketSigningKey`
    contract + SigV4 middleware `BucketSigningResolver` (TTL via
    `STRATA_DEK_CACHE_TTL`) + 3 admin endpoints
    (`POST/GET/DELETE /admin/v1/buckets/{name}/signing-key{/rotate,/status,}`) +
    `STRATA_KEY_MAX_AGE` enforcement (401 KeyExpired) + fail-closed semantics
    (503 KMSUnavailable + Retry-After: 30, no IAM fallback) + LocalStack +
    Vault Transit integration tests + operator runbook.
  - **P3 line 260** "`bench-rgw-comparison` full 144-row sweep blocked by lima
    capacity" — Done. Summary references US-005 + self-hosted runner
    (`[self-hosted, strata-bench]`) + weekly cron (`0 4 * * 0`) + auto-PR
    with markdown table + `BENCH_REGRESSION_THRESHOLD` 10% gate +
    operator runbook `docs/site/content/developers/bench-runner-setup.md`.
  - **P3 line 386** "Module tags cleanup" — Done. Summary references US-003 +
    `internal/data/rados/cephimpl/` separate Go module (`module
    github.com/danchupin/strata/cephimpl`) + `go.work` workspace MVS pitfall
    + `replace google.golang.org/genproto` for split-monolithic conflict +
    serverapp + bench branching via `//go:build ceph` files + hermetic check
    `GOWORK=off go mod graph | grep -c go-ceph` → 0.
  - **P3 line 498** "Streaming chunked-trailer non-sha256 algos" — Done.
    Summary references US-004 + `selectTrailerHash` factory + all 4 algos
    (sha256 + sha1 + crc32 + crc32c) via stdlib (`hash/crc32` Castagnoli
    table + `crypto/sha1`) + shared chain-HMAC scheme + env-gated regen
    test for fixtures.
  - Each entry carries `(commit pending)` placeholder; SHA backfill on
    `main` as fast-follow per cycle convention.
- `tasks/prd-auth-dx-trailer-lima.md` removed via `git rm`.
- Smoke validation (clean stack — `make down && make up-all && make
  wait-tikv && make wait-ceph && make wait-strata-lab` first, since the
  21h-old TiKV had wedged into a Raft heartbeat slow-op loop):
  - `make smoke` — green (== smoke OK).
  - `make smoke-signed` — green (requires `STRATA_AUTH_MODE=required` on
    strata-a/b + `AWS_ACCESS_KEY_ID=admin AWS_SECRET_ACCESS_KEY=adminpass`
    matching compose default static creds).
  - aws-cli 2.22.35 4-algo chunked-trailer smoke (verifies US-004 end-to-
    end): `for algo in sha256 sha1 crc32 crc32c; do
    AWS_REQUEST_CHECKSUM_CALCULATION=when_required aws --endpoint-url
    http://127.0.0.1:9999 s3api put-object --bucket trailertest --key
    key-$algo --body /tmp/tt.txt --checksum-algorithm $algo; done` —
    all 4 returned 200 + identical ETag.
  - Hermetic default-build check (verifies US-003): `GOWORK=off go mod
    graph | grep -c 'github.com/ceph/go-ceph'` → 0. Equivalent to the
    PRD's "clean-clone" recipe (both compute the same answer: main
    module's go.mod has no direct go-ceph require).
  - `make docs-build` — green (94 pages, 0 errors via `hugo
    --quiet`).
  - `go vet ./...` + `make vet` — clean. cephimpl/ vet skipped locally
    (no librados); CI exercises it via the `docker-build` + `e2e` jobs.
  - `go test -race ./...` — green across the full main-module package
    set (cassandra + memory + tikv contract suites; auth; adminapi;
    serverapp; s3api; gc; lifecycle; rebalance; …). Duration ~3 min.
- Smoke not executed locally this cycle (already validated in prior
  cycles and/or not feasible in the current envelope):
  - `make smoke-tikv-default-lab` — Scenario A burst-count gate fails
    against a working LB (see new P3 Known-latent-bug entry below).
    nginx logs confirm 10/10 burst requests round-robin successfully;
    the assertion just greps `docker logs strata-{a,b}` for an
    `/admin/v1/clusters` line that the logging middleware never emits.
    Pre-existing harness bug, not regressed by this cycle.
  - `make smoke-rgw-lab-restart` — covered by `ralph/p1-fixes` US-006
    (commit `8c6d7be` shipped the 3-cycle smoke + green capture); not
    re-run here to save the ~10 minute three-cycle teardown.
  - `make test-integration` — Cassandra + LocalStack + Vault
    testcontainers exercised in US-001 + US-002 of this cycle; not
    re-run holistically (each takes 1-2 minutes per container start).
  - cephimpl module under ceph docker image — covered by CI's `e2e`
    job (`cd internal/data/rados/cephimpl && go test -tags integration
    ./...`); no librados on the dev box.
  - bench-rgw workflow manual trigger — needs a registered
    `[self-hosted, strata-bench]` runner; operator-step per
    `docs/site/content/developers/bench-runner-setup.md`.
- New ROADMAP entry (Known latent bugs section):
  - P3 "`scripts/smoke-tikv-default-lab.sh` Scenario A burst-count
    assertion can never match" — surfaced during the US-006 smoke pass;
    the assertion greps `docker logs strata-{a,b}` for per-request
    `/admin/v1/clusters` access-log lines that `internal/logging.
    Middleware` does not emit. PRD's "NO new ROADMAP entries" wish
    yields to the CLAUDE.md `Discovering a new gap` invariant — gaps
    surfaced during a cycle must be filed in the same commit. Fix
    scope (suggested): either add a per-request slog InfoContext line
    to the logging middleware (preferred — gives operators a real
    stdout access log on every replica) and update the harness gate
    to match the new shape, OR rewrite the assertion to inspect
    `docker logs strata-lb-nginx` for the burst path.
- Files: `ROADMAP.md` (4 close-flips + 1 new P3 entry — net change
  -3 entries vs the PRD's wish of -4), `scripts/ralph/prd.json`
  (US-006 passes=true), `scripts/ralph/progress.txt` (this block),
  `tasks/prd-auth-dx-trailer-lima.md` (git rm).

**Learnings for future iterations:**
- The TiKV-default lab's `docker compose` services include
  `strata-tikv-storage` as a long-running container. After ~21 hours
  of uptime on the lima dev box it can wedge into a Raft slow-op loop
  (`peer.rs:744` 200+ms tick handlers, "store messages" backlog) where
  PUT requests through strata-a/b time out 504 even though `/healthz`
  returns 200. Symptom: `docker ps` shows `strata-tikv-storage Up Less
  than a second` (auto-restart loop). Fix is `make down && make up-all`
  — full teardown of the volumes is NOT needed; the persisted Raft state
  recovers cleanly on a fresh container.
- `make smoke-signed` runs against the same compose default but with
  `STRATA_AUTH_MODE=required` recreated on strata-a/b + the matching
  `AWS_ACCESS_KEY_ID=admin AWS_SECRET_ACCESS_KEY=adminpass` (compose
  default static creds). Recreate via `STRATA_AUTH_MODE=required
  docker compose up -d strata-a strata-b strata-lb-nginx
  --force-recreate` — the LB needs the recreate too so it picks up
  fresh upstream endpoints.
- The "discovered gap" invariant in `CLAUDE.md` (`## Roadmap
  maintenance` — Every commit that surfaces something not yet on the
  roadmap MUST add a new entry) overrides any per-cycle PRD wish like
  "NO new ROADMAP entries". Better to file a P3 in the same commit
  than to suppress visibility into a latent harness bug.
- `internal/logging.Middleware` (`internal/logging/logging.go:103`)
  does NOT emit a per-request stdout log line — it only attaches a
  `request_id` to the context. If you want a per-request access log
  in stdout for operator debugging, add a `slog.InfoContext(ctx,
  "request", "method", r.Method, "path", r.URL.Path, "status", code,
  "duration_ms", elapsed)` call from a `responseRecorder` shim. Any
  smoke harness greping `docker logs <strata>` for `/admin/v1/...`
  will fail until that emission exists.
- aws-cli 2.22.35 `s3api put-object --checksum-algorithm <algo>`
  drives the chunked-trailer codepath when
  `AWS_REQUEST_CHECKSUM_CALCULATION=when_required` is set; without
  the env, aws-cli falls back to the legacy non-trailer signing for
  many object sizes. Useful seam to verify trailer-side changes
  end-to-end on the lab.
---

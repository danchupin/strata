# Ralph Progress Log
Started: Fri May  8 15:24:33 MSK 2026
Cycle: ralph/docs-site-hugo
---

## Codebase Patterns
- Hugo site lives under `docs/site/`. Theme is `hugo-book` as a git submodule at `docs/site/themes/hugo-book/`. Fresh checkouts need `git submodule update --init --recursive`.
- Hugo config: `docs/site/hugo.toml`. `BookSection = '*'` so every top-level content section appears in the sidebar (we don't nest under `content/docs/`).
- Build artifacts (`docs/site/public/`, `docs/site/resources/`, `.hugo_build.lock`) are gitignored. Drop them via `make clean`.
- Makefile targets: `make docs-serve` (dev preview on :1313), `make docs-build` (minified bundle). Both `cd` into `docs/site` first.
- Hugo CLI on Homebrew is hugo-extended (>= 0.128 needed for hugo-book). The future GH Action workflow (US-011) must pin a hugo-extended version.
- **Canonical doc paths** are now under `docs/site/content/<section>/<page>.md`. Content tree mirrors CockroachDB shape: `get-started/`, `deploy/`, `architecture/{backends,benchmarks,migrations}/`, `best-practices/`, `s3-compatibility/`, `reference/`, `developers/`. Tooling refs (CLAUDE.md / ROADMAP.md / code comments) point at the canonical-source path; user-facing copy in README/ROADMAP may use the published-URL form (US-010 finalises that).
- **Cross-link inside Hugo content**: use `{{< ref "/section/page" >}}` shortcode. Hugo errors on missing refs at build time — that's the safety net.
- **Inline-code escape gotcha**: backticked text containing `{{< ... >}}` is still parsed by Hugo. Escape with the comment form `{{</* ... */>}}` to render the literal token (e.g. when documenting the shortcode itself in `developers/_index.md`).
- **Theme-agnostic styling**: the hugo-book theme exposes CSS custom properties `--body-background`, `--body-font-color`, `--color-link`, `--gray-100/200/500` (see `themes/hugo-book/assets/plugins/_themes.scss`). Custom landing/marketing markup should reference those vars so it adapts to every theme variant (light, dark, catppuccin, etc.) without duplicating palettes.
- **Hero/CTA pattern for content pages**: `content/_index.md` uses `unsafe = true` markdown + inline `<style>` + raw `<div>` blocks for the hero + feature grid. Use `{{< relref "/section/" >}}` (NOT `{{< ref >}}`) inside `href="..."` so the rendered URL is properly prefixed by the site `baseURL` subpath (`/strata/...`). `relref` renders a relative link; `ref` renders absolute — the latter breaks under GitHub Pages subpath hosting.
- **Apply-test k8s manifests with `kubectl apply --dry-run=client -f deploy/k8s/`** before committing. Catches typos / wrong API versions in seconds without a real cluster. ingress-nginx annotations are not validated (free-text), so cross-check those against the upstream docs directly.

## Sat May  9 12:00:00 MSK 2026 - US-001
- Implemented Hugo scaffolding under `docs/site/`:
  - `docs/site/hugo.toml` — site title 'Strata Documentation', baseURL `https://danchupin.github.io/strata/`, theme `hugo-book` with `BookSection = '*'` (renders all top-level sections in sidebar), search enabled, comments off.
  - Added `hugo-book` theme as git submodule (`.gitmodules`, `docs/site/themes/hugo-book/`).
  - Stub `content/_index.md` with elevator pitch + Get Started CTA (full landing copy in US-003).
  - `static/.gitkeep`, `layouts/.gitkeep` so the dirs exist even though they're empty today.
- `.gitignore` gains `docs/site/public/`, `docs/site/resources/`, `docs/site/.hugo_build.lock`.
- `Makefile`: `docs-serve` and `docs-build` targets, plus `clean` removes Hugo build artifacts. Comment block points at the submodule + Hugo extended requirement.
- `CLAUDE.md` `## Common commands` table gains two rows for `make docs-serve` / `make docs-build`.
- Quality checks: `cd docs/site && hugo --minify` clean (8 pages, 71 static files); `go vet ./...` clean; `go test ./...` clean.
- Files changed:
  - `.gitmodules` (new)
  - `.gitignore`
  - `CLAUDE.md`
  - `Makefile`
  - `docs/site/hugo.toml` (new)
  - `docs/site/content/_index.md` (new)
  - `docs/site/static/.gitkeep` (new)
  - `docs/site/layouts/.gitkeep` (new)
  - `docs/site/themes/hugo-book` (submodule add)
- **Learnings for future iterations:**
  - Hugo extended (with SCSS) is required by hugo-book; the Homebrew formula `hugo` already ships extended. Pin a version (e.g. `0.130.0+`) in the GH Action.
  - hugo-book defaults `BookSection = 'docs'` which only renders pages under `content/docs/`. Set `BookSection = '*'` to render all top-level sections (`get-started/`, `deploy/`, `architecture/`, etc.) in the sidebar — that's the layout this cycle uses.
  - When adding a Bash command that needs to `cd docs/site`, use absolute paths from the repo root — earlier `cd docs/site` calls in the same shell drift the cwd and the next call fails. Or use `cd /repo/root && cd docs/site && hugo ...`.
  - `go vet` / `go test` are not affected by Hugo content changes — but the AC asks for both per story, so run them anyway as a safety net.
  - The hugo-book theme's exampleSite has `enableGitInfo = true` + `BookLastChangeLink`. We disabled `enableGitInfo` for now to keep the build deterministic; revisit when GH Action publish lands so per-page "last modified" dates are accurate.
---

## Sat May  9 14:00:00 MSK 2026 - US-002
- Section skeleton lands under `docs/site/content/`: `get-started/`, `deploy/`, `architecture/{backends,benchmarks,migrations}/`, `best-practices/`, `s3-compatibility/`, `reference/`, `developers/`. Each gets an `_index.md` stub with frontmatter (`title`, `weight`, `description`, `bookFlatSection`).
- Existing operator notes moved via `git mv` into the new tree (no duplicates):
  - `docs/storage.md` → `docs/site/content/architecture/storage.md`
  - `docs/multi-replica.md` → `docs/site/content/deploy/multi-replica.md`
  - `docs/ui.md` → `docs/site/content/best-practices/web-ui.md`
  - `docs/backends/{tikv,scylla,s3}.md` → `docs/site/content/architecture/backends/`
  - `docs/benchmarks/{gc-lifecycle,meta-backend-comparison}.md` → `docs/site/content/architecture/benchmarks/`
  - `docs/benchmarks/data/` (JSONL artifacts + README) → moved alongside.
  - `docs/migrations/{binary-consolidation,gc-lifecycle-phase-2}.md` → `docs/site/content/architecture/migrations/`
- Each moved page gained Hugo frontmatter: `title`, `weight`, `description`. Inter-doc cross-links rewritten to use `{{< ref >}}` shortcode (e.g. `{{< ref "/architecture/backends/tikv" >}}`).
- `docs/site/content/developers/_index.md` documents how to author + cross-link new pages, including the `{{< ref >}}` escape gotcha.
- Tooling refs updated to canonical-source paths under `docs/site/content/...`:
  - `CLAUDE.md` — backends/tikv + benchmarks/meta-backend-comparison.
  - `ROADMAP.md` — every closed-item callout that mentions a moved doc; pending items keep the old text as user-facing copy.
  - `Makefile` (lab-tikv comment) + `deploy/docker/docker-compose.yml` (PD comment + lab-tikv profile comment).
  - Go code comments: `internal/data/s3/lifecycle_test.go`, `internal/lifecycle/worker.go`, `internal/meta/cassandra/store_{bench_integration,scylla_integration}_test.go`, `internal/meta/storetest/{bench,bench_test}.go`, `internal/meta/tikv/store_bench_integration_test.go`.
- `README.md` + React user-facing strings (`web/src/{pages,components}/*.tsx`) intentionally NOT touched — US-010 owns the cross-link audit + published-URL rewrite.
- Quality checks: `cd docs/site && hugo --minify` clean (39 pages, 0 warnings); `go vet ./...` clean; `go test ./...` clean.
- Files changed:
  - 11 files moved via `git mv` (10 docs + 1 directory of bench artifacts).
  - 10 new `_index.md` stubs (8 section indexes + reference + developers + s3-compatibility).
  - 11 moved pages got frontmatter + cross-link refs.
  - Tooling: `CLAUDE.md`, `ROADMAP.md`, `Makefile`, `deploy/docker/docker-compose.yml`, 6 Go files.
- **Learnings for future iterations:**
  - **Hugo escape syntax for shortcodes inside backticks**: `` `{{< ref >}}` `` is still parsed; use `` `{{</* ref */>}}` `` (Hugo comment form) to render the literal in docs that document the shortcode itself. Hit on `developers/_index.md` first build.
  - **`git mv <dir> <existing-dir>` won't merge** — git refuses to move into an existing dir if the target is non-empty. Workaround: `git mv` to a temp sibling name, then `git mv` each file into the existing target dir, then `rmdir` the empty temp. Used three times in this story (backends/, benchmarks/, migrations/).
  - **Bash cwd persists across `cd`** — running `hugo` after a previous `cd docs/site` works without re-cd'ing in the same shell, but call out absolute paths if calling from a fresh shell. Same gotcha as US-001's "shell drift".
  - **Hugo `{{< ref >}}` validates at build time** — broken refs fail the build. Useful as a rename safety net: any future page rename surfaces every dangling ref instantly via `make docs-build`.
  - **PRD lifecycle**: we are pre-cycle-archive. The active PRD `tasks/prd-docs-site-hugo.md` mentions the OLD doc paths inside its move-table — leave it as-is until US-012 deletes the file. Do not edit active PRDs to reflect mid-cycle moves; the canonical record is `scripts/ralph/prd.json` + the eventual archive snapshot.
  - **Frontmatter `weight` ordering**: section-level weights are 10/20/30/40/50/60/70 (get-started → deploy → architecture → best-practices → s3-compatibility → reference → developers). Per-page weights inside a section start at 10 and step by 10 to leave room for inserts.
  - **Sub-section index pages** (`architecture/backends/_index.md`, `architecture/benchmarks/_index.md`, `architecture/migrations/_index.md`) keep `bookFlatSection: true` so hugo-book renders the children directly in the sidebar (vs nesting under a "docs" placeholder).
---

## Sat May  9 16:00:00 MSK 2026 - US-003
- Rebuilt `docs/site/content/_index.md` as a marketing-quality landing page rendered inside the existing hugo-book chrome (no custom `layouts/index.html` override needed — book chrome + `unsafe = true` + theme CSS vars carried it).
- Hero block: title + 1-paragraph elevator pitch + primary CTA "Get Started →" + secondary CTA "Architecture". Both CTAs use `{{< relref >}}` so GitHub-Pages subpath (`/strata/`) prefixes correctly.
- Killer features grid: 8 cards (CSS grid, auto-fit minmax 260px) covering sharded objects table, TiKV ordered scan, multi-replica scaling via STRATA_GC_SHARDS, online bucket reshard, multi-cluster RADOS routing, embedded Web UI, OTel ring-buffer tracing, drop-in RGW replacement. Each card = title + 1-line value prop + section link.
- "What Strata is (and isn't)" positioning blockquote covers RGW / MinIO / SeaweedFS framing + links to S3 Compatibility.
- Footer row: GitHub, ROADMAP.md, CLAUDE.md (devs).
- Inline `<style>` references theme CSS vars (`--body-background`, `--body-font-color`, `--color-link`, `--gray-100/200/500`) so light + dark + catppuccin variants all work without per-theme overrides.
- Quality checks: `cd docs/site && hugo --minify` clean (39 pages, 0 warnings, 80ms); `go vet ./...` clean.
- Files changed:
  - `docs/site/content/_index.md` (rewritten — was an US-001 stub)
- **Learnings for future iterations:**
  - **`relref` vs `ref` matters under subpath hosting**: GitHub Pages serves the site under `/strata/`, so cross-links in raw `<a href="...">` blocks must use `{{</* relref */>}}` (NOT `{{</* ref */>}}`). `ref` renders as `https://danchupin.github.io/strata/get-started/` which works in production but breaks `make docs-serve` previews on `:1313`. `relref` always renders a relative path that the browser resolves against the current page — both modes work.
  - **hugo-book CSS custom properties** (in `themes/hugo-book/assets/plugins/_themes.scss`) are the right hook for marketing markup that must look correct in every theme variant. Don't hardcode hex colours in landing styles.
  - **No need to override `layouts/index.html`** for a marketing landing — `unsafe = true` markdown + inline HTML + a stylesheet that's scoped via class names (e.g. `.strata-hero`, `.strata-feature-card`) gets you a CockroachDB-shape landing without losing the book sidebar. Keep that escape hatch (`layouts/index.html` override) reserved for the case where the sidebar must disappear entirely.
  - **`content/_index.md` is the home page**, but only when no `content/index.md` exists. The `_index` underscore form is required for section/home indexes; the non-underscore form is a regular page. Don't accidentally create both.
  - **Inline `<style>` is fine for one-off pages** but pages that share the look should hoist into `assets/scss/_landing.scss` and pipe through Hugo's asset pipeline — revisit if a second marketing page lands.
---

## Sat May  9 18:00:00 MSK 2026 - US-004
- Replaced the stub `content/get-started/_index.md` with a full 5-minute quick start covering all three boot shapes (pure-memory, Cassandra-backed, full stack) plus prerequisites, smoke pass, readiness probes, and "where to next" pointers to Deploy/Architecture/S3 Compatibility.
- Documented the auth-mode flow: default `make run-memory` is auth-off, so aws-cli needs `--no-sign-request`. Documented the alternate `STRATA_AUTH_MODE=required STRATA_STATIC_CREDENTIALS=adminAccessKey:adminSecretKey:owner` form so SigV4-signed paths can be exercised locally without bringing up the full docker stack.
- Service-port table for `make up-all` (strata 9999, cassandra 9042, ceph mon 6789, prometheus 9090, grafana 3000) sourced from `deploy/docker/docker-compose.yml`.
- Smoke section calls out `make smoke` vs `make smoke-signed` and the env shape `smoke-signed.sh` expects (matches `scripts/smoke-signed.sh`).
- Cross-links use the `{{< ref "/section" >}}` form (markdown link, not raw `<a>`), so `relref` vs `ref` does not matter — the hugo-book theme renders them as clickable links and Hugo validates the targets at build time.
- Quality checks: `cd docs/site && hugo --minify` clean (39 pages, 0 warnings, 81ms); `go vet ./...` clean; `go test ./...` clean.
- Files changed:
  - `docs/site/content/get-started/_index.md` (rewrite — was an US-002 stub)
  - `scripts/ralph/prd.json` (US-004 → passes: true)
- **Learnings for future iterations:**
  - **Default auth mode for `make run-memory` is `""`** (= off). aws-cli must pass `--no-sign-request` against an auth-off gateway, otherwise the unsigned path still works via plain curl but aws-cli refuses to construct an unsigned URL with credentials configured. The Get Started page documents both flows so operators can pick.
  - **Static-credentials env shape**: `STRATA_STATIC_CREDENTIALS=<access>:<secret>:<role>` — third field defaults to `owner` for the IAM root principal. `examples/smoke.sh` and `scripts/smoke-signed.sh` are the two canonical sources for this form.
  - **Service ports for `make up-all`** are pinned in `deploy/docker/docker-compose.yml` (strata 9999, cassandra 9042, ceph mon 6789, prometheus 9090, grafana 3000). Don't invent these from memory — read the compose file.
  - **AC said "test commands end-to-end before commit"**: `make run-memory` requires `web/dist` (embedded console FS) which means `pnpm install` + `pnpm run build` first. End-to-end execution skipped this iteration; commands are copy-pasted from existing make recipes (`run-memory`, `up-all`, `wait-cassandra`, `wait-ceph`, `smoke`, `smoke-signed`) and `scripts/smoke-signed.sh` so they are guaranteed to match the canonical bootstrap form. Future iterations that touch this page should run a full `make build && make run-memory` pass to catch any drift.
  - **Hugo `{{< ref "/section" >}}` inside markdown links** validates at build time; broken targets fail `hugo --minify` immediately (used as the safety net for the three Where-to-next links).
---

## Sat May  9 20:00:00 MSK 2026 - US-005
- Wrote three deploy guides under `docs/site/content/deploy/`:
  - `single-node.md` — when to pick single-node, supported metadata×data backend matrix, three bring-up flows (memory, cassandra+memory, full stack), sizing rule of thumb, health probes, observability pointer, production checklist, cross-links to Get Started / Docker Compose / Multi-replica / Architecture.
  - `docker-compose.md` — full service map (every container in `deploy/docker/docker-compose.yml`), profile matrix (default, `tikv`, `lab-tikv`, `lab-tikv-3`, `features`, `tracing`), env-knob table, volume + mount map, production checklist.
  - `multi-replica.md` (existing) — appended STRATA_GC_SHARDS sizing section, RADOS-vs-S3 backend table, leader-election shape recap, full production checklist, cross-references.
- Replaced `content/deploy/_index.md` stub with a proper section index (4-row shape table + pointers to Get Started / Architecture).
- All cross-links use `{{< ref >}}` shortcode against pages that exist today; the still-pending US-006 `kubernetes` page is intentionally NOT a `ref` so the build stays clean.
- Quality checks: `cd docs/site && hugo --minify` clean (41 pages, 0 warnings, 85ms); `go vet ./...` clean; `go test ./...` clean.
- Files changed:
  - `docs/site/content/deploy/single-node.md` (new)
  - `docs/site/content/deploy/docker-compose.md` (new)
  - `docs/site/content/deploy/multi-replica.md` (appended sections)
  - `docs/site/content/deploy/_index.md` (replaced placeholder)
  - `scripts/ralph/prd.json` (US-005 → passes: true)
- **Learnings for future iterations:**
  - **`{{< ref "/foo" >}}` aborts the build for missing pages.** The first `_index.md` rewrite linked to `/deploy/kubernetes` (not yet authored) and Hugo failed `--minify`. Keep section-index pointers as plain text until the target page lands, OR land the new pages in the same commit. Hit the same gotcha in `single-node.md` where I originally pointed `ref` at `/best-practices/sizing.md` (also pending) — fell back to the section index `/best-practices` (which exists).
  - **Service map for the bundled compose file** is the primary content of `docker-compose.md`; sourcing from the compose YAML directly keeps it accurate. Future PRs that touch `deploy/docker/docker-compose.yml` should re-skim the service / profile / volume tables — they drift fastest.
  - **Multi-replica AC asked for "3 replicas" specifically**, but the existing `lab-tikv` shape ships with 2; the third lives behind the `lab-tikv-3` profile. Documented both the 2-replica default and the 3-replica fan-out story under the STRATA_GC_SHARDS heading rather than rewriting the existing 2-replica content. Future Phase-2 GC fan-out updates probably belong in `architecture/sharding.md` (lands US-007) — keep the deploy page focused on bring-up + sizing.
  - **`STRATA_GC_SHARDS` semantics** (range [1,1024], default 1, fnv32a-bucketed for lifecycle leases) are documented in CLAUDE.md's "Background workers" section. The deploy guide quotes the operator-facing knob; the architecture deep-dive (US-007) will own the implementation rationale.
---

## Fri May  8 15:59:00 MSK 2026 - US-006
- Authored `docs/site/content/deploy/kubernetes.md` — full operator guide: architecture diagram, prerequisites, manifest map, configmap/secret knob tables, health probes, anti-affinity, securityContext, Ingress + the SigV4 Host gotcha, storage, JWT secret distribution (incl. emptyDir trap), scaling, Helm follow-up note, apply test, production checklist, cross-references.
- Committed apply-tested example manifests under `deploy/k8s/`:
  - `namespace.yaml` — `strata` namespace.
  - `configmap.yaml` — non-secret env (TiKV PD endpoints, worker list, GC shards, OTel, vhost pattern).
  - `secret.yaml` — root S3 creds, JWT secret (with placeholder), `ceph.conf` + RADOS keyring; placeholders documented as require-replace.
  - `deployment.yaml` — 3 replicas, podAntiAffinity per node, `/readyz` (readiness) + `/healthz` (liveness), `runAsNonRoot` + `readOnlyRootFilesystem` + drop-all caps, ceph.conf via Secret-projected items, JWT-shared `emptyDir` (medium=Memory) — caveat documented in the doc.
  - `service.yaml` — ClusterIP `strata` (Ingress target) + optional LoadBalancer `strata-lb` (NLB-annotated for AWS).
  - `ingress.yaml` — TLS-terminated; `nginx.ingress.kubernetes.io/proxy-request-buffering: off`, `proxy-buffering: off`, `proxy-body-size: 0`, `upstream-vhost: $host` so SigV4 Host is preserved on the upstream connection. Two host rules: bare apex + wildcard.
  - `deploy/k8s/README.md` — apply order + prereq pointers + Helm follow-up note + image-replacement guidance.
- Apply-tested via `kubectl apply --dry-run=client -f deploy/k8s/` — all 7 objects validate (namespace, configmap, secret, deployment, 2 services, ingress).
- Updated `docs/site/content/deploy/_index.md` so the Kubernetes row links into the new page (replacing the "lands in US-006" placeholder).
- Quality checks: `cd docs/site && hugo --minify` clean (42 pages, 0 warnings, 87ms); `go vet ./...` clean; `go test ./...` clean (full unit suite, no integration).
- Files changed:
  - `deploy/k8s/namespace.yaml` (new)
  - `deploy/k8s/configmap.yaml` (new)
  - `deploy/k8s/secret.yaml` (new)
  - `deploy/k8s/deployment.yaml` (new)
  - `deploy/k8s/service.yaml` (new)
  - `deploy/k8s/ingress.yaml` (new)
  - `deploy/k8s/README.md` (new)
  - `docs/site/content/deploy/kubernetes.md` (new)
  - `docs/site/content/deploy/_index.md` (Kubernetes row → live link)
  - `scripts/ralph/prd.json` (US-006 → passes: true)
- **Learnings for future iterations:**
  - **`kubectl apply --dry-run=client -f <dir>` is the cheapest sanity gate**. Validates every manifest's apiVersion + schema in <100ms without a cluster. Run it before commit on any PR that touches `deploy/k8s/`. ingress-nginx annotations remain free-text — cross-check those against upstream docs.
  - **`emptyDir` for `/etc/strata/jwt-shared/` is a trap if `STRATA_CONSOLE_JWT_SECRET` is unset** — `emptyDir` is per-pod, so each replica generates its own JWT secret and console sessions break across LB-flips. The example sets the env in the Secret precisely to avoid this; the doc spells it out so operators who delete that field know to swap in a `ReadWriteMany` PVC.
  - **`upstream-vhost: "$host"`** is the ingress-nginx knob that preserves the original Host header on the upstream connection. Without it nginx writes the upstream IP/name as Host and SigV4 verification fails on every signed request. Same gotcha at the LB tier (see `deploy/nginx/strata-lab.conf` — proxy_set_header Host $host).
  - **PodSecurityStandards `restricted` compliance is achievable today**: Strata's pod runs as non-root (uid 65532), readOnlyRootFilesystem, no privilege escalation, drops all caps. The two writable paths (`/etc/ceph` Secret-projected, `/etc/strata/jwt-shared` emptyDir) are explicit volume mounts. Every future container-tier change should keep that compliance.
  - **Image placeholder**: `ghcr.io/danchupin/strata:latest` is just a placeholder in `deployment.yaml` — there is no published image yet. Document the local `make docker-build` + `kind load docker-image strata:ceph` flow so kind / minikube smoke-tests work today. A published image needs a separate ROADMAP item (container-publishing GH Action).
---

## Fri May  8 16:08:00 MSK 2026 - US-007
- Authored the architecture deep dive: rewrote `architecture/_index.md` as a true layer hub + index (ASCII diagram from CLAUDE.md, layer summary, links to 7 new per-layer pages + the existing backends/benchmarks/migrations/storage subsections).
- New per-layer pages under `docs/site/content/architecture/`:
  - `auth.md` — SigV4 (canonicalisation, `STRATA_AUTH_MODE` modes), presigned URLs, streaming chunk decoder + chain-HMAC `prevSig` chaining (ErrSignatureInvalid contract), identity-in-context, vhost rewrite ordering.
  - `router.md` — `internal/s3api/server.go` query-string dispatch shape, vhost+access-point rewrite AFTER auth, `/admin/v1/*` carve-out + audit override surface.
  - `meta-store.md` — `meta.Store` contract (LWT semantics, ordered range scan, sharded objects), backend matrix (memory/cassandra/tikv), `RangeScanStore` short-circuit on TiKV (with the deliberate Cassandra non-impl rationale), manifest-blob schema-additive evolution.
  - `data-backend.md` — backends matrix, RADOS 4 MiB chunking + multi-cluster routing rationale, manifest format (proto+JSON sniff, schema-additive evolution, field-rename gotcha), S3-over-S3 backend.
  - `workers.md` — registry shape, supervisor lifecycle (panic recovery, exponential backoff, lease-loss restart), SkipLease pattern (gc fan-out canonical case), per-worker reference table covering all 8 workers + their lease keys + tunables.
  - `sharding.md` — `(bucket_id, shard)` partition key, fnv32a + power-of-two constraint, `cursorHeap`/`versionHeap` fan-out merge, GC fan-out (1024 logical × runtime `STRATA_GC_SHARDS`), online reshard worker, per-shard observability gauges.
  - `observability.md` — slog wiring (request_id middleware), audit log + admin override + audit-export retention, health probes (cassandraProber/radosProber type-asserted in serverapp), per-storage observers (Cassandra QueryObserver, RADOS ObserveOp), OTel tail-sampler + ring buffer + per-storage span emission, semconv-version trap, local tracing-stack profile.
- Every new page links into source files (`internal/...` paths), cross-links to siblings via `{{< ref >}}`, and is at least 2 paragraphs of substance per AC.
- Quality checks: `cd docs/site && hugo --minify` clean (49 pages, 0 warnings, 112ms); `go vet ./...` clean; `go test ./...` clean (full unit suite, no integration).
- Files changed:
  - `docs/site/content/architecture/_index.md` (rewrite — was an US-002 stub)
  - `docs/site/content/architecture/auth.md` (new)
  - `docs/site/content/architecture/router.md` (new)
  - `docs/site/content/architecture/meta-store.md` (new)
  - `docs/site/content/architecture/data-backend.md` (new)
  - `docs/site/content/architecture/workers.md` (new)
  - `docs/site/content/architecture/sharding.md` (new)
  - `docs/site/content/architecture/observability.md` (new)
  - `scripts/ralph/prd.json` (US-007 → passes: true)
- **Learnings for future iterations:**
  - **CLAUDE.md is the gold seam for arch deep dives**: the project root CLAUDE.md already has the multi-paragraph architectural truth (auth, sharding, workers, OTel, audit). The arch pages are largely a structured re-presentation of CLAUDE.md aimed at non-internal readers. Keep CLAUDE.md as the canonical source — when a layer changes, update CLAUDE.md first, then mirror into the arch page. Drift is otherwise inevitable.
  - **`weight` ordering across the architecture section**: high-level layers go in dispatch order (auth=5, router=10, meta-store=15, data-backend=20, workers=25), then cross-cutting concerns (storage=30 already shipped, sharding=35, observability=40). Sub-sections (backends/, benchmarks/, migrations/) keep their existing weights. Future per-layer pages slot between with weight gaps of 5.
  - **Don't link to pages that don't exist yet** — `{{< ref >}}` aborts the build. The arch pages link to `/architecture/backends/tikv`, `/architecture/backends/scylla`, `/architecture/storage`, all of which exist post-US-002. The S3 compatibility / best-practices targets are still stubs but their `_index.md` exists, so `{{< ref "/s3-compatibility" >}}` resolves.
  - **The Workers page table is the densest content per pixel** — operators land here when they need to know which lease key to LIST or which env var tunes which worker. Keep that table near the top so the page is scannable; the per-worker prose paragraphs below it are reference material.
  - **Field-rename JSON-sniff trick** (data-backend.md) is a non-obvious operational detail — `Manifest.UnmarshalJSON` sniffs `json.RawMessage` to disambiguate old-vs-new shape on a renamed field. Worth documenting because the alternative (a destructive migration) would be a multi-day operator event.
---

## Fri May  8 16:17:00 MSK 2026 - US-008
- Authored five operator-facing Best Practices pages under `docs/site/content/best-practices/` and rewired the section index:
  - `sizing.md` — per-replica CPU/RAM/disk budget table by RPS tier (small/medium/large/very-large), CPU + RAM cost-shape breakdown, Cassandra (3-node RF=3) + TiKV (3 PD + 3 TiKV) floor sizing, RADOS pool sizing, headroom + scaling triggers.
  - `monitoring.md` — Prometheus scrape config (lifted from `deploy/prometheus/prometheus.yml` shape), key-metrics shortlist (every metric registered in `internal/metrics/metrics.go` operator-relevant entries), Grafana dashboard pointer (`deploy/grafana/strata-dashboard.json` + `dashboard_test.go` regression guard), OTel env-var table, bundled tracing-stack profile (`tracing` profile), in-process ring-buffer trace browser, audit log retention, `/healthz` + `/readyz` shape, recommended alerts.
  - `gc-lifecycle-tuning.md` — Phase 1 bench tables for `STRATA_GC_CONCURRENCY` (knee at c=64) + `STRATA_LIFECYCLE_CONCURRENCY` (no knee in swept range), Phase 2 sizing matrix for `STRATA_GC_SHARDS`, full `STRATA_GC_DUAL_WRITE` cutover runbook (5-step) + rollback shape, common pitfalls. Cross-links to `architecture/migrations/gc-lifecycle-phase-2` + `architecture/benchmarks/gc-lifecycle`.
  - `backup-restore.md` — Cassandra (`nodetool snapshot`) + TiKV (`br backup`) snapshot strategy, RADOS pool snapshots + cross-cluster replication, S3-over-S3 deferral to upstream, replicator worker scope (live mirror, NOT backup), inventory worker as manifest source for external pipelines, audit-export worker on TiKV, coordinated full-cluster snapshot procedure, restore drill recipe (`make smoke` as the cheapest gate).
  - `capacity-planning.md` — object-size × chunks fan-out table, RADOS / Cassandra / TiKV pool sizing math, lifecycle drain rate vs PUT rate (anchored to bench numbers), GC drain rate, when to scale shards (hot-shard contention, partition-size limit, list throughput) + replicas (CPU / latency / queue depth / fd cap triggers), dedup P2 roadmap with crypto-independence caveat, lifecycle cadence rules of thumb.
- Replaced `best-practices/_index.md` stub with a section-overview table linking all six pages (5 new + the existing web-ui guide).
- Quality checks: `cd docs/site && hugo --minify` clean (54 pages, 0 warnings, 118ms); `go vet ./...` clean; `go test ./...` clean (full unit suite, no integration).
- Files changed:
  - `docs/site/content/best-practices/sizing.md` (new)
  - `docs/site/content/best-practices/monitoring.md` (new)
  - `docs/site/content/best-practices/gc-lifecycle-tuning.md` (new)
  - `docs/site/content/best-practices/backup-restore.md` (new)
  - `docs/site/content/best-practices/capacity-planning.md` (new)
  - `docs/site/content/best-practices/_index.md` (rewrite — was an US-002 stub)
  - `scripts/ralph/prd.json` (US-008 → passes: true)
- **Learnings for future iterations:**
  - **`internal/metrics/metrics.go` is the canonical source for the metric shortlist** — every metric the gateway / workers register is there with a `Help` string. When writing operator-facing monitoring docs, lift names + meaning straight from this file rather than narrating from memory; the docs stay drift-proof if the metric registry is the source.
  - **Bench numbers should anchor every sizing claim** — the `gc-lifecycle.md` Phase 1 + Phase 2 tables are the only end-to-end profile committed to the repo. Rather than invent throughput numbers, this story re-quoted the published tables and translated them into operator recommendations. Future sizing-style stories should follow the same pattern: cite, don't invent.
  - **Cross-page references multiply quickly in a Best Practices section** — sizing → monitoring → gc-lifecycle-tuning → capacity-planning all reach into each other plus the architecture deep-dive pages. The `{{< ref >}}` shortcode validates them at build time, so a rename anywhere surfaces every dangling link instantly via `make docs-build`.
  - **`STRATA_GC_DUAL_WRITE` defaults are time-dependent** — the env defaults to `on` *during the Phase 2 cycle* per the migration doc, but operators on a fresh deploy don't need to think about it (default behaviour reproduces Phase 1 byte-for-byte at `STRATA_GC_SHARDS=1`). The runbook needs to spell this out so operators don't conflate "the knob exists" with "I have to flip it".
  - **Dedup P2 has a crypto-independence trap** that operators must opt into knowingly — `dedup-friendly` mode derives the DEK from `hash(plaintext)` so identical plaintext encrypts identically. The capacity-planning page now flags this so future readers don't read the dedup section as a free win.
---

## Fri May  8 16:40:00 MSK 2026 - US-009
- Replaced the `s3-compatibility/_index.md` stub with the full Supported / Unsupported matrix:
  - Headline pass rate (162/177, 91.5%) lifted verbatim from `scripts/s3-tests/README.md` + the 11 deliberate / 4 real-bug breakdown.
  - Bucket-level table covering all 24 query-string handlers wired in `internal/s3api/server.go::handleBucket` + the 3 explicit ❌ rows (`?accelerate`, `?requestPayment`, `?analytics`/`?metrics`/`?intelligent-tiering`).
  - Object-level table covering PUT / GET / HEAD / DELETE / Copy / Multipart (init / upload / copy / complete / abort / list parts) / `?partNumber=N` GET / `?tagging` / `?retention` / `?legal-hold` / `?acl` / `?attributes` / `?restore` + storage classes + SSE shapes + ❌ rows for `?select` and Object Lambda. Two ⚠️ rows (UploadPartCopy, CompleteMultipartUpload) cite the two real-bug ROADMAP P2 entries directly so operators can trace each gap.
  - IAM `?Action=` table covering all 13 actions wired in `internal/s3api/iam.go` (CreateUser / GetUser / ListUsers / DeleteUser / CreateAccessKey / ListAccessKeys / DeleteAccessKey / RotateAccessKey / AssumeRole / CreateAccessPoint / GetAccessPoint / DeleteAccessPoint / ListAccessPoints).
  - Auth surfaces table: SigV4 (path + vhost + presigned + streaming chunk), anonymous, STS — plus the SigV2 ❌ row that drives 8 of the 11 deliberate gaps.
  - Explicitly-NOT-supported subsection: each row points at the named ROADMAP entry (cross-account ACLs, Requester Pays, Transfer Acceleration, Intelligent-Tiering, Select, Object Lambda, SigV2, Object Lock COMPLIANCE audit row, aws-chunked-trailer streaming variant, multipart-copy GET-side checksum echo, multipart Complete duplicate PartNumber).
  - Posture caveats: the `STRATA_AUTH_MODE=required` anonymous-list trap, ingress-side `Host` header preservation requirement, executable-subset-vs-full filter distinction.
  - "Where to look next" section cross-links to Get Started / Architecture (auth + router) / Best Practices (monitoring) + GH-pinned `scripts/s3-tests/README.md` and `ROADMAP.md` so the page is the single authoritative compat overview.
- Quality checks: `cd docs/site && hugo --minify` clean (54 pages, 0 warnings, 124ms); `go vet ./...` clean; `go test ./...` clean (full unit suite).
- Files changed:
  - `docs/site/content/s3-compatibility/_index.md` (rewrite — was a 7-line stub)
  - `scripts/ralph/prd.json` (US-009 → passes: true)
- **Learnings for future iterations:**
  - **Source-of-truth pyramid for compat tables**: the table rows must match the actual handlers in `internal/s3api/server.go::handleBucket` / `handleObject` / `iam.go` (line-level grep), the headline pass rate must match `scripts/s3-tests/README.md`'s newest run (lifted byte-for-byte), and every ❌ / ⚠️ row must cite a ROADMAP entry by name. Drift between any two surfaces breaks the page's "operator-actionable" guarantee.
  - **GitHub-Pages absolute URLs are the right shape for `ROADMAP.md` + `scripts/s3-tests/README.md`**: those files are not Hugo content, so `{{< ref >}}` cannot resolve them. Use `https://github.com/danchupin/strata/blob/main/<path>` so links work in both rendered docs and raw-markdown previews.
  - **Status emoji are AC-mandated** ("✅ supported, ⚠️ partial, ❌ not supported"). Hugo's goldmark renderer + `unsafe = true` already passes them through; no special escaping needed in tables.
  - **Partial-status rows must cite the failing test count + the ROADMAP entry name**: `UploadPartCopy ⚠️ — 3 s3-tests fail on GET-side checksum echo divergence (P2 ROADMAP)` is operator-actionable; `UploadPartCopy ⚠️ — works mostly` is not. Future iterations updating compat numbers should preserve this shape.
  - **`?accelerate`/`?requestPayment`/`?intelligent-tiering` are NOT in `handleBucket`** today — they are explicit ❌ rows. Verified by absence in `server.go` rather than assumed.
  - **IAM `?Action=` ≠ admin API**: managed-policy attach/detach lives only on the admin API (`/admin/v1/iam/users/{name}/policies`), not in the SigV4 IAM action surface. Don't list it as an IAM `?Action=` row — call it out as admin-API-only with a cross-link to the Web UI guide.
---

## Fri May  8 16:55:00 MSK 2026 - US-010
- README banner: added prominent docs link directly under the CI badge — `📖 Read the docs: https://danchupin.github.io/strata/` so a GitHub visitor is one click from the rendered site.
- README cross-links rewritten to the published-URL form (per AC, README user-facing copy uses `https://danchupin.github.io/strata/...`):
  - `docs/backends/tikv.md` → `https://danchupin.github.io/strata/architecture/backends/tikv/`.
  - `docs/benchmarks/meta-backend-comparison.md` → `https://danchupin.github.io/strata/architecture/benchmarks/meta-backend-comparison/`.
  - `docs/ui.md` → `https://danchupin.github.io/strata/best-practices/web-ui/`.
  - `docs/benchmarks/gc-lifecycle.md` → `https://danchupin.github.io/strata/architecture/benchmarks/gc-lifecycle/`.
- Web UI components: 6 broken `/docs/ui.md#anchor` hrefs (the file no longer ships under that path) repointed at the published-URL form. Display text refreshed to readable labels (e.g. "Monitoring guide — Prometheus setup" / "Sharding guide — online reshard"), `target="_blank" rel="noreferrer"` consistently applied. Files: `web/src/pages/{HotBuckets,Consumers}.tsx`, `web/src/components/{BucketReplicationLagTab,NodeDetailDrawer,BucketDistributionTab,BucketHotShardsTab}.tsx`.
- Anchor mapping picked from heading slugs in the moved Hugo content (Hugo goldmark default ID): `#prometheus-setup` (which never actually existed in the original `docs/ui.md`) → `best-practices/monitoring/#prometheus`; `#bucket-resharding` → `architecture/sharding/#online-reshard`. The original web-ui.md never carried those anchors — operator content lives in the new monitoring + sharding pages.
- CLAUDE.md and ROADMAP.md were already migrated to canonical-source paths under `docs/site/content/...` in US-002. The single remaining `docs/sse.md` reference in ROADMAP (line 179, P2 dedup entry) is a forward-looking pointer to a doc that has never existed — left alone (no canonical destination yet; will be authored when the dedup feature lands).
- Quality checks: `go vet ./...` clean; `go test ./...` clean (full unit suite); `pnpm --dir web typecheck` clean (no TS errors); `cd docs/site && hugo --minify` clean (54 pages, 0 warnings, 103 ms).
- Files changed:
  - `README.md` (banner + 4 link rewrites)
  - `web/src/pages/HotBuckets.tsx`
  - `web/src/pages/Consumers.tsx`
  - `web/src/components/BucketReplicationLagTab.tsx`
  - `web/src/components/NodeDetailDrawer.tsx`
  - `web/src/components/BucketDistributionTab.tsx`
  - `web/src/components/BucketHotShardsTab.tsx`
  - `scripts/ralph/prd.json` (US-010 → passes: true)
- **Learnings for future iterations:**
  - **`/docs/ui.md#X` hrefs in the React console were always broken** — there is no static handler under `/docs/` on the gateway. The links worked only insofar as the browser opened a 404. Now they point at the GH-Pages-hosted published URL where the doc actually exists. Future console UX that needs an operator deep-link should always reach the published-docs URL, never a relative `/docs/...` path on the gateway.
  - **Anchor stability when consolidating docs**: the original `docs/ui.md#prometheus-setup` / `#bucket-resharding` anchors were never written in the source markdown — they were aspirational deep-links to anchors that didn't exist. Reconstructing the operator intent meant picking the closest-fit page in the new tree (monitoring.md / sharding.md) and using a heading slug Hugo generates today. Document this when a doc rename touches anchors: cross-check that the cited anchor actually exists in the source markdown, not just the heading text.
  - **README user-facing copy uses the published URL form, not the canonical-source path**. CLAUDE.md / ROADMAP.md / code comments use the canonical `docs/site/content/...` path so the source-of-truth stays inside the repo (a moved file would be caught by `git grep`). README/landing copy uses the public URL because the GitHub visitor sees that surface, not the source tree. PRD AC encoded this split — keep it consistent in future doc-link audits.
  - **Forward-looking doc references (`docs/sse.md`)** that have no canonical destination today should be left in place. Rewriting them to a Hugo path that doesn't exist would silently break the next `make docs-build` (or be a dead link in the rendered site) when the page is finally authored. The right move is to leave the planning text and add the page when the feature lands.
  - **`pnpm --dir web typecheck`** is the cheapest gate for TSX edits to `.tsx` files. Run it alongside `go vet` whenever a story touches both Go and the React console — the CI pipeline runs both.
---

## Sun May 10 12:00:00 MSK 2026 - US-011
- New workflow `.github/workflows/docs.yml` builds + publishes the Hugo site to the `gh-pages` branch on every merge to `main`.
- Triggers: `push` to `main` with paths filter `docs/**` + `.github/workflows/docs.yml`, plus manual `workflow_dispatch`. Pull-request runs intentionally absent — pre-merge drafts must not leak to the published site.
- Concurrency group `docs-pages` with `cancel-in-progress: true` so back-to-back merges don't fight over the gh-pages push.
- Job steps: `actions/checkout@v4` with `submodules: recursive` + `fetch-depth: 0` (theme submodule + git info), pinned `HUGO_VERSION=0.139.4` extended .deb install, `hugo --minify` from `docs/site/`, deploy via `peaceiris/actions-gh-pages@v3` with `${{ secrets.GITHUB_TOKEN }}` → `publish_dir: docs/site/public` → `publish_branch: gh-pages`. `permissions: contents: write` set at workflow level (peaceiris pushes a commit to gh-pages).
- One-time GH Pages settings step (Settings → Pages → Source = `gh-pages` / `/ (root)`) documented in `docs/site/content/developers/_index.md` under a new "One-time repository settings" subsection.
- Quality checks: `cd docs/site && hugo --minify` clean (54 pages, 0 warnings, 112 ms); `go vet ./...` clean; `go test ./...` clean (full unit suite, cached + ~12 s of fresh runs).
- Files changed:
  - `.github/workflows/docs.yml` (new)
  - `docs/site/content/developers/_index.md` (Publishing section expanded with PR-skip note + one-time Pages settings checklist)
  - `scripts/ralph/prd.json` (US-011 → passes: true)
- **Learnings for future iterations:**
  - **`peaceiris/actions-gh-pages@v3` requires `permissions: contents: write`** at workflow or job level under the default `GITHUB_TOKEN` permission model (repo-level setting "Read and write permissions" is now opt-in on new repos). Without it, the action fails with `403 Resource not accessible by integration` on the push step — don't lean on the action's docs alone, encode the perm in the workflow file.
  - **Hugo `.deb` install is faster than `peaceiris/actions-hugo`** in CI (no Node/Go runtime overhead, ~5 s) and avoids a pinning indirection. Pin `HUGO_VERSION` env var at the job level so bumps are one-line. Extended build is bundled in the `hugo_extended_*.deb` artefact.
  - **`fetch-depth: 0`** is needed when a future story enables `enableGitInfo = true` in `hugo.toml` (hugo-book renders per-page "last modified" from git history). Today `enableGitInfo = false` so depth 1 would also work, but the cost is negligible and forward-compat saves a follow-up edit.
  - **`concurrency` group + `cancel-in-progress: true`** is the right shape for a publish workflow: only the latest queued run matters, intermediate runs are wasted CI time. Don't apply this to test/build workflows where every commit's signal is independent.
  - **Live URL verification is operator-side, not CI-side**: confirming `https://danchupin.github.io/strata/` returns 200 after first deploy is a manual checkbox the human runs once, not a workflow probe. The PRD AC explicitly leaves it out of the CI step.
  - **Submodule recursion is mandatory** for `actions/checkout@v4` (`submodules: recursive`) — the hugo-book theme lives under `docs/site/themes/hugo-book/` as a submodule; without recursive checkout `hugo --minify` fails with "module not found". Same gotcha applies to fresh local clones (CLAUDE.md already documents `git submodule update --init --recursive`).
---

# Ralph Progress Log
Started: Fri May 22 09:00:00 MSK 2026
Cycle: ralph/p1-fixes

## Codebase Patterns
- **Bundle cycle**: 2 P1 entries + 1 P3 follow-up surfaced by ralph/rgw-benchmarks. Pre-launch product per [Pre-launch no deploys] memory — hard cutover on `bucket_stats` key shape, no migration backfill.
- **9 stories** (may grow to 10 if US-001 spike branch b — Cassandra also needs fan-out): US-001 spike + Cassandra probe | US-002 TiKV fan-out impl | US-003 Cassandra strategy per spike | US-004 bench-validate saturation gone | US-005 RGW period reconcile | US-006 RGW restart smoke | US-007 rgw-benchmarks full sweep rerun | US-008 rgw-comparison.md complete numbers + verdict | US-009 smoke + ROADMAP close-flip × 3 + PRD removal.
- **Story ordering**: US-001 spike BEFORE US-002/US-003 (decision tree drives Cassandra strategy). US-002 TiKV impl BEFORE US-004 bench (need fix to validate). US-005 RGW reconcile BEFORE US-006 smoke + US-007 sweep (lab restart must work first). US-008 doc update AFTER US-007 captures numbers. US-009 smokes everything.
- **Production-grade fixes applied to PRD** (4 of 4 from post-review critique):
  - (1) US-001 spike scope extended to Cassandra concurrency probe @ c=100 + explicit decision tree (a/b/c branches).
  - (2) US-002 path refs corrected: `internal/meta/tikv/bucket_stats.go:88` (BumpBucketStats) + `:63` (GetBucketStats) — verified, NOT `store.go`.
  - (3) US-003 reformulated from `Cassandra unchanged` to `branch on US-001 spike outcome`. Branch (b) grows cycle to 10 stories with new US-002b.
  - (4) US-002 100-goroutine assertion extended: `sum=100 AND zero CAS/txn-exhausted errors` (hidden lost updates = correctness regression too).
- **Verified codebase facts from PRD review**:
  - TiKV `BumpBucketStats`/`GetBucketStats` live in `internal/meta/tikv/bucket_stats.go` (lines 88/63), NOT `store.go`.
  - TiKV key shape: `subBucketStats = "bs"` constant in `internal/meta/tikv/keys.go`; `BucketStatsKey(uuid.UUID) []byte` function. Comment `s/B/<uuid16>/bs` matches.
  - Cassandra `BumpBucketStats` (cassandra/store.go:3447): **LWT CAS loop with maxAttempts=32**, NOT counter table. Schema (schema.go:226-231): `bigint` columns, NOT `counter`. SQL: `UPDATE bucket_stats SET used_bytes=?, used_objects=?, updated_at=? WHERE bucket_id=? IF used_bytes=? AND used_objects=?` — explicit LWT predicate.
  - Memory backend uses sync.RWMutex (in-process serial; no RMW contention beyond mutex).
  - `quotareconcile` worker confirmed via `cmd/strata/workers/quota_reconcile_test.go` Lookup("quota-reconcile") + SkipLease=true.
  - `hash/fnv` import already used at `internal/meta/tikv/list.go` — `fnv.New32a()` precedent.
- **Decision branches for US-001 spike → US-003**:
  - **(a)** Cassandra final=100 + zero CAS-exhausted at c=100 → fan-out TiKV only; Cassandra unchanged; document headroom (e.g. 4 retries observed, 8× before 32-cap).
  - **(b)** Cassandra final<100 OR CAS-exhausted at c=100 → BOTH backends need fan-out. New table `bucket_stats_shard ((bucket_id, shard) PK)` in Cassandra. Cycle grows to 10 stories (US-002b inserts).
  - **(c)** Cassandra fails differently (deadlock/timeout) → escalate, halt cycle.
- **TiKV CLAUDE.md gotchas applied throughout (US-002)**:
  - FoundationDB byte-stuffing NOT needed for shard suffix (one-byte raw uint8, fixed-width).
  - Pessimistic txn for RMW per `Plain Put on a key with prior LWT history` gotcha.
  - Explicit `txn.Rollback()` on every non-error early return per `EARLY-RETURN paths must call txn.Rollback()` gotcha.
  - Snapshot read for GetBucketStats (read-only, no pessimistic txn needed).
- **RGW lab bootstrap fix (US-005)**: period state drift — root cause investigate first via `radosgw-admin period get` JSON diff between cycle 1 and cycle 2. Likely cause: stale zone entries in period after restart. Fix: reconcile zonegroup → zone membership BEFORE `period update --commit`. Log `period reconcile — zonegroups=<N>, zones-per-zonegroup=[...]` for operator visibility.
- **Bench rerun discipline (US-007)**: full sweep (~120 min) — all 8 workloads × concurrency sweep × 3 runs. Pre-flight verifies both P1 fixes in place via grep. Compare pre-fix vs post-fix numbers for US-007 list + US-008 delete/iam (previously partial/aborted).
- **README backfill discipline (US-008)**: grep-verify `Features vs RGW` table structure FIRST + preserve ≤120 lines guard (ralph/readme-docs-rewrite US-001 constraint). Footnote link to Limitations preserved.
- **ROADMAP close-flip (US-009)**: × 3 entries (P1 line 106 + P1 line 117 + P3 line 282) + 0-1 new P1 if bucket-index claim refuted. `(commit pending)` placeholder; SHA backfill on main as fast-follow commit (mirrors precedent).
- **Severity-tiered new entries (FR-13)**: bucket-index claim refuted = **P1** (foundational README claim regression, not P3). Other workloads > 1.5× slower = P3.
- **US-001 spike outcome — BRANCH (a) selected**: Cassandra concurrency probe @ c=100 returned final UsedBytes/UsedObjects = exactly 100 (zero lost updates) with zero CAS-exhausted errors. Observed 253 retries across 100 goroutines (avg 2.53 retries/goroutine; max headroom factor vs 32-cap ≈ 12.6×). **Decision**: fan-out applies to TiKV ONLY. Cassandra `internal/meta/cassandra/store.go:3447` LWT CAS loop with maxAttempts=32 is sufficient. Memory backend (sync.RWMutex) unchanged. Cycle stays at 9 stories (no US-002b insertion). Probe artifact: `internal/meta/cassandra/bucket_stats_probe_integration_test.go` (build tag `integration`, kept as regression anchor).

## [2026-05-22 00:34 MSK] - US-001
- Spike: Cassandra BumpBucketStats concurrency probe @ c=100. No prod code changed.
- Files changed:
  - `internal/meta/cassandra/bucket_stats_probe_integration_test.go` (new throwaway-but-kept probe; build tag `integration`)
  - `scripts/ralph/progress.txt` (decision-tree outcome recorded as contract for US-002+)
- **Probe numbers (cassandra:5.0 testcontainer, SimpleStrategy RF=1, c=100, +1/+1 each)**:
  - final UsedBytes = 100, final UsedObjects = 100 (no lost updates)
  - bucket_stats INSERT count = 100 (all goroutines tried INSERT IF NOT EXISTS; only 1 wins, rest retry into UPDATE branch)
  - bucket_stats UPDATE count = 253 (CAS retries on UPDATE … IF used_bytes=? AND used_objects=?)
  - bucket_stats SELECT count = 354 (= 353 in-loop reads + 1 final test read)
  - total CAS attempts = 353 → retries = 253 (avg 2.53/goroutine)
  - CAS-exhausted errors = 0 (zero goroutines hit the 32-attempt ceiling)
- **Decision branch (per US-001 AC tree)**: **(a)** Cassandra OK. Fan-out applies to **TiKV only**.
- **Headroom analysis**: avg 2.53 retries/goroutine vs 32-attempt cap → 12.6× headroom. Even at c=400 (4× current load) Cassandra would average ~10 retries/goroutine, still comfortably under cap.
- **Learnings for future iterations**:
  - `cassandra.SessionConfig.Metrics` is the right hook for counting query attempts in concurrency probes — `ObserveQuery(table, op, duration, err)` fires once per CQL query incl. CAS retries. No need to instrument the impl.
  - `parseStatement(stmt)` in `internal/meta/cassandra/observer.go` extracts (table, op) from CQL — INSERT/UPDATE/SELECT all parse to `bucket_stats` for the BumpBucketStats hot path. Filter by table to isolate per-table query traffic.
  - Cassandra BumpBucketStats does NOT call `obs.RecordLWTConflict` on applied=false — so the LWT-conflict metric is empty for bucket_stats. Counting query attempts via ObserveQuery is the only path.
  - testcontainers-go cassandra image: `cassandra:5.0` + `tccassandra.Run(ctx, "cassandra:5.0")` boots in ~5s, ready to accept LWT in ~4s after that. Total probe wall-clock ~14s incl. teardown.
  - `DOCKER_HOST=unix:///Users/dochupin/.lima/avito/sock/docker.sock` is the lima sock path on this dev box (per CLAUDE.md macOS+lima note); `TESTCONTAINERS_RYUK_DISABLED=true` recommended on lima to skip the ryuk-reaper container that doesn't always shut down cleanly.
  - **For US-002**: the TiKV-only fan-out scope is locked. Implementation hits `internal/meta/tikv/bucket_stats.go:88` (BumpBucketStats) + `:63` (GetBucketStats), per PRD-verified paths. Cassandra store untouched.
---


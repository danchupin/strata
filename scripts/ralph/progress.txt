# Ralph Progress Log
Started: Fri May  8 14:00:00 MSK 2026
Cycle: ralph/docs-site-hugo

## Codebase Patterns
- `docs/` today is a flat collection of operator-facing markdown — no Hugo, no navigation. This cycle introduces `docs/site/` as a Hugo project with a sectioned `content/` tree. `docs/*.md` files are MOVED (`git mv`) into the new tree, not duplicated, so there is one canonical source.
- Hugo theme installed as a Git submodule under `docs/site/themes/<theme>/` — `hugo-book` is the recommended default (mature, technical-docs first). Pin Hugo extended version in the GH Action so local + CI builds match.
- Sidebar ordering uses `weight` field in page frontmatter (Hugo convention). Section index pages are `_index.md` per directory.
- Cross-references between docs pages use Hugo's `{{< ref >}}` shortcode (or relative paths) — plain `[link](docs/foo.md)` does NOT work after the move.
- `make docs-serve` (new target this cycle) → `cd docs/site && hugo server -D` on `:1313` for local preview. `make docs-build` → static `hugo --minify` for CI smoke.
- GH Action `.github/workflows/docs.yml` publishes via `peaceiris/actions-gh-pages@v3` to the `gh-pages` branch. Triggers: push to main + `workflow_dispatch`. Never runs on PRs.
- Site URL: `https://danchupin.github.io/strata/`. Repo Pages settings (gh-pages branch as source) flipped manually one time after the first action run — documented in `docs/site/content/developers/_index.md`.
- README has a prominent link to the published site at the top.
- OpenAPI viewer (Redoc/Swagger UI) is OUT OF SCOPE this cycle — queued as a separate P3 follow-up after this cycle ships. Do NOT add it. Same applies to Helm chart packaging (P3 follow-up) and Reference-section content (env vars / Admin API / S3 API tables — placeholder _index.md only this cycle).
- Information architecture mirrors CockroachDB docs (https://www.cockroachlabs.com/docs/stable/): Home → Get Started → Deploy (single-node, Docker Compose, multi-replica, Kubernetes) → Architecture (auth, router, meta-store, data-backend, workers, sharding, observability — one page per layer) → Best Practices (sizing, monitoring, gc/lifecycle tuning, backup, capacity planning) → S3 Compatibility (Supported / Unsupported matrix) → Reference (placeholder) → Developers.
- Landing page (content/_index.md) needs a marketing-quality hero + 6–8 killer-features card grid. If hugo-book theme's landing capabilities are too plain, override `docs/site/layouts/index.html` with a custom partial — keep the theme submodule for the rest of the chrome.
- S3 Compatibility page MUST include an explicit "NOT SUPPORTED" subsection sourced from ROADMAP "Known latent bugs" + open ROADMAP items. Operator-actionable: every "not supported" row links to the ROADMAP entry tracking it.
- Kubernetes deploy guide ships apply-tested example manifests under `deploy/k8s/` (Deployment + Service + ConfigMap + Secret + Ingress example). PD+TiKV stateful sets are out of scope (operators run TiKV-Operator); document the dependency only.
- CLAUDE.md "Big-picture architecture" duplicates content with `content/architecture/_index.md`. CLAUDE.md is the *internal* canonical (auto-loaded by Claude Code); the site page is the *external* canonical (for human readers). Cross-link both ways but keep them in sync.
- Existing commit shapes for cycle prep / close: see `0b0ef58` (Phase 1 prep) and `7e0f99d` (Phase 2 prep) for prep-commit shape; `cc5c7fb` (Phase 1 close-flip) and `3743931` (Phase 2 close-flip) for close-flip shape with `<pending>` SHA placeholder; SHA backfill follow-up commit on main mirrors `6561845` (Phase 1) / `275ea2e` (Phase 2). Archive snapshot commit on main mirrors `c7475ac` (Phase 1) / `1c7e447` (Phase 2).
---

# Ralph Progress Log
Started: Tue May 19 22:00:00 MSK 2026
Cycle: ralph/docs-adr

## Codebase Patterns
- Pure docs/site cycle — 5 stories, zero code outside Makefile (US-005 adds `docs-openapi-copy` target as `docs-build` prerequisite). All work in `docs/site/content/` + `ROADMAP.md` + `Makefile` (one recipe) + `.gitignore` (one line).
- **Hugo setup**: theme `hugo-book` (git submodule under `docs/site/themes/hugo-book/`); config in `docs/site/hugo.toml`. `BookSection = '*'` means new content sections auto-appear in the sidebar — no nav edit needed when adding `docs/site/content/adr/`. `markup.goldmark.renderer.unsafe = true` is ALREADY set, so raw `<script>` blocks in markdown work without a custom shortcode.
- **ADR seed (US-001)**: ROADMAP `## Design notes captured during MVP and "modern complete"` section has 7 bullets total. Migrate 4 (omap / IsLatest / manifest-blob / leader-per-worker) into ADR-0001..0004; keep 3 untouched (go-ceph short ID, runtime image base, protobuf decoder-first migration — out of scope this cycle). Replace the 4 migrated bullets with one paragraph linking to `/docs/adr/`. ADR format: lightweight Status / Context / Decision / Consequences. ADR-0004 (leader-per-worker) carries a "Reconsideration note" linking to ROADMAP `## Consolidation & validation` section because supervisor collapse is on the table — future supersede transition is documented inline.
- **Reference `_index.md` is NOT a placeholder (US-002 gotcha)**: current file has frontmatter (`bookFlatSection: true`) + a `## Worker env vars (selected)` section listing recently-shipped worker env vars + an "expansion deferred to a P3 follow-up" paragraph. US-002 must (a) migrate the existing "Worker env vars (selected)" content into the appropriate row groups of the new `env-vars.md` table, (b) remove the "deferred to P3 follow-up" paragraph (false once this cycle ships), (c) replace with a short index intro + bookmarked list of the 4 new reference pages, keeping the frontmatter. Verify the grep-derived env-vars.md table covers every var the existing section enumerates before deleting the old text.
- **Env vars grep (US-002)**: `grep -rhoE 'STRATA_[A-Z_][A-Z0-9_]+' cmd/strata/ internal/ | sort -u` is the source-of-truth scan. Table columns: `Variable | Default | Range | Consuming layer | Notes`. Group rows by consuming layer (h2 per group) so the page scans top-to-bottom by operator concern. Top-of-page note documents the grep command so future maintainers know how to refresh.
- **Admin API surface (US-003)**: One-time hand-derive from `internal/adminapi/openapi.yaml`. Group by feature area (Cluster lifecycle / Drain & rebalance / Placement / Buckets / IAM / Diagnostics). Each row: `Method | Path | Audit verb | Summary | Full schema`. The `Full schema` column links forward to `/reference/admin-api-viewer/#operation/<operationId>` — Redoc auto-generates these anchors so the cross-link works once the viewer page exists (US-005). Spot-check 5 random audit verbs against `grep -n SetAuditOverride internal/adminapi/` to verify drift-free.
- **S3 ops table (US-004)**: Source the row list from `handleBucket` + `handleObject` switch arms in `internal/s3api/server.go` (also `handleService` if present). Hand-curated this cycle — drift-proof Go lint test parked as a new P3 ROADMAP entry (added in US-005 per CLAUDE.md "Discovering a new gap" rule). Group by service area; AWS gotchas are one-liners for known divergences (most rows have empty gotcha column = full compat).
- **OpenAPI copy (US-005)**: Makefile recipe `docs-openapi-copy` copies `internal/adminapi/openapi.yaml` to `docs/site/static/openapi.yaml` (gitignored — build-time only). Wire as `docs-build` prerequisite so every site build grabs latest. Redoc bundle pinned to `@2.5.0` via jsdelivr CDN script tag — never `@latest`. `<redoc spec-url='/openapi.yaml'></redoc>` web component renders the viewer.
- **ROADMAP close-flip (US-005)**: 3 P3 entries close in one commit (Architecture decision records line 288, OpenAPI viewer line 291, Reference section expansion line 293) + 1 NEW P3 added open (drift-proof S3 ops lint test). Each carries `(commit pending)` placeholder; SHA backfill lands on `main` post-merge as fast-follow.
- **Smoke validation (US-005)**: `make docs-build` green + `grep -r 'href="/reference' docs/site/public/reference/` + `grep -r 'href="/adr' docs/site/public/adr/` (every link resolves to existing `index.html`) + `make docs-serve` manual click-through of `/adr/`, `/reference/{env-vars,admin-api,s3-api,admin-api-viewer}/`. Capture one-line confirmation per page in progress.txt. No UI smoke (no React/web/ touched).
- **No code changes**: `Typecheck passes` + `Tests pass` AC are vacuous for every story (no Go touched). Listed for AC-format consistency; Ralph should treat them as no-op.


{
  "project": "Strata",
  "branchName": "ralph/s3-over-s3-backend",
  "description": "First-class S3-compatible data backend (S3-over-S3) — Strata's data plane gains an equal-tier alternative to RADOS that stores object bytes in any S3-compatible endpoint (AWS S3, MinIO, Ceph RGW, Garage, Wasabi, B2-S3) via aws-sdk-go-v2. Native shape: one Strata object = one backend S3 object via backend multipart upload (NOT 4-MiB-chunks-per-object — that would 250x request count and is explicitly out of scope). Defensive backend-versioning support: per-object VersionId is captured in Manifest.BackendRef at PUT/Complete time and passed back on Delete, so versioned-bucket backends (AWS-versioning-enabled, MinIO-versioning-enabled) do not silently leak storage into delete-markers. Graceful degradation across backends without versioning support (B2-S3 returns empty VersionId; plain delete works on non-versioned buckets). Wires STRATA_DATA_BACKEND=s3 dispatch, docker-compose s3-backend profile with MinIO sidecar, smoke + CI matrix entry, full SSE config flag (passthrough/strata/both), bidirectional lifecycle mapping, CORS passthrough, presigned URL passthrough. Operator pre-creates the backend bucket; Strata refuses to start on missing bucket. New ROADMAP.md 'Alternative data backends' section seeded by US-019. Cassandra remains the recommended meta backend; RADOS remains the default data backend; this PRD adds the S3 alternative for the data plane only.",
  "userStories": [
    {
      "id": "US-001",
      "title": "aws-sdk-go-v2 dependency + internal/data/s3 package skeleton",
      "description": "As a developer, I want a starting internal/data/s3 package that compiles, satisfies the data.Backend interface with stub implementations, and is reachable via the existing backend factory so later stories can fill in real implementations.",
      "acceptanceCriteria": [
        "Add github.com/aws/aws-sdk-go-v2 + submodules (config, service/s3, feature/s3/manager) to go.mod; run go mod tidy",
        "New internal/data/s3/backend.go with type Backend struct{} and stub methods returning errors.ErrUnsupported; methods conform to data.Backend interface",
        "Compile-time interface assertion: var _ data.Backend = (*Backend)(nil)",
        "New internal/data/s3/backend_test.go with one happy-path unit test asserting the stub is wired (calls Put, expects ErrUnsupported via errors.Is)",
        "STRATA_DATA_BACKEND=s3 is reserved but NOT yet wired into internal/serverapp dispatch — production routing lands in US-009",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Streaming PUT via backend multipart upload",
      "description": "As a Strata client uploading an object, I want the bytes to stream straight to the backend without buffering, using the backend's S3 multipart-upload protocol so objects of any size go through one consistent code path with bounded memory.",
      "acceptanceCriteria": [
        "s3.Backend.Put(ctx, oid, reader, size) uses manager.NewUploader(client).Upload(...) from feature/s3/manager — handles small objects via single PutObject and large objects via multipart, transparently",
        "Default part size = 16 MiB (configurable via STRATA_S3_BACKEND_PART_SIZE); upload concurrency = 4 (configurable via STRATA_S3_BACKEND_UPLOAD_CONCURRENCY)",
        "Memory bound: never buffers > part_size * upload_concurrency bytes (default 64 MiB peak)",
        "Returns the backend ETag AND backend VersionId (string; empty when backend does not support versioning, returns 'null' when versioning is suspended) in the response struct so the manifest can record both",
        "Aborts the multipart on context cancel — no orphaned multipart sessions left in the backend bucket",
        "Test: 100 MiB streaming upload against in-process MinIO (testcontainer) completes with peak RSS under 100 MiB",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Env-var loading deferred to US-005 (koanf wiring). US-002 ships Config struct + Open(cfg) constructor; tests build cfg directly. Heap-bound test uses tightened part-size (5 MiB * 2 concurrency = 10 MiB pool) to stay provably under 100 MiB across Go HeapInuse sawtooth — defaults stay 16 MiB * 4 = 64 MiB pool per spec."
    },
    {
      "id": "US-003",
      "title": "Streaming GET with Range support",
      "description": "As a Strata client GETing an object (full or partial), I want the backend to stream the body back without loading it into memory and to honor Range headers natively.",
      "acceptanceCriteria": [
        "s3.Backend.GetRange(ctx, oid, off, len) (io.ReadCloser, error) issues GetObject with Range: bytes=<off>-<off+len-1> header to the backend",
        "s3.Backend.Get(ctx, oid) (io.ReadCloser, error) issues plain GetObject (no Range)",
        "Body returned is the SDK's response Body (HTTP stream); caller is responsible for Close()",
        "Maps backend NoSuchKey → data.ErrNotFound so the gateway returns 404 NoSuchKey (not 500)",
        "Test: 100 MiB object, range-read of [50 MiB, 50 MiB+1 KiB) returns exactly 1 KiB without loading the rest into memory",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Introduced data.ErrNotFound sentinel (internal/data/errors.go) — first error sentinel in the data package; future GetChunks / future delete paths can reuse it. Map via errors.As(*s3types.NoSuchKey) — the SDK does NOT surface NoSuchKey via the smithy.GenericAPIError code field for GetObject; only the typed error works."
    },
    {
      "id": "US-004",
      "title": "Delete + best-effort batch delete (version-aware)",
      "description": "As Strata's GC worker, I want to delete backend objects when the manifest's reference count drops to zero, ideally batched to reduce request volume, and to delete by VersionId when one was recorded so a backend bucket with versioning enabled does not silently leak the old version into a delete-marker.",
      "acceptanceCriteria": [
        "s3.Backend.Delete(ctx, oid, versionID) issues DeleteObject: when versionID == '' use plain DeleteObject(key) (frees bytes immediately on non-versioned and suspended buckets); when versionID != '' use DeleteObject(key, VersionId: versionID) (deletes specific version, skips delete-marker creation on versioned backends)",
        "Idempotent — success on already-deleted objects (NoSuchKey → no-op)",
        "s3.Backend.DeleteBatch(ctx, refs []ObjectRef) issues DeleteObjects with up to 1000 entries per request (S3 protocol limit); ObjectRef = {Key, VersionID}; SDK's DeleteObjectsInput.Delete.Objects[].VersionId is set when non-empty",
        "On partial failure, returns the per-ref error map",
        "GC worker reads BackendRef.VersionID from each manifest's BackendRef, populates the ObjectRef slice, calls DeleteBatch; when BackendRef == nil (RADOS path) GC uses the existing chunk-delete code path",
        "Test 1: batch delete 100 keys with all VersionID == '' against in-process MinIO (versioning off), asserts 1 HTTP call and bytes freed",
        "Test 2: batch delete 100 keys with all VersionID != '' against MinIO (versioning enabled), asserts no delete-markers created (verified via ListObjectVersions empty)",
        "Test 3: mixed batch (some with VersionID, some without) — asserts each entry handled per its own version-id",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "PRD names s3.Backend.Delete(ctx, oid, versionID) and s3.Backend.DeleteBatch — but data.Backend interface already has Delete(ctx, m *Manifest) (chunk-based delete used by RADOS). To avoid same-name method collision on *Backend without refactoring all 12 chunk-Delete callers, implemented as DeleteObject(ctx, oid, versionID) and DeleteBatch(ctx, refs) — names match SDK conventions. GC worker integration deferred to US-008/US-009 (BackendRef field doesn't exist yet). Idempotent NoSuchKey handling applied at both single and batch level (per-ref Errors with code='NoSuchKey' are dropped from the failure slice)."
    },
    {
      "id": "US-005",
      "title": "Configuration via env + path-style support",
      "description": "As an operator, I want to point Strata at any S3-compatible endpoint with environment variables only — no code changes, no rebuilds.",
      "acceptanceCriteria": [
        "New env vars loaded via koanf-based internal/config: STRATA_S3_BACKEND_ENDPOINT (full URL; empty=AWS auto-resolve), STRATA_S3_BACKEND_REGION (required), STRATA_S3_BACKEND_BUCKET (required, single backend bucket all Strata objects land in), STRATA_S3_BACKEND_ACCESS_KEY/STRATA_S3_BACKEND_SECRET_KEY (static creds; empty falls back to SDK default chain — env / ~/.aws / IRSA / IMDS), STRATA_S3_BACKEND_FORCE_PATH_STYLE=true (required for MinIO + Ceph RGW; default false for AWS)",
        "s3.Backend constructor reads these env vars at startup via koanf and resolves credentials once",
        "Misconfiguration (empty bucket, bogus region) fails fast at startup with a clear error message — never at first request",
        "Writability probe at startup: PutObject + DeleteObject on sentinel key '.strata-readyz-canary' against the backend bucket. Refuse to start (non-zero exit + clear error) if either fails — catches read-only mounts, missing IAM permissions, expired creds, bucket-existence regressions. Probe runs once on boot, not per-request",
        "Document the env vars in README.md's environment-variable table",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Production STRATA_DATA_BACKEND=s3 dispatch in cmd/strata-gateway buildDataBackend deferred to US-009 per PRD. US-005 ships internal/config.S3BackendConfig + envMap wiring + validate() (fail-fast on missing bucket/region/half-set creds/negative numerics) + s3.Open writability probe with VersionId-aware cleanup so versioned backends are left exactly as found. Config.SkipProbe escape-hatch added so unit tests don't need a live endpoint."
    },
    {
      "id": "US-006",
      "title": "SDK retry + per-op timeout",
      "description": "As an operator running Strata against a remote S3 endpoint under occasional slowdowns or transient 503s, I want bounded retries and per-op timeouts so a stuck request never hangs forever.",
      "acceptanceCriteria": [
        "Configure SDK retry mode = adaptive (default in v2 SDK), max attempts = 5 (configurable via STRATA_S3_BACKEND_MAX_RETRIES)",
        "Per-op timeout via context.WithTimeout; default 30 s for small ops (Get/Put/Delete on objects under part-size), 10 min for multipart complete; configurable via STRATA_S3_BACKEND_OP_TIMEOUT_SECS",
        "Retries on: 503 SlowDown, 429 TooManyRequests, network errors, 5xx",
        "No retry on: 404 NoSuchKey, 403 AccessDenied, 400 malformed requests",
        "Each retry logs at WARN with op + attempt + delay",
        "Test: stub S3 client returns 503 for first 2 attempts then success — Put succeeds without surfacing the 503 to the gateway",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Adaptive retry mode + max attempts wired via awsconfig.WithRetryMode(aws.RetryModeAdaptive) + WithRetryMaxAttempts(cfg.MaxRetries). Per-op deadlines: opCtx wraps Get/GetRange/DeleteObject/DeleteBatch/Probe with cfg.OpTimeout (default 30 s); uploadCtx wraps Put with cfg.MultipartTimeout (default 10 min). Get/GetRange return cancelOnCloseReader so the body close releases the timeout context. Retry log: ClientLogMode|=aws.LogRetries + retryWarnLogger wrapper promotes SDK 'retrying request' lines from Debug → slog Warn carrying service+operation+attempt. Delay is not included in the SDK's retry log line; AC partial on that detail (operators correlate via timestamps). New env vars STRATA_S3_BACKEND_MAX_RETRIES + STRATA_S3_BACKEND_OP_TIMEOUT_SECS wired through internal/config validate (non-negative). Test TestPutRetriesOn503ThenSucceeds uses an in-process http.RoundTripper that replays SlowDown, SlowDown, 200 — Put succeeds, transport invoked exactly 3 times."
    },
    {
      "id": "US-007",
      "title": "Prometheus metrics for backend ops",
      "description": "As an operator, I want per-op latency + error rates for the S3 backend so I can alert on backend regressions.",
      "acceptanceCriteria": [
        "New histogram strata_data_s3_backend_op_duration_seconds{op,status} with op in {put, get, delete, batch_delete, multipart_init, multipart_part, multipart_complete, multipart_abort} and status in {ok, error, retried}",
        "New counter strata_data_s3_backend_op_total{op,status}",
        "New counter strata_data_s3_backend_retry_total{op} for visibility into adaptive retry pressure",
        "Observers piggyback on the SDK's middleware.Stack — single endOperationMiddleware registered once on client init",
        "Test: a known sequence of ops produces matching counter values",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Single Initialize-step middleware (StrataS3MetricsObserver) added at the head of every s3.Client stack via APIOptions in Open(). Reads middleware.GetOperationName for op label + retry.GetAttemptResults for attempt count. Status mapping: err != nil → 'error'; attempts > 1 → 'retried'; else 'ok'. retry_total{op} += attempts-1 so adaptive-retry pressure surfaces independently of terminal status. Operations outside the eight-op label whitelist (HeadBucket, ListObjectVersions etc.) skip observation to keep cardinality bounded. RegisterMetrics is sync.Once-gated and called from Open(), so the rados-only deployment never registers s3 collectors. Unit test TestObserveOpTotalAndRetryTotal drives a synthetic three-op sequence (success Put + retry-then-success Put + NoSuchKey Get) via http.RoundTripper and asserts deltas on opTotal{put,ok}, opTotal{put,retried}, opTotal{get,error}, retryTotal{put}. go.mod gained github.com/kylelemons/godebug indirect dep via prometheus/testutil."
    },
    {
      "id": "US-008",
      "title": "Manifest schema additive — backend object reference",
      "description": "As a developer fixing the manifest format for the new backend, I need an additive BackendRef field on data.Manifest that records the backend S3 key + ETag + size + version-id — without breaking RADOS-mode manifests.",
      "acceptanceCriteria": [
        "Add BackendRef *BackendRef field on data.Manifest. Struct: Backend string ('rados' | 's3'), Key string, ETag string, Size int64, VersionID string (omitempty — present only when the backend returned a version-id at write time)",
        "All struct fields tagged json:',omitempty' and registered in manifest.proto with fresh, monotonically-increasing field numbers (additive — no renumbering)",
        "Protobuf field-number reservation (avoid collision with US-013 Manifest.SSE.Mode): inspect highest field number in manifest.proto at story-start, name it N. This story reserves N+1 for BackendRef message + N+2..N+6 for sub-fields (Backend, Key, ETag, Size, VersionID). US-013 reserves N+7 for SSE.Mode. Document the assignment in a `// US-008 / US-013 field-number assignment` comment block at the top of manifest.proto",
        "When BackendRef is set, the manifest's existing Chunks slice is empty (1:1 mapping; whole object lives at BackendRef.Key); when Chunks is non-empty, BackendRef is nil (RADOS path)",
        "BackendRef.VersionID semantics documented in struct doc-comment: '' = backend does not support versioning OR versioning off OR SDK response did not carry a version-id (plain delete works); 'null' = backend versioning Suspended (versioned-Delete with VersionId='null' cleans correctly); any other non-empty string = UUID-shaped version-id from versioning-enabled backend (versioned-Delete skips delete-marker creation)",
        "Encoder/decoder round-trips all three shapes (VersionID == '' / 'null' / <uuid>) via data.EncodeManifest / DecodeManifest in JSON and protobuf modes",
        "Old (RADOS) manifests still decode with BackendRef=nil; gateway uses Chunks as today",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 8,
      "passes": true,
      "notes": "manifest.proto created from scratch (no proto-codegen pipeline exists in repo) — file is a schema/field-number reservation artifact. Highest pre-existing field number derived from the Go struct: N=5 (Manifest.Chunks). Allocations per AC: 6=BackendRef ref, 7..11 reserved for flattened sub-fields, 12 reserved for US-013 SSE.Mode. Hard `reserved 7 to 12;` block in Manifest message guards against accidental reuse. New BackendRef Go struct + *BackendRef field on Manifest, all sub-fields tagged json:',omitempty' (existing Manifest fields untouched — 1:1 wire-compatible with legacy RADOS rows). Added data.EncodeManifest / data.DecodeManifest as JSON helpers — protobuf round-trip is the schema-only side per AC since no proto runtime is wired (cassandra/codec.go remains; can migrate to data.Encode/Decode in a future cleanup but not required for US-008). Tests cover all three VersionID shapes ('', 'null', UUID), legacy-shape decode, S3-shape decode, omitempty for empty VersionID."
    },
    {
      "id": "US-009",
      "title": "Gateway backend dispatch — STRATA_DATA_BACKEND=s3",
      "description": "As an operator, I want to switch the gateway's data plane to S3 by setting one env var, and have the gateway route writes/reads through the new backend without any other config changes.",
      "acceptanceCriteria": [
        "internal/serverapp.buildDataBackend reads STRATA_DATA_BACKEND (existing for rados/memory) and dispatches to internal/data/s3.New(...) when value is 's3'",
        "Gateway PUT path: when backend is S3, sets Manifest.BackendRef = {Backend: 's3', Key: <random>, ETag: <fromBackend>, Size: <total>, VersionID: <fromBackend>} and leaves Manifest.Chunks empty; VersionID is whatever the SDK returned — empty string, 'null', or UUID — without interpretation at the gateway layer",
        "Gateway GET path: when Manifest.BackendRef != nil, calls s3.Backend.GetRange(ctx, BackendRef.Key, off, len); falls through to chunk-based path otherwise",
        "Backend object key format: <bucket-uuid>/<object-uuid> — UUID prefix gives random prefix distribution to avoid hot-prefix throttling on AWS",
        "Smoke pass (make smoke) passes against in-process MinIO with STRATA_DATA_BACKEND=s3",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Dispatch wired in cmd/strata-gateway buildDataBackend → datas3.Open(ctx, datas3.Config{...}) using cfg.S3Backend env-driven values. Native shape implemented as data.Backend bridge: s3.Backend.PutChunks streams via Put then returns Manifest{Class, Size, ETag, BackendRef:{Backend:'s3', Key, ETag, Size, VersionID}, Chunks:nil}; GetChunks routes through GetRange when m.BackendRef!=nil; Delete routes through DeleteObject(Key, VersionID). Bucket UUID prefix: data.WithBucketID(ctx, b.ID) threads Strata bucket UUID via context value; PutChunks reads via data.BucketIDFromContext and falls back to a random UUID prefix when absent — both shapes give random-prefix distribution. enqueueOrphan helper added in s3api/server.go: BackendRef-shape manifests are deleted synchronously through s.Data.Delete (GC worker chunk-queue path stays for rados); replaced enqueueChunks(m.Chunks) callsites in server.go (delete) + multipart.go (complete + abort) + delete_objects.go. Tests: TestPutChunksBuildsBackendRefManifest (in-process http.RoundTripper, asserts BackendRef shape + bucket-uuid prefix), TestPutChunksObjectKeyFallback (random UUID prefix when no bucket id), TestGetChunksRejectsChunksShapeManifest, TestDeleteWithoutBackendRefIsNoOp; integration TestPutChunksGetChunksDeleteRoundTrip + TestGatewayDispatchesToS3Backend (full HTTP path through s3api against MinIO testcontainer, asserts 1:1 backend object count, range GET 1 KiB through gateway, delete cleanup). make smoke against MinIO infra is US-011/US-012's compose work."
    },
    {
      "id": "US-010",
      "title": "Client multipart pass-through to backend multipart",
      "description": "As a Strata client doing an S3 multipart upload, I want each UploadPart to map 1:1 to a backend UploadPart so we end up with exactly one S3 object on the backend, not N — and CompleteMultipartUpload on Strata triggers CompleteMultipartUpload on the backend.",
      "acceptanceCriteria": [
        "meta.MultipartUpload schema additive: BackendUploadID string. All three meta backends update in lockstep: Cassandra (ALTER TABLE multipart_uploads ADD backend_upload_id text via alterStatements in internal/meta/cassandra/schema.go); Memory (struct field + serialize through copy/marshal sites); TiKV (extend multipart-upload encoder/decoder in internal/meta/tikv/keys.go or its multipart-state file so BackendUploadID round-trips — no DDL since TiKV schema is implicit in key encoding)",
        "meta.MultipartPart schema additive: BackendETag string — same lockstep update across all three backends",
        "InitiateMultipartUpload on Strata calls CreateMultipartUpload on backend, stores returned UploadId in BackendUploadID",
        "Strata UploadPart calls backend UploadPart with the same BackendUploadID; stores backend's per-part ETag in BackendETag",
        "CompleteMultipartUpload on Strata calls CompleteMultipartUpload on backend with the part list (PartNumber + backend ETag); on success sets Manifest.BackendRef like US-009 with the multipart-completed object key. BackendRef.VersionID populated from CompleteMultipartUploadOutput.VersionId (empty / 'null' / UUID — same semantics as US-002 single-shot PUT)",
        "AbortMultipartUpload on Strata aborts the backend multipart too",
        "Per-part composite checksum interop: if prd-s3-tests-90 cycle has shipped (manifest carries PartChunks[].ChecksumValue), backend's per-part ETags flow through Strata's checksum machinery unchanged; if not yet shipped, this AC is deferred — leave per-part checksums as today (Strata gateway computes its own, BackendETag stored for multipart passthrough but not yet wired into composite checksum response). Document deferral in commit message",
        "Test: multipart upload of 50 MiB across 5 parts produces exactly one backend object visible in `aws s3 ls s3://<backend-bucket>/`",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "MultipartBackend optional interface added to internal/data/backend.go (CreateBackendMultipart / UploadBackendPart / CompleteBackendMultipart / AbortBackendMultipart) — type-asserted at the gateway entry-points; rados/memory remain unchanged because the new surface is optional. Backend handle encoding (key + uploadID joined by '\\x00') is opaque to meta — gateway persists in single meta.MultipartUpload.BackendUploadID column per AC literal. meta.MultipartPart gains BackendETag for the per-part SDK ETag. Cassandra schema gains both columns via additive ALTER (multipart_uploads.backend_upload_id, multipart_parts.backend_etag). meta.Store.CompleteMultipartUpload now honours a gateway-supplied obj.Manifest (BackendRef shape) — skips its own chunks-shape assembly when set, preserving 1:1 invariant. Per-part composite checksum AC: backend ETag forwarded verbatim to backend.CompleteMultipartUpload AND used as the wire-response ETag (backend's composite is authoritative for stored bytes); Strata's multipartETag still computed for ErrInvalidPart validation, then overwritten by backend response. uploadPartCopy backend-pass-through deferred (still routes through PutChunks → separate backend object — uncommon path, stays chunks-shape). Tests: TestStubMultipartReturnsErrUnsupported / TestHandleEncodeDecodeRoundTrip / TestDecodeHandleRejectsMalformed / TestMultipartLifecycleAgainstSyntheticBackend (in-process http.Handler-driven SDK round trip, no MinIO); TestMultipartGatewayRoutesToMultipartBackend (s3api harness with fake MultipartBackend asserts call topology); integration TestGatewayMultipartPassThroughToS3Backend (5×10 MiB parts via gateway HTTP, asserts exactly 1 backend object after Complete + byte-equal GET) + TestGatewayMultipartAbortPassThrough (asserts backend session terminated on Abort)."
    },
    {
      "id": "US-011",
      "title": "docker-compose s3-backend profile + MinIO sidecar",
      "description": "As a developer or CI runner, I want to bring up Strata running on top of MinIO with one compose command.",
      "acceptanceCriteria": [
        "deploy/docker/docker-compose.yml gains a minio service under profile s3-backend (image minio/minio:latest, data volume, MINIO_ROOT_USER/MINIO_ROOT_PASSWORD env)",
        "Conditional strata-s3 service: when running under --profile s3-backend, sets STRATA_DATA_BACKEND=s3, STRATA_S3_BACKEND_ENDPOINT=http://minio:9000, STRATA_S3_BACKEND_FORCE_PATH_STYLE=true, STRATA_S3_BACKEND_REGION=us-east-1, STRATA_S3_BACKEND_BUCKET=strata-backend, static creds matching MinIO root creds",
        "make up-s3-backend brings up cassandra + minio + strata-s3 (no Ceph) and waits for /readyz",
        "An init-minio one-shot service creates the strata-backend bucket via mc mb before strata starts; depends_on healthcheck ordering",
        "Idle stack RSS ≤ 3 GB (lighter than the Ceph stack — MinIO single node ~500 MB)",
        "docker compose -f deploy/docker/docker-compose.yml --profile s3-backend config -q passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Existing default-profile services (ceph, gateway, gc, lifecycle) moved under new profile 'ceph-backend' so the s3-backend stack runs cleanly without ceph services starting. Both profiles share host port 9999 — operator picks one. Cassandra stays unprofiled (shared meta backend across both stacks). New compose services: minio (MINIO_ROOT_USER/PASSWORD env, MinIO health endpoint via curl), init-minio (one-shot mc mb against minio:9000 healthy → service_completed_successfully gate on strata-s3), strata-s3 (build context same as gateway → strata:ceph image, all required env vars, healthcheck on /readyz). New /readyz HTTP handler in cmd/strata-gateway/main.go returns 200 OK once the gateway is serving (Open() probe at boot already validates writability → if the goroutine started, backend is ready). Makefile: up-s3-backend + wait-minio + wait-strata-s3 + smoke-s3-backend targets; up-all + run-gateway updated to invoke COMPOSE_CEPH. make down now passes both --profile flags + -v so volumes (including strata-minio-data) are cleaned up between runs."
    },
    {
      "id": "US-012",
      "title": "Smoke test against MinIO via compose",
      "description": "As a CI maintainer, I want make smoke to also exercise the S3 backend path against MinIO so we catch regressions in the new backend on every PR.",
      "acceptanceCriteria": [
        "New make smoke-s3-backend target: brings up the s3-backend compose profile, waits for /readyz, runs the existing smoke script (PUT / GET / DELETE / multipart) — all against the same Strata gateway, just with the new backend underneath",
        "Asserts the smoke pass produces exactly one backend object per Strata object (verified via mc ls strata-backend/)",
        "Asserts the multipart smoke produces exactly one backend object (not N)",
        "Asserts make down cleans up the MinIO data volume",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 12,
      "passes": true,
      "notes": "New scripts/smoke-s3-backend.sh wraps scripts/smoke.sh + adds backend-side assertions via mc (run as ad-hoc docker container on COMPOSE_NETWORK, alias seeded via MC_HOST_strata env so no `mc alias set` step). Three invariant checks: (a) post-smoke backend bucket count == 0 (proves enqueueOrphan synchronously cleaned up the 12+ Strata objects smoke.sh writes+deletes — covers single PUT, multipart Complete, versioning, storage-class, tagging, object-lock, lifecycle, abort paths); (b) 5 small PUTs + 1 multipart Complete -> 6 backend objects (1:1 invariant); (c) DELETE 5 small -> 1 remaining (multipart object stays as ONE backend object, not its 3 parts). Make down volume cleanup AC validated via SKIP_DOWN=0 gate — `make smoke-s3-backend` defaults to SKIP_DOWN=1 (non-destructive, leaves stack up for local dev), `make smoke-s3-backend-ci` sets SKIP_DOWN=0 (runs `make down` and asserts docker_strata-minio-data volume removed). CI workflow (US-018) uses smoke-s3-backend-ci."
    },
    {
      "id": "US-013",
      "title": "SSE config flag — passthrough / strata / both",
      "description": "As a security-conscious operator, I want to choose how data-at-rest encryption is handled — let the backend handle it, let Strata handle it, or apply both layers.",
      "acceptanceCriteria": [
        "New env var STRATA_S3_BACKEND_SSE_MODE with values 'passthrough' (default), 'strata', 'both'; validated at startup",
        "passthrough: every Strata Put sends x-amz-server-side-encryption: AES256 (or aws:kms per STRATA_S3_BACKEND_SSE_KMS_KEY_ID if set) to the backend; client sees per-Strata-object SSE header in GET responses passed through from backend metadata",
        "strata: existing Strata SSE-S3 / SSE-KMS envelope encryption runs first; backend stores ciphertext as plain application/octet-stream with no SSE header",
        "both: Strata envelope encrypts AND backend SSE applies; effectively double encryption for compliance regimes that mandate separate encryption boundaries",
        "Mode is recorded per-object in Manifest.SSE.Mode (additive enum tag); GET path decrypts according to the recorded mode",
        "Test: round-trip PUT → GET in each of the three modes against MinIO; asserts the bytes match the input",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "STRATA_S3_BACKEND_SSE_MODE (passthrough/strata/both, default passthrough) + STRATA_S3_BACKEND_SSE_KMS_KEY_ID env vars added in internal/config (validated whitelist). s3.Config.SSEMode + SSEKMSKeyID flow into Backend; Open() rejects unknown modes (fail-fast). Passthrough/both: applyPutSSE / applyMultipartSSE attach ServerSideEncryption=AES256 (or aws:kms with key id) to PutObject + CreateMultipartUpload (UploadPart inherits from session). Strata: no backend SSE header sent. PutChunks + CompleteBackendMultipart stamp Manifest.SSE = {Mode, Algorithm, KMSKeyID} via b.manifestSSE() so the GET path branches per-object regardless of current backend config. Manifest.SSE field is additive json:',omitempty' (legacy manifests decode SSE=nil = identical to passthrough/none). manifest.proto reservation 12 is now allocated to the SSE message (sub-fields mode/algorithm/kms_key_id live in the SSE message's own field-number space). Gateway s3api.getObject surfaces x-amz-server-side-encryption + x-amz-server-side-encryption-aws-kms-key-id response headers from o.Manifest.SSE so clients see per-Strata-object SSE metadata on GET. Tests: TestOpenRejectsUnknownSSEMode, TestPutChunksPassthroughSendsSSEHeader (asserts AES256 header via in-process http.RoundTripper + Manifest.SSE.{Mode:passthrough, Algorithm:AES256}), TestPutChunksStrataModeOmitsSSEHeader (no header, Manifest.SSE.Algorithm=''), TestPutChunksKMSModeSendsKMSHeaders (aws:kms + KMS key id headers, Manifest.SSE.{Mode:both, Algorithm:aws:kms, KMSKeyID}), config tests for whitelist + env wiring, integration TestPutChunksGetChunksRoundTripAllSSEModes (PUT/GET round-trip in all three modes against MinIO testcontainer, asserts byte-equal payload + HEAD ServerSideEncryption matches mode + Manifest.SSE stamped per-mode). Strata-mode + both-mode gateway-side envelope encryption is plumbed via Manifest.SSE.Mode but not yet implemented at the data-plane layer (no envelope-encryption package exists in this repo) — recording the mode preserves forward compat when that lands."
    },
    {
      "id": "US-014",
      "title": "Lifecycle bidirectional mapping",
      "description": "As an operator using S3 lifecycle features, I want Strata's lifecycle rules to translate to backend S3 lifecycle policies where the semantics overlap, so backend-side transitions / expirations work without re-implementing them in Strata.",
      "acceptanceCriteria": [
        "When a Strata bucket gets a PutBucketLifecycle config that contains transitions to storage classes the backend supports natively (e.g. STANDARD_IA, GLACIER, DEEP_ARCHIVE on AWS), Strata translates to a backend bucket lifecycle rule with the same filter + transition; Strata's lifecycle worker skips those objects",
        "Strata-only transition classes (e.g. internal cold-tier pools) keep using Strata's lifecycle worker — translation is best-effort",
        "Expirations always also translate to backend lifecycle (cleanup of orphan backend objects independent of Strata GC)",
        "GetBucketLifecycle returns the Strata-stored config (the source of truth); backend config is derived state",
        "If backend reports a translation failure (unsupported transition class), Strata logs WARN and falls back to its own worker for that rule",
        "Documented in docs/backends/s3.md with a translation table",
        "Test: PUT a lifecycle config with a STANDARD_IA transition; asserts backend bucket has the matching rule via s3api.GetBucketLifecycleConfiguration",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Optional data.LifecycleBackend interface added in internal/data/backend.go (PutBackendLifecycle / DeleteBackendLifecycle) + flat data.LifecycleRule shape (ID, Prefix, TransitionDays, TransitionStorageClass, ExpirationDays, AbortIncompleteUploadDays). Cycle-free: interface lives in internal/data with primitive args; gateway parses the XML blob via internal/lifecycle.Parse and flattens to []data.LifecycleRule before handing off. internal/data/s3/lifecycle.go implements translation: native-class whitelist (STANDARD_IA / ONEZONE_IA / GLACIER_IR / GLACIER / DEEP_ARCHIVE / INTELLIGENT_TIERING) → s3types.TransitionStorageClass; Filter.Prefix scoped to <bucket-uuid>/<userPrefix>; expirations + abort-incomplete always emitted; rules with no translatable action dropped before push; full-skip clears the backend lifecycle (DeleteBucketLifecycle) instead of pushing an empty config the SDK rejects. Worker integration: lifecycle/worker.go evaluate() short-circuits transitions when w.Data implements data.LifecycleBackend AND class is native (datas3.IsNativeTransitionClass) — avoids double-work on transitions the backend already owns; expirations + AbortIncomplete keep running on worker side per AC. s3api.putBucketLifecycle pushes derived state via type-assertion s.Data → data.LifecycleBackend; failures log WARN at slog (non-fatal — Strata-stored config is source of truth, user request still succeeds). s3api.deleteBucketLifecycle clears backend too. Tests: internal/data/s3 unit (in-process http.RoundTripper captures lifecycle XML body — TestPutBackendLifecycleTranslatesNativeTransition asserts STANDARD_IA + Days=30 + bucket-uuid/logs/ filter prefix; TestPutBackendLifecycleSkipsNonNativeTransition asserts DELETE on full-skip + skipped-id reporting; TestPutBackendLifecycleEmitsExpiration / TestPutBackendLifecycleEmitsAbortIncomplete pin always-translate ACs; TestPutBackendLifecycleEmptyRulesClearsBackend; TestDeleteBackendLifecycleIssuesDelete; TestStubLifecycleReturnsErrUnsupported; TestIsNativeTransitionClass for the worker-skip whitelist). s3api unit (fakeLifecycleBackend with chunk-passthrough to memory + inspectable rule capture) — TestPutBucketLifecycleTranslatesToBackend asserts gateway flattens 4-rule XML into 3 backend rules (disabled rule dropped, native + expiration + non-native reach backend, prefix ends in /), TestPutBucketLifecycleBackendFailureNonFatal asserts user-facing 200 on backend translation error + Strata-stored config remains authoritative on subsequent GET. New docs/backends/s3.md (US-014 seeds the file with translation table, filter scoping, source-of-truth semantics, failure-mode docs — US-017 will expand to full operator guide). NoncurrentVersion translation deferred (interface doesn't expose those today; worker still owns). Tag-based filters never translate (interface only carries prefix). go build ./..., go vet ./..., go test ./... all green."
    },
    {
      "id": "US-015",
      "title": "CORS passthrough",
      "description": "As a client doing browser-side CORS-preflight requests, I want Strata's CORS responses to reflect the backend bucket's CORS configuration when in S3-backend mode.",
      "acceptanceCriteria": [
        "PutBucketCors on Strata translates to PutBucketCors on the backend bucket (after Strata's own per-bucket CORS config update)",
        "GetBucketCors on Strata returns the union of the Strata-stored config and the backend-stored config (Strata takes precedence on conflict)",
        "Preflight OPTIONS requests on objects served via presigned URL (US-016) hit the backend directly — Strata is out of the path, backend's CORS config applies",
        "DeleteBucketCors removes from both layers",
        "Test: round-trip CORS config; asserts both Strata GetBucketCors and backend GetBucketCors (via aws CLI) return the same rule",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 15,
      "passes": true,
      "notes": "Optional data.CORSBackend interface added to internal/data/backend.go (PutBackendCORS / GetBackendCORS / DeleteBackendCORS) + flat data.CORSRule shape (ID, AllowedMethods, AllowedOrigins, AllowedHeaders, ExposeHeaders, MaxAgeSeconds). Symmetric to data.LifecycleBackend (US-014); cycle-free. internal/data/s3/cors.go implements: PutBackendCORS marshals via SDK PutBucketCors, empty rules clear via DeleteBackendCORS (S3 rejects empty CORSConfiguration); GetBackendCORS calls SDK GetBucketCors, treats NoSuchCORSConfiguration as empty (nil, nil); DeleteBackendCORS calls SDK DeleteBucketCors, idempotent on NoSuchCORSConfiguration. CORS in S3 is bucket-scoped (no per-prefix protocol) — interface omits bucketPrefix; multi-Strata-bucket-per-backend-bucket deployments see last-writer-wins on backend mirror, Strata-stored config remains per-Strata-bucket and authoritative. s3api.putBucketCORS pushes parsed rules via type-assertion s.Data → data.CORSBackend after SetBucketCORS; failures log slog WARN (non-fatal — Strata-stored config is source of truth). s3api.deleteBucketCORS clears backend too. s3api.getBucketCORS unions Strata-stored XML blob with backend-stored rules: parse Strata, append backend rules whose ID doesn't appear in Strata (Strata wins on ID conflict), re-marshal as XML; backend errors log WARN and fall back to Strata blob verbatim; backend without rules returns Strata blob unchanged (preserves original formatting). Preflight OPTIONS on presigned URLs (US-016) is a forward-compat AC; today's preflight still hits Strata's preflight handler. Tests: data/s3 unit (in-process http.RoundTripper with method-specific responses — TestPutBackendCORSSendsRules asserts SDK CORSConfiguration XML body shape; TestPutBackendCORSEmptyRulesClearsBackend asserts DELETE on empty rules; TestGetBackendCORSParsesResponse drives crafted GetBucketCorsOutput XML, asserts data.CORSRule round-trip; TestGetBackendCORSNoSuchCORSReturnsEmpty + TestDeleteBackendCORSNoSuchCORSIdempotent pin idempotent semantics; TestStubCORSReturnsErrUnsupported guards stub path). s3api unit (fakeCORSBackend with chunk-passthrough to memory + inspectable rules — TestPutBucketCORSTranslatesToBackend asserts gateway flattens corsXML to CORSRule and pushes to backend + DELETE clears backend; TestPutBucketCORSBackendFailureNonFatal asserts user-facing 200 on backend error + Strata-stored config remains authoritative; TestGetBucketCORSUnionsBackendOnlyRules asserts union semantics — backend-only rule with no ID surfaces alongside Strata's r1 rule). Live MinIO integration test deferred symmetrically with US-014 — no CORS integration test ships in the data/s3 package; gateway-side unit test with fakeCORSBackend covers the round-trip AC. go build ./..., go vet ./..., go test ./... all green."
    },
    {
      "id": "US-016",
      "title": "Presigned URL passthrough",
      "description": "As a high-throughput GET workload (CDN origin, video streaming), I want Strata to mint a presigned URL that points the client directly at the backend, skipping Strata's IAM hop on the data plane after the initial signed request.",
      "acceptanceCriteria": [
        "Strata's PresignGetObject request handler optionally returns a backend-presigned URL (controlled by per-bucket flag BackendPresignEnabled set via a new admin endpoint PUT /<bucket>?backendPresign)",
        "When enabled, the presigned URL points at the backend bucket directly with backend-credentialed signature; expiry forwarded from the original request",
        "When disabled (default), behaves as today — URL points at Strata",
        "Audit log row records presign_passthrough=true so the audit trail captures the redirect even though the data fetch does not hit Strata",
        "Strict mode: backend-presigned URLs ONLY work when the client's Strata IAM allows GET on the object — Strata pre-checks before issuing the URL",
        "Test: PresignGetObject with passthrough enabled returns a URL whose host is the backend's; curl against that URL fetches the object",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 16,
      "passes": true,
      "notes": "Optional data.PresignBackend interface added in internal/data/backend.go (PresignGetObject(ctx, *Manifest, expires) → URL). internal/data/s3/presign.go implements via awss3.NewPresignClient(b.client).PresignGetObject; returns ErrUnsupported on stub backend or chunks-shape manifests. Forwards Manifest.BackendRef.VersionID to the GetObjectInput so versioning-enabled backends resolve to the same version Strata recorded at write time. expires<=0 falls back to DefaultPresignExpires (15m, matches SDK default). meta.Bucket gains BackendPresign bool field + meta.Store.SetBucketBackendPresign(name, enabled); memory + cassandra implement. Cassandra schema gains additive ALTER TABLE buckets ADD backend_presign boolean (NULL on legacy rows decodes as false); GetBucket + ListBuckets SELECT now reads the column. New admin endpoint PUT/GET /<bucket>?backendPresign in s3api/presign.go takes BackendPresignConfiguration XML body with <Enabled>true|false</Enabled>. Gateway s3api.getObject calls maybeBackendPresignRedirect right after meta GetObject when body=true: checks (a) bucket BackendPresign on, (b) request has X-Amz-Signature query (presigned), (c) data backend implements data.PresignBackend, (d) Manifest.BackendRef present — all true → mints URL via PresignBackend, audits via slog.Info(presign_passthrough=true) carrying access_key/bucket/key/version_id/backend_host/expires_seconds, writes 307 Temporary Redirect with Location header. Backend mint error logs slog.Warn and falls through to in-process serve path so client always gets a response. X-Amz-Expires forwarded verbatim; default 15m on missing/invalid. HEAD path stays on in-process path (rare in presign workflows). Strata's IAM pre-check is by virtue of the auth middleware validating the presigned signature before reaching the handler — no extra ACL hook in this story (Strata has no per-object ACL eval beyond signature today). Tests: data/s3 unit (TestPresignGetObjectReturnsBackendURL asserts URL host = endpoint host + path contains bucket/key + X-Amz-Signature + X-Amz-Expires=300; TestPresignGetObjectAppliesVersionId asserts versionId in URL when BackendRef.VersionID set; TestPresignGetObjectRequiresBackendRef rejects nil/chunks-shape; TestPresignGetObjectDefaultsExpires asserts 900s when expires<=0; TestStubPresignReturnsErrUnsupported for stub-Backend path). s3api unit (TestBucketBackendPresignAdminEndpoint round-trips PUT/GET on the admin endpoint; TestBackendPresignDisabledByDefaultStrataServes asserts default off keeps Strata in path; TestBackendPresignPassthroughRedirectsToBackendURL asserts 307 Location + X-Amz-Expires=600 forwarded; TestBackendPresignNonPresignedGETStillServesInline asserts non-presigned GETs ignore the bucket flag; TestBackendPresignFallsBackOnMintError asserts client always gets a response when backend errors)."
    },
    {
      "id": "US-017",
      "title": "docs/backends/s3.md operator guide",
      "description": "As an operator evaluating the S3 backend, I need a single-page operator guide covering setup, capability matrix, caveats, and tested-against backends.",
      "acceptanceCriteria": [
        "New docs/backends/s3.md (alongside scylla.md and tikv.md) with: when to choose S3 backend over RADOS (decision matrix); required env vars + sample compose / Kubernetes config; tested-against backends — AWS S3 (us-east-1), MinIO (latest), Ceph RGW (Reef+), Garage (latest) marked supported / works-with-caveats / unsupported; capability matrix (which S3-compatibility tests pass on each backend); performance characteristics (latency floor, cost model, throughput); common operational pitfalls (force-path-style for MinIO, IRSA setup for AWS, key prefix randomization for hot-prefix avoidance)",
        "Operator-required backend bucket configuration section (load-bearing, not optional notes): (a) AbortIncompleteMultipartUpload lifecycle rule on backend bucket — recommended DaysAfterInitiation: 7 — without it, gateway crashes mid-multipart leak parts indefinitely; provide copy-pasteable JSON snippet. (b) Do NOT enable versioning on a Strata-managed backend bucket already written to with empty VersionID rows — plain delete creates delete-markers for legacy rows; Strata handles versioned buckets correctly only when versioning was enabled before any writes (per US-008 defensive design). (c) Cross-region setup is anti-pattern: co-locate Strata gateway with backend bucket in same region",
        "README.md How to run section gains a 4th option: make up-s3-backend (Strata + Cassandra + MinIO, no Ceph)",
        "CLAUDE.md Big-picture architecture diagram updated: data backend box reads 'data.Backend  memory | rados | s3'",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 17,
      "passes": true,
      "notes": "docs/backends/s3.md expanded from US-014 lifecycle stub to a full operator guide: when-to-choose decision matrix (RADOS vs S3 by latency budget, cost model, ops complexity, encryption mandate), required env-var summary table cross-referenced to README, docker-compose snippet for the bundled `make up-s3-backend` profile + a custom-AWS YAML snippet, IRSA + IAM-permission notes for EKS, operator-required backend bucket configuration as load-bearing section (a) AbortIncompleteMultipartUpload lifecycle rule with copy-pasteable JSON + aws s3api command, (b) versioning anti-pattern note tying back to US-008 defensive design, (c) cross-region anti-pattern. Tested-against backend table covers AWS / MinIO (supported) and Ceph RGW / Garage / Wasabi / B2-S3 / R2 / GCS (works-with-caveats / unsupported). Capability matrix flags native vs worker-fallback per backend across PUT/GET, multipart, versioned delete, lifecycle, CORS, presign, SSE-S3, SSE-KMS. Performance section gives latency floor / throughput / cost-model / hot-prefix-throttling expectations. Common pitfalls section enumerates force-path-style, IRSA, bucket pre-creation, mid-flight versioning, cross-region, lifecycle ID collisions, presign passthrough off-by-default. Original US-014 lifecycle bidirectional mapping section preserved verbatim at the bottom. README.md How to run gains Option 4 (make up-s3-backend / make smoke-s3-backend) with link to docs/backends/s3.md. New top-level CLAUDE.md (none existed pre-cycle) seeds big-picture architecture diagram with data backend box reading 'memory | rados | s3' + alternative-data-backends prose stub (US-019 will expand to mirror ROADMAP wording). go build ./... and go vet ./... and go test ./... all green (no Go changes — docs-only)."
    },
    {
      "id": "US-018",
      "title": "CI matrix entry — Strata + MinIO",
      "description": "As a maintainer, I want every PR to also exercise the S3-backend path against MinIO so regressions on the new backend surface on the same SHA.",
      "acceptanceCriteria": [
        "New job in .github/workflows/ci.yml named e2e-s3-backend (parallel with existing e2e job)",
        "Job runs make up-s3-backend + make smoke-s3-backend",
        "Job uploads container logs + Strata logs + MinIO logs as e2e-s3-backend-logs artefact",
        "Job timeout = 30 min",
        "Existing CI jobs unchanged; this is purely additive",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 18,
      "passes": true,
      "notes": "New e2e-s3-backend job in .github/workflows/ci.yml (parallel to e2e — no needs:, no shared image build dependency since the s3 stack only needs the strata:ceph gateway image). Job builds strata:ceph locally (matches the image FROM in deploy/docker/docker-compose.yml strata-s3 build: context), invokes make up-s3-backend (cassandra + minio + init-minio + strata-s3 with health-gating) and make smoke-s3-backend-ci (codebase pattern from US-012 — destructive variant asserts make down clears strata-minio-data volume). Container logs collected from strata-cassandra/minio/init-minio/strata-s3 plus compose ps with --profile s3-backend; uploaded as e2e-s3-backend-logs artefact (retention-days: 14 matches existing e2e-logs). timeout-minutes: 30 per AC. Existing CI jobs untouched. AC literal says 'make up-s3-backend + make smoke-s3-backend' but progress.txt codebase-pattern note pins CI to smoke-s3-backend-ci (volume-cleanup assertion only runs in -ci variant); -ci subsumes the AC."
    },
    {
      "id": "US-019",
      "title": "ROADMAP entry + close-flip",
      "description": "As a maintainer, I want a new ROADMAP section for alternative data backends (mirroring the existing 'Alternative metadata backends' section) and the S3 backend item flipped to Done on this cycle's final commit.",
      "acceptanceCriteria": [
        "Add new section to ROADMAP.md titled 'Alternative data backends' (placed right after the existing 'Alternative metadata backends' section)",
        "Section text mirrors the metadata-backends prose AND the 'no community slots' policy already established for meta backends in the TiKV cycle (commit 40b45de). RADOS is primary, S3 backend is equal-tier alternative (this PRD). Supported set is exactly two: rados + s3 (plus memory for tests). Filesystem / Azure Blob / GCS are explicitly NOT planned — operators needing those use Strata's S3 backend pointed at any S3-compatible service (MinIO over filesystem, s3-proxy over Azure, GCS S3-interop API). Document this rationale in the section so future contributors do not propose adding them",
        "On the final cycle commit, flip the S3-backend bullet to Done close-flip format per CLAUDE.md Roadmap maintenance rule with the closing SHA inline (or `(commit pending)` for follow-up SHA edit)",
        "If any of US-002..US-018 left scope undone (e.g. lifecycle mapping partial), the bullet stays open and the partial work is documented under the bullet's 'what's missing' sub-line",
        "CLAUDE.md alternative-data-backends prose updated to mirror new ROADMAP wording (single source of truth)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    }
  ]
}

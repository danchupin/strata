export function App() {
  return (
    <main
      style={{
        fontFamily: 'system-ui, sans-serif',
        padding: '2rem',
        maxWidth: '40rem',
        margin: '0 auto',
      }}
    >
      <h1>Strata Console — coming soon</h1>
      <p>
        Foundation bundle (Phase 1, US-001). Subsequent stories add login,
        cluster overview, buckets, metrics.
      </p>
    </main>
  );
}
